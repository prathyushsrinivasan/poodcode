[
  {
    "key": "array_patterns",
    "name": "Array Problem Patterns",
    "category": "Arrays",
    "what": "A decision guide that maps the wording of an array question to the technique that solves it.",
    "deep": "Almost every array problem is one of a handful of shapes. The real skill is reading the prompt, spotting the signal words ('contiguous', 'sorted', 'longest', 'size k', 'in place'), and jumping straight to the matching skeleton instead of re-deriving an approach each time.",
    "java": "No single API — this is a routing table. Learn the signal → pattern → skeleton mapping, then reach for iteration, two pointers, sliding window, prefix sums, or in-place reversal.",
    "language": "java",
    "lesson": "### The core idea\nNearly every array question is one of a **small number of shapes**. If you can name the shape from the wording, you already know the code skeleton. Read the prompt, find the signal word, jump to the pattern.\n\n### Signal → pattern cheat sheet\n| The question says… | Reach for | Key move |\n| --- | --- | --- |\n| \"sum / max / min / count of the array\", \"in one pass\" | **Single-pass iteration** | Carry a running accumulator |\n| \"**contiguous** subarray with the largest sum\" | **Kadane** | Extend-or-restart at each index |\n| \"sum of `a[l..r]`\", many **range-sum queries** | **Prefix sums** | Precompute once, answer in O(1) |\n| \"**sorted** array\" + \"pair that sums to T / closest / container\" | **Two pointers** (ends inward) | Move the end that helps |\n| \"remove / move / dedupe **in place**\" | **Two pointers** (slow write, fast read) | Slow pointer writes kept elements |\n| \"**longest / shortest** subarray or substring with <property>\" | **Sliding window** (variable) | Grow right, shrink left on break |\n| \"window / subarray of **size k**\" | **Sliding window** (fixed) | Add entering, drop leaving |\n| \"reverse\", \"**rotate by k**\", \"O(1) extra space\" | **In-place reversal** | Swap ends; rotate = 3 reversals |\n| \"count occurrences\", values are small integers | **Counting array** `int[]` | Index by value |\n| \"element appears **more than n/2** times\" | **Boyer–Moore majority** | Cancel non-matches |\n| \"**sorted**\" + \"find / first index of / does X exist\" | **Binary search** | Halve the search space |\n\n### The five skeletons you will reuse most\n```java\n// 1. Single pass — one summary of the whole array\nlong acc = 0;                       // or best = a[0], count = 0\nfor (int x : a) acc += x;           // combine each element\n\n// 2. Two pointers, converging (needs a SORTED array)\nint lo = 0, hi = n - 1;\nwhile (lo < hi) {\n    int s = a[lo] + a[hi];\n    if (s == target) { /* found */ break; }\n    else if (s < target) lo++;      // need a bigger value\n    else hi--;                      // need a smaller value\n}\n\n// 3. Two pointers, slow write / fast read (in-place filter)\nint w = 0;\nfor (int r = 0; r < n; r++)\n    if (keep(a[r])) a[w++] = a[r];  // a[0..w-1] is the result, w its length\n\n// 4. Sliding window (variable size)\nint left = 0; long cur = 0, best = 0;\nfor (int r = 0; r < n; r++) {\n    cur += a[r];                        // extend right\n    while (invariantBroken(cur)) cur -= a[left++]; // shrink left\n    best = Math.max(best, r - left + 1);\n}\n\n// 5. Prefix sums (answer any range in O(1))\nlong[] pre = new long[n + 1];\nfor (int i = 0; i < n; i++) pre[i + 1] = pre[i] + a[i];\nlong sumLR = pre[r + 1] - pre[l];       // sum of a[l..r], inclusive\n```\n\n### How to use this page\n1. Underline the signal words in the prompt: *contiguous, sorted, longest, size k, in place, range sum*.\n2. Match a row in the table above.\n3. Paste the skeleton, then fill in the one problem-specific line (the test, the update, the accumulator).\n\nThe drills below make you write that one decisive line for each pattern.\n",
    "exercises": [
      {
        "id": "patterns-kadane",
        "title": "Contiguous max sum (Kadane)",
        "prompt": "Signal: *\"contiguous subarray with the largest sum\"* → **Kadane**. At each index, the best subarray ending here either extends the previous one or restarts at the current element. Replace `____` with that choice.",
        "hint": "Extend or restart: cur = max(a[i], cur + a[i]). Then track best separately.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        long cur = a[0], best = a[0];\n        for (int i = 1; i < n; i++) {\n            ____\n            best = Math.max(best, cur);\n        }\n        System.out.println(best);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        long cur = a[0], best = a[0];\n        for (int i = 1; i < n; i++) {\n            cur = Math.max(a[i], cur + a[i]);\n            best = Math.max(best, cur);\n        }\n        System.out.println(best);\n    }\n}\n",
        "tests": [
          {
            "input": "9\n-2 1 -3 4 -1 2 1 -5 4",
            "output": "6"
          },
          {
            "input": "3\n-5 -2 -8",
            "output": "-2"
          },
          {
            "input": "4\n1 2 3 4",
            "output": "10"
          }
        ],
        "source_slug": "maximum-subarray"
      },
      {
        "id": "patterns-counting",
        "title": "Most frequent value (counting array)",
        "prompt": "Signal: *\"count occurrences\"* with small integer values (0–100) → a **counting array**. Replace `____` to tally each value into `cnt` indexed by the value itself.",
        "hint": "Use the value as the index: cnt[x]++ for each x.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int[] cnt = new int[101];\n        ____\n        int best = 0, mode = 0;\n        for (int v = 0; v <= 100; v++) {\n            if (cnt[v] > best) { best = cnt[v]; mode = v; }\n        }\n        System.out.println(mode);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int[] cnt = new int[101];\n        for (int x : a) cnt[x]++;\n        int best = 0, mode = 0;\n        for (int v = 0; v <= 100; v++) {\n            if (cnt[v] > best) { best = cnt[v]; mode = v; }\n        }\n        System.out.println(mode);\n    }\n}\n",
        "tests": [
          {
            "input": "5\n1 2 2 3 2",
            "output": "2"
          },
          {
            "input": "3\n5 5 1",
            "output": "5"
          },
          {
            "input": "1\n42",
            "output": "42"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "patterns-majority",
        "title": "Majority element (Boyer–Moore)",
        "prompt": "Signal: *\"element that appears more than n/2 times\"* → **Boyer–Moore voting**. Keep a candidate and a counter; matches add 1, mismatches subtract 1, and a zero counter adopts the next value. Replace `____` with the vote step.",
        "hint": "Same as candidate → +1, otherwise −1: count += (x == cand) ? 1 : -1;",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int cand = a[0], count = 0;\n        for (int x : a) {\n            if (count == 0) cand = x;\n            ____\n        }\n        System.out.println(cand);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int cand = a[0], count = 0;\n        for (int x : a) {\n            if (count == 0) cand = x;\n            count += (x == cand) ? 1 : -1;\n        }\n        System.out.println(cand);\n    }\n}\n",
        "tests": [
          {
            "input": "5\n3 3 4 2 3",
            "output": "3"
          },
          {
            "input": "3\n1 1 1",
            "output": "1"
          },
          {
            "input": "7\n5 5 5 5 1 2 3",
            "output": "5"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "iteration",
    "name": "Array Iteration",
    "category": "Arrays",
    "what": "Walking through elements one at a time while maintaining running state such as a sum, max, or counter.",
    "deep": "The workhorse of array problems: one for-loop that updates accumulators as it goes. Most linear-time solutions are just one well-chosen pass, so the real skill is deciding what state to carry so a single sweep suffices.",
    "java": "Use for (int i = 0; i < n; i++) or for (int x : nums). Prefer a primitive int[] over Integer[] to avoid autoboxing overhead.",
    "language": "java",
    "lesson": "### When to reach for this\nThe question wants a **single summary of the whole array** — a sum, max, min, count, or the index of one of those — and the order does not matter. Carry a running variable and update it as you sweep once, left to right.\n\n### Simulated solve — max of `[3, 9, 2, 7]`\nStart `best` at the first element, then update only when you see something bigger.\n\n| i | a[i] | best before | bigger? | best after |\n| - | ---- | ----------- | ------- | ---------- |\n| 0 | 3 | — | init | 3 |\n| 1 | 9 | 3 | 9 > 3 ✓ | 9 |\n| 2 | 2 | 9 | 2 > 9 ✗ | 9 |\n| 3 | 7 | 9 | 7 > 9 ✗ | 9 |\n\nAnswer: **9**. One sweep — O(n) time, O(1) extra space.\n\n### In code (Java)\n```java\nint[] a = {3, 9, 2, 7};\nint best = a[0];\nfor (int i = 1; i < a.length; i++) {\n    if (a[i] > best) best = a[i];\n}\nSystem.out.println(best); // 9\n```\n\n### Watch out for\n- Initialize `best` from `a[0]`, not `0` — the array might be all negative.\n- One pass is O(n); avoid nesting a second loop when a running variable suffices.\n",
    "exercises": [
      {
        "id": "iteration-sum",
        "title": "Sum in one pass",
        "prompt": "Read `n` then `n` integers, and add each to a running total. Replace `____` so `sum` accumulates every element.",
        "hint": "Add the current element to the accumulator each pass: sum += x;",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        long sum = 0;\n        for (int i = 0; i < n; i++) {\n            int x = sc.nextInt();\n            ____\n        }\n        System.out.println(sum);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        long sum = 0;\n        for (int i = 0; i < n; i++) {\n            int x = sc.nextInt();\n            sum += x;\n        }\n        System.out.println(sum);\n    }\n}\n",
        "tests": [
          {
            "input": "4\n1 2 3 4",
            "output": "10"
          },
          {
            "input": "1\n5",
            "output": "5"
          },
          {
            "input": "3\n-1 -2 -3",
            "output": "-6"
          }
        ],
        "source_slug": "array-sum"
      },
      {
        "id": "iteration-max",
        "title": "Maximum element",
        "prompt": "`best` starts at `a[0]`. Replace `____` so a single pass leaves `best` holding the largest value.",
        "hint": "Update only when the current element beats the best so far.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int best = a[0];\n        for (int i = 1; i < n; i++) {\n            ____\n        }\n        System.out.println(best);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int best = a[0];\n        for (int i = 1; i < n; i++) {\n            if (a[i] > best) best = a[i];\n        }\n        System.out.println(best);\n    }\n}\n",
        "tests": [
          {
            "input": "4\n3 9 2 7",
            "output": "9"
          },
          {
            "input": "3\n-5 -1 -9",
            "output": "-1"
          },
          {
            "input": "1\n42",
            "output": "42"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "iteration-count-above",
        "title": "Count above a threshold",
        "prompt": "Input is `n t` then `n` values. Replace `____` to count how many values are strictly greater than `t`.",
        "hint": "Increment count whenever x > t.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int t = sc.nextInt();\n        int count = 0;\n        for (int i = 0; i < n; i++) {\n            int x = sc.nextInt();\n            ____\n        }\n        System.out.println(count);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int t = sc.nextInt();\n        int count = 0;\n        for (int i = 0; i < n; i++) {\n            int x = sc.nextInt();\n            if (x > t) count++;\n        }\n        System.out.println(count);\n    }\n}\n",
        "tests": [
          {
            "input": "4 5\n3 6 1 9",
            "output": "2"
          },
          {
            "input": "3 0\n-1 -2 -3",
            "output": "0"
          },
          {
            "input": "3 0\n1 2 3",
            "output": "3"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "iteration-argmax",
        "title": "Index of the maximum",
        "prompt": "Track the *index* of the largest element seen. Replace `____` so `bi` ends at the position of the first maximum.",
        "hint": "Compare against a[bi] and move bi only on a strict improvement (keeps the first max).",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int bi = 0;\n        for (int i = 1; i < n; i++) {\n            ____\n        }\n        System.out.println(bi);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int bi = 0;\n        for (int i = 1; i < n; i++) {\n            if (a[i] > a[bi]) bi = i;\n        }\n        System.out.println(bi);\n    }\n}\n",
        "tests": [
          {
            "input": "4\n3 9 2 7",
            "output": "1"
          },
          {
            "input": "3\n5 5 1",
            "output": "0"
          },
          {
            "input": "5\n1 2 3 2 3",
            "output": "2"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "hashing",
    "name": "Hash Maps & Sets",
    "category": "Data Structures",
    "what": "Structures giving average O(1) insertion and lookup, trading memory for speed.",
    "deep": "A hash table answers 'have I seen X?' or 'what maps to X?' in expected constant time, converting many O(n^2) pair scans into O(n) single passes. Worst case is O(n) per op under adversarial hashing, but that is rare in practice.",
    "java": "HashMap<K,V> and HashSet<E>. Use getOrDefault(k, 0) and merge(k, 1, Integer::sum) for counting, containsKey/add for membership. For dense int keys, an int[] beats a HashMap.",
    "language": "java",
    "lesson": "### Worked example\nDoes the array have a duplicate? Track seen values in a set; if one appears twice, yes.\n\n### In code (Java)\n```java\nint[] a = {1, 3, 2, 3};\nSet<Integer> seen = new HashSet<>();\nfor (int x : a) {\n    if (!seen.add(x)) { System.out.println(\"duplicate\"); break; }\n}\n```\n\n### Watch out for\n- `add` returns false if the value was already present — a tidy one-line check.\n- To count occurrences use `map.merge(key, 1, Integer::sum)` or `getOrDefault`.\n",
    "exercises": [
      {
        "id": "hashing-dup",
        "title": "Spot a duplicate",
        "prompt": "Replace `____` with the condition that is true when `x` was ALREADY in the set (`add` returns false in that case).",
        "hint": "seen.add(x) returns false when x is already present, so !seen.add(x) detects a repeat.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        Set<Integer> seen = new HashSet<>();\n        boolean dup = false;\n        for (int i = 0; i < n; i++) {\n            int x = sc.nextInt();\n            if (____) { dup = true; break; }\n        }\n        System.out.println(dup ? \"duplicate\" : \"unique\");\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        Set<Integer> seen = new HashSet<>();\n        boolean dup = false;\n        for (int i = 0; i < n; i++) {\n            int x = sc.nextInt();\n            if (!seen.add(x)) { dup = true; break; }\n        }\n        System.out.println(dup ? \"duplicate\" : \"unique\");\n    }\n}\n",
        "tests": [
          {
            "input": "4\n1 3 2 3",
            "output": "duplicate"
          },
          {
            "input": "3\n1 2 3",
            "output": "unique"
          },
          {
            "input": "2\n5 5",
            "output": "duplicate"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "hashing-freq",
        "title": "Highest frequency",
        "prompt": "Replace `____` with the line that increments the count of `x` in the map (creating it at 1 the first time).",
        "hint": "map.merge(x, 1, Integer::sum) adds 1 to x's count, starting from 1.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        Map<Integer,Integer> freq = new HashMap<>();\n        for (int i = 0; i < n; i++) {\n            int x = sc.nextInt();\n            ____\n        }\n        int best = 0;\n        for (int c : freq.values()) best = Math.max(best, c);\n        System.out.println(best);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        Map<Integer,Integer> freq = new HashMap<>();\n        for (int i = 0; i < n; i++) {\n            int x = sc.nextInt();\n            freq.merge(x, 1, Integer::sum);\n        }\n        int best = 0;\n        for (int c : freq.values()) best = Math.max(best, c);\n        System.out.println(best);\n    }\n}\n",
        "tests": [
          {
            "input": "4\n1 2 2 2",
            "output": "3"
          },
          {
            "input": "3\n5 6 7",
            "output": "1"
          },
          {
            "input": "5\n1 1 2 2 2",
            "output": "3"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "hashing-firstunique",
        "title": "First non-repeating value",
        "prompt": "Write the solution: count every value, then scan the array IN ORDER and print the first value whose count is 1 (or `-1` if none).",
        "hint": "First build the counts, then walk the original order and stop at the first count of 1.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        Map<Integer,Integer> cnt = new HashMap<>();\n        for (int i = 0; i < n; i++) {\n            a[i] = sc.nextInt();\n            cnt.merge(a[i], 1, Integer::sum);\n        }\n____\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        Map<Integer,Integer> cnt = new HashMap<>();\n        for (int i = 0; i < n; i++) {\n            a[i] = sc.nextInt();\n            cnt.merge(a[i], 1, Integer::sum);\n        }\n        int ans = -1;\n        for (int x : a) {\n            if (cnt.get(x) == 1) { ans = x; break; }\n        }\n        System.out.println(ans);\n    }\n}\n",
        "tests": [
          {
            "input": "5\n1 2 2 3 1",
            "output": "3"
          },
          {
            "input": "3\n1 1 2",
            "output": "2"
          },
          {
            "input": "2\n4 4",
            "output": "-1"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "complement",
    "name": "Complement Thinking",
    "category": "Data Structures",
    "what": "Instead of testing all pairs, ask what single value would complete the current one, and look it up.",
    "deep": "Reframes 'find two things that combine to T' as 'for each x, does T - x already exist?'. Paired with a hash structure this removes an entire nested loop, and the idea generalizes to differences, XORs, and remainders.",
    "java": "Keep a HashSet<Integer> or HashMap<Integer,Integer> of seen values and query target - x before inserting x.",
    "language": "java",
    "lesson": "### Worked example\nDo two values in `[2, 7, 4, 8]` sum to 10? For 2 you need 8; store values as you go and check for the partner.\n\n### In code (Java)\n```java\nint[] a = {2, 7, 4, 8}; int target = 10;\nSet<Integer> seen = new HashSet<>();\nfor (int x : a) {\n    if (seen.contains(target - x)) { System.out.println(\"found\"); break; }\n    seen.add(x);\n}\n```\n\n### Watch out for\n- Check for the complement **before** inserting the current value.\n- Each lookup is O(1), so the whole scan is O(n).\n",
    "exercises": [
      {
        "id": "complement-value",
        "title": "The missing partner",
        "prompt": "For a pair summing to `target`, the partner of `x` is `target - x`. Replace `____` with that complement.",
        "hint": "If x + partner = target, then partner = target - x.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int target = sc.nextInt();\n        int x = sc.nextInt();\n        System.out.println(____);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int target = sc.nextInt();\n        int x = sc.nextInt();\n        System.out.println(target - x);\n    }\n}\n",
        "tests": [
          {
            "input": "10 3",
            "output": "7"
          },
          {
            "input": "5 5",
            "output": "0"
          },
          {
            "input": "8 10",
            "output": "-2"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "complement-twosum",
        "title": "Does a pair sum to target?",
        "prompt": "Replace `____` with the lookup that checks whether the partner of `x` (its complement) has already been seen.",
        "hint": "Check for target - x BEFORE inserting x, so a value is never paired with itself.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int target = sc.nextInt();\n        int n = sc.nextInt();\n        Set<Integer> seen = new HashSet<>();\n        boolean found = false;\n        for (int i = 0; i < n; i++) {\n            int x = sc.nextInt();\n            if (____) { found = true; break; }\n            seen.add(x);\n        }\n        System.out.println(found ? \"found\" : \"none\");\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int target = sc.nextInt();\n        int n = sc.nextInt();\n        Set<Integer> seen = new HashSet<>();\n        boolean found = false;\n        for (int i = 0; i < n; i++) {\n            int x = sc.nextInt();\n            if (seen.contains(target - x)) { found = true; break; }\n            seen.add(x);\n        }\n        System.out.println(found ? \"found\" : \"none\");\n    }\n}\n",
        "tests": [
          {
            "input": "10 4\n2 7 4 8",
            "output": "found"
          },
          {
            "input": "5 3\n1 2 9",
            "output": "none"
          },
          {
            "input": "6 2\n3 3",
            "output": "found"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "complement-countpairs",
        "title": "Count pairs that sum to target",
        "prompt": "Write the solution: for each value add how many earlier values equal its complement, then record the value. Print the total number of pairs.",
        "hint": "Each new x forms a pair with every earlier complement; getOrDefault handles the missing case.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int target = sc.nextInt();\n        int n = sc.nextInt();\n        Map<Integer,Integer> seen = new HashMap<>();\n____\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int target = sc.nextInt();\n        int n = sc.nextInt();\n        Map<Integer,Integer> seen = new HashMap<>();\n        int pairs = 0;\n        for (int i = 0; i < n; i++) {\n            int x = sc.nextInt();\n            pairs += seen.getOrDefault(target - x, 0);\n            seen.merge(x, 1, Integer::sum);\n        }\n        System.out.println(pairs);\n    }\n}\n",
        "tests": [
          {
            "input": "6 4\n1 5 3 3",
            "output": "2"
          },
          {
            "input": "10 3\n2 7 4",
            "output": "0"
          },
          {
            "input": "4 4\n2 2 2 2",
            "output": "6"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "two_pointers",
    "name": "Two Pointers",
    "category": "Arrays",
    "what": "Two indices moving through a sequence (from both ends, or at different speeds) to replace a nested loop.",
    "deep": "By advancing pointers based on a comparison you explore O(n) states instead of O(n^2). Common shapes are opposite ends converging (sorted pair sums, palindromes, rain water) and fast/slow (cycle detection). Correctness hinges on why the pointer you move cannot skip a better answer.",
    "java": "Two int indices lo/hi (or i/j) in a while (lo < hi) loop; no special class needed.",
    "language": "java",
    "lesson": "### When to reach for this\nTwo signals point here. **(1)** The array is **sorted** and you need a pair/triple (sum to T, closest, most water) → converge from both ends. **(2)** You must **remove / move / dedupe in place** → a slow *write* pointer trails a fast *read* pointer.\n\n### Simulated solve — does a pair in sorted `[1, 2, 4, 7, 11]` sum to 9?\nLook at the two ends. Too big? lower the high end. Too small? raise the low end.\n\n| lo | hi | a[lo]+a[hi] | vs 9 | move |\n| -- | -- | ----------- | ---- | ---- |\n| 0 | 4 | 1 + 11 = 12 | too big | hi-- |\n| 0 | 3 | 1 + 7 = 8 | too small | lo++ |\n| 1 | 3 | 2 + 7 = 9 | equal ✓ | **found** |\n\nAnswer: **yes** (2 + 7). Each pointer only moves inward, so it is O(n).\n\n### In code (Java)\n```java\nint lo = 0, hi = n - 1;                 // converging variant (sorted input)\nwhile (lo < hi) {\n    int s = a[lo] + a[hi];\n    if (s == target) return true;\n    else if (s < target) lo++;          // need bigger → raise the low end\n    else hi--;                          // need smaller → lower the high end\n}\n\nint w = 0;                              // slow/fast variant (in-place filter)\nfor (int r = 0; r < n; r++)\n    if (a[r] != 0) a[w++] = a[r];       // keep non-zeros, in order\n```\n\n### Watch out for\n- The converging variant needs a **sorted** array — sort first if it isn't.\n- The slow/fast variant preserves order: only kept elements advance `w`.\n- The loop ends when `lo >= hi`; a lone middle element needs no test.\n",
    "exercises": [
      {
        "id": "twoptr-pair-sum",
        "title": "Pair sum in a sorted array",
        "prompt": "The array is **sorted**. Decide whether any two elements sum to `target`, converging from both ends. Replace `____` with the move when the current sum is too small.",
        "hint": "Too small a sum? You need a bigger value, so raise the low pointer: lo++.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int target = sc.nextInt();\n        int lo = 0, hi = n - 1;\n        boolean found = false;\n        while (lo < hi) {\n            int s = a[lo] + a[hi];\n            if (s == target) { found = true; break; }\n            ____\n            else hi--;\n        }\n        System.out.println(found ? \"yes\" : \"no\");\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int target = sc.nextInt();\n        int lo = 0, hi = n - 1;\n        boolean found = false;\n        while (lo < hi) {\n            int s = a[lo] + a[hi];\n            if (s == target) { found = true; break; }\n            else if (s < target) lo++;\n            else hi--;\n        }\n        System.out.println(found ? \"yes\" : \"no\");\n    }\n}\n",
        "tests": [
          {
            "input": "5\n1 2 4 7 11\n9",
            "output": "yes"
          },
          {
            "input": "4\n1 2 3 4\n100",
            "output": "no"
          },
          {
            "input": "3\n-3 0 3\n0",
            "output": "yes"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "twoptr-palindrome",
        "title": "Is the array a palindrome?",
        "prompt": "Compare elements from both ends moving inward. Replace `____` with the mismatch check that fails fast.",
        "hint": "If the mirrored pair differs, it is not a palindrome: if (a[i] != a[j]) ...",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int i = 0, j = n - 1;\n        boolean ok = true;\n        while (i < j) {\n            ____\n            i++;\n            j--;\n        }\n        System.out.println(ok);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int i = 0, j = n - 1;\n        boolean ok = true;\n        while (i < j) {\n            if (a[i] != a[j]) { ok = false; break; }\n            i++;\n            j--;\n        }\n        System.out.println(ok);\n    }\n}\n",
        "tests": [
          {
            "input": "3\n1 2 1",
            "output": "true"
          },
          {
            "input": "4\n1 2 2 1",
            "output": "true"
          },
          {
            "input": "3\n1 2 3",
            "output": "false"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "twoptr-move-zeros",
        "title": "Move zeros to the end",
        "prompt": "Push every non-zero to the front in order, then pad with zeros — the slow/fast write pattern. Replace `____` so kept elements are written at `w`.",
        "hint": "Write only non-zeros and advance the write index: a[w] = a[r]; w++.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int w = 0;\n        for (int r = 0; r < n; r++) {\n            ____\n        }\n        while (w < n) { a[w] = 0; w++; }\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < n; i++) { sb.append(a[i]); if (i < n - 1) sb.append(' '); }\n        System.out.println(sb.toString());\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int w = 0;\n        for (int r = 0; r < n; r++) {\n            if (a[r] != 0) { a[w] = a[r]; w++; }\n        }\n        while (w < n) { a[w] = 0; w++; }\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < n; i++) { sb.append(a[i]); if (i < n - 1) sb.append(' '); }\n        System.out.println(sb.toString());\n    }\n}\n",
        "tests": [
          {
            "input": "5\n0 1 0 3 12",
            "output": "1 3 12 0 0"
          },
          {
            "input": "3\n0 0 0",
            "output": "0 0 0"
          },
          {
            "input": "3\n1 2 3",
            "output": "1 2 3"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "twoptr-dedupe-sorted",
        "title": "Count uniques in a sorted array",
        "prompt": "The array is sorted, so duplicates are adjacent. Replace `____` with the test that keeps only the first copy of each value; print the unique count `w`.",
        "hint": "Keep a[r] when it is the first element or differs from its predecessor.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int w = 0;\n        for (int r = 0; r < n; r++) {\n            ____ { a[w] = a[r]; w++; }\n        }\n        System.out.println(w);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int w = 0;\n        for (int r = 0; r < n; r++) {\n            if (r == 0 || a[r] != a[r - 1]) { a[w] = a[r]; w++; }\n        }\n        System.out.println(w);\n    }\n}\n",
        "tests": [
          {
            "input": "5\n1 1 2 2 3",
            "output": "3"
          },
          {
            "input": "4\n1 2 3 4",
            "output": "4"
          },
          {
            "input": "3\n5 5 5",
            "output": "1"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "sliding_window",
    "name": "Sliding Window",
    "category": "Arrays",
    "what": "A moving sub-range [left, right] whose bounds expand and contract to maintain an invariant.",
    "deep": "Turns 'longest/best sub-array meeting a condition' into an amortized O(n) scan: extend the right edge, and when the invariant breaks advance the left edge. Each index enters and leaves the window at most once, which is why it stays linear.",
    "java": "Two int pointers plus a HashMap<Character,Integer> or an int[128] frequency table to track window contents.",
    "language": "java",
    "lesson": "### When to reach for this\nSignals: **\"longest / shortest\"** subarray or substring meeting a condition, or a window of **fixed size k**. Extend the right edge; when the invariant breaks (or the window overflows size k), advance the left edge. Each index enters and leaves once → O(n).\n\n### Simulated solve — max sum of a size-2 window in `[1, 3, -1, 5, 2]`\nSlide by **adding the element that enters** and **subtracting the one that leaves**.\n\n| r | enters | leaves | window sum | best |\n| - | ------ | ------ | ---------- | ---- |\n| 1 | 1+3 (init) | — | 4 | 4 |\n| 2 | -1 | 1 | 2 | 4 |\n| 3 | 5 | 3 | 4 | 4 |\n| 4 | 2 | -1 | 7 | **7** |\n\nAnswer: **7** — the window `[5, 2]`.\n\n### In code (Java) — variable window (longest with sum ≤ limit)\n```java\nint left = 0; long cur = 0; int best = 0;\nfor (int r = 0; r < n; r++) {\n    cur += a[r];                             // extend right\n    while (cur > limit) cur -= a[left++];    // shrink until valid again\n    best = Math.max(best, r - left + 1);\n}\n```\n\n### Watch out for\n- The inner `while` looks quadratic but is not: `left` only ever moves forward → O(n) total.\n- Variable-size windows assume **non-negative** values, so shrinking always helps.\n- Fixed size k? Skip the `while`; just do `cur += a[r] - a[r - k]` once `r >= k`.\n",
    "exercises": [
      {
        "id": "window-maxsum-k",
        "title": "Max sum of a size-k window",
        "prompt": "Fixed window: after the first `k` elements, slide by adding the entering element and dropping the one that left. Replace `____` with that slide step.",
        "hint": "Add the new element, subtract the one k positions back: sum += a[r] - a[r-k].",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int k = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        long sum = 0;\n        for (int i = 0; i < k; i++) sum += a[i];\n        long best = sum;\n        for (int r = k; r < n; r++) {\n            ____\n            best = Math.max(best, sum);\n        }\n        System.out.println(best);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int k = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        long sum = 0;\n        for (int i = 0; i < k; i++) sum += a[i];\n        long best = sum;\n        for (int r = k; r < n; r++) {\n            sum += a[r] - a[r - k];\n            best = Math.max(best, sum);\n        }\n        System.out.println(best);\n    }\n}\n",
        "tests": [
          {
            "input": "5 2\n1 3 -1 5 2",
            "output": "7"
          },
          {
            "input": "3 3\n1 2 3",
            "output": "6"
          },
          {
            "input": "4 1\n4 1 3 2",
            "output": "4"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "window-longest-atmost",
        "title": "Longest window with sum ≤ S",
        "prompt": "Values are non-negative. Grow the window right; when the sum exceeds `S`, shrink from the left. Replace `____` with the shrink loop.",
        "hint": "Shrink while the invariant is broken: while (sum > S) { sum -= a[left]; left++; }",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        long S = sc.nextLong();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        long sum = 0; int left = 0, best = 0;\n        for (int r = 0; r < n; r++) {\n            sum += a[r];\n            ____\n            best = Math.max(best, r - left + 1);\n        }\n        System.out.println(best);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        long S = sc.nextLong();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        long sum = 0; int left = 0, best = 0;\n        for (int r = 0; r < n; r++) {\n            sum += a[r];\n            while (sum > S) { sum -= a[left]; left++; }\n            best = Math.max(best, r - left + 1);\n        }\n        System.out.println(best);\n    }\n}\n",
        "tests": [
          {
            "input": "5 3\n2 1 1 1 1",
            "output": "3"
          },
          {
            "input": "3 100\n1 2 3",
            "output": "3"
          },
          {
            "input": "3 0\n1 2 3",
            "output": "0"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "window-min-atleast",
        "title": "Shortest window with sum ≥ S",
        "prompt": "Non-negative values. Whenever the window sum reaches `S`, record its length and shrink. Replace `____` with the length update. Print 0 if impossible.",
        "hint": "Window length is r - left + 1; keep the smallest that still satisfies sum ≥ S.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        long S = sc.nextLong();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        long sum = 0; int left = 0, best = Integer.MAX_VALUE;\n        for (int r = 0; r < n; r++) {\n            sum += a[r];\n            while (sum >= S) {\n                ____\n                sum -= a[left];\n                left++;\n            }\n        }\n        System.out.println(best == Integer.MAX_VALUE ? 0 : best);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        long S = sc.nextLong();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        long sum = 0; int left = 0, best = Integer.MAX_VALUE;\n        for (int r = 0; r < n; r++) {\n            sum += a[r];\n            while (sum >= S) {\n                best = Math.min(best, r - left + 1);\n                sum -= a[left];\n                left++;\n            }\n        }\n        System.out.println(best == Integer.MAX_VALUE ? 0 : best);\n    }\n}\n",
        "tests": [
          {
            "input": "6 7\n2 3 1 2 4 3",
            "output": "2"
          },
          {
            "input": "3 100\n1 2 3",
            "output": "0"
          },
          {
            "input": "3 6\n1 2 3",
            "output": "3"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "stack",
    "name": "Stack (LIFO)",
    "category": "Data Structures",
    "what": "A last-in-first-out structure whose top is always the most recent unmatched item.",
    "deep": "Ideal when the newest open item must be resolved first: bracket matching, monotonic-stack range queries, expression parsing, or iterative DFS. Push, pop, and peek are all O(1).",
    "java": "Use ArrayDeque<T> as a stack (push/pop/peek) - it is faster than the legacy Stack class. For brackets use Deque<Character>.",
    "language": "java",
    "lesson": "### Worked example\nAre the brackets in `\"([])\"` balanced? Push openers; each closer must match the top of the stack.\n\n### In code (Java)\n```java\nDeque<Character> st = new ArrayDeque<>();\nMap<Character,Character> match = Map.of(')', '(', ']', '[', '}', '{');\nboolean ok = true;\nfor (char c : \"([])\".toCharArray()) {\n    if (c == '(' || c == '[' || c == '{') st.push(c);\n    else if (st.isEmpty() || st.pop() != match.get(c)) { ok = false; break; }\n}\nok = ok && st.isEmpty();\n```\n\n### Watch out for\n- Always check `isEmpty()` before `pop()`.\n- Use `ArrayDeque` as your stack, not the legacy `Stack` class.\n",
    "exercises": [
      {
        "id": "stack-push",
        "title": "Push the openers",
        "prompt": "Replace `____` with the statement that pushes an opening bracket onto the stack.",
        "hint": "st.push(c) puts the opener on top; a closer later pops it back off.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String s = sc.next();\n        Deque<Character> st = new ArrayDeque<>();\n        boolean ok = true;\n        for (char c : s.toCharArray()) {\n            if (c == '(') ____\n            else if (st.isEmpty()) { ok = false; break; }\n            else st.pop();\n        }\n        System.out.println(ok && st.isEmpty());\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String s = sc.next();\n        Deque<Character> st = new ArrayDeque<>();\n        boolean ok = true;\n        for (char c : s.toCharArray()) {\n            if (c == '(') st.push(c);\n            else if (st.isEmpty()) { ok = false; break; }\n            else st.pop();\n        }\n        System.out.println(ok && st.isEmpty());\n    }\n}\n",
        "tests": [
          {
            "input": "(())",
            "output": "true"
          },
          {
            "input": "(()",
            "output": "false"
          },
          {
            "input": "())",
            "output": "false"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "stack-reverse",
        "title": "Reverse with a stack",
        "prompt": "Replace `____` to push each number onto the stack as it is read, so popping them prints the list reversed.",
        "hint": "A stack is last-in-first-out, so pushing in order and popping reverses it.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        Deque<Integer> st = new ArrayDeque<>();\n        for (int i = 0; i < n; i++) ____;\n        StringBuilder sb = new StringBuilder();\n        while (!st.isEmpty()) sb.append(st.pop()).append(' ');\n        System.out.println(sb.toString().trim());\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        Deque<Integer> st = new ArrayDeque<>();\n        for (int i = 0; i < n; i++) st.push(sc.nextInt());\n        StringBuilder sb = new StringBuilder();\n        while (!st.isEmpty()) sb.append(st.pop()).append(' ');\n        System.out.println(sb.toString().trim());\n    }\n}\n",
        "tests": [
          {
            "input": "3\n1 2 3",
            "output": "3 2 1"
          },
          {
            "input": "1\n5",
            "output": "5"
          },
          {
            "input": "4\n1 2 3 4",
            "output": "4 3 2 1"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "stack-brackets",
        "title": "Match three bracket types",
        "prompt": "Write the solution: verify `( ) [ ] { }` are balanced and properly nested. Push openers; each closer must pop its matching opener. Print `true`/`false`.",
        "hint": "On a closer, the top of the stack must be its matching opener; the stack must end empty.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String s = sc.next();\n____\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String s = sc.next();\n        Deque<Character> st = new ArrayDeque<>();\n        Map<Character,Character> match = Map.of(')', '(', ']', '[', '}', '{');\n        boolean ok = true;\n        for (char c : s.toCharArray()) {\n            if (c == '(' || c == '[' || c == '{') st.push(c);\n            else if (st.isEmpty() || st.pop() != match.get(c)) { ok = false; break; }\n        }\n        System.out.println(ok && st.isEmpty());\n    }\n}\n",
        "tests": [
          {
            "input": "([])",
            "output": "true"
          },
          {
            "input": "([)]",
            "output": "false"
          },
          {
            "input": "{[()]}",
            "output": "true"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "recursion",
    "name": "Recursion",
    "category": "Recursion & DP",
    "what": "A function that calls itself on smaller inputs, with base cases that stop the descent.",
    "deep": "Expresses divide-and-conquer and tree/graph walks naturally. Watch the recursion depth against stack limits, and whether subproblems overlap - if they do, add memoization to avoid exponential blow-up.",
    "java": "A private static helper that calls itself. The JVM stack handles ~10^4 depth; convert to an explicit ArrayDeque if you risk StackOverflowError.",
    "language": "java",
    "lesson": "### Worked example\nFactorial: `fact(4) = 4 * fact(3) = ... = 24`. The base case `fact(0) = 1` stops the descent.\n\n### In code (Java)\n```java\nstatic long fact(int n) {\n    if (n <= 1) return 1;      // base case\n    return n * fact(n - 1);    // smaller subproblem\n}\n```\n\n### Watch out for\n- Every recursion needs a base case, or it never stops (StackOverflowError).\n- If the same subproblem recurs, cache results (memoization) to avoid exponential work.\n",
    "exercises": [
      {
        "id": "recursion-factorial",
        "title": "Factorial",
        "prompt": "Replace `____` with the recursive step: `n` times the factorial of `n - 1`.",
        "hint": "fact(n) = n * fact(n - 1); the base case n <= 1 stops the descent.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    static long fact(int n) {\n        if (n <= 1) return 1;\n        ____\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        System.out.println(fact(n));\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    static long fact(int n) {\n        if (n <= 1) return 1;\n        return n * fact(n - 1);\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        System.out.println(fact(n));\n    }\n}\n",
        "tests": [
          {
            "input": "4",
            "output": "24"
          },
          {
            "input": "0",
            "output": "1"
          },
          {
            "input": "5",
            "output": "120"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "recursion-basecase",
        "title": "The base case",
        "prompt": "Replace `____` with the base-case condition that stops the recursion (sum of 0 is 0).",
        "hint": "Without a base case the calls never stop; sum(0) should return 0.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    static int sum(int n) {\n        if (____) return 0;\n        return n + sum(n - 1);\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        System.out.println(sum(sc.nextInt()));\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    static int sum(int n) {\n        if (n == 0) return 0;\n        return n + sum(n - 1);\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        System.out.println(sum(sc.nextInt()));\n    }\n}\n",
        "tests": [
          {
            "input": "5",
            "output": "15"
          },
          {
            "input": "1",
            "output": "1"
          },
          {
            "input": "10",
            "output": "55"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "recursion-fib",
        "title": "Nth Fibonacci",
        "prompt": "Write the recursive `fib` (fib(0)=0, fib(1)=1). Fill in both the base case and the recursive step.",
        "hint": "Below 2, fib(n) is n itself; otherwise sum the two previous Fibonacci numbers.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    static long fib(int n) {\n____\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        System.out.println(fib(sc.nextInt()));\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    static long fib(int n) {\n        if (n < 2) return n;\n        return fib(n - 1) + fib(n - 2);\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        System.out.println(fib(sc.nextInt()));\n    }\n}\n",
        "tests": [
          {
            "input": "0",
            "output": "0"
          },
          {
            "input": "7",
            "output": "13"
          },
          {
            "input": "10",
            "output": "55"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "sorting",
    "name": "Sorting",
    "category": "Searching & Sorting",
    "what": "Ordering elements (usually O(n log n)) so structure like duplicates and order statistics becomes easy.",
    "deep": "Sorting is a preprocessing multiplier: it enables binary search, two-pointer sweeps, greedy choices, and grouping. The cost is O(n log n) comparisons, though counting/radix sort can beat that for bounded integer keys.",
    "java": "Arrays.sort(int[]) or Arrays.sort(T[], Comparator) / Collections.sort. For custom order use Comparator.comparingInt(...). Note primitive sort has no comparator overload.",
    "language": "java",
    "lesson": "### Worked example\nSorting `[3, 1, 2]` gives `[1, 2, 3]`, after which smallest/largest and neighbours are trivial to read.\n\n### In code (Java)\n```java\nint[] a = {3, 1, 2};\nArrays.sort(a);                 // [1, 2, 3]\n\nInteger[] b = {3, 1, 2};\nArrays.sort(b, Comparator.reverseOrder()); // [3, 2, 1]\n```\n\n### Watch out for\n- Primitive `int[]` sort has no comparator; use `Integer[]` for custom order.\n- Sorting is O(n log n) — often the step that unlocks an otherwise-hard problem.\n",
    "exercises": [
      {
        "id": "sorting-asc",
        "title": "Sort ascending",
        "prompt": "Replace `____` with the call that sorts the primitive array `a` in ascending order.",
        "hint": "Arrays.sort(a) sorts an int[] ascending in place.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        ____\n        StringBuilder sb = new StringBuilder();\n        for (int x : a) sb.append(x).append(' ');\n        System.out.println(sb.toString().trim());\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        Arrays.sort(a);\n        StringBuilder sb = new StringBuilder();\n        for (int x : a) sb.append(x).append(' ');\n        System.out.println(sb.toString().trim());\n    }\n}\n",
        "tests": [
          {
            "input": "3\n3 1 2",
            "output": "1 2 3"
          },
          {
            "input": "1\n5",
            "output": "5"
          },
          {
            "input": "5\n5 4 3 2 1",
            "output": "1 2 3 4 5"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "sorting-desc",
        "title": "Sort descending",
        "prompt": "Primitive `int[]` has no comparator, so this uses `Integer[]`. Replace `____` with the comparator that sorts largest-first.",
        "hint": "Comparator.reverseOrder() gives descending order (needs Integer[], not int[]).",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        Integer[] a = new Integer[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        Arrays.sort(a, ____);\n        StringBuilder sb = new StringBuilder();\n        for (int x : a) sb.append(x).append(' ');\n        System.out.println(sb.toString().trim());\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        Integer[] a = new Integer[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        Arrays.sort(a, Comparator.reverseOrder());\n        StringBuilder sb = new StringBuilder();\n        for (int x : a) sb.append(x).append(' ');\n        System.out.println(sb.toString().trim());\n    }\n}\n",
        "tests": [
          {
            "input": "3\n1 3 2",
            "output": "3 2 1"
          },
          {
            "input": "1\n5",
            "output": "5"
          },
          {
            "input": "4\n4 1 3 2",
            "output": "4 3 2 1"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "sorting-kth",
        "title": "Kth smallest",
        "prompt": "Write the solution: read `n` and `k`, then `n` numbers; sort them and print the k-th smallest (1-indexed).",
        "hint": "After sorting ascending, the k-th smallest sits at index k - 1.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int k = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n____\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int k = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        Arrays.sort(a);\n        System.out.println(a[k - 1]);\n    }\n}\n",
        "tests": [
          {
            "input": "5 2\n5 1 4 2 3",
            "output": "2"
          },
          {
            "input": "3 1\n9 7 8",
            "output": "7"
          },
          {
            "input": "4 4\n1 2 3 4",
            "output": "4"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "binary_search",
    "name": "Binary Search",
    "category": "Searching & Sorting",
    "what": "Halving a sorted search space to find a target or boundary in O(log n).",
    "deep": "Beyond exact matches it locates boundaries (first/last occurrence, lower/upper bound) and can 'search on the answer' for any monotonic predicate. The subtle parts are the loop invariant and avoiding off-by-one at the boundary.",
    "java": "Arrays.binarySearch does not give first/last, so hand-roll while (lo <= hi) with mid = lo + (hi - lo) / 2 to avoid overflow.",
    "language": "java",
    "lesson": "### Worked example\nFind 7 in sorted `[1, 3, 5, 7, 9]`. Check the middle (5), go right, check 7 — found in O(log n).\n\n### In code (Java)\n```java\nint[] a = {1,3,5,7,9}; int target = 7, lo = 0, hi = a.length - 1, ans = -1;\nwhile (lo <= hi) {\n    int mid = lo + (hi - lo) / 2;\n    if (a[mid] == target) { ans = mid; break; }\n    else if (a[mid] < target) lo = mid + 1;\n    else hi = mid - 1;\n}\n```\n\n### Watch out for\n- Use `lo + (hi - lo) / 2` to avoid overflow on large indices.\n- For the *first* match, keep searching left after a hit instead of stopping.\n",
    "exercises": [
      {
        "id": "binary_search-mid",
        "title": "The midpoint",
        "prompt": "Replace `____` with the overflow-safe midpoint of `lo` and `hi`.",
        "hint": "lo + (hi - lo) / 2 is the midpoint without the overflow risk of (lo + hi) / 2.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int target = sc.nextInt();\n        int lo = 0, hi = n - 1, ans = -1;\n        while (lo <= hi) {\n            int mid = ____;\n            if (a[mid] == target) { ans = mid; break; }\n            else if (a[mid] < target) lo = mid + 1;\n            else hi = mid - 1;\n        }\n        System.out.println(ans);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int target = sc.nextInt();\n        int lo = 0, hi = n - 1, ans = -1;\n        while (lo <= hi) {\n            int mid = lo + (hi - lo) / 2;\n            if (a[mid] == target) { ans = mid; break; }\n            else if (a[mid] < target) lo = mid + 1;\n            else hi = mid - 1;\n        }\n        System.out.println(ans);\n    }\n}\n",
        "tests": [
          {
            "input": "5\n1 3 5 7 9\n7",
            "output": "3"
          },
          {
            "input": "5\n1 3 5 7 9\n4",
            "output": "-1"
          },
          {
            "input": "3\n2 4 6\n2",
            "output": "0"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "binary_search-loop",
        "title": "The loop condition",
        "prompt": "Replace `____` with the condition that keeps the search running while the window `[lo, hi]` is non-empty.",
        "hint": "Keep going while lo <= hi; once lo passes hi the window is empty.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int target = sc.nextInt();\n        int lo = 0, hi = n - 1;\n        boolean found = false;\n        while (____) {\n            int mid = lo + (hi - lo) / 2;\n            if (a[mid] == target) { found = true; break; }\n            else if (a[mid] < target) lo = mid + 1;\n            else hi = mid - 1;\n        }\n        System.out.println(found);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int target = sc.nextInt();\n        int lo = 0, hi = n - 1;\n        boolean found = false;\n        while (lo <= hi) {\n            int mid = lo + (hi - lo) / 2;\n            if (a[mid] == target) { found = true; break; }\n            else if (a[mid] < target) lo = mid + 1;\n            else hi = mid - 1;\n        }\n        System.out.println(found);\n    }\n}\n",
        "tests": [
          {
            "input": "5\n1 3 5 7 9\n5",
            "output": "true"
          },
          {
            "input": "4\n2 4 6 8\n3",
            "output": "false"
          },
          {
            "input": "3\n1 2 3\n3",
            "output": "true"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "binary_search-first",
        "title": "First occurrence",
        "prompt": "The sorted array may contain duplicates. Write the solution: find the FIRST index equal to `target` (keep searching left after a hit), or print `-1`.",
        "hint": "On a match, record it but move hi left (hi = mid - 1) to look for an earlier one.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int target = sc.nextInt();\n____\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int target = sc.nextInt();\n        int lo = 0, hi = n - 1, ans = -1;\n        while (lo <= hi) {\n            int mid = lo + (hi - lo) / 2;\n            if (a[mid] == target) { ans = mid; hi = mid - 1; }\n            else if (a[mid] < target) lo = mid + 1;\n            else hi = mid - 1;\n        }\n        System.out.println(ans);\n    }\n}\n",
        "tests": [
          {
            "input": "6\n1 2 2 2 3 4\n2",
            "output": "1"
          },
          {
            "input": "5\n1 2 3 4 5\n6",
            "output": "-1"
          },
          {
            "input": "4\n2 2 2 2\n2",
            "output": "0"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "dp",
    "name": "Dynamic Programming",
    "category": "Recursion & DP",
    "what": "Breaking a problem into overlapping subproblems and reusing their answers.",
    "deep": "Applies when there is optimal substructure and overlapping subproblems. Two styles exist: top-down memoized recursion and bottom-up tabulation. Defining the state and transition precisely is the whole game, and space can often be rolled down a dimension.",
    "java": "A memo HashMap or an int[]/int[][] table. For memoization initialize a sentinel with Arrays.fill(dp, -1).",
    "language": "java",
    "lesson": "### Worked example\nWays to climb n stairs taking 1 or 2 steps: `ways(n) = ways(n-1) + ways(n-2)`. Build up from the bottom.\n\n### In code (Java)\n```java\nint n = 5;\nlong a = 1, b = 1;           // ways(0), ways(1)\nfor (int i = 2; i <= n; i++) { long c = a + b; a = b; b = c; }\nSystem.out.println(b);       // 8\n```\n\n### Watch out for\n- Identify the **state** and the **transition** first — the code follows.\n- Bottom-up with a couple of variables often replaces an exponential recursion.\n",
    "exercises": [
      {
        "id": "dp-stairs",
        "title": "Climbing stairs",
        "prompt": "Ways to climb `n` stairs (1 or 2 steps at a time) follow `ways(i) = ways(i-1) + ways(i-2)`. Replace `____` with that transition.",
        "hint": "Each step's ways is the sum of the previous two — carry them in a and b.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        long a = 1, b = 1;\n        for (int i = 2; i <= n; i++) {\n            ____\n            a = b;\n            b = c;\n        }\n        System.out.println(b);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        long a = 1, b = 1;\n        for (int i = 2; i <= n; i++) {\n            long c = a + b;\n            a = b;\n            b = c;\n        }\n        System.out.println(b);\n    }\n}\n",
        "tests": [
          {
            "input": "2",
            "output": "2"
          },
          {
            "input": "5",
            "output": "8"
          },
          {
            "input": "1",
            "output": "1"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "dp-robber",
        "title": "House robber",
        "prompt": "You can't take two adjacent values. `incl`/`excl` track the best if you take or skip the current house. Replace `____` with the new 'take' value (skip-the-previous plus this house).",
        "hint": "To take house x you must have skipped the previous one, so it's excl + x.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] v = new int[n];\n        for (int i = 0; i < n; i++) v[i] = sc.nextInt();\n        long incl = 0, excl = 0;\n        for (int x : v) {\n            ____\n            excl = Math.max(incl, excl);\n            incl = newIncl;\n        }\n        System.out.println(Math.max(incl, excl));\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] v = new int[n];\n        for (int i = 0; i < n; i++) v[i] = sc.nextInt();\n        long incl = 0, excl = 0;\n        for (int x : v) {\n            long newIncl = excl + x;\n            excl = Math.max(incl, excl);\n            incl = newIncl;\n        }\n        System.out.println(Math.max(incl, excl));\n    }\n}\n",
        "tests": [
          {
            "input": "4\n1 2 3 1",
            "output": "4"
          },
          {
            "input": "3\n2 7 9",
            "output": "11"
          },
          {
            "input": "1\n5",
            "output": "5"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "dp-coinchange",
        "title": "Coin change (fewest coins)",
        "prompt": "Write the solution: given an amount and a set of coin values, print the fewest coins that make the amount, or `-1` if impossible. Build `dp[a]` = fewest coins for amount `a`.",
        "hint": "dp[a] = min over coins c of dp[a - c] + 1; an untouched sentinel means 'impossible'.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int amount = sc.nextInt();\n        int m = sc.nextInt();\n        int[] coins = new int[m];\n        for (int i = 0; i < m; i++) coins[i] = sc.nextInt();\n____\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int amount = sc.nextInt();\n        int m = sc.nextInt();\n        int[] coins = new int[m];\n        for (int i = 0; i < m; i++) coins[i] = sc.nextInt();\n        int[] dp = new int[amount + 1];\n        Arrays.fill(dp, amount + 1);\n        dp[0] = 0;\n        for (int a = 1; a <= amount; a++) {\n            for (int c : coins) {\n                if (c <= a) dp[a] = Math.min(dp[a], dp[a - c] + 1);\n            }\n        }\n        System.out.println(dp[amount] > amount ? -1 : dp[amount]);\n    }\n}\n",
        "tests": [
          {
            "input": "11\n3\n1 2 5",
            "output": "3"
          },
          {
            "input": "3\n1\n2",
            "output": "-1"
          },
          {
            "input": "6\n2\n1 3",
            "output": "2"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "recurrence",
    "name": "Recurrence Relations",
    "category": "Recursion & DP",
    "what": "Expressing an answer in terms of smaller answers, e.g. f(n) = f(n-1) + f(n-2).",
    "deep": "A recurrence is the mathematical heart of many DP and divide-and-conquer solutions. Once you name the base cases and the transition you can either recurse with memoization or iterate bottom-up, often reducing space to a few rolling variables.",
    "java": "Iterate bottom-up with a few long variables, or memoize with a long[] dp. Use long when terms grow fast.",
    "language": "java",
    "lesson": "### Worked example\nFibonacci is a recurrence: each term sums the previous two — 0, 1, 1, 2, 3, 5, 8, ...\n\n### In code (Java)\n```java\nstatic long fib(int n) {\n    long a = 0, b = 1;\n    for (int i = 0; i < n; i++) { long c = a + b; a = b; b = c; }\n    return a;\n}\n```\n\n### Watch out for\n- Nail the base cases (`fib(0)=0`, `fib(1)=1`) before the transition.\n- Terms grow fast — use `long` to avoid overflow.\n",
    "exercises": [
      {
        "id": "recurrence-fib",
        "title": "Fibonacci",
        "prompt": "Fibonacci sums the previous two terms. Replace `____` with the next term `a + b`.",
        "hint": "Advance the pair (a, b) -> (b, a + b) n times; a holds fib(n).",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        long a = 0, b = 1;\n        for (int i = 0; i < n; i++) {\n            ____\n            a = b;\n            b = c;\n        }\n        System.out.println(a);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        long a = 0, b = 1;\n        for (int i = 0; i < n; i++) {\n            long c = a + b;\n            a = b;\n            b = c;\n        }\n        System.out.println(a);\n    }\n}\n",
        "tests": [
          {
            "input": "0",
            "output": "0"
          },
          {
            "input": "7",
            "output": "13"
          },
          {
            "input": "10",
            "output": "55"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "recurrence-tribonacci",
        "title": "Tribonacci",
        "prompt": "The tribonacci sequence (0, 1, 1, 2, 4, 7, 13, ...) sums the previous THREE terms. Replace `____` with that transition.",
        "hint": "Each term is the sum of the three before it; seed t[0..2] = 0, 1, 1.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        long[] t = new long[Math.max(n + 1, 3)];\n        t[0] = 0; t[1] = 1; t[2] = 1;\n        for (int i = 3; i <= n; i++) {\n            ____\n        }\n        System.out.println(t[n]);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        long[] t = new long[Math.max(n + 1, 3)];\n        t[0] = 0; t[1] = 1; t[2] = 1;\n        for (int i = 3; i <= n; i++) {\n            t[i] = t[i - 1] + t[i - 2] + t[i - 3];\n        }\n        System.out.println(t[n]);\n    }\n}\n",
        "tests": [
          {
            "input": "4",
            "output": "4"
          },
          {
            "input": "6",
            "output": "13"
          },
          {
            "input": "0",
            "output": "0"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "recurrence-custom",
        "title": "A custom recurrence",
        "prompt": "Solve `f(n) = 2*f(n-1) + 3*f(n-2)` with `f(0) = 1`, `f(1) = 2`. Write the solution: iterate bottom-up and print `f(n)`.",
        "hint": "Carry the two previous terms; each new term is 2*(previous) + 3*(one before that).",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n____\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        long a = 1, b = 2;\n        if (n == 0) { System.out.println(a); return; }\n        for (int i = 2; i <= n; i++) {\n            long c = 2 * b + 3 * a;\n            a = b;\n            b = c;\n        }\n        System.out.println(b);\n    }\n}\n",
        "tests": [
          {
            "input": "0",
            "output": "1"
          },
          {
            "input": "2",
            "output": "7"
          },
          {
            "input": "3",
            "output": "20"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "dp2d",
    "name": "2-D Dynamic Programming",
    "category": "Recursion & DP",
    "what": "A table dp[i][j] indexed by two dimensions, filled from smaller states.",
    "deep": "Used when the state needs two coordinates: prefixes of two sequences, positions in a grid, or index-plus-capacity. The fill order must respect dependencies, and many 2-D DPs compress to one row because dp[i] depends only on dp[i-1].",
    "java": "int[][] dp = new int[m + 1][n + 1] filled row by row; compress to two int[] rows (prev/cur) to save memory.",
    "language": "java",
    "lesson": "### Worked example\nEdit distance between `\"ab\"` and `\"abc\"` is 1 (insert `c`). A table `dp[i][j]` compares the first i and j characters.\n\n### In code (Java)\n```java\nString a = \"ab\", b = \"abc\";\nint[][] dp = new int[a.length()+1][b.length()+1];\nfor (int i = 0; i <= a.length(); i++) dp[i][0] = i;\nfor (int j = 0; j <= b.length(); j++) dp[0][j] = j;\nfor (int i = 1; i <= a.length(); i++)\n  for (int j = 1; j <= b.length(); j++)\n    dp[i][j] = a.charAt(i-1) == b.charAt(j-1)\n             ? dp[i-1][j-1]\n             : 1 + Math.min(dp[i-1][j-1], Math.min(dp[i-1][j], dp[i][j-1]));\n```\n\n### Watch out for\n- Initialize the first row and column (matching against an empty prefix).\n- Fill in an order where every cell you read is already computed.\n",
    "exercises": [],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "graph_repr",
    "name": "Graph Representation",
    "category": "Graphs",
    "what": "Modeling entities as nodes and relationships as edges, usually an adjacency list.",
    "deep": "The representation dictates efficiency: adjacency lists are O(V+E) space and iterate neighbors quickly, while adjacency matrices are O(V^2) but give O(1) edge tests. Directed vs undirected and weighted vs unweighted change how you build and traverse it.",
    "java": "List<List<Integer>> adj, or List<int[]>[] adj = new List[n] storing new int[]{to, w} for weighted edges.",
    "language": "java",
    "lesson": "### Worked example\nStore edges 0-1 and 0-2 as an adjacency list: node 0 lists [1, 2]; nodes 1 and 2 list [0].\n\n### In code (Java)\n```java\nint n = 3;\nList<List<Integer>> adj = new ArrayList<>();\nfor (int i = 0; i < n; i++) adj.add(new ArrayList<>());\nadj.get(0).add(1); adj.get(1).add(0);   // undirected edge 0-1\nadj.get(0).add(2); adj.get(2).add(0);\n```\n\n### Watch out for\n- For undirected graphs, add the edge in **both** directions.\n- Adjacency lists use O(V+E) memory and iterate neighbours quickly.\n",
    "exercises": [],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "bfs",
    "name": "Breadth-First Search",
    "category": "Graphs",
    "what": "Exploring a graph level by level with a queue; finds shortest paths in unweighted graphs.",
    "deep": "BFS visits nodes in order of distance from the source, so the first time it reaches a node is via a shortest unweighted path. It needs a queue and a visited marker and runs in O(V+E).",
    "java": "ArrayDeque<Integer> as the queue (offer/poll) plus a boolean[] visited. Mark visited when enqueuing, not dequeuing, to avoid duplicates.",
    "language": "java",
    "lesson": "### Worked example\nFrom node 0 in the chain 0-1-2, BFS visits 0, then 1, then 2 — in order of distance.\n\n### In code (Java)\n```java\nQueue<Integer> q = new ArrayDeque<>();\nboolean[] seen = new boolean[n];\nq.offer(0); seen[0] = true;\nwhile (!q.isEmpty()) {\n    int u = q.poll();\n    for (int v : adj.get(u)) if (!seen[v]) { seen[v] = true; q.offer(v); }\n}\n```\n\n### Watch out for\n- Mark `seen` when you **enqueue**, not when you dequeue, or nodes get added twice.\n- BFS gives shortest paths only in *unweighted* graphs.\n",
    "exercises": [
      {
        "id": "bfs-expand",
        "title": "Visit neighbours",
        "prompt": "The graph is built as an adjacency list. Replace `____` with the step that, for an unseen neighbour `w`, marks it seen and enqueues it (mark on ENQUEUE to avoid duplicates).",
        "hint": "Only enqueue a neighbour you haven't seen, and mark it seen at that moment.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int m = sc.nextInt();\n        List<List<Integer>> adj = new ArrayList<>();\n        for (int i = 0; i < n; i++) adj.add(new ArrayList<>());\n        for (int i = 0; i < m; i++) {\n            int u = sc.nextInt();\n            int v = sc.nextInt();\n            adj.get(u).add(v);\n            adj.get(v).add(u);\n        }\n        Queue<Integer> q = new ArrayDeque<>();\n        boolean[] seen = new boolean[n];\n        q.offer(0);\n        seen[0] = true;\n        int count = 0;\n        while (!q.isEmpty()) {\n            int u = q.poll();\n            count++;\n            for (int w : adj.get(u)) {\n                ____\n            }\n        }\n        System.out.println(count);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int m = sc.nextInt();\n        List<List<Integer>> adj = new ArrayList<>();\n        for (int i = 0; i < n; i++) adj.add(new ArrayList<>());\n        for (int i = 0; i < m; i++) {\n            int u = sc.nextInt();\n            int v = sc.nextInt();\n            adj.get(u).add(v);\n            adj.get(v).add(u);\n        }\n        Queue<Integer> q = new ArrayDeque<>();\n        boolean[] seen = new boolean[n];\n        q.offer(0);\n        seen[0] = true;\n        int count = 0;\n        while (!q.isEmpty()) {\n            int u = q.poll();\n            count++;\n            for (int w : adj.get(u)) {\n                if (!seen[w]) { seen[w] = true; q.offer(w); }\n            }\n        }\n        System.out.println(count);\n    }\n}\n",
        "tests": [
          {
            "input": "3 2\n0 1\n1 2",
            "output": "3"
          },
          {
            "input": "4 2\n0 1\n2 3",
            "output": "2"
          },
          {
            "input": "1 0",
            "output": "1"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "bfs-distance",
        "title": "Shortest distance",
        "prompt": "BFS finds shortest distances in an unweighted graph. Replace `____` with the line that sets a newly-reached node's distance to one more than the node you came from.",
        "hint": "Each BFS layer is one step further, so a neighbour's distance is dist[u] + 1.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int m = sc.nextInt();\n        List<List<Integer>> adj = new ArrayList<>();\n        for (int i = 0; i < n; i++) adj.add(new ArrayList<>());\n        for (int i = 0; i < m; i++) {\n            int u = sc.nextInt();\n            int v = sc.nextInt();\n            adj.get(u).add(v);\n            adj.get(v).add(u);\n        }\n        int t = sc.nextInt();\n        int[] dist = new int[n];\n        Arrays.fill(dist, -1);\n        Queue<Integer> q = new ArrayDeque<>();\n        q.offer(0);\n        dist[0] = 0;\n        while (!q.isEmpty()) {\n            int u = q.poll();\n            for (int w : adj.get(u)) {\n                if (dist[w] == -1) { ____ q.offer(w); }\n            }\n        }\n        System.out.println(dist[t]);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int m = sc.nextInt();\n        List<List<Integer>> adj = new ArrayList<>();\n        for (int i = 0; i < n; i++) adj.add(new ArrayList<>());\n        for (int i = 0; i < m; i++) {\n            int u = sc.nextInt();\n            int v = sc.nextInt();\n            adj.get(u).add(v);\n            adj.get(v).add(u);\n        }\n        int t = sc.nextInt();\n        int[] dist = new int[n];\n        Arrays.fill(dist, -1);\n        Queue<Integer> q = new ArrayDeque<>();\n        q.offer(0);\n        dist[0] = 0;\n        while (!q.isEmpty()) {\n            int u = q.poll();\n            for (int w : adj.get(u)) {\n                if (dist[w] == -1) { dist[w] = dist[u] + 1; q.offer(w); }\n            }\n        }\n        System.out.println(dist[t]);\n    }\n}\n",
        "tests": [
          {
            "input": "3 2\n0 1\n1 2\n2",
            "output": "2"
          },
          {
            "input": "4 2\n0 1\n2 3\n3",
            "output": "-1"
          },
          {
            "input": "2 1\n0 1\n1",
            "output": "1"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "bfs-components",
        "title": "Count connected components",
        "prompt": "Write the solution: run a BFS from every not-yet-seen node and count how many separate components the undirected graph has.",
        "hint": "Every time you start a BFS from an unseen node you've found a new component.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int m = sc.nextInt();\n        List<List<Integer>> adj = new ArrayList<>();\n        for (int i = 0; i < n; i++) adj.add(new ArrayList<>());\n        for (int i = 0; i < m; i++) {\n            int u = sc.nextInt();\n            int v = sc.nextInt();\n            adj.get(u).add(v);\n            adj.get(v).add(u);\n        }\n____\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int m = sc.nextInt();\n        List<List<Integer>> adj = new ArrayList<>();\n        for (int i = 0; i < n; i++) adj.add(new ArrayList<>());\n        for (int i = 0; i < m; i++) {\n            int u = sc.nextInt();\n            int v = sc.nextInt();\n            adj.get(u).add(v);\n            adj.get(v).add(u);\n        }\n        boolean[] seen = new boolean[n];\n        int components = 0;\n        for (int start = 0; start < n; start++) {\n            if (seen[start]) continue;\n            components++;\n            Queue<Integer> q = new ArrayDeque<>();\n            q.offer(start);\n            seen[start] = true;\n            while (!q.isEmpty()) {\n                int u = q.poll();\n                for (int w : adj.get(u)) {\n                    if (!seen[w]) { seen[w] = true; q.offer(w); }\n                }\n            }\n        }\n        System.out.println(components);\n    }\n}\n",
        "tests": [
          {
            "input": "5 3\n0 1\n1 2\n3 4",
            "output": "2"
          },
          {
            "input": "4 0",
            "output": "4"
          },
          {
            "input": "3 2\n0 1\n1 2",
            "output": "1"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "flood_fill",
    "name": "Flood Fill",
    "category": "Graphs",
    "what": "Spreading outward from a start cell to mark an entire connected region of a grid.",
    "deep": "A grid-specialized BFS/DFS over 4- or 8-directional neighbors. Each launch consumes exactly one connected component, so counting launches counts components - islands, regions, or paint buckets.",
    "java": "From (r, c) run BFS/DFS over the four deltas {{1,0},{-1,0},{0,1},{0,-1}}, guarding bounds and the visited/grid state.",
    "language": "java",
    "lesson": "### Worked example\nCount islands in a grid: each time you find unvisited land, flood its whole region and add one to the count.\n\n### In code (Java)\n```java\nstatic int[][] DIR = {{1,0},{-1,0},{0,1},{0,-1}};\n// from (r, c): mark visited, then visit each in-bounds land neighbour\nfor (int[] d : DIR) {\n    int nr = r + d[0], nc = c + d[1];\n    // if in bounds and grid[nr][nc] == '1' and not visited -> recurse/enqueue\n}\n```\n\n### Watch out for\n- Guard the array bounds before touching a neighbour.\n- Mark cells visited (or overwrite them) so the flood cannot loop forever.\n",
    "exercises": [],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "topo",
    "name": "Topological Sort",
    "category": "Graphs",
    "what": "Ordering the nodes of a DAG so every edge points forward; impossible iff there is a cycle.",
    "deep": "Applies to dependency problems like build order and course scheduling. Two algorithms exist: Kahn's (BFS on in-degrees) and DFS post-order. If not all nodes are emitted, the graph has a cycle.",
    "java": "Kahn's: an int[] indegree and an ArrayDeque queue of zero-indegree nodes; count how many you pop.",
    "language": "java",
    "lesson": "### Worked example\nTasks with `A before B` and `B before C` order as A, B, C. A cycle (`A before B`, `B before A`) has no valid order.\n\n### In code (Java)\n```java\n// Kahn's algorithm: start from indegree-0 nodes, remove them,\n// decrement each neighbour, and add newly-zeroed nodes. If you\n// emit fewer than n nodes, there is a cycle.\n```\n\n### Watch out for\n- Emitting fewer than n nodes means the graph has a cycle.\n- Edge direction matters: `b before a` is an edge b -> a.\n",
    "exercises": [],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "indegree",
    "name": "In-degree / Kahn's Algorithm",
    "category": "Graphs",
    "what": "Counting incoming edges and repeatedly removing zero-indegree nodes to order a DAG.",
    "deep": "In-degree is how many prerequisites a node still has. Start from nodes with none, complete them, and decrement their neighbors; newly-zeroed nodes become available. Finishing all n nodes means no cycle exists.",
    "java": "int[] indegree = new int[n]; increment per edge; seed an ArrayDeque with every node whose indegree is 0.",
    "language": "java",
    "lesson": "### Worked example\nCourse 1 needs course 0: `indegree[1] = 1`, `indegree[0] = 0`. Take 0, drop `indegree[1]` to 0, then take 1.\n\n### In code (Java)\n```java\nint[] indeg = new int[n];\nfor (int[] e : edges) indeg[e[0]]++;   // edge b->a increments a\nDeque<Integer> q = new ArrayDeque<>();\nfor (int i = 0; i < n; i++) if (indeg[i] == 0) q.offer(i);\n```\n\n### Watch out for\n- Indegree counts remaining prerequisites; seed the queue with the zeros.\n- Count how many you pop — it must equal n for a valid ordering.\n",
    "exercises": [],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "heap",
    "name": "Priority Queue (Heap)",
    "category": "Data Structures",
    "what": "A structure that always yields the smallest or largest pending item in O(log n).",
    "deep": "A binary heap supports push and pop-min/max in O(log n) and peek in O(1). It powers greedy expansion (Dijkstra, Prim), top-k selection, and merging sorted streams, but it has no fast arbitrary lookup or deletion.",
    "java": "PriorityQueue<T> is a min-heap by default. For pairs use long[]/int[] with a comparator: new PriorityQueue<>((a, b) -> Long.compare(a[0], b[0])).",
    "language": "java",
    "lesson": "### Worked example\nAlways take the smallest pending item: a min-heap of `{5, 1, 3}` pops 1, then 3, then 5.\n\n### In code (Java)\n```java\nPriorityQueue<Integer> pq = new PriorityQueue<>();\npq.offer(5); pq.offer(1); pq.offer(3);\nwhile (!pq.isEmpty()) System.out.print(pq.poll() + \" \"); // 1 3 5\n```\n\n### Watch out for\n- `PriorityQueue` is a **min**-heap; for max use `Comparator.reverseOrder()`.\n- Peeking is O(1), but there is no fast search or arbitrary removal.\n",
    "exercises": [
      {
        "id": "heap-minorder",
        "title": "Pop in sorted order",
        "prompt": "A `PriorityQueue` is a min-heap. Replace `____` with the call that removes and returns the smallest remaining item.",
        "hint": "poll() always returns the current minimum of a PriorityQueue.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        PriorityQueue<Integer> pq = new PriorityQueue<>();\n        for (int i = 0; i < n; i++) pq.offer(sc.nextInt());\n        StringBuilder sb = new StringBuilder();\n        while (!pq.isEmpty()) sb.append(____).append(' ');\n        System.out.println(sb.toString().trim());\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        PriorityQueue<Integer> pq = new PriorityQueue<>();\n        for (int i = 0; i < n; i++) pq.offer(sc.nextInt());\n        StringBuilder sb = new StringBuilder();\n        while (!pq.isEmpty()) sb.append(pq.poll()).append(' ');\n        System.out.println(sb.toString().trim());\n    }\n}\n",
        "tests": [
          {
            "input": "3\n5 1 3",
            "output": "1 3 5"
          },
          {
            "input": "1\n9",
            "output": "9"
          },
          {
            "input": "4\n4 3 2 1",
            "output": "1 2 3 4"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "heap-max",
        "title": "Largest element",
        "prompt": "Replace `____` with the comparator that turns the default min-heap into a max-heap, so `poll()` returns the largest value.",
        "hint": "Comparator.reverseOrder() makes the heap yield the maximum first.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        PriorityQueue<Integer> pq = new PriorityQueue<>(____);\n        for (int i = 0; i < n; i++) pq.offer(sc.nextInt());\n        System.out.println(pq.poll());\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        PriorityQueue<Integer> pq = new PriorityQueue<>(Comparator.reverseOrder());\n        for (int i = 0; i < n; i++) pq.offer(sc.nextInt());\n        System.out.println(pq.poll());\n    }\n}\n",
        "tests": [
          {
            "input": "3\n5 1 3",
            "output": "5"
          },
          {
            "input": "1\n9",
            "output": "9"
          },
          {
            "input": "4\n2 8 4 1",
            "output": "8"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "heap-kthlargest",
        "title": "Kth largest element",
        "prompt": "Write the solution: keep a min-heap of size `k`; after adding each value, drop the smallest if the heap grew past `k`. The heap's minimum is then the k-th largest — print it.",
        "hint": "A size-k min-heap keeps the k largest seen so far; its smallest is the k-th largest.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int k = sc.nextInt();\n____\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int k = sc.nextInt();\n        PriorityQueue<Integer> pq = new PriorityQueue<>();\n        for (int i = 0; i < n; i++) {\n            pq.offer(sc.nextInt());\n            if (pq.size() > k) pq.poll();\n        }\n        System.out.println(pq.poll());\n    }\n}\n",
        "tests": [
          {
            "input": "5 2\n3 2 1 5 4",
            "output": "4"
          },
          {
            "input": "3 1\n7 8 9",
            "output": "9"
          },
          {
            "input": "4 3\n10 5 20 15",
            "output": "10"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "greedy",
    "name": "Greedy Choice",
    "category": "Searching & Sorting",
    "what": "Taking the locally-best option at each step when that provably reaches a global optimum.",
    "deep": "Greedy is fast and simple but correct only when the problem has the greedy-choice property and optimal substructure; otherwise DP is the fallback. Proving correctness (often an exchange argument) matters more than the code.",
    "java": "Often just Arrays.sort plus a linear scan, or a PriorityQueue to always grab the current best.",
    "language": "java",
    "lesson": "### Worked example\nFewest coins for 30 with {25, 10, 5}: take the biggest that fits each time — 25, then 5 — two coins.\n\n### In code (Java)\n```java\nint[] coins = {25, 10, 5}; int amount = 30, used = 0;\nfor (int c : coins) { used += amount / c; amount %= c; }\n```\n\n### Watch out for\n- Greedy is only correct for some problems (this coin set works; arbitrary sets need DP).\n- Sorting the input first is a common setup step.\n",
    "exercises": [
      {
        "id": "greedy-coins",
        "title": "Fewest coins",
        "prompt": "Coins `{25, 10, 5, 1}` are tried largest-first. Replace `____` with the line that adds as many of coin `c` as fit into what remains.",
        "hint": "amount / c is how many of coin c fit; amount %= c leaves the remainder.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int amount = sc.nextInt();\n        int[] coins = {25, 10, 5, 1};\n        int used = 0;\n        for (int c : coins) {\n            ____\n            amount %= c;\n        }\n        System.out.println(used);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int amount = sc.nextInt();\n        int[] coins = {25, 10, 5, 1};\n        int used = 0;\n        for (int c : coins) {\n            used += amount / c;\n            amount %= c;\n        }\n        System.out.println(used);\n    }\n}\n",
        "tests": [
          {
            "input": "30",
            "output": "2"
          },
          {
            "input": "41",
            "output": "4"
          },
          {
            "input": "99",
            "output": "9"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "greedy-largest",
        "title": "Largest number from digits",
        "prompt": "Greedily place the biggest digits first. The digits are sorted ascending, so replace `____` with the call that flips them into descending order.",
        "hint": "Sorting ascending then reversing puts the largest digit first.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String s = sc.next();\n        char[] d = s.toCharArray();\n        Arrays.sort(d);\n        StringBuilder sb = new StringBuilder(new String(d));\n        ____\n        System.out.println(sb.toString());\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String s = sc.next();\n        char[] d = s.toCharArray();\n        Arrays.sort(d);\n        StringBuilder sb = new StringBuilder(new String(d));\n        sb.reverse();\n        System.out.println(sb.toString());\n    }\n}\n",
        "tests": [
          {
            "input": "312",
            "output": "321"
          },
          {
            "input": "1000",
            "output": "1000"
          },
          {
            "input": "52341",
            "output": "54321"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "greedy-cookies",
        "title": "Assign cookies",
        "prompt": "Children have greed factors, cookies have sizes; a child is satisfied by a cookie at least as big as their greed. Write the solution: sort both, then greedily match the smallest cookie that satisfies the least-greedy remaining child, and print how many children are satisfied.",
        "hint": "Sort both, then sweep the cookies; each cookie that clears the current child's greed satisfies them.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] g = new int[n];\n        for (int i = 0; i < n; i++) g[i] = sc.nextInt();\n        int m = sc.nextInt();\n        int[] s = new int[m];\n        for (int i = 0; i < m; i++) s[i] = sc.nextInt();\n____\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] g = new int[n];\n        for (int i = 0; i < n; i++) g[i] = sc.nextInt();\n        int m = sc.nextInt();\n        int[] s = new int[m];\n        for (int i = 0; i < m; i++) s[i] = sc.nextInt();\n        Arrays.sort(g);\n        Arrays.sort(s);\n        int child = 0;\n        for (int j = 0; j < m && child < n; j++) {\n            if (s[j] >= g[child]) child++;\n        }\n        System.out.println(child);\n    }\n}\n",
        "tests": [
          {
            "input": "3\n1 2 3\n2\n1 1",
            "output": "1"
          },
          {
            "input": "2\n1 2\n3\n1 2 3",
            "output": "2"
          },
          {
            "input": "2\n10 20\n2\n5 5",
            "output": "0"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "prefix_max",
    "name": "Running Maxima / Prefix Aggregates",
    "category": "Arrays",
    "what": "Tracking the best value seen so far from the left/right to answer range questions in one pass.",
    "deep": "Precomputing prefix (and suffix) maxima, sums, or counts converts per-query O(n) work into O(1). It is the idea behind prefix sums and the left/right walls in rain-water style problems.",
    "java": "int[] prefix arrays, or two running int variables when you only need the maxima at the current index.",
    "language": "java",
    "lesson": "### When to reach for this\nSignals: **\"sum of `a[l..r]`\"** with many queries, or **\"best value so far\"** — max profit, running maximum, cheapest-so-far. Precompute an aggregate from the left so each later answer is O(1).\n\n### Simulated solve — best profit on prices `[7, 1, 5, 3, 6]`\n\"Buy low, sell later\" = for each day, how much could you make selling today given the cheapest price seen so far? Track `minSoFar` and the best gap.\n\n| i | price | minSoFar | price − minSoFar | best |\n| - | ----- | -------- | ---------------- | ---- |\n| 0 | 7 | 7 | — | 0 |\n| 1 | 1 | 1 | 0 | 0 |\n| 2 | 5 | 1 | 4 | 4 |\n| 3 | 3 | 1 | 2 | 4 |\n| 4 | 6 | 1 | 5 | **5** |\n\nAnswer: **5** (buy at 1, sell at 6). One pass, no nested loop.\n\n### Prefix sums in code (Java)\n```java\nlong[] pre = new long[n + 1];               // pre[0] = 0\nfor (int i = 0; i < n; i++) pre[i + 1] = pre[i] + a[i];\nlong sumLR = pre[r + 1] - pre[l];           // sum of a[l..r], inclusive\n```\n\n### Watch out for\n- Use a size **n + 1** prefix array so `pre[0] = 0` and the query needs no special case.\n- Sums grow fast — accumulate in `long` to dodge overflow.\n- \"Best so far\" problems keep just one or two running variables, not a whole array.\n",
    "exercises": [
      {
        "id": "prefix-running-sum",
        "title": "Running (prefix) sums",
        "prompt": "Print each prefix total: after reading `[1,2,3,4]` print `1 3 6 10`. Replace `____` so `run` accumulates as you go.",
        "hint": "Each prefix total is the previous total plus the current element: run += a[i];",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        long run = 0;\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < n; i++) {\n            ____\n            sb.append(run);\n            if (i < n - 1) sb.append(' ');\n        }\n        System.out.println(sb.toString());\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        long run = 0;\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < n; i++) {\n            run += a[i];\n            sb.append(run);\n            if (i < n - 1) sb.append(' ');\n        }\n        System.out.println(sb.toString());\n    }\n}\n",
        "tests": [
          {
            "input": "4\n1 2 3 4",
            "output": "1 3 6 10"
          },
          {
            "input": "3\n5 0 5",
            "output": "5 5 10"
          },
          {
            "input": "1\n7",
            "output": "7"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "prefix-range-sum",
        "title": "Range sum with a prefix array",
        "prompt": "Build a size `n+1` prefix array, then answer the query `l r` (inclusive, 0-based) in O(1). Replace `____` so `pre` is the array of prefix sums.",
        "hint": "pre[i+1] = pre[i] + a[i]; then sum of a[l..r] is pre[r+1] - pre[l].",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        long[] pre = new long[n + 1];\n        for (int i = 0; i < n; i++) ____\n        int l = sc.nextInt(), r = sc.nextInt();\n        System.out.println(pre[r + 1] - pre[l]);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        long[] pre = new long[n + 1];\n        for (int i = 0; i < n; i++) pre[i + 1] = pre[i] + a[i];\n        int l = sc.nextInt(), r = sc.nextInt();\n        System.out.println(pre[r + 1] - pre[l]);\n    }\n}\n",
        "tests": [
          {
            "input": "5\n1 2 3 4 5\n1 3",
            "output": "9"
          },
          {
            "input": "3\n10 20 30\n0 2",
            "output": "60"
          },
          {
            "input": "4\n5 5 5 5\n2 2",
            "output": "5"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "prefix-running-max",
        "title": "Running maximum",
        "prompt": "Print the max of every prefix: `[3,1,4,1,5]` → `3 3 4 4 5`. Replace `____` so `best` tracks the largest value so far.",
        "hint": "best = Math.max(best, a[i]); before appending it.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int best = a[0];\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < n; i++) {\n            ____\n            sb.append(best);\n            if (i < n - 1) sb.append(' ');\n        }\n        System.out.println(sb.toString());\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int best = a[0];\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < n; i++) {\n            best = Math.max(best, a[i]);\n            sb.append(best);\n            if (i < n - 1) sb.append(' ');\n        }\n        System.out.println(sb.toString());\n    }\n}\n",
        "tests": [
          {
            "input": "5\n3 1 4 1 5",
            "output": "3 3 4 4 5"
          },
          {
            "input": "3\n5 4 3",
            "output": "5 5 5"
          },
          {
            "input": "1\n9",
            "output": "9"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "prefix-best-profit",
        "title": "Best buy-low sell-high",
        "prompt": "Prices come in time order; find the max `price[j] - price[i]` with `i < j` (0 if never profitable). Replace `____` so `best` uses the cheapest price seen so far.",
        "hint": "Selling today earns a[i] - minSoFar; keep the best of those.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int minSoFar = a[0], best = 0;\n        for (int i = 1; i < n; i++) {\n            ____\n            minSoFar = Math.min(minSoFar, a[i]);\n        }\n        System.out.println(best);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int minSoFar = a[0], best = 0;\n        for (int i = 1; i < n; i++) {\n            best = Math.max(best, a[i] - minSoFar);\n            minSoFar = Math.min(minSoFar, a[i]);\n        }\n        System.out.println(best);\n    }\n}\n",
        "tests": [
          {
            "input": "6\n7 1 5 3 6 4",
            "output": "5"
          },
          {
            "input": "5\n7 6 4 3 1",
            "output": "0"
          },
          {
            "input": "2\n1 5",
            "output": "4"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "string_basics",
    "name": "String Traversal",
    "category": "Strings",
    "what": "Treating a string as an indexable sequence of characters to count and compare.",
    "deep": "Most string problems reduce to a single pass with charAt or a char[]. Remember Java strings are immutable, so build output with StringBuilder, and character math relies on ASCII codes.",
    "java": "s.charAt(i), s.length(), s.toCharArray(); build results with StringBuilder, never += inside a loop.",
    "language": "java",
    "lesson": "### Worked example\nCount the letter `a` in `\"banana\"` — the answer is 3.\n\n### In code (Java)\n```java\nString s = \"banana\";\nint count = 0;\nfor (int i = 0; i < s.length(); i++) {\n    if (s.charAt(i) == 'a') count++;\n}\nSystem.out.println(count); // 3\n```\n\n### Watch out for\n- Strings are immutable; build output with `StringBuilder`, not `+=` in a loop.\n- Index characters with `charAt(i)` (0-based) and compare Strings with `.equals`.\n",
    "exercises": [
      {
        "id": "string_basics-count",
        "title": "Count a letter",
        "prompt": "Replace `____` with the condition that is true when the character at index `i` is `'a'`, so the program counts the `a`s in the line.",
        "hint": "charAt(i) returns the character at position i; compare it with the char literal 'a'.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String s = sc.nextLine();\n        int count = 0;\n        for (int i = 0; i < s.length(); i++) {\n            if (____) count++;\n        }\n        System.out.println(count);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String s = sc.nextLine();\n        int count = 0;\n        for (int i = 0; i < s.length(); i++) {\n            if (s.charAt(i) == 'a') count++;\n        }\n        System.out.println(count);\n    }\n}\n",
        "tests": [
          {
            "input": "banana",
            "output": "3"
          },
          {
            "input": "apple",
            "output": "1"
          },
          {
            "input": "xyz",
            "output": "0"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "string_basics-length",
        "title": "Length of a word",
        "prompt": "Replace `____` to print the number of characters in `s`. On a String, length is a method call.",
        "hint": "Strings use s.length() (with parentheses); arrays use arr.length (without).",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String s = sc.next();\n        System.out.println(____);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String s = sc.next();\n        System.out.println(s.length());\n    }\n}\n",
        "tests": [
          {
            "input": "hello",
            "output": "5"
          },
          {
            "input": "a",
            "output": "1"
          },
          {
            "input": "banana",
            "output": "6"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "string_basics-vowels",
        "title": "Count the vowels",
        "prompt": "Write the solution: count how many characters of the line are vowels (a, e, i, o, u) and print the total.",
        "hint": "Walk each character with charAt(i); add to the count when it is one of the five vowels.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String s = sc.nextLine();\n____\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String s = sc.nextLine();\n        int count = 0;\n        for (int i = 0; i < s.length(); i++) {\n            char ch = s.charAt(i);\n            if (ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u') count++;\n        }\n        System.out.println(count);\n    }\n}\n",
        "tests": [
          {
            "input": "banana",
            "output": "3"
          },
          {
            "input": "sky",
            "output": "0"
          },
          {
            "input": "education",
            "output": "5"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "canonical",
    "name": "Canonical Form",
    "category": "Strings",
    "what": "Reducing equivalent items to one identical representative so they collide in a map.",
    "deep": "A canonical key makes 'are these the same up to X?' a simple equality or hash test. For anagrams it is the sorted letters, for fractions the reduced form, for graphs a normalized encoding. Choosing a cheap, collision-free key is the trick.",
    "java": "For anagrams: char[] c = s.toCharArray(); Arrays.sort(c); String key = new String(c); or a length-26 count signature.",
    "language": "java",
    "lesson": "### Worked example\n`\"eat\"` and `\"tea\"` are anagrams because both share the canonical key `\"aet\"` (sorted letters).\n\n### In code (Java)\n```java\nchar[] c = \"tea\".toCharArray();\nArrays.sort(c);\nString key = new String(c); // \"aet\"\n```\n\n### Watch out for\n- A 26-length count signature is an O(n) alternative to O(n log n) sorting.\n- The key must be identical for equivalent items and different otherwise.\n",
    "exercises": [
      {
        "id": "canonical-sortkey",
        "title": "Sort the letters",
        "prompt": "Replace `____` with the call that sorts the char array `c` in place, so the printed key is the letters in order (`tea` -> `aet`).",
        "hint": "Arrays.sort(c) orders the characters; the sorted letters are the canonical key.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        char[] c = sc.next().toCharArray();\n        ____\n        System.out.println(new String(c));\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        char[] c = sc.next().toCharArray();\n        Arrays.sort(c);\n        System.out.println(new String(c));\n    }\n}\n",
        "tests": [
          {
            "input": "tea",
            "output": "aet"
          },
          {
            "input": "banana",
            "output": "aaabnn"
          },
          {
            "input": "dcba",
            "output": "abcd"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "canonical-anagram",
        "title": "Are they anagrams?",
        "prompt": "Two words are read. Replace `____` with the test that is true when their sorted-letter keys are equal.",
        "hint": "Compare the two canonical keys with .equals — equal keys mean anagrams.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        char[] a = sc.next().toCharArray();\n        char[] b = sc.next().toCharArray();\n        Arrays.sort(a);\n        Arrays.sort(b);\n        System.out.println(____);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        char[] a = sc.next().toCharArray();\n        char[] b = sc.next().toCharArray();\n        Arrays.sort(a);\n        Arrays.sort(b);\n        System.out.println(new String(a).equals(new String(b)));\n    }\n}\n",
        "tests": [
          {
            "input": "eat tea",
            "output": "true"
          },
          {
            "input": "abc abd",
            "output": "false"
          },
          {
            "input": "listen silent",
            "output": "true"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "canonical-groups",
        "title": "Count anagram groups",
        "prompt": "The first line is a count `n`, then `n` words. Write the solution: reduce each word to its sorted-letter key, and print how many DISTINCT keys there are.",
        "hint": "Anagrams share a canonical key, so a Set of keys counts the distinct groups.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n____\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        Set<String> keys = new HashSet<>();\n        for (int i = 0; i < n; i++) {\n            char[] c = sc.next().toCharArray();\n            Arrays.sort(c);\n            keys.add(new String(c));\n        }\n        System.out.println(keys.size());\n    }\n}\n",
        "tests": [
          {
            "input": "3\neat tea ate",
            "output": "1"
          },
          {
            "input": "3\nabc bca xyz",
            "output": "2"
          },
          {
            "input": "2\ncat dog",
            "output": "2"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "math_digits",
    "name": "Digit Manipulation",
    "category": "Math",
    "what": "Working with a number's decimal digits or its string form while handling signs and edge cases.",
    "deep": "Digit problems peel digits with % 10 and / 10, or operate on the string form. Classic gotchas are negative numbers, trailing zeros, and overflow when reversing, so the first choice is arithmetic vs string handling.",
    "java": "n % 10 and n / 10 to extract digits, or Long.toString(n); use long to avoid overflow on reversal.",
    "language": "java",
    "lesson": "### Worked example\nSum the digits of 123: `123 % 10 = 3`, then `123 / 10 = 12`, and so on — 3 + 2 + 1 = 6.\n\n### In code (Java)\n```java\nint n = 123, sum = 0;\nwhile (n > 0) { sum += n % 10; n /= 10; }\nSystem.out.println(sum); // 6\n```\n\n### Watch out for\n- Handle 0 and negatives explicitly (take `Math.abs` first if sign should not matter).\n- Reversing a number arithmetically can overflow — consider the String form.\n",
    "exercises": [
      {
        "id": "math_digits-sum",
        "title": "Sum the digits",
        "prompt": "Replace `____` with the expression for the last digit of `n`, so the loop adds up every digit.",
        "hint": "n % 10 peels off the last digit; n /= 10 removes it.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int sum = 0;\n        while (n > 0) {\n            sum += ____;\n            n /= 10;\n        }\n        System.out.println(sum);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int sum = 0;\n        while (n > 0) {\n            sum += n % 10;\n            n /= 10;\n        }\n        System.out.println(sum);\n    }\n}\n",
        "tests": [
          {
            "input": "123",
            "output": "6"
          },
          {
            "input": "9",
            "output": "9"
          },
          {
            "input": "100",
            "output": "1"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "math_digits-count",
        "title": "Count the digits",
        "prompt": "Replace `____` with the statement that drops the last digit of `n` each pass, so the loop counts how many digits it has.",
        "hint": "Integer division n /= 10 shortens the number by one digit.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int count = 0;\n        while (n > 0) {\n            count++;\n            ____\n        }\n        System.out.println(count);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int count = 0;\n        while (n > 0) {\n            count++;\n            n /= 10;\n        }\n        System.out.println(count);\n    }\n}\n",
        "tests": [
          {
            "input": "123",
            "output": "3"
          },
          {
            "input": "7",
            "output": "1"
          },
          {
            "input": "1000",
            "output": "4"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "math_digits-reverse",
        "title": "Reverse the digits",
        "prompt": "Write the solution: build the reversed number by repeatedly taking the last digit of `n` and appending it (`123` -> `321`).",
        "hint": "rev = rev * 10 + (last digit) shifts the built-up number left and adds the new digit.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n____\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int rev = 0;\n        while (n > 0) {\n            rev = rev * 10 + n % 10;\n            n /= 10;\n        }\n        System.out.println(rev);\n    }\n}\n",
        "tests": [
          {
            "input": "123",
            "output": "321"
          },
          {
            "input": "1230",
            "output": "321"
          },
          {
            "input": "5",
            "output": "5"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "number_theory",
    "name": "Number Theory Basics",
    "category": "Math",
    "what": "Divisibility, gcd, and modular arithmetic facts used to reason about integers.",
    "deep": "Foundations like the Euclidean algorithm, prime factorization, and modular arithmetic underpin many math problems. Even simple tasks benefit from gcd/lcm identities and knowing how modulo distributes over addition and multiplication.",
    "java": "Math.floorMod for correct modulo of negatives; write a short gcd helper (Euclid); BigInteger has gcd() if needed.",
    "language": "java",
    "lesson": "### Worked example\ngcd(12, 18): 18 % 12 = 6, then 12 % 6 = 0, so the gcd is 6.\n\n### In code (Java)\n```java\nstatic long gcd(long a, long b) { return b == 0 ? a : gcd(b, a % b); }\n```\n\n### Watch out for\n- `Math.floorMod` gives a non-negative remainder for negative inputs.\n- lcm(a, b) = a / gcd(a, b) * b — divide before multiplying to avoid overflow.\n",
    "exercises": [
      {
        "id": "number_theory-gcd",
        "title": "Euclid's GCD",
        "prompt": "Replace `____` with the recursive step of Euclid's algorithm (`gcd(b, a % b)`), with the base case `b == 0`.",
        "hint": "gcd(a, b) = gcd(b, a % b), and gcd(a, 0) = a.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    static long gcd(long a, long b) {\n        ____\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        long a = sc.nextLong();\n        long b = sc.nextLong();\n        System.out.println(gcd(a, b));\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    static long gcd(long a, long b) {\n        return b == 0 ? a : gcd(b, a % b);\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        long a = sc.nextLong();\n        long b = sc.nextLong();\n        System.out.println(gcd(a, b));\n    }\n}\n",
        "tests": [
          {
            "input": "12 18",
            "output": "6"
          },
          {
            "input": "7 5",
            "output": "1"
          },
          {
            "input": "100 10",
            "output": "10"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "number_theory-prime",
        "title": "Is it prime?",
        "prompt": "Replace `____` with the divisibility test that, when true, proves `n` is not prime.",
        "hint": "If n % d == 0 for some d in [2, sqrt(n)], then d divides n, so n isn't prime.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        boolean prime = n >= 2;\n        for (int d = 2; (long) d * d <= n; d++) {\n            if (____) { prime = false; break; }\n        }\n        System.out.println(prime);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        boolean prime = n >= 2;\n        for (int d = 2; (long) d * d <= n; d++) {\n            if (n % d == 0) { prime = false; break; }\n        }\n        System.out.println(prime);\n    }\n}\n",
        "tests": [
          {
            "input": "7",
            "output": "true"
          },
          {
            "input": "12",
            "output": "false"
          },
          {
            "input": "1",
            "output": "false"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "number_theory-lcm",
        "title": "Least common multiple",
        "prompt": "Write the solution: compute `lcm(a, b)` as `a / gcd(a, b) * b` (divide before multiplying to avoid overflow) and print it.",
        "hint": "lcm(a, b) = a / gcd(a, b) * b — dividing first keeps the intermediate value small.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    static long gcd(long a, long b) {\n        return b == 0 ? a : gcd(b, a % b);\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        long a = sc.nextLong();\n        long b = sc.nextLong();\n____\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    static long gcd(long a, long b) {\n        return b == 0 ? a : gcd(b, a % b);\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        long a = sc.nextLong();\n        long b = sc.nextLong();\n        long result = a / gcd(a, b) * b;\n        System.out.println(result);\n    }\n}\n",
        "tests": [
          {
            "input": "4 6",
            "output": "12"
          },
          {
            "input": "3 5",
            "output": "15"
          },
          {
            "input": "10 10",
            "output": "10"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "modulo",
    "name": "Modular Reduction",
    "category": "Math",
    "what": "Using n % k to wrap indices around and drop redundant whole cycles.",
    "deep": "Modulo collapses equivalent states: rotating by k equals rotating by k % n, and hashing or bucketing uses remainders. In Java, beware that % can return a negative result for negative operands.",
    "java": "Use k % n after guarding n > 0; for possibly-negative values use Math.floorMod(x, n).",
    "language": "java",
    "lesson": "### Worked example\nRotating a length-5 array by k = 7 equals rotating by `7 % 5 = 2` — the extra full turns do nothing.\n\n### In code (Java)\n```java\nint n = 5, k = 7;\nk = k % n;                        // 2\nint idx = Math.floorMod(-1, n);   // 4, safe for negatives\n```\n\n### Watch out for\n- Java's `%` can be negative; `Math.floorMod` keeps the result in [0, n).\n- Reduce `k % n` before using it to index, and guard `n > 0`.\n",
    "exercises": [
      {
        "id": "modulo-reduce",
        "title": "Drop the extra turns",
        "prompt": "Rotating a length-`n` array by `k` is the same as rotating by `k % n`. Replace `____` with that reduced amount.",
        "hint": "k % n removes whole cycles, leaving the effective offset in [0, n).",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int k = sc.nextInt();\n        System.out.println(____);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int k = sc.nextInt();\n        System.out.println(k % n);\n    }\n}\n",
        "tests": [
          {
            "input": "5 7",
            "output": "2"
          },
          {
            "input": "3 3",
            "output": "0"
          },
          {
            "input": "10 4",
            "output": "4"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "modulo-floormod",
        "title": "Safe modulo of negatives",
        "prompt": "Java's `%` can be negative. Replace `____` with the call that always returns a non-negative remainder in `[0, n)`.",
        "hint": "Math.floorMod(x, n) wraps negatives correctly, unlike the % operator.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int x = sc.nextInt();\n        int n = sc.nextInt();\n        System.out.println(____);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int x = sc.nextInt();\n        int n = sc.nextInt();\n        System.out.println(Math.floorMod(x, n));\n    }\n}\n",
        "tests": [
          {
            "input": "-1 5",
            "output": "4"
          },
          {
            "input": "7 5",
            "output": "2"
          },
          {
            "input": "-7 3",
            "output": "2"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "modulo-clock",
        "title": "Wrap around a 24-hour clock",
        "prompt": "Write the solution: given a start hour and hours to add, print the resulting hour on a 24-hour clock (it must stay in 0..23).",
        "hint": "Add the hours, then reduce modulo 24 so the clock wraps past midnight.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int start = sc.nextInt();\n        int add = sc.nextInt();\n____\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int start = sc.nextInt();\n        int add = sc.nextInt();\n        int hour = Math.floorMod(start + add, 24);\n        System.out.println(hour);\n    }\n}\n",
        "tests": [
          {
            "input": "22 5",
            "output": "3"
          },
          {
            "input": "0 0",
            "output": "0"
          },
          {
            "input": "10 40",
            "output": "2"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "inplace_reverse",
    "name": "In-place Reversal",
    "category": "Arrays",
    "what": "Reversing a segment by swapping symmetric elements, using O(1) extra space.",
    "deep": "Swapping a[i] with a[n-1-i] for i in [0, n/2) reverses a range without allocation. It is the building block for array rotation (reverse-whole-then-parts) and for string reversal.",
    "java": "A helper void reverse(int[] a, int i, int j) { while (i < j) { int t = a[i]; a[i++] = a[j]; a[j--] = t; } }.",
    "language": "java",
    "lesson": "### When to reach for this\nSignals: **\"reverse\"**, **\"rotate by k\"**, or \"**O(1) extra space**\". Swap symmetric positions inward. The neat trick: a right-rotation is just three reversals.\n\n### Simulated solve — rotate `[1, 2, 3, 4, 5]` right by k = 2\nReverse the whole thing, then reverse the first `k`, then reverse the rest.\n\n| step | operation | array |\n| ---- | --------- | ----- |\n| start | — | 1 2 3 4 5 |\n| 1 | reverse all | 5 4 3 2 1 |\n| 2 | reverse first k = 2 | 4 5 3 2 1 |\n| 3 | reverse last n − k = 3 | **4 5 1 2 3** |\n\nAnswer: **4 5 1 2 3** — the last 2 elements wrapped to the front, no second array.\n\n### In code (Java)\n```java\nstatic void rev(int[] a, int i, int j) {\n    while (i < j) { int t = a[i]; a[i] = a[j]; a[j] = t; i++; j--; }\n}\n// rotate right by k:\nk %= n;                                  // rotating by n is a no-op\nrev(a, 0, n - 1);\nrev(a, 0, k - 1);\nrev(a, k, n - 1);\n```\n\n### Watch out for\n- Reduce `k %= n` first, or `k` past the length breaks the partial reversals.\n- Each swap is `a[i] ↔ a[j]`; the loop stops when `i >= j`.\n- O(1) extra space — that is the whole point over \"copy into a new array\".\n",
    "exercises": [
      {
        "id": "reverse-array",
        "title": "Reverse in place",
        "prompt": "Swap symmetric elements moving inward — no second array. Replace `____` with the three-line swap of `a[i]` and `a[j]`.",
        "hint": "Classic swap via a temp: int t = a[i]; a[i] = a[j]; a[j] = t;",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int i = 0, j = n - 1;\n        while (i < j) {\n            ____\n            i++;\n            j--;\n        }\n        StringBuilder sb = new StringBuilder();\n        for (int k = 0; k < n; k++) { sb.append(a[k]); if (k < n - 1) sb.append(' '); }\n        System.out.println(sb.toString());\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int i = 0, j = n - 1;\n        while (i < j) {\n            int t = a[i]; a[i] = a[j]; a[j] = t;\n            i++;\n            j--;\n        }\n        StringBuilder sb = new StringBuilder();\n        for (int k = 0; k < n; k++) { sb.append(a[k]); if (k < n - 1) sb.append(' '); }\n        System.out.println(sb.toString());\n    }\n}\n",
        "tests": [
          {
            "input": "4\n1 2 3 4",
            "output": "4 3 2 1"
          },
          {
            "input": "1\n7",
            "output": "7"
          },
          {
            "input": "3\n5 6 7",
            "output": "7 6 5"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "reverse-subrange",
        "title": "Reverse a sub-range",
        "prompt": "Reverse only `a[l..r]` (inclusive), leaving the rest untouched. Replace `____` with the swap of the current ends.",
        "hint": "Swap a[l] and a[r], then step l forward and r backward.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int l = sc.nextInt(), r = sc.nextInt();\n        while (l < r) {\n            ____\n            l++;\n            r--;\n        }\n        StringBuilder sb = new StringBuilder();\n        for (int k = 0; k < n; k++) { sb.append(a[k]); if (k < n - 1) sb.append(' '); }\n        System.out.println(sb.toString());\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int l = sc.nextInt(), r = sc.nextInt();\n        while (l < r) {\n            int t = a[l]; a[l] = a[r]; a[r] = t;\n            l++;\n            r--;\n        }\n        StringBuilder sb = new StringBuilder();\n        for (int k = 0; k < n; k++) { sb.append(a[k]); if (k < n - 1) sb.append(' '); }\n        System.out.println(sb.toString());\n    }\n}\n",
        "tests": [
          {
            "input": "5\n1 2 3 4 5\n1 3",
            "output": "1 4 3 2 5"
          },
          {
            "input": "3\n1 2 3\n0 2",
            "output": "3 2 1"
          },
          {
            "input": "4\n1 2 3 4\n2 2",
            "output": "1 2 3 4"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "reverse-rotate",
        "title": "Rotate right by k (three reversals)",
        "prompt": "Rotate the array right by `k` using the reversal trick: reverse all, reverse the first k, reverse the rest. The `rev` helper is written. Replace `____` with the two partial reversals.",
        "hint": "After reversing the whole array, reverse a[0..k-1] and a[k..n-1].",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    static void rev(int[] a, int i, int j) {\n        while (i < j) { int t = a[i]; a[i] = a[j]; a[j] = t; i++; j--; }\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int k = sc.nextInt() % n;\n        rev(a, 0, n - 1);\n        ____\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < n; i++) { sb.append(a[i]); if (i < n - 1) sb.append(' '); }\n        System.out.println(sb.toString());\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    static void rev(int[] a, int i, int j) {\n        while (i < j) { int t = a[i]; a[i] = a[j]; a[j] = t; i++; j--; }\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int k = sc.nextInt() % n;\n        rev(a, 0, n - 1);\n        rev(a, 0, k - 1);\n        rev(a, k, n - 1);\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < n; i++) { sb.append(a[i]); if (i < n - 1) sb.append(' '); }\n        System.out.println(sb.toString());\n    }\n}\n",
        "tests": [
          {
            "input": "5\n1 2 3 4 5\n2",
            "output": "4 5 1 2 3"
          },
          {
            "input": "3\n1 2 3\n0",
            "output": "1 2 3"
          },
          {
            "input": "4\n1 2 3 4\n5",
            "output": "4 1 2 3"
          }
        ],
        "source_slug": "rotate-array"
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "big_o",
    "name": "Big-O / Complexity Analysis",
    "category": "Foundations",
    "what": "Reasoning about how time and memory grow with the input size.",
    "deep": "Big-O bounds the dominant growth term and tells you whether an approach scales to the constraints. Read the limits: n up to 10^5 suggests O(n) or O(n log n), while 10^9 suggests O(log n) or O(1). Sanity-check the work against roughly 10^8 operations per second.",
    "java": "No API - this is analysis. Match your algorithm to the constraints stated in each problem before you start coding.",
    "language": "java",
    "lesson": "### Worked example\nWith n up to 100,000, an O(n^2) approach is ~10^10 operations (too slow), but O(n log n) is ~1.7 million — easily fast enough.\n\n### Rules of thumb\n```text\nn <= 20        -> even O(2^n) may pass\nn <= 5,000     -> O(n^2) is fine\nn <= 100,000   -> aim for O(n) or O(n log n)\nn <= 1e9       -> O(log n) or O(1)\n```\n\n### Watch out for\n- Read the constraints first; they hint at the intended complexity.\n- Budget roughly 10^8 simple operations per second as a guide.\n",
    "exercises": [],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "overflow",
    "name": "Integer Overflow (int vs long)",
    "category": "Foundations",
    "what": "Guarding against results that exceed a 32-bit int's range of about 2.1x10^9.",
    "deep": "Sums, products, and reversed numbers can silently wrap a 32-bit int, giving a wrong answer with no error. The fix is a wide enough type or a modulo reduction, so estimate the maximum magnitude before choosing the type.",
    "java": "int overflows past ~2.1e9; long reaches ~9.2e18. Cast early with (long) a * b and accumulate sums into a long.",
    "language": "java",
    "lesson": "### Worked example\n`100000 * 100000` overflows an `int` (max ~2.1e9) into a garbage negative number; as a `long` it is correctly 10^10.\n\n### In code (Java)\n```java\nint a = 100000;\nlong bad = a * a;          // overflow happens BEFORE the assignment\nlong good = (long) a * a;  // cast first -> 10_000_000_000\n```\n\n### Watch out for\n- Cast to `long` **before** the multiplication, not after.\n- Accumulate sums of many or large values into a `long`.\n",
    "exercises": [
      {
        "id": "overflow-sum-long",
        "title": "Sum without overflow",
        "prompt": "`a + b` can exceed the `int` range and wrap to a wrong value. Replace `____` so `sum` is computed in `long`.",
        "hint": "Promote the maths to long before it overflows: (long) a + b.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int a = sc.nextInt();\n        int b = sc.nextInt();\n        long sum = ____;\n        System.out.println(sum);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int a = sc.nextInt();\n        int b = sc.nextInt();\n        long sum = (long) a + b;\n        System.out.println(sum);\n    }\n}\n",
        "tests": [
          {
            "input": "2000000000 2000000000",
            "output": "4000000000"
          },
          {
            "input": "1 2",
            "output": "3"
          },
          {
            "input": "2147483647 1",
            "output": "2147483648"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "overflow-product",
        "title": "Product without overflow",
        "prompt": "Two ints can multiply past the `int` limit. Replace `____` so `product` is the correct `long`.",
        "hint": "Cast one factor first so the multiply happens in long: (long) a * b.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int a = sc.nextInt();\n        int b = sc.nextInt();\n        long product = ____;\n        System.out.println(product);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int a = sc.nextInt();\n        int b = sc.nextInt();\n        long product = (long) a * b;\n        System.out.println(product);\n    }\n}\n",
        "tests": [
          {
            "input": "100000 100000",
            "output": "10000000000"
          },
          {
            "input": "46341 46341",
            "output": "2147488281"
          },
          {
            "input": "3 4",
            "output": "12"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "overflow-factorial",
        "title": "Factorial that fits",
        "prompt": "13! already overflows `int`. Replace `____` so the running factorial `f` is a `long` (correct up to 20!).",
        "hint": "Declare f as long (starting at 1) so it can hold the huge product.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        ____\n        for (int i = 2; i <= n; i++) {\n            f *= i;\n        }\n        System.out.println(f);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        long f = 1;\n        for (int i = 2; i <= n; i++) {\n            f *= i;\n        }\n        System.out.println(f);\n    }\n}\n",
        "tests": [
          {
            "input": "13",
            "output": "6227020800"
          },
          {
            "input": "20",
            "output": "2432902008176640000"
          },
          {
            "input": "5",
            "output": "120"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "char_arrays",
    "name": "Char Arrays & ASCII",
    "category": "Strings",
    "what": "Manipulating characters via their numeric codes and mutable char arrays.",
    "deep": "Characters are small integers, so 'a'..'z' map to a 26-slot table via c - 'a'. Because Java strings are immutable, in-place character work needs a char[]. This underlies frequency counts, canonical keys, and in-place edits.",
    "java": "int idx = c - 'a'; char[] arr = s.toCharArray(); new String(arr) to rebuild. A length-26 or length-128 int[] beats a HashMap for letters.",
    "language": "java",
    "lesson": "### Worked example\nReverse `\"code\"` in place by converting to a char array and swapping the ends.\n\n### In code (Java)\n```java\nchar[] c = \"code\".toCharArray();\nint i = 0, j = c.length - 1;\nwhile (i < j) { char t = c[i]; c[i++] = c[j]; c[j--] = t; }\nSystem.out.println(new String(c)); // edoc\n\nint[] freq = new int[26];\nfor (char ch : \"aab\".toCharArray()) freq[ch - 'a']++;\n```\n\n### Watch out for\n- `ch - 'a'` maps 'a'..'z' to 0..25 — a fast index into a size-26 array.\n- Rebuild a String with `new String(charArray)`.\n",
    "exercises": [
      {
        "id": "char_arrays-index",
        "title": "Letter to index",
        "prompt": "Replace `____` with the expression that maps a lowercase letter to 0..25 (`'a'` -> 0, `'z'` -> 25).",
        "hint": "Subtracting the char 'a' turns a letter into its 0-based offset: c - 'a'.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String s = sc.next();\n        char c = s.charAt(0);\n        System.out.println(____);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String s = sc.next();\n        char c = s.charAt(0);\n        System.out.println(c - 'a');\n    }\n}\n",
        "tests": [
          {
            "input": "a",
            "output": "0"
          },
          {
            "input": "z",
            "output": "25"
          },
          {
            "input": "c",
            "output": "2"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "char_arrays-rebuild",
        "title": "Rebuild the string",
        "prompt": "The characters were reversed in place inside `c`. Replace `____` with the expression that turns the char array back into a String.",
        "hint": "new String(charArray) builds a String from a char[].",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        char[] c = sc.next().toCharArray();\n        int i = 0, j = c.length - 1;\n        while (i < j) {\n            char t = c[i];\n            c[i++] = c[j];\n            c[j--] = t;\n        }\n        System.out.println(____);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        char[] c = sc.next().toCharArray();\n        int i = 0, j = c.length - 1;\n        while (i < j) {\n            char t = c[i];\n            c[i++] = c[j];\n            c[j--] = t;\n        }\n        System.out.println(new String(c));\n    }\n}\n",
        "tests": [
          {
            "input": "code",
            "output": "edoc"
          },
          {
            "input": "abc",
            "output": "cba"
          },
          {
            "input": "racecar",
            "output": "racecar"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "char_arrays-maxfreq",
        "title": "Most frequent letter count",
        "prompt": "Write the solution: using a length-26 int array indexed by `ch - 'a'`, count each letter of the lowercase word and print the highest single-letter count.",
        "hint": "freq[ch - 'a']++ tallies each letter; then scan freq for the largest value.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String s = sc.next();\n____\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String s = sc.next();\n        int[] freq = new int[26];\n        for (char ch : s.toCharArray()) freq[ch - 'a']++;\n        int max = 0;\n        for (int f : freq) if (f > max) max = f;\n        System.out.println(max);\n    }\n}\n",
        "tests": [
          {
            "input": "banana",
            "output": "3"
          },
          {
            "input": "abc",
            "output": "1"
          },
          {
            "input": "aabbbc",
            "output": "3"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "visited_set",
    "name": "Visited Tracking",
    "category": "Data Structures",
    "what": "Recording which nodes or cells are already processed so you never revisit or double-count.",
    "deep": "Graph and grid traversals loop forever or over-count without visited marks. Mark as early as possible (on enqueue) so a node is not added twice; for grids you can use a boolean[][] or mutate the input to a sentinel value.",
    "java": "boolean[] visited / boolean[][] seen, or overwrite grid cells (set '1' to '0') when mutation is allowed.",
    "language": "java",
    "lesson": "### Worked example\nWalking a graph with a cycle loops forever unless you remember visited nodes; marking them makes each node processed once.\n\n### In code (Java)\n```java\nboolean[] seen = new boolean[n];        // graph\nboolean[][] seen2 = new boolean[rows][cols]; // grid\n```\n\n### Watch out for\n- Mark visited as early as possible (on enqueue) to avoid duplicates.\n- For grids you can overwrite the cell (set '1' to '0') instead of a separate array.\n",
    "exercises": [],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "queue",
    "name": "Queue (FIFO)",
    "category": "Data Structures",
    "what": "A first-in-first-out structure that returns items in arrival order.",
    "deep": "Queues drive breadth-first processing: BFS levels, Kahn's topological sort, and streaming buffers. Enqueue at the back and dequeue from the front, both in O(1).",
    "java": "ArrayDeque<T> via offer/poll (avoid the slower LinkedList, and never the legacy Stack/Vector).",
    "language": "java",
    "lesson": "### Worked example\nA queue serves in arrival order: offer 1, 2, 3 and `poll` returns 1, then 2, then 3.\n\n### In code (Java)\n```java\nQueue<Integer> q = new ArrayDeque<>();\nq.offer(1); q.offer(2);\nint first = q.poll(); // 1\n```\n\n### Watch out for\n- Use `ArrayDeque`, not `LinkedList` (slower) or the legacy `Stack`.\n- `poll()` returns null on an empty queue — check `isEmpty()` in loops.\n",
    "exercises": [
      {
        "id": "queue-offer",
        "title": "Join the queue",
        "prompt": "Replace `____` to add each number to the back of the queue, so polling returns them in arrival order.",
        "hint": "offer adds to the back; poll removes from the front — FIFO keeps arrival order.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        Queue<Integer> q = new ArrayDeque<>();\n        for (int i = 0; i < n; i++) ____;\n        StringBuilder sb = new StringBuilder();\n        while (!q.isEmpty()) sb.append(q.poll()).append(' ');\n        System.out.println(sb.toString().trim());\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        Queue<Integer> q = new ArrayDeque<>();\n        for (int i = 0; i < n; i++) q.offer(sc.nextInt());\n        StringBuilder sb = new StringBuilder();\n        while (!q.isEmpty()) sb.append(q.poll()).append(' ');\n        System.out.println(sb.toString().trim());\n    }\n}\n",
        "tests": [
          {
            "input": "3\n1 2 3",
            "output": "1 2 3"
          },
          {
            "input": "1\n9",
            "output": "9"
          },
          {
            "input": "4\n4 3 2 1",
            "output": "4 3 2 1"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "queue-first",
        "title": "First served",
        "prompt": "Replace `____` with the call that removes and returns the front of the queue.",
        "hint": "poll() takes the item at the front — the one that was added first.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        Queue<Integer> q = new ArrayDeque<>();\n        for (int i = 0; i < n; i++) q.offer(sc.nextInt());\n        System.out.println(____);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        Queue<Integer> q = new ArrayDeque<>();\n        for (int i = 0; i < n; i++) q.offer(sc.nextInt());\n        System.out.println(q.poll());\n    }\n}\n",
        "tests": [
          {
            "input": "3\n5 6 7",
            "output": "5"
          },
          {
            "input": "1\n9",
            "output": "9"
          },
          {
            "input": "2\n2 1",
            "output": "2"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "queue-potato",
        "title": "Hot potato (last one standing)",
        "prompt": "Players `1..n` stand in a circle; every `k`th player is removed. Write the solution using a queue: move `k-1` players to the back, remove the next, repeat until one remains, then print them.",
        "hint": "Rotating k-1 to the back puts the k-th player at the front, ready to be polled off.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int k = sc.nextInt();\n        Queue<Integer> q = new ArrayDeque<>();\n        for (int i = 1; i <= n; i++) q.offer(i);\n____\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int k = sc.nextInt();\n        Queue<Integer> q = new ArrayDeque<>();\n        for (int i = 1; i <= n; i++) q.offer(i);\n        while (q.size() > 1) {\n            for (int i = 0; i < k - 1; i++) q.offer(q.poll());\n            q.poll();\n        }\n        System.out.println(q.poll());\n    }\n}\n",
        "tests": [
          {
            "input": "5 2",
            "output": "3"
          },
          {
            "input": "1 3",
            "output": "1"
          },
          {
            "input": "7 3",
            "output": "4"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "io_basics",
    "name": "Reading Input & Printing",
    "category": "Foundations",
    "what": "Getting values in from standard input and writing answers out to standard output.",
    "deep": "Every problem here communicates over stdin/stdout: your program reads the input the grader feeds it and prints the answer. Reading the input format correctly is half the battle when you are starting out, so always match how many numbers or lines the problem describes.",
    "java": "Scanner sc = new Scanner(System.in); then sc.nextInt(), sc.next(), or sc.nextLine(). Print with System.out.println(...). Switch to BufferedReader later for speed.",
    "language": "java",
    "lesson": "### Worked example\nThe grader sends text on **standard input**; you reply on **standard output**. If the input is `3 4`, you read the two numbers and print their sum, `7`.\n\n### In code (Java)\n```java\nimport java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int a = sc.nextInt();       // reads 3\n        int b = sc.nextInt();       // reads 4\n        System.out.println(a + b);  // prints 7\n    }\n}\n```\n\n### Watch out for\n- `nextInt()` reads one number; `nextLine()` reads a whole line (use it for text).\n- Print **only** the answer. Extra text like `Enter a number:` will fail the grader.\n",
    "exercises": [
      {
        "id": "io_basics-sum",
        "title": "Print the sum",
        "prompt": "The two numbers are already read into `a` and `b`. Replace `____` so the program prints their sum.",
        "hint": "System.out.println(...) prints one line — put a + b inside the parentheses.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int a = sc.nextInt();\n        int b = sc.nextInt();\n        ____\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int a = sc.nextInt();\n        int b = sc.nextInt();\n        System.out.println(a + b);\n    }\n}\n",
        "tests": [
          {
            "input": "3 4",
            "output": "7"
          },
          {
            "input": "10 -2",
            "output": "8"
          },
          {
            "input": "0 0",
            "output": "0"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "io_basics-echo-line",
        "title": "Read a whole line",
        "prompt": "The program should echo one full line of text. Replace `____` with the statement that reads the line into `line`.",
        "hint": "nextLine() reads a whole line (spaces and all); nextInt() would only grab one number.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        ____\n        System.out.println(line);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String line = sc.nextLine();\n        System.out.println(line);\n    }\n}\n",
        "tests": [
          {
            "input": "hello world",
            "output": "hello world"
          },
          {
            "input": "Poodcode rocks",
            "output": "Poodcode rocks"
          },
          {
            "input": "42 is just text here",
            "output": "42 is just text here"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "io_basics-greet",
        "title": "Build the greeting",
        "prompt": "The name is in `name`. Replace `____` to print exactly `Hi, <name>!` — for input `Ada` print `Hi, Ada!`.",
        "hint": "Glue text together with + : \"Hi, \" + name + \"!\".",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String name = sc.nextLine();\n        ____\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String name = sc.nextLine();\n        System.out.println(\"Hi, \" + name + \"!\");\n    }\n}\n",
        "tests": [
          {
            "input": "Ada",
            "output": "Hi, Ada!"
          },
          {
            "input": "Bob",
            "output": "Hi, Bob!"
          },
          {
            "input": "Grace Hopper",
            "output": "Hi, Grace Hopper!"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "variables",
    "name": "Variables & Types",
    "category": "Foundations",
    "what": "Named boxes that hold values of a specific type such as int, long, double, boolean, or String.",
    "deep": "A variable stores a value you can read and update. Java is statically typed, so you declare the type up front and it never changes. Picking the right type (int vs long, int vs double) prevents overflow and rounding surprises down the line.",
    "java": "Declare with a type: int n = 5; long big = 10000000000L; double d = 3.14; boolean ok = true; String s = ... .",
    "language": "java",
    "lesson": "### Worked example\nA variable is a labelled box. `int score = 0;` makes a box named `score` holding 0; `score = score + 10;` updates it to 10.\n\n### In code (Java)\n```java\nint count = 5;               // whole number\nlong big = 5_000_000_000L;   // too big for int -> long, note the L\ndouble avg = 4.5;            // decimal\nboolean done = false;        // true / false\nString name = \"Ada\";         // text\n```\n\n### Watch out for\n- `int` holds about plus or minus 2.1 billion; if a value can exceed that, use `long`.\n- Dividing two `int`s drops the fraction (`7 / 2` is `3`). Use `double` for `3.5`.\n",
    "exercises": [
      {
        "id": "variables-long",
        "title": "Hold a big number",
        "prompt": "The input is larger than an `int` can hold (over 2.1 billion). Replace `____` to read it into a `long` named `big`.",
        "hint": "Declare the variable as long and read it with sc.nextLong().",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        ____\n        System.out.println(big);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        long big = sc.nextLong();\n        System.out.println(big);\n    }\n}\n",
        "tests": [
          {
            "input": "5000000000",
            "output": "5000000000"
          },
          {
            "input": "9999999999",
            "output": "9999999999"
          },
          {
            "input": "42",
            "output": "42"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "variables-avg",
        "title": "Average as a decimal",
        "prompt": "Replace `____` so `avg` is the true average of `a` and `b` — a decimal, not rounded down.",
        "hint": "Divide by 2.0 (a double) so the fraction survives, and store it in a double.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int a = sc.nextInt();\n        int b = sc.nextInt();\n        ____\n        System.out.println(avg);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int a = sc.nextInt();\n        int b = sc.nextInt();\n        double avg = (a + b) / 2.0;\n        System.out.println(avg);\n    }\n}\n",
        "tests": [
          {
            "input": "3 4",
            "output": "3.5"
          },
          {
            "input": "10 10",
            "output": "10.0"
          },
          {
            "input": "1 2",
            "output": "1.5"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "variables-boolean",
        "title": "Store a yes/no",
        "prompt": "Replace `____` so `isZero` is `true` when `n` equals 0 and `false` otherwise.",
        "hint": "Compare numbers with ==, and keep the true/false result in a boolean.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        ____\n        System.out.println(isZero);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        boolean isZero = (n == 0);\n        System.out.println(isZero);\n    }\n}\n",
        "tests": [
          {
            "input": "0",
            "output": "true"
          },
          {
            "input": "5",
            "output": "false"
          },
          {
            "input": "-3",
            "output": "false"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "arithmetic",
    "name": "Arithmetic & Operators",
    "category": "Foundations",
    "what": "Doing math with +, -, *, / and the remainder operator %.",
    "deep": "Operators combine values. The Java gotchas: integer division truncates (7 / 2 is 3, not 3.5), % gives the remainder (great for even/odd and wrapping), and mixing an int with a double promotes the result to double.",
    "java": "Use +, -, *, /, %. Note 7 / 2 == 3 (int) but 7.0 / 2 == 3.5 (double). Math.max, Math.min, Math.abs help too.",
    "language": "java",
    "lesson": "### Worked example\n`%` (remainder) is the beginner's secret weapon: `17 % 5` is `2`, and `n % 2 == 0` tests even.\n\n### In code (Java)\n```java\nint a = 17, b = 5;\nSystem.out.println(a + b);   // 22\nSystem.out.println(a / b);   // 3   (integer division)\nSystem.out.println(a % b);   // 2   (remainder)\nSystem.out.println(7.0 / 2); // 3.5 (double math)\n```\n\n### Watch out for\n- Integer division truncates toward zero — there is no rounding.\n- `%` of a negative can be negative in Java (`-3 % 2` is `-1`).\n",
    "exercises": [
      {
        "id": "arithmetic-divmod",
        "title": "Quotient and remainder",
        "prompt": "Replace `____` to print the integer quotient and remainder of `a / b`, separated by a space (e.g. `17 5` -> `3 2`).",
        "hint": "/ is integer division; % is the remainder.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int a = sc.nextInt();\n        int b = sc.nextInt();\n        System.out.println(____);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int a = sc.nextInt();\n        int b = sc.nextInt();\n        System.out.println(a / b + \" \" + a % b);\n    }\n}\n",
        "tests": [
          {
            "input": "17 5",
            "output": "3 2"
          },
          {
            "input": "20 4",
            "output": "5 0"
          },
          {
            "input": "7 3",
            "output": "2 1"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "arithmetic-lastdigit",
        "title": "Last digit",
        "prompt": "Replace `____` to print the last digit of `n` (e.g. 1234 -> 4).",
        "hint": "The last decimal digit is the remainder after dividing by 10.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        System.out.println(____);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        System.out.println(n % 10);\n    }\n}\n",
        "tests": [
          {
            "input": "1234",
            "output": "4"
          },
          {
            "input": "7",
            "output": "7"
          },
          {
            "input": "40",
            "output": "0"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "arithmetic-double-div",
        "title": "Keep the fraction",
        "prompt": "`a / b` on two ints throws the fraction away. Replace `____` to print the exact decimal result (e.g. 7 2 -> 3.5).",
        "hint": "Cast one side to double first: (double) a / b.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int a = sc.nextInt();\n        int b = sc.nextInt();\n        System.out.println(____);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int a = sc.nextInt();\n        int b = sc.nextInt();\n        System.out.println((double) a / b);\n    }\n}\n",
        "tests": [
          {
            "input": "7 2",
            "output": "3.5"
          },
          {
            "input": "9 4",
            "output": "2.25"
          },
          {
            "input": "5 2",
            "output": "2.5"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "conditionals",
    "name": "Conditionals (if / else)",
    "category": "Foundations",
    "what": "Choosing what to do based on whether a condition is true.",
    "deep": "if/else branches let your program react to data. The condition is a boolean expression built from comparisons (==, !=, <, >) and logic (&&, ||, !). Chain several with else if when there are more than two outcomes.",
    "java": "if (n % 2 == 0) { ... } else { ... }. Compare ints with ==, but compare Strings with .equals(), never ==.",
    "language": "java",
    "lesson": "### Worked example\nLabel a number: if it divides evenly by 2 it is Even, otherwise Odd.\n\n### In code (Java)\n```java\nint n = 7;\nif (n % 2 == 0) {\n    System.out.println(\"Even\");\n} else {\n    System.out.println(\"Odd\");\n}\n```\n\n### Watch out for\n- Compare numbers with `==`, but compare Strings with `a.equals(b)`.\n- Use `else if` to chain more than two outcomes.\n",
    "exercises": [
      {
        "id": "conditionals-evenodd",
        "title": "Even or odd",
        "prompt": "Replace `____` with the condition that is true when `n` is even, so the program prints `Even` or `Odd`.",
        "hint": "A number is even when its remainder mod 2 is 0.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        if (____) {\n            System.out.println(\"Even\");\n        } else {\n            System.out.println(\"Odd\");\n        }\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        if (n % 2 == 0) {\n            System.out.println(\"Even\");\n        } else {\n            System.out.println(\"Odd\");\n        }\n    }\n}\n",
        "tests": [
          {
            "input": "4",
            "output": "Even"
          },
          {
            "input": "7",
            "output": "Odd"
          },
          {
            "input": "0",
            "output": "Even"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "conditionals-sign",
        "title": "Positive, negative, zero",
        "prompt": "The `Positive` and `Zero` branches are written. Replace `____` with the middle branch that catches negative numbers.",
        "hint": "Chain another test with `else if (...)` between the first if and the final else.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        if (n > 0) {\n            System.out.println(\"Positive\");\n        } ____ {\n            System.out.println(\"Negative\");\n        } else {\n            System.out.println(\"Zero\");\n        }\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        if (n > 0) {\n            System.out.println(\"Positive\");\n        } else if (n < 0) {\n            System.out.println(\"Negative\");\n        } else {\n            System.out.println(\"Zero\");\n        }\n    }\n}\n",
        "tests": [
          {
            "input": "5",
            "output": "Positive"
          },
          {
            "input": "-2",
            "output": "Negative"
          },
          {
            "input": "0",
            "output": "Zero"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "conditionals-max2",
        "title": "The larger of two",
        "prompt": "Replace `____` so the program prints the larger of `a` and `b` (print either when they are equal).",
        "hint": "Test whether a is greater than b.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int a = sc.nextInt();\n        int b = sc.nextInt();\n        ____ {\n            System.out.println(a);\n        } else {\n            System.out.println(b);\n        }\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int a = sc.nextInt();\n        int b = sc.nextInt();\n        if (a > b) {\n            System.out.println(a);\n        } else {\n            System.out.println(b);\n        }\n    }\n}\n",
        "tests": [
          {
            "input": "3 8",
            "output": "8"
          },
          {
            "input": "10 4",
            "output": "10"
          },
          {
            "input": "5 5",
            "output": "5"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "loops_basic",
    "name": "Loops (for / while)",
    "category": "Foundations",
    "what": "Repeating a block of code a set number of times or until a condition changes.",
    "deep": "A for-loop fits when you know the count; a while-loop when you stop on a condition. Loops are how you process every element or accumulate a running result, and getting the start/end bounds right is the main beginner pitfall.",
    "java": "for (int i = 1; i <= n; i++) { ... } or while (condition) { ... }. Build output strings with StringBuilder inside the loop.",
    "language": "java",
    "lesson": "### Worked example\nAdd 1..n. For n = 5 you want 1+2+3+4+5 = 15.\n\n### In code (Java)\n```java\nint n = 5;\nlong sum = 0;\nfor (int i = 1; i <= n; i++) {\n    sum += i;   // 1, 3, 6, 10, 15\n}\nSystem.out.println(sum);\n```\n\n### Watch out for\n- Off-by-one: `i <= n` includes n, `i < n` stops one short.\n- Count down with `for (int i = n; i >= 1; i--)`.\n",
    "exercises": [
      {
        "id": "loops_basic-sum",
        "title": "Sum 1..n",
        "prompt": "The loop runs `i` from 1 to n. Replace `____` in the loop body so `sum` ends up as 1+2+...+n.",
        "hint": "Add the current i to sum each pass: sum += i;",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        long sum = 0;\n        for (int i = 1; i <= n; i++) {\n            ____\n        }\n        System.out.println(sum);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        long sum = 0;\n        for (int i = 1; i <= n; i++) {\n            sum += i;\n        }\n        System.out.println(sum);\n    }\n}\n",
        "tests": [
          {
            "input": "5",
            "output": "15"
          },
          {
            "input": "1",
            "output": "1"
          },
          {
            "input": "100",
            "output": "5050"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "loops_basic-factorial",
        "title": "Factorial",
        "prompt": "Replace `____` so `f` becomes n! = 1*2*...*n (5! is 120).",
        "hint": "Multiply f by each i: f *= i;",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        long f = 1;\n        for (int i = 2; i <= n; i++) {\n            ____\n        }\n        System.out.println(f);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        long f = 1;\n        for (int i = 2; i <= n; i++) {\n            f *= i;\n        }\n        System.out.println(f);\n    }\n}\n",
        "tests": [
          {
            "input": "5",
            "output": "120"
          },
          {
            "input": "1",
            "output": "1"
          },
          {
            "input": "10",
            "output": "3628800"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "loops_basic-countdown",
        "title": "Count down",
        "prompt": "Replace `____` in the for-header so the loop counts down from n to 1, printing `n ... 2 1` (e.g. 3 -> `3 2 1`).",
        "hint": "Start i at n, keep going while i >= 1, and step with i--.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        StringBuilder sb = new StringBuilder();\n        for (____) {\n            sb.append(i);\n            if (i > 1) sb.append(\" \");\n        }\n        System.out.println(sb.toString());\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        StringBuilder sb = new StringBuilder();\n        for (int i = n; i >= 1; i--) {\n            sb.append(i);\n            if (i > 1) sb.append(\" \");\n        }\n        System.out.println(sb.toString());\n    }\n}\n",
        "tests": [
          {
            "input": "3",
            "output": "3 2 1"
          },
          {
            "input": "1",
            "output": "1"
          },
          {
            "input": "5",
            "output": "5 4 3 2 1"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "boolean_logic",
    "name": "Booleans & Comparison",
    "category": "Foundations",
    "what": "True/false values and the operators that combine them.",
    "deep": "Comparisons produce booleans, and &&, ||, ! combine them. Short-circuit evaluation means the right side of && is skipped when the left is false, which is both handy and sometimes necessary to avoid errors.",
    "java": "boolean type; comparisons ==, !=, <, <=, >, >=; logic && (and), || (or), ! (not).",
    "language": "java",
    "lesson": "### Worked example\nA number is in the range [1, 10] when it is `>= 1` **and** `<= 10`.\n\n### In code (Java)\n```java\nint x = 7;\nboolean inRange = (x >= 1) && (x <= 10); // true\nboolean outside = (x < 1) || (x > 10);   // false\nSystem.out.println(inRange && !outside); // true\n```\n\n### Watch out for\n- `&&` and `||` short-circuit: the right side is skipped once the result is known.\n- `=` assigns while `==` compares — a very common typo.\n",
    "exercises": [
      {
        "id": "boolean_logic-inrange",
        "title": "Inside a range",
        "prompt": "Replace `____` so `inRange` is true exactly when x is between 1 and 10, inclusive.",
        "hint": "Both parts must hold at once — combine them with &&.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int x = sc.nextInt();\n        boolean inRange = ____;\n        System.out.println(inRange);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int x = sc.nextInt();\n        boolean inRange = (x >= 1) && (x <= 10);\n        System.out.println(inRange);\n    }\n}\n",
        "tests": [
          {
            "input": "5",
            "output": "true"
          },
          {
            "input": "10",
            "output": "true"
          },
          {
            "input": "11",
            "output": "false"
          },
          {
            "input": "0",
            "output": "false"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "boolean_logic-div3or5",
        "title": "Divisible by 3 or 5",
        "prompt": "Replace `____` so `hit` is true when x is divisible by 3 or by 5 (or both).",
        "hint": "Either condition is enough — combine them with ||.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int x = sc.nextInt();\n        boolean hit = ____;\n        System.out.println(hit);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int x = sc.nextInt();\n        boolean hit = (x % 3 == 0) || (x % 5 == 0);\n        System.out.println(hit);\n    }\n}\n",
        "tests": [
          {
            "input": "9",
            "output": "true"
          },
          {
            "input": "10",
            "output": "true"
          },
          {
            "input": "7",
            "output": "false"
          },
          {
            "input": "15",
            "output": "true"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "boolean_logic-not-even",
        "title": "Flip a boolean",
        "prompt": "`even` is already computed. Replace `____` so `odd` is its logical opposite using `!`.",
        "hint": "! flips a boolean: !even is true when even is false.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int x = sc.nextInt();\n        boolean even = (x % 2 == 0);\n        boolean odd = ____;\n        System.out.println(odd);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int x = sc.nextInt();\n        boolean even = (x % 2 == 0);\n        boolean odd = !even;\n        System.out.println(odd);\n    }\n}\n",
        "tests": [
          {
            "input": "7",
            "output": "true"
          },
          {
            "input": "4",
            "output": "false"
          },
          {
            "input": "0",
            "output": "false"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "bit_manip",
    "name": "Bit Manipulation",
    "category": "Foundations",
    "what": "Operating on the individual binary bits of an integer with AND, OR, XOR, and shifts.",
    "deep": "Bit tricks give O(1) tools that replace loops: `x & (x-1)` clears the lowest set bit, `x & -x` isolates it, XOR cancels equal values, and shifting multiplies or divides by powers of two. They shine in set membership (bitmasks), parity, and counting.",
    "java": "Use &, |, ^, ~, <<, >>, and >>> (unsigned shift). Integer.bitCount(x) counts set bits; x & (x-1) drops the lowest one.",
    "language": "java",
    "lesson": "### Worked example\nBitwise operators act on the binary form of an integer. `5` is `101`, so bits 0 and 2 are set. `5 & 1` (AND with `001`) is `1`, telling you the lowest bit is on — i.e. the number is odd.\n\n### In code (Java)\n```java\nint n = 5;               // 101\nn & 1;                   // 1  — lowest bit (odd?)\nn >> 1;                  // 2  — divide by 2 (drop lowest bit)\nn << 1;                  // 10 — multiply by 2\nInteger.bitCount(n);     // 2  — number of set bits\nn & (n - 1);             // 4  — clears the lowest set bit\n```\n\n### Watch out for\n- `&` AND, `|` OR, `^` XOR, `~` NOT, `<<` / `>>` shift, `>>>` unsigned shift.\n- Test bit `i` with `(n >> i) & 1`; set it with `n | (1 << i)`.\n- For `x > 0`, `(x & (x - 1)) == 0` means `x` is a power of two.\n",
    "exercises": [
      {
        "id": "bit_manip-testbit",
        "title": "Is a bit set?",
        "prompt": "Replace `____` with the expression that is true when bit `i` of `n` is 1.",
        "hint": "Shift bit i down to position 0 with n >> i, then mask it with & 1.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int i = sc.nextInt();\n        boolean set = ____;\n        System.out.println(set);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int i = sc.nextInt();\n        boolean set = ((n >> i) & 1) == 1;\n        System.out.println(set);\n    }\n}\n",
        "tests": [
          {
            "input": "5 0",
            "output": "true"
          },
          {
            "input": "5 1",
            "output": "false"
          },
          {
            "input": "5 2",
            "output": "true"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "bit_manip-popcount",
        "title": "Count the set bits",
        "prompt": "Replace `____` with the trick that clears the lowest set bit of `n`, so the loop runs once per 1-bit.",
        "hint": "n & (n - 1) removes the lowest set bit; count how many times you can do it.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int count = 0;\n        while (n != 0) {\n            n = ____;\n            count++;\n        }\n        System.out.println(count);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int count = 0;\n        while (n != 0) {\n            n = n & (n - 1);\n            count++;\n        }\n        System.out.println(count);\n    }\n}\n",
        "tests": [
          {
            "input": "5",
            "output": "2"
          },
          {
            "input": "7",
            "output": "3"
          },
          {
            "input": "8",
            "output": "1"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "bit_manip-poweroftwo",
        "title": "Power of two?",
        "prompt": "Write the solution: print `true` when `n` is a positive power of two. A power of two has exactly one set bit.",
        "hint": "For x > 0, (x & (x - 1)) == 0 is true exactly when x has a single set bit.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n____\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        boolean pow = n > 0 && (n & (n - 1)) == 0;\n        System.out.println(pow);\n    }\n}\n",
        "tests": [
          {
            "input": "8",
            "output": "true"
          },
          {
            "input": "6",
            "output": "false"
          },
          {
            "input": "1",
            "output": "true"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "prefix_sum",
    "name": "Prefix Sums",
    "category": "Arrays",
    "what": "Precomputed running totals so any range sum is a single subtraction.",
    "deep": "Build P[i] = a[0]+…+a[i-1]; then the sum of a[l..r] is P[r+1] − P[l] in O(1). Paired with a hash map of seen prefixes it counts subarrays with a target sum in one pass. The same idea extends to prefix XOR, products, and 2-D grids.",
    "java": "A long[] prefix of length n+1, or a running long combined with a HashMap<Long,Integer> of prefix counts.",
    "language": "java",
    "lesson": "### Worked example\nTo answer \"sum of elements from index 1 to 3\" instantly, precompute running totals. With `a = [1, 2, 3, 4, 5]` the prefix array is `[0, 1, 3, 6, 10, 15]` (length n+1). The range sum `a[1..3]` is `prefix[4] - prefix[1] = 10 - 1 = 9` — one subtraction, no loop.\n\n### In code (Java)\n```java\nint[] a = {1, 2, 3, 4, 5};\nlong[] prefix = new long[a.length + 1];\nfor (int i = 0; i < a.length; i++) prefix[i + 1] = prefix[i] + a[i];\nlong rangeSum = prefix[4] - prefix[1]; // sum of a[1..3] = 9\n```\n\n### Watch out for\n- `prefix[i]` is the sum of the first `i` elements, so `prefix[0] = 0` and `prefix[n]` is the grand total.\n- The inclusive range `[l, r]` is `prefix[r + 1] - prefix[l]`.\n- Use a `long[]` prefix — repeated sums overflow `int` quickly.\n",
    "exercises": [
      {
        "id": "prefix_sum-build",
        "title": "Build the prefix array",
        "prompt": "Replace `____` with the line that fills `prefix[i + 1]` as the running total (previous prefix plus the next value).",
        "hint": "Each prefix entry is the one before it plus the current element.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        long[] prefix = new long[n + 1];\n        for (int i = 0; i < n; i++) {\n            ____\n        }\n        int l = sc.nextInt();\n        int r = sc.nextInt();\n        System.out.println(prefix[r + 1] - prefix[l]);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        long[] prefix = new long[n + 1];\n        for (int i = 0; i < n; i++) {\n            prefix[i + 1] = prefix[i] + sc.nextInt();\n        }\n        int l = sc.nextInt();\n        int r = sc.nextInt();\n        System.out.println(prefix[r + 1] - prefix[l]);\n    }\n}\n",
        "tests": [
          {
            "input": "5\n1 2 3 4 5\n1 3",
            "output": "9"
          },
          {
            "input": "3\n10 20 30\n0 2",
            "output": "60"
          },
          {
            "input": "4\n1 1 1 1\n2 3",
            "output": "2"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "prefix_sum-total",
        "title": "The grand total",
        "prompt": "`prefix[n]` holds the sum of all `n` elements. Replace `____` with that value.",
        "hint": "prefix[n] is the running total after adding every element.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        long[] prefix = new long[n + 1];\n        for (int i = 0; i < n; i++) {\n            prefix[i + 1] = prefix[i] + sc.nextInt();\n        }\n        System.out.println(____);\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        long[] prefix = new long[n + 1];\n        for (int i = 0; i < n; i++) {\n            prefix[i + 1] = prefix[i] + sc.nextInt();\n        }\n        System.out.println(prefix[n]);\n    }\n}\n",
        "tests": [
          {
            "input": "5\n1 2 3 4 5",
            "output": "15"
          },
          {
            "input": "3\n10 20 30",
            "output": "60"
          },
          {
            "input": "1\n7",
            "output": "7"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "prefix_sum-maxrange",
        "title": "Best range among queries",
        "prompt": "After the values comes a count `q` and then `q` ranges `l r`. Write the solution: print the largest range sum among the queries (each is `prefix[r + 1] - prefix[l]`).",
        "hint": "Each query is one subtraction on the prefix array; keep the maximum.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        long[] prefix = new long[n + 1];\n        for (int i = 0; i < n; i++) prefix[i + 1] = prefix[i] + sc.nextInt();\n____\n    }\n}\n",
        "solution": "import java.util.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        long[] prefix = new long[n + 1];\n        for (int i = 0; i < n; i++) prefix[i + 1] = prefix[i] + sc.nextInt();\n        int q = sc.nextInt();\n        long best = Long.MIN_VALUE;\n        for (int i = 0; i < q; i++) {\n            int l = sc.nextInt();\n            int r = sc.nextInt();\n            best = Math.max(best, prefix[r + 1] - prefix[l]);\n        }\n        System.out.println(best);\n    }\n}\n",
        "tests": [
          {
            "input": "5\n1 2 3 4 5\n2\n0 4\n1 2",
            "output": "15"
          },
          {
            "input": "3\n10 20 30\n2\n0 0\n1 2",
            "output": "50"
          },
          {
            "input": "4\n1 1 1 1\n1\n0 3",
            "output": "4"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "grid",
    "name": "Grid / Matrix Traversal",
    "category": "Arrays",
    "what": "Walking a 2-D board with nested loops, neighbour offsets, and bounds checks.",
    "deep": "Most grid problems are two nested loops over rows and columns. Neighbours come from offset lists — 4-directional {(-1,0),(1,0),(0,-1),(0,1)} or 8-directional including diagonals — and every access must be bounds-checked (0<=r<H and 0<=c<W). Manhattan distance |r1-r2|+|c1-c2| counts 4-directional steps between two cells.",
    "java": "char[][] or int[][]; loop for(i..H) for(j..W). Keep int[] dr={-1,1,0,0}, dc={0,0,-1,1} for the four neighbours (or all 8) and guard indices before reading.",
    "language": "java",
    "lesson": "",
    "exercises": [],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "simulation",
    "name": "Simulation",
    "category": "Foundations",
    "what": "Directly modelling a process step by step, exactly as the statement describes, instead of finding a formula.",
    "deep": "When constraints are small, the intended solution is often to just do what the problem says: hold the state, apply each event or rule in order, and read the answer at the end. The skill is translating rules into precise state transitions and handling edge cases and 1- vs 0-indexing carefully.",
    "java": "Model state in an array or a small int/enum code; process events in a loop, updating state per the rules. Watch off-by-one and 1-indexed input.",
    "language": "java",
    "lesson": "",
    "exercises": [],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "tree_basics",
    "name": "Binary Trees",
    "category": "Trees",
    "what": "Nodes each linking to a left and right child, forming a hierarchy with one root and leaves at the bottom.",
    "deep": "A binary tree is defined recursively: a node plus a left subtree and a right subtree. Most tree algorithms are one recursive function whose base case is the empty subtree (null). The height is the longest root-to-leaf chain; balanced trees keep it near log n.",
    "java": "class TreeNode { int val; TreeNode left, right; }. Recurse with a helper; the base case is `if (node == null) return ...;`.",
    "language": "java",
    "lesson": "# Binary Trees\n\nA **binary tree** is either empty (`null`) or a node holding a value plus a **left** and **right** subtree — each itself a binary tree. That recursive shape is why almost every tree algorithm is a recursive function whose base case is `null`.\n\n```java\nclass TreeNode { int val; TreeNode left, right; TreeNode(int v){ val = v; } }\n\nint size(TreeNode n) {\n    if (n == null) return 0;            // base case\n    return 1 + size(n.left) + size(n.right);\n}\n```\n\n## When to reach for this\nAny time the input is a hierarchy: parents/children, folders, expression trees. Signals: *'root'*, *'leaf'*, *'subtree'*, *'depth/height'*.\n\n## Serialization used here\nProblems give the tree in **level order** with `null` for missing children, e.g. `3 9 20 null null 15 7`:\n\n| token | 3 | 9 | 20 | null | null | 15 | 7 |\n|---|---|---|---|---|---|---|---|\n| role | root | 3.left | 3.right | 9.left | 9.right | 20.left | 20.right |\n\nThe starter ships a `build()` helper so you can focus on the algorithm.",
    "exercises": [
      {
        "id": "tree_basics-size",
        "title": "Count the nodes",
        "prompt": "The recursion is set up. Fill the blank so `depth` returns 1 plus the deeper child (this drill measures height).",
        "hint": "Height = 1 + the taller of the two subtrees.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    static class TreeNode { int val; TreeNode left, right; TreeNode(int v){ val = v; } }\n    static TreeNode build(String[] a) {\n        if (a.length == 0 || a[0].equals(\"null\")) return null;\n        TreeNode root = new TreeNode(Integer.parseInt(a[0]));\n        ArrayDeque<TreeNode> q = new ArrayDeque<>(); q.add(root); int i = 1;\n        while (!q.isEmpty() && i < a.length) {\n            TreeNode n = q.poll();\n            if (i < a.length && !a[i].equals(\"null\")) { n.left = new TreeNode(Integer.parseInt(a[i])); q.add(n.left); } i++;\n            if (i < a.length && !a[i].equals(\"null\")) { n.right = new TreeNode(Integer.parseInt(a[i])); q.add(n.right); } i++;\n        }\n        return root;\n    }\n    static int depth(TreeNode n) {\n        if (n == null) return 0;\n        return ____;\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String line = sc.hasNextLine() ? sc.nextLine() : \"\";\n        String[] a = line.trim().isEmpty() ? new String[0] : line.trim().split(\"\\\\s+\");\n        System.out.println(depth(build(a)));\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    static class TreeNode { int val; TreeNode left, right; TreeNode(int v){ val = v; } }\n    static TreeNode build(String[] a) {\n        if (a.length == 0 || a[0].equals(\"null\")) return null;\n        TreeNode root = new TreeNode(Integer.parseInt(a[0]));\n        ArrayDeque<TreeNode> q = new ArrayDeque<>(); q.add(root); int i = 1;\n        while (!q.isEmpty() && i < a.length) {\n            TreeNode n = q.poll();\n            if (i < a.length && !a[i].equals(\"null\")) { n.left = new TreeNode(Integer.parseInt(a[i])); q.add(n.left); } i++;\n            if (i < a.length && !a[i].equals(\"null\")) { n.right = new TreeNode(Integer.parseInt(a[i])); q.add(n.right); } i++;\n        }\n        return root;\n    }\n    static int depth(TreeNode n) {\n        if (n == null) return 0;\n        return 1 + Math.max(depth(n.left), depth(n.right));\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String line = sc.hasNextLine() ? sc.nextLine() : \"\";\n        String[] a = line.trim().isEmpty() ? new String[0] : line.trim().split(\"\\\\s+\");\n        System.out.println(depth(build(a)));\n    }\n}\n",
        "tests": [
          {
            "input": "3 9 20 null null 15 7",
            "output": "3"
          },
          {
            "input": "1",
            "output": "1"
          },
          {
            "input": "1 2 null 3",
            "output": "3"
          }
        ],
        "source_slug": "max-depth-tree"
      },
      {
        "id": "tree_basics-base",
        "title": "The base case",
        "prompt": "Every tree recursion needs a base case. Fill the blank so an empty subtree contributes height 0.",
        "hint": "When the node is null there is nothing below it — return 0.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    static class TreeNode { int val; TreeNode left, right; TreeNode(int v){ val = v; } }\n    static TreeNode build(String[] a) {\n        if (a.length == 0 || a[0].equals(\"null\")) return null;\n        TreeNode root = new TreeNode(Integer.parseInt(a[0]));\n        ArrayDeque<TreeNode> q = new ArrayDeque<>(); q.add(root); int i = 1;\n        while (!q.isEmpty() && i < a.length) {\n            TreeNode n = q.poll();\n            if (i < a.length && !a[i].equals(\"null\")) { n.left = new TreeNode(Integer.parseInt(a[i])); q.add(n.left); } i++;\n            if (i < a.length && !a[i].equals(\"null\")) { n.right = new TreeNode(Integer.parseInt(a[i])); q.add(n.right); } i++;\n        }\n        return root;\n    }\n    static int depth(TreeNode n) {\n        ____\n        return 1 + Math.max(depth(n.left), depth(n.right));\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String line = sc.hasNextLine() ? sc.nextLine() : \"\";\n        String[] a = line.trim().isEmpty() ? new String[0] : line.trim().split(\"\\\\s+\");\n        System.out.println(depth(build(a)));\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    static class TreeNode { int val; TreeNode left, right; TreeNode(int v){ val = v; } }\n    static TreeNode build(String[] a) {\n        if (a.length == 0 || a[0].equals(\"null\")) return null;\n        TreeNode root = new TreeNode(Integer.parseInt(a[0]));\n        ArrayDeque<TreeNode> q = new ArrayDeque<>(); q.add(root); int i = 1;\n        while (!q.isEmpty() && i < a.length) {\n            TreeNode n = q.poll();\n            if (i < a.length && !a[i].equals(\"null\")) { n.left = new TreeNode(Integer.parseInt(a[i])); q.add(n.left); } i++;\n            if (i < a.length && !a[i].equals(\"null\")) { n.right = new TreeNode(Integer.parseInt(a[i])); q.add(n.right); } i++;\n        }\n        return root;\n    }\n    static int depth(TreeNode n) {\n        if (n == null) return 0;\n        return 1 + Math.max(depth(n.left), depth(n.right));\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String line = sc.hasNextLine() ? sc.nextLine() : \"\";\n        String[] a = line.trim().isEmpty() ? new String[0] : line.trim().split(\"\\\\s+\");\n        System.out.println(depth(build(a)));\n    }\n}\n",
        "tests": [
          {
            "input": "1 2 3",
            "output": "2"
          },
          {
            "input": "1",
            "output": "1"
          },
          {
            "input": "",
            "output": "0"
          }
        ],
        "source_slug": "max-depth-tree"
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "tree_traversal",
    "name": "Tree Traversals (DFS & BFS)",
    "category": "Trees",
    "what": "The orders in which you visit nodes: preorder, inorder, postorder (DFS) and level-order (BFS).",
    "deep": "DFS uses the call stack (or an explicit stack): preorder visits the node before its children, inorder goes left-node-right (sorted for a BST), postorder visits children first (needed when a node's answer depends on its subtrees). BFS uses a queue to sweep level by level — the tool for shortest-depth and per-level questions.",
    "java": "DFS: a recursive helper. BFS: an ArrayDeque<TreeNode> queue, processing one level (queue.size() nodes) at a time.",
    "language": "java",
    "lesson": "# Tree Traversals\n\nFour orders, two engines.\n\n- **DFS (stack / recursion)** — *preorder* `node,left,right`; *inorder* `left,node,right`; *postorder* `left,right,node`.\n- **BFS (queue)** — *level order*, one row at a time.\n\n```java\nvoid inorder(TreeNode n, List<Integer> out) {\n    if (n == null) return;\n    inorder(n.left, out);\n    out.add(n.val);        // between the two calls\n    inorder(n.right, out);\n}\n```\n\n## When to reach for this\n| Signal in the prompt | Use |\n|---|---|\n| 'sorted order', BST values | **inorder** |\n| copy / serialize / prefix | **preorder** |\n| answer depends on subtrees | **postorder** |\n| 'level', 'shallowest', 'each row' | **BFS** |\n\n## Simulated solve — BFS level order of `1 2 3 4 5`\n| step | queue | emitted |\n|---|---|---|\n| start | [1] | |\n| pop 1 | [2,3] | 1 |\n| pop 2 | [3,4,5] | 1,2 |\n| pop 3 | [4,5] | 1,2,3 |\n| pop 4,5 | [] | 1,2,3,4,5 |\n",
    "exercises": [
      {
        "id": "tree_traversal-inorder-visit",
        "title": "Inorder: visit between",
        "prompt": "Inorder is left, node, right. Fill the blank so the node's value is recorded between the two recursive calls.",
        "hint": "Append n.val between inorder(left) and inorder(right).",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    static class TreeNode { int val; TreeNode left, right; TreeNode(int v){ val = v; } }\n    static TreeNode build(String[] a) {\n        if (a.length == 0 || a[0].equals(\"null\")) return null;\n        TreeNode root = new TreeNode(Integer.parseInt(a[0]));\n        ArrayDeque<TreeNode> q = new ArrayDeque<>(); q.add(root); int i = 1;\n        while (!q.isEmpty() && i < a.length) {\n            TreeNode n = q.poll();\n            if (i < a.length && !a[i].equals(\"null\")) { n.left = new TreeNode(Integer.parseInt(a[i])); q.add(n.left); } i++;\n            if (i < a.length && !a[i].equals(\"null\")) { n.right = new TreeNode(Integer.parseInt(a[i])); q.add(n.right); } i++;\n        }\n        return root;\n    }\n    static void inorder(TreeNode n, List<Integer> out) {\n        if (n == null) return;\n        inorder(n.left, out);\n        ____\n        inorder(n.right, out);\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String line = sc.hasNextLine() ? sc.nextLine() : \"\";\n        String[] a = line.trim().isEmpty() ? new String[0] : line.trim().split(\"\\\\s+\");\n        List<Integer> out = new ArrayList<>();\n        inorder(build(a), out);\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < out.size(); i++) { if (i > 0) sb.append(' '); sb.append(out.get(i)); }\n        System.out.println(sb.toString());\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    static class TreeNode { int val; TreeNode left, right; TreeNode(int v){ val = v; } }\n    static TreeNode build(String[] a) {\n        if (a.length == 0 || a[0].equals(\"null\")) return null;\n        TreeNode root = new TreeNode(Integer.parseInt(a[0]));\n        ArrayDeque<TreeNode> q = new ArrayDeque<>(); q.add(root); int i = 1;\n        while (!q.isEmpty() && i < a.length) {\n            TreeNode n = q.poll();\n            if (i < a.length && !a[i].equals(\"null\")) { n.left = new TreeNode(Integer.parseInt(a[i])); q.add(n.left); } i++;\n            if (i < a.length && !a[i].equals(\"null\")) { n.right = new TreeNode(Integer.parseInt(a[i])); q.add(n.right); } i++;\n        }\n        return root;\n    }\n    static void inorder(TreeNode n, List<Integer> out) {\n        if (n == null) return;\n        inorder(n.left, out);\n        out.add(n.val);\n        inorder(n.right, out);\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String line = sc.hasNextLine() ? sc.nextLine() : \"\";\n        String[] a = line.trim().isEmpty() ? new String[0] : line.trim().split(\"\\\\s+\");\n        List<Integer> out = new ArrayList<>();\n        inorder(build(a), out);\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < out.size(); i++) { if (i > 0) sb.append(' '); sb.append(out.get(i)); }\n        System.out.println(sb.toString());\n    }\n}\n",
        "tests": [
          {
            "input": "1 null 2 3",
            "output": "1 3 2"
          },
          {
            "input": "5 3 8 2 4 7 9",
            "output": "2 3 4 5 7 8 9"
          },
          {
            "input": "1",
            "output": "1"
          }
        ],
        "source_slug": "inorder-traversal"
      },
      {
        "id": "tree_traversal-inorder-left",
        "title": "Inorder: recurse left first",
        "prompt": "Fill the blank so the traversal descends into the left subtree before recording the node.",
        "hint": "Go all the way left before you emit anything.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    static class TreeNode { int val; TreeNode left, right; TreeNode(int v){ val = v; } }\n    static TreeNode build(String[] a) {\n        if (a.length == 0 || a[0].equals(\"null\")) return null;\n        TreeNode root = new TreeNode(Integer.parseInt(a[0]));\n        ArrayDeque<TreeNode> q = new ArrayDeque<>(); q.add(root); int i = 1;\n        while (!q.isEmpty() && i < a.length) {\n            TreeNode n = q.poll();\n            if (i < a.length && !a[i].equals(\"null\")) { n.left = new TreeNode(Integer.parseInt(a[i])); q.add(n.left); } i++;\n            if (i < a.length && !a[i].equals(\"null\")) { n.right = new TreeNode(Integer.parseInt(a[i])); q.add(n.right); } i++;\n        }\n        return root;\n    }\n    static void inorder(TreeNode n, List<Integer> out) {\n        if (n == null) return;\n        ____\n        out.add(n.val);\n        inorder(n.right, out);\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String line = sc.hasNextLine() ? sc.nextLine() : \"\";\n        String[] a = line.trim().isEmpty() ? new String[0] : line.trim().split(\"\\\\s+\");\n        List<Integer> out = new ArrayList<>();\n        inorder(build(a), out);\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < out.size(); i++) { if (i > 0) sb.append(' '); sb.append(out.get(i)); }\n        System.out.println(sb.toString());\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    static class TreeNode { int val; TreeNode left, right; TreeNode(int v){ val = v; } }\n    static TreeNode build(String[] a) {\n        if (a.length == 0 || a[0].equals(\"null\")) return null;\n        TreeNode root = new TreeNode(Integer.parseInt(a[0]));\n        ArrayDeque<TreeNode> q = new ArrayDeque<>(); q.add(root); int i = 1;\n        while (!q.isEmpty() && i < a.length) {\n            TreeNode n = q.poll();\n            if (i < a.length && !a[i].equals(\"null\")) { n.left = new TreeNode(Integer.parseInt(a[i])); q.add(n.left); } i++;\n            if (i < a.length && !a[i].equals(\"null\")) { n.right = new TreeNode(Integer.parseInt(a[i])); q.add(n.right); } i++;\n        }\n        return root;\n    }\n    static void inorder(TreeNode n, List<Integer> out) {\n        if (n == null) return;\n        inorder(n.left, out);\n        out.add(n.val);\n        inorder(n.right, out);\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String line = sc.hasNextLine() ? sc.nextLine() : \"\";\n        String[] a = line.trim().isEmpty() ? new String[0] : line.trim().split(\"\\\\s+\");\n        List<Integer> out = new ArrayList<>();\n        inorder(build(a), out);\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < out.size(); i++) { if (i > 0) sb.append(' '); sb.append(out.get(i)); }\n        System.out.println(sb.toString());\n    }\n}\n",
        "tests": [
          {
            "input": "5 3 8 2 4 7 9",
            "output": "2 3 4 5 7 8 9"
          },
          {
            "input": "2 1",
            "output": "1 2"
          },
          {
            "input": "1",
            "output": "1"
          }
        ],
        "source_slug": "inorder-traversal"
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "bst",
    "name": "Binary Search Trees",
    "category": "Trees",
    "what": "A binary tree with the ordering invariant left < node < right, giving O(height) search, insert, and delete.",
    "deep": "The BST invariant turns a tree into a searchable structure: at each node you discard half the remaining tree by comparing with the target, just like binary search. Inorder traversal emits values in sorted order. Validity depends on a value range, not just immediate children.",
    "java": "Descend with `if (v < node.val) node = node.left; else node = node.right;`. Validate by passing down a (low, high) bound.",
    "language": "java",
    "lesson": "# Binary Search Trees\n\nA BST keeps **left < node < right** at *every* node. That single invariant makes search behave like binary search — one comparison discards half the tree.\n\n```java\nboolean search(TreeNode n, int v) {\n    while (n != null) {\n        if (n.val == v) return true;\n        n = (v < n.val) ? n.left : n.right;   // discard half\n    }\n    return false;\n}\n```\n\n## When to reach for this\nSignals: *'sorted tree'*, *'kth smallest'*, *'validate BST'*, *'range'*. Inorder of a BST is **sorted** — many BST problems are 'do X to a sorted stream'.\n\n## The validation trap\nChecking only immediate children is wrong. A node deep on the left must still be below an ancestor. Carry a **range**:\n\n| node | allowed (low, high) |\n|---|---|\n| root 5 | (-inf, +inf) |\n| left 3 | (-inf, 5) |\n| 3.right 4 | (3, 5) |\n| right 8 | (5, +inf) |\n",
    "exercises": [
      {
        "id": "bst-descend",
        "title": "Descend a BST",
        "prompt": "Fill the blank so the search goes left when `v` is smaller than the node, otherwise right.",
        "hint": "Compare v with n.val to pick the child.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    static class TreeNode { int val; TreeNode left, right; TreeNode(int v){ val = v; } }\n    static TreeNode build(String[] a) {\n        if (a.length == 0 || a[0].equals(\"null\")) return null;\n        TreeNode root = new TreeNode(Integer.parseInt(a[0]));\n        ArrayDeque<TreeNode> q = new ArrayDeque<>(); q.add(root); int i = 1;\n        while (!q.isEmpty() && i < a.length) {\n            TreeNode n = q.poll();\n            if (i < a.length && !a[i].equals(\"null\")) { n.left = new TreeNode(Integer.parseInt(a[i])); q.add(n.left); } i++;\n            if (i < a.length && !a[i].equals(\"null\")) { n.right = new TreeNode(Integer.parseInt(a[i])); q.add(n.right); } i++;\n        }\n        return root;\n    }\n    static boolean search(TreeNode n, int v) {\n        while (n != null) {\n            if (n.val == v) return true;\n            ____\n        }\n        return false;\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String line = sc.nextLine();\n        int v = Integer.parseInt(sc.nextLine().trim());\n        String[] a = line.trim().isEmpty() ? new String[0] : line.trim().split(\"\\\\s+\");\n        System.out.println(search(build(a), v) ? \"true\" : \"false\");\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    static class TreeNode { int val; TreeNode left, right; TreeNode(int v){ val = v; } }\n    static TreeNode build(String[] a) {\n        if (a.length == 0 || a[0].equals(\"null\")) return null;\n        TreeNode root = new TreeNode(Integer.parseInt(a[0]));\n        ArrayDeque<TreeNode> q = new ArrayDeque<>(); q.add(root); int i = 1;\n        while (!q.isEmpty() && i < a.length) {\n            TreeNode n = q.poll();\n            if (i < a.length && !a[i].equals(\"null\")) { n.left = new TreeNode(Integer.parseInt(a[i])); q.add(n.left); } i++;\n            if (i < a.length && !a[i].equals(\"null\")) { n.right = new TreeNode(Integer.parseInt(a[i])); q.add(n.right); } i++;\n        }\n        return root;\n    }\n    static boolean search(TreeNode n, int v) {\n        while (n != null) {\n            if (n.val == v) return true;\n            n = (v < n.val) ? n.left : n.right;\n        }\n        return false;\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String line = sc.nextLine();\n        int v = Integer.parseInt(sc.nextLine().trim());\n        String[] a = line.trim().isEmpty() ? new String[0] : line.trim().split(\"\\\\s+\");\n        System.out.println(search(build(a), v) ? \"true\" : \"false\");\n    }\n}\n",
        "tests": [
          {
            "input": "5 3 8 2 4 7 9\n7",
            "output": "true"
          },
          {
            "input": "5 3 8 2 4 7 9\n6",
            "output": "false"
          },
          {
            "input": "5\n5",
            "output": "true"
          }
        ],
        "source_slug": "bst-search"
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "tree_dp",
    "name": "Tree DP (Postorder Aggregation)",
    "category": "Trees",
    "what": "Computing an answer for each node from the answers of its subtrees in one bottom-up pass.",
    "deep": "When a node's result needs information from below (height, whether balanced, best path), compute it postorder: recurse into both children, combine their returned values, and often update a global best. Returning one value up while recording another (like the split path in diameter / max-path-sum) is the signature move.",
    "java": "A recursive helper that returns the 'upward' value (e.g. height) and updates a shared field (an int[1] or class member) with the combined 'through' value.",
    "language": "java",
    "lesson": "# Tree DP — Postorder Aggregation\n\nWhen a node's answer depends on its subtrees, compute **bottom-up**: recurse both sides, then combine. The trick in problems like *diameter* and *max path sum* is returning **one** value upward while recording **another** globally.\n\n```java\nint best = 0;\nint height(TreeNode n) {          // returns height, updates diameter\n    if (n == null) return 0;\n    int L = height(n.left), R = height(n.right);\n    best = Math.max(best, L + R);  // path THROUGH n (can't go up)\n    return 1 + Math.max(L, R);     // value handed to the parent\n}\n```\n\n## When to reach for this\nSignals: *'longest path'*, *'balanced'*, *'diameter'*, *'max path sum'*, *'sum of subtree'*. If the answer can bend at a node but a parent can only take one side, you need the return-one / record-other split.\n\n## Simulated solve — diameter of `1 2 3 4 5`\n| node | L | R | best=max(best,L+R) | returns |\n|---|---|---|---|---|\n| 4 | 0 | 0 | 0 | 1 |\n| 5 | 0 | 0 | 0 | 1 |\n| 2 | 1 | 1 | 2 | 2 |\n| 3 | 0 | 0 | 2 | 1 |\n| 1 | 2 | 1 | 3 | 3 |\n",
    "exercises": [
      {
        "id": "tree_dp-through",
        "title": "Path through a node",
        "prompt": "The height recursion is written. Fill the blank so `best` records the longest path *through* the current node (left height + right height).",
        "hint": "A path bending at n uses both sides: L + R edges.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    static class TreeNode { int val; TreeNode left, right; TreeNode(int v){ val = v; } }\n    static TreeNode build(String[] a) {\n        if (a.length == 0 || a[0].equals(\"null\")) return null;\n        TreeNode root = new TreeNode(Integer.parseInt(a[0]));\n        ArrayDeque<TreeNode> q = new ArrayDeque<>(); q.add(root); int i = 1;\n        while (!q.isEmpty() && i < a.length) {\n            TreeNode n = q.poll();\n            if (i < a.length && !a[i].equals(\"null\")) { n.left = new TreeNode(Integer.parseInt(a[i])); q.add(n.left); } i++;\n            if (i < a.length && !a[i].equals(\"null\")) { n.right = new TreeNode(Integer.parseInt(a[i])); q.add(n.right); } i++;\n        }\n        return root;\n    }\n    static int best = 0;\n    static int height(TreeNode n) {\n        if (n == null) return 0;\n        int L = height(n.left), R = height(n.right);\n        ____\n        return 1 + Math.max(L, R);\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String line = sc.hasNextLine() ? sc.nextLine() : \"\";\n        String[] a = line.trim().isEmpty() ? new String[0] : line.trim().split(\"\\\\s+\");\n        height(build(a));\n        System.out.println(best);\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    static class TreeNode { int val; TreeNode left, right; TreeNode(int v){ val = v; } }\n    static TreeNode build(String[] a) {\n        if (a.length == 0 || a[0].equals(\"null\")) return null;\n        TreeNode root = new TreeNode(Integer.parseInt(a[0]));\n        ArrayDeque<TreeNode> q = new ArrayDeque<>(); q.add(root); int i = 1;\n        while (!q.isEmpty() && i < a.length) {\n            TreeNode n = q.poll();\n            if (i < a.length && !a[i].equals(\"null\")) { n.left = new TreeNode(Integer.parseInt(a[i])); q.add(n.left); } i++;\n            if (i < a.length && !a[i].equals(\"null\")) { n.right = new TreeNode(Integer.parseInt(a[i])); q.add(n.right); } i++;\n        }\n        return root;\n    }\n    static int best = 0;\n    static int height(TreeNode n) {\n        if (n == null) return 0;\n        int L = height(n.left), R = height(n.right);\n        best = Math.max(best, L + R);\n        return 1 + Math.max(L, R);\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String line = sc.hasNextLine() ? sc.nextLine() : \"\";\n        String[] a = line.trim().isEmpty() ? new String[0] : line.trim().split(\"\\\\s+\");\n        height(build(a));\n        System.out.println(best);\n    }\n}\n",
        "tests": [
          {
            "input": "1 2 3 4 5",
            "output": "3"
          },
          {
            "input": "1 2 null 3",
            "output": "2"
          },
          {
            "input": "1",
            "output": "0"
          }
        ],
        "source_slug": "diameter-of-tree"
      },
      {
        "id": "tree_dp-return",
        "title": "What to hand the parent",
        "prompt": "Fill the blank so `height` returns the value a parent needs: 1 plus the taller side.",
        "hint": "A parent can only extend one side, so return the taller child + 1.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    static class TreeNode { int val; TreeNode left, right; TreeNode(int v){ val = v; } }\n    static TreeNode build(String[] a) {\n        if (a.length == 0 || a[0].equals(\"null\")) return null;\n        TreeNode root = new TreeNode(Integer.parseInt(a[0]));\n        ArrayDeque<TreeNode> q = new ArrayDeque<>(); q.add(root); int i = 1;\n        while (!q.isEmpty() && i < a.length) {\n            TreeNode n = q.poll();\n            if (i < a.length && !a[i].equals(\"null\")) { n.left = new TreeNode(Integer.parseInt(a[i])); q.add(n.left); } i++;\n            if (i < a.length && !a[i].equals(\"null\")) { n.right = new TreeNode(Integer.parseInt(a[i])); q.add(n.right); } i++;\n        }\n        return root;\n    }\n    static int best = 0;\n    static int height(TreeNode n) {\n        if (n == null) return 0;\n        int L = height(n.left), R = height(n.right);\n        best = Math.max(best, L + R);\n        ____\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String line = sc.hasNextLine() ? sc.nextLine() : \"\";\n        String[] a = line.trim().isEmpty() ? new String[0] : line.trim().split(\"\\\\s+\");\n        height(build(a));\n        System.out.println(best);\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    static class TreeNode { int val; TreeNode left, right; TreeNode(int v){ val = v; } }\n    static TreeNode build(String[] a) {\n        if (a.length == 0 || a[0].equals(\"null\")) return null;\n        TreeNode root = new TreeNode(Integer.parseInt(a[0]));\n        ArrayDeque<TreeNode> q = new ArrayDeque<>(); q.add(root); int i = 1;\n        while (!q.isEmpty() && i < a.length) {\n            TreeNode n = q.poll();\n            if (i < a.length && !a[i].equals(\"null\")) { n.left = new TreeNode(Integer.parseInt(a[i])); q.add(n.left); } i++;\n            if (i < a.length && !a[i].equals(\"null\")) { n.right = new TreeNode(Integer.parseInt(a[i])); q.add(n.right); } i++;\n        }\n        return root;\n    }\n    static int best = 0;\n    static int height(TreeNode n) {\n        if (n == null) return 0;\n        int L = height(n.left), R = height(n.right);\n        best = Math.max(best, L + R);\n        return 1 + Math.max(L, R);\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        String line = sc.hasNextLine() ? sc.nextLine() : \"\";\n        String[] a = line.trim().isEmpty() ? new String[0] : line.trim().split(\"\\\\s+\");\n        height(build(a));\n        System.out.println(best);\n    }\n}\n",
        "tests": [
          {
            "input": "1 2 3 4 5",
            "output": "3"
          },
          {
            "input": "1",
            "output": "0"
          },
          {
            "input": "1 2 3",
            "output": "2"
          }
        ],
        "source_slug": "diameter-of-tree"
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "list_basics",
    "name": "Linked Lists",
    "category": "Linked Lists",
    "what": "A chain of nodes where each holds a value and a pointer to the next node; the dummy-head trick simplifies edits.",
    "deep": "Unlike arrays, linked lists give O(1) insertion/removal once you hold the right node but no random access — you must walk from the head. Most bugs come from losing a pointer before you use it (save node.next first) or from special-casing the head, which a sentinel 'dummy' node removes.",
    "java": "class ListNode { int val; ListNode next; }. Use a dummy head: ListNode dummy = new ListNode(0); build/append behind a tail pointer, return dummy.next.",
    "language": "java",
    "lesson": "# Linked Lists\n\nA linked list is a chain of **nodes**, each holding a value and a reference to the **next** node. You reach any node only by walking from the head — there is no indexing.\n\n```java\nclass ListNode { int val; ListNode next; ListNode(int v){ val = v; } }\n\n// Build behind a DUMMY head so appends need no special case:\nListNode dummy = new ListNode(0), tail = dummy;\nfor (int x : values) { tail.next = new ListNode(x); tail = tail.next; }\nListNode head = dummy.next;\n```\n\n## When to reach for this\nSignals: *'reverse the list'*, *'remove the nth node'*, *'merge sorted lists'*, *'detect a cycle'*, O(1) insert/delete.\n\n## The two survival rules\n1. **Cache `next` before you overwrite it** — otherwise the rest of the list is lost.\n2. **Use a dummy head** whenever the real head might be removed or reassigned, so you never special-case it.",
    "exercises": [
      {
        "id": "list_basics-append",
        "title": "Append behind the tail",
        "prompt": "Building a list behind a dummy head. Fill the blank so a new node is linked after the current tail.",
        "hint": "Link the new node onto t.next.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    static class ListNode { int val; ListNode next; ListNode(int v){ val = v; } }\n    static ListNode build(int[] a){ ListNode dummy=new ListNode(0), t=dummy; for(int x:a){ t.next=new ListNode(x); t=t.next; } return dummy.next; }\n    static int[] readInts(Scanner sc){ String line = sc.hasNextLine() ? sc.nextLine() : \"\"; line = line.trim(); if (line.isEmpty()) return new int[0]; String[] t = line.split(\"\\\\s+\"); int[] a = new int[t.length]; for (int i=0;i<t.length;i++) a[i]=Integer.parseInt(t[i]); return a; }\n    static ListNode buildAppend(int[] a){\n        ListNode dummy = new ListNode(0), t = dummy;\n        for (int x : a) {\n            ____\n            t = t.next;\n        }\n        return dummy.next;\n    }\n    public static void main(String[] args){\n        Scanner sc = new Scanner(System.in);\n        ListNode h = buildAppend(readInts(sc));\n        StringBuilder sb = new StringBuilder();\n        while (h != null) { if (sb.length()>0) sb.append(' '); sb.append(h.val); h = h.next; }\n        System.out.println(sb.toString());\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    static class ListNode { int val; ListNode next; ListNode(int v){ val = v; } }\n    static ListNode build(int[] a){ ListNode dummy=new ListNode(0), t=dummy; for(int x:a){ t.next=new ListNode(x); t=t.next; } return dummy.next; }\n    static int[] readInts(Scanner sc){ String line = sc.hasNextLine() ? sc.nextLine() : \"\"; line = line.trim(); if (line.isEmpty()) return new int[0]; String[] t = line.split(\"\\\\s+\"); int[] a = new int[t.length]; for (int i=0;i<t.length;i++) a[i]=Integer.parseInt(t[i]); return a; }\n    static ListNode buildAppend(int[] a){\n        ListNode dummy = new ListNode(0), t = dummy;\n        for (int x : a) {\n            t.next = new ListNode(x);\n            t = t.next;\n        }\n        return dummy.next;\n    }\n    public static void main(String[] args){\n        Scanner sc = new Scanner(System.in);\n        ListNode h = buildAppend(readInts(sc));\n        StringBuilder sb = new StringBuilder();\n        while (h != null) { if (sb.length()>0) sb.append(' '); sb.append(h.val); h = h.next; }\n        System.out.println(sb.toString());\n    }\n}\n",
        "tests": [
          {
            "input": "1 2 3",
            "output": "1 2 3"
          },
          {
            "input": "5",
            "output": "5"
          },
          {
            "input": "",
            "output": ""
          }
        ],
        "source_slug": "reverse-linked-list"
      },
      {
        "id": "list_basics-advance",
        "title": "Advance the tail",
        "prompt": "Fill the blank so the tail pointer moves onto the node you just appended.",
        "hint": "After appending, the tail is the new last node.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    static class ListNode { int val; ListNode next; ListNode(int v){ val = v; } }\n    static ListNode build(int[] a){ ListNode dummy=new ListNode(0), t=dummy; for(int x:a){ t.next=new ListNode(x); t=t.next; } return dummy.next; }\n    static int[] readInts(Scanner sc){ String line = sc.hasNextLine() ? sc.nextLine() : \"\"; line = line.trim(); if (line.isEmpty()) return new int[0]; String[] t = line.split(\"\\\\s+\"); int[] a = new int[t.length]; for (int i=0;i<t.length;i++) a[i]=Integer.parseInt(t[i]); return a; }\n    static ListNode buildAppend(int[] a){\n        ListNode dummy = new ListNode(0), t = dummy;\n        for (int x : a) {\n            t.next = new ListNode(x);\n            ____\n        }\n        return dummy.next;\n    }\n    public static void main(String[] args){\n        Scanner sc = new Scanner(System.in);\n        ListNode h = buildAppend(readInts(sc));\n        StringBuilder sb = new StringBuilder();\n        while (h != null) { if (sb.length()>0) sb.append(' '); sb.append(h.val); h = h.next; }\n        System.out.println(sb.toString());\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    static class ListNode { int val; ListNode next; ListNode(int v){ val = v; } }\n    static ListNode build(int[] a){ ListNode dummy=new ListNode(0), t=dummy; for(int x:a){ t.next=new ListNode(x); t=t.next; } return dummy.next; }\n    static int[] readInts(Scanner sc){ String line = sc.hasNextLine() ? sc.nextLine() : \"\"; line = line.trim(); if (line.isEmpty()) return new int[0]; String[] t = line.split(\"\\\\s+\"); int[] a = new int[t.length]; for (int i=0;i<t.length;i++) a[i]=Integer.parseInt(t[i]); return a; }\n    static ListNode buildAppend(int[] a){\n        ListNode dummy = new ListNode(0), t = dummy;\n        for (int x : a) {\n            t.next = new ListNode(x);\n            t = t.next;\n        }\n        return dummy.next;\n    }\n    public static void main(String[] args){\n        Scanner sc = new Scanner(System.in);\n        ListNode h = buildAppend(readInts(sc));\n        StringBuilder sb = new StringBuilder();\n        while (h != null) { if (sb.length()>0) sb.append(' '); sb.append(h.val); h = h.next; }\n        System.out.println(sb.toString());\n    }\n}\n",
        "tests": [
          {
            "input": "4 5 6",
            "output": "4 5 6"
          },
          {
            "input": "9",
            "output": "9"
          },
          {
            "input": "1 1 1",
            "output": "1 1 1"
          }
        ],
        "source_slug": "reverse-linked-list"
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "list_reversal",
    "name": "In-place List Reversal",
    "category": "Linked Lists",
    "what": "Flipping the direction of `next` pointers using a rolling prev/cur/next trio.",
    "deep": "Reversal is the backbone of palindrome checks, reorder, and k-group problems. The invariant: prev is the already-reversed prefix, cur is the next node to flip. You must cache cur.next before overwriting it, or you lose the rest of the list.",
    "java": "ListNode prev=null, cur=head; while(cur!=null){ ListNode nx=cur.next; cur.next=prev; prev=cur; cur=nx; } return prev;",
    "language": "java",
    "lesson": "# In-place Reversal\n\nReverse a list by walking it once, flipping each `next` to point backwards.\n\n```java\nListNode prev = null, cur = head;\nwhile (cur != null) {\n    ListNode next = cur.next;  // 1. cache\n    cur.next = prev;           // 2. flip\n    prev = cur;                // 3. advance prev\n    cur = next;                // 4. advance cur\n}\nreturn prev;                   // new head\n```\n\n## Simulated solve — reversing `1 2 3`\n| step | prev | cur | list so far |\n|---|---|---|---|\n| start | null | 1 | 1→2→3 |\n| after 1 | 1 | 2 | 1→null, 2→3 |\n| after 2 | 2 | 3 | 2→1→null |\n| after 3 | 3 | null | 3→2→1→null |\n\n## When to reach for this\nReversal underlies **palindrome check**, **reorder list**, and **reverse-in-k-groups** — any time you must consume the list back-to-front in O(1) space.",
    "exercises": [
      {
        "id": "list_reversal-flip",
        "title": "Flip the link",
        "prompt": "The three-pointer reversal is set up. Fill the blank so the current node points back to prev.",
        "hint": "Reverse the arrow: cur.next should become prev.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    static class ListNode { int val; ListNode next; ListNode(int v){ val = v; } }\n    static ListNode build(int[] a){ ListNode dummy=new ListNode(0), t=dummy; for(int x:a){ t.next=new ListNode(x); t=t.next; } return dummy.next; }\n    static int[] readInts(Scanner sc){ String line = sc.hasNextLine() ? sc.nextLine() : \"\"; line = line.trim(); if (line.isEmpty()) return new int[0]; String[] t = line.split(\"\\\\s+\"); int[] a = new int[t.length]; for (int i=0;i<t.length;i++) a[i]=Integer.parseInt(t[i]); return a; }\n    static ListNode reverse(ListNode head){\n        ListNode prev = null, cur = head;\n        while (cur != null) {\n            ListNode next = cur.next;\n            ____\n            prev = cur;\n            cur = next;\n        }\n        return prev;\n    }\n    public static void main(String[] args){\n        Scanner sc = new Scanner(System.in);\n        ListNode h = reverse(build(readInts(sc)));\n        StringBuilder sb = new StringBuilder();\n        while (h != null) { if (sb.length()>0) sb.append(' '); sb.append(h.val); h = h.next; }\n        System.out.println(sb.toString());\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    static class ListNode { int val; ListNode next; ListNode(int v){ val = v; } }\n    static ListNode build(int[] a){ ListNode dummy=new ListNode(0), t=dummy; for(int x:a){ t.next=new ListNode(x); t=t.next; } return dummy.next; }\n    static int[] readInts(Scanner sc){ String line = sc.hasNextLine() ? sc.nextLine() : \"\"; line = line.trim(); if (line.isEmpty()) return new int[0]; String[] t = line.split(\"\\\\s+\"); int[] a = new int[t.length]; for (int i=0;i<t.length;i++) a[i]=Integer.parseInt(t[i]); return a; }\n    static ListNode reverse(ListNode head){\n        ListNode prev = null, cur = head;\n        while (cur != null) {\n            ListNode next = cur.next;\n            cur.next = prev;\n            prev = cur;\n            cur = next;\n        }\n        return prev;\n    }\n    public static void main(String[] args){\n        Scanner sc = new Scanner(System.in);\n        ListNode h = reverse(build(readInts(sc)));\n        StringBuilder sb = new StringBuilder();\n        while (h != null) { if (sb.length()>0) sb.append(' '); sb.append(h.val); h = h.next; }\n        System.out.println(sb.toString());\n    }\n}\n",
        "tests": [
          {
            "input": "1 2 3 4",
            "output": "4 3 2 1"
          },
          {
            "input": "5",
            "output": "5"
          },
          {
            "input": "9 8",
            "output": "8 9"
          }
        ],
        "source_slug": "reverse-linked-list"
      },
      {
        "id": "list_reversal-cache",
        "title": "Cache before you flip",
        "prompt": "Fill the blank so the rest of the list is saved before the link is overwritten.",
        "hint": "Save cur.next into a temp before cur.next = prev destroys it.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    static class ListNode { int val; ListNode next; ListNode(int v){ val = v; } }\n    static ListNode build(int[] a){ ListNode dummy=new ListNode(0), t=dummy; for(int x:a){ t.next=new ListNode(x); t=t.next; } return dummy.next; }\n    static int[] readInts(Scanner sc){ String line = sc.hasNextLine() ? sc.nextLine() : \"\"; line = line.trim(); if (line.isEmpty()) return new int[0]; String[] t = line.split(\"\\\\s+\"); int[] a = new int[t.length]; for (int i=0;i<t.length;i++) a[i]=Integer.parseInt(t[i]); return a; }\n    static ListNode reverse(ListNode head){\n        ListNode prev = null, cur = head;\n        while (cur != null) {\n            ____\n            cur.next = prev;\n            prev = cur;\n            cur = next;\n        }\n        return prev;\n    }\n    public static void main(String[] args){\n        Scanner sc = new Scanner(System.in);\n        ListNode h = reverse(build(readInts(sc)));\n        StringBuilder sb = new StringBuilder();\n        while (h != null) { if (sb.length()>0) sb.append(' '); sb.append(h.val); h = h.next; }\n        System.out.println(sb.toString());\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    static class ListNode { int val; ListNode next; ListNode(int v){ val = v; } }\n    static ListNode build(int[] a){ ListNode dummy=new ListNode(0), t=dummy; for(int x:a){ t.next=new ListNode(x); t=t.next; } return dummy.next; }\n    static int[] readInts(Scanner sc){ String line = sc.hasNextLine() ? sc.nextLine() : \"\"; line = line.trim(); if (line.isEmpty()) return new int[0]; String[] t = line.split(\"\\\\s+\"); int[] a = new int[t.length]; for (int i=0;i<t.length;i++) a[i]=Integer.parseInt(t[i]); return a; }\n    static ListNode reverse(ListNode head){\n        ListNode prev = null, cur = head;\n        while (cur != null) {\n            ListNode next = cur.next;\n            cur.next = prev;\n            prev = cur;\n            cur = next;\n        }\n        return prev;\n    }\n    public static void main(String[] args){\n        Scanner sc = new Scanner(System.in);\n        ListNode h = reverse(build(readInts(sc)));\n        StringBuilder sb = new StringBuilder();\n        while (h != null) { if (sb.length()>0) sb.append(' '); sb.append(h.val); h = h.next; }\n        System.out.println(sb.toString());\n    }\n}\n",
        "tests": [
          {
            "input": "1 2 3",
            "output": "3 2 1"
          },
          {
            "input": "1",
            "output": "1"
          },
          {
            "input": "2 4 6 8",
            "output": "8 6 4 2"
          }
        ],
        "source_slug": "reverse-linked-list"
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "fast_slow",
    "name": "Fast & Slow Pointers",
    "category": "Linked Lists",
    "what": "Two pointers advancing at different speeds to find a midpoint, a cycle, or the k-th-from-end node in one pass.",
    "deep": "Because the fast pointer covers twice the ground, it reaches the end when the slow pointer is halfway — the middle. In a cycle the fast pointer laps the slow one, so they must eventually meet (Floyd). Resetting one pointer to the head after a meeting locates the cycle's entry.",
    "java": "ListNode slow=head, fast=head; while(fast!=null && fast.next!=null){ slow=slow.next; fast=fast.next.next; }",
    "language": "java",
    "lesson": "# Fast & Slow Pointers\n\nMove one pointer twice as fast as the other. Two payoffs fall out for free:\n\n```java\nListNode slow = head, fast = head;\nwhile (fast != null && fast.next != null) {\n    slow = slow.next;          // +1\n    fast = fast.next.next;     // +2\n}\n// slow is now the MIDDLE (second middle if even length)\n```\n\n## When to reach for this\n| Signal | Technique |\n|---|---|\n| 'middle of the list' | fast/slow, return slow |\n| 'does it have a cycle' | Floyd — do they meet? |\n| 'where does the cycle start' | meet, then reset one to head, step both by 1 |\n| 'nth node from the end' | gap of n, then move together |\n\n## Why cycle detection works\nInside a loop the fast pointer gains one position on the slow pointer each step, so the gap shrinks to 0 — they **must** meet. If `fast` hits null, the list is straight.",
    "exercises": [
      {
        "id": "fast_slow-jump",
        "title": "Fast pointer jumps two",
        "prompt": "Fast/slow midpoint finder. Fill the blank so the fast pointer advances two nodes per step.",
        "hint": "Two hops: fast.next.next.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    static class ListNode { int val; ListNode next; ListNode(int v){ val = v; } }\n    static ListNode build(int[] a){ ListNode dummy=new ListNode(0), t=dummy; for(int x:a){ t.next=new ListNode(x); t=t.next; } return dummy.next; }\n    static int[] readInts(Scanner sc){ String line = sc.hasNextLine() ? sc.nextLine() : \"\"; line = line.trim(); if (line.isEmpty()) return new int[0]; String[] t = line.split(\"\\\\s+\"); int[] a = new int[t.length]; for (int i=0;i<t.length;i++) a[i]=Integer.parseInt(t[i]); return a; }\n    static int middle(ListNode head){\n        ListNode slow = head, fast = head;\n        while (fast != null && fast.next != null) {\n            slow = slow.next;\n            ____\n        }\n        return slow.val;\n    }\n    public static void main(String[] args){\n        Scanner sc = new Scanner(System.in);\n        System.out.println(middle(build(readInts(sc))));\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    static class ListNode { int val; ListNode next; ListNode(int v){ val = v; } }\n    static ListNode build(int[] a){ ListNode dummy=new ListNode(0), t=dummy; for(int x:a){ t.next=new ListNode(x); t=t.next; } return dummy.next; }\n    static int[] readInts(Scanner sc){ String line = sc.hasNextLine() ? sc.nextLine() : \"\"; line = line.trim(); if (line.isEmpty()) return new int[0]; String[] t = line.split(\"\\\\s+\"); int[] a = new int[t.length]; for (int i=0;i<t.length;i++) a[i]=Integer.parseInt(t[i]); return a; }\n    static int middle(ListNode head){\n        ListNode slow = head, fast = head;\n        while (fast != null && fast.next != null) {\n            slow = slow.next;\n            fast = fast.next.next;\n        }\n        return slow.val;\n    }\n    public static void main(String[] args){\n        Scanner sc = new Scanner(System.in);\n        System.out.println(middle(build(readInts(sc))));\n    }\n}\n",
        "tests": [
          {
            "input": "1 2 3 4 5",
            "output": "3"
          },
          {
            "input": "1 2 3 4 5 6",
            "output": "4"
          },
          {
            "input": "7",
            "output": "7"
          }
        ],
        "source_slug": "middle-of-list"
      },
      {
        "id": "fast_slow-guard",
        "title": "Guard the double hop",
        "prompt": "Fill the blank with the loop condition that keeps the two-step jump safe.",
        "hint": "Both fast and fast.next must exist before fast.next.next.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    static class ListNode { int val; ListNode next; ListNode(int v){ val = v; } }\n    static ListNode build(int[] a){ ListNode dummy=new ListNode(0), t=dummy; for(int x:a){ t.next=new ListNode(x); t=t.next; } return dummy.next; }\n    static int[] readInts(Scanner sc){ String line = sc.hasNextLine() ? sc.nextLine() : \"\"; line = line.trim(); if (line.isEmpty()) return new int[0]; String[] t = line.split(\"\\\\s+\"); int[] a = new int[t.length]; for (int i=0;i<t.length;i++) a[i]=Integer.parseInt(t[i]); return a; }\n    static int middle(ListNode head){\n        ListNode slow = head, fast = head;\n        while (____) {\n            slow = slow.next;\n            fast = fast.next.next;\n        }\n        return slow.val;\n    }\n    public static void main(String[] args){\n        Scanner sc = new Scanner(System.in);\n        System.out.println(middle(build(readInts(sc))));\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    static class ListNode { int val; ListNode next; ListNode(int v){ val = v; } }\n    static ListNode build(int[] a){ ListNode dummy=new ListNode(0), t=dummy; for(int x:a){ t.next=new ListNode(x); t=t.next; } return dummy.next; }\n    static int[] readInts(Scanner sc){ String line = sc.hasNextLine() ? sc.nextLine() : \"\"; line = line.trim(); if (line.isEmpty()) return new int[0]; String[] t = line.split(\"\\\\s+\"); int[] a = new int[t.length]; for (int i=0;i<t.length;i++) a[i]=Integer.parseInt(t[i]); return a; }\n    static int middle(ListNode head){\n        ListNode slow = head, fast = head;\n        while (fast != null && fast.next != null) {\n            slow = slow.next;\n            fast = fast.next.next;\n        }\n        return slow.val;\n    }\n    public static void main(String[] args){\n        Scanner sc = new Scanner(System.in);\n        System.out.println(middle(build(readInts(sc))));\n    }\n}\n",
        "tests": [
          {
            "input": "1 2 3",
            "output": "2"
          },
          {
            "input": "1 2 3 4",
            "output": "3"
          },
          {
            "input": "1",
            "output": "1"
          }
        ],
        "source_slug": "middle-of-list"
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "backtracking",
    "name": "Backtracking",
    "category": "Recursion & DP",
    "what": "Systematically building candidates one choice at a time, undoing a choice when it can't lead to a solution.",
    "deep": "Backtracking is DFS over a decision tree. The template is choose → explore → un-choose: append a choice, recurse, then remove it so the next choice starts clean. It enumerates subsets, permutations, combinations, and constraint solutions (N-queens, Sudoku). Cost is roughly the number of leaves, so it needs small n.",
    "java": "A recursive helper carrying the partial solution (often a List you add()/remove() from) and the current position/used-set; add to results at a complete state.",
    "language": "java",
    "lesson": "# Backtracking\n\nBacktracking is depth-first search over a tree of **choices**. The template never changes:\n\n```java\nvoid backtrack(List<Integer> path, /* state */) {\n    if (isComplete(path)) { results.add(new ArrayList<>(path)); return; }\n    for (int choice : options(state)) {\n        path.add(choice);        // 1. choose\n        backtrack(path, next);   // 2. explore\n        path.remove(path.size()-1); // 3. UN-choose\n    }\n}\n```\n\nThe `remove` is the whole trick — it restores state so the next choice explores a clean branch.\n\n## When to reach for this\nSignals: *'generate all…'*, *'how many ways'*, *'all combinations/permutations/subsets'*, constraint puzzles (N-queens, Sudoku, word search). n is always small — exponential is expected.\n\n## Simulated solve — subsets of `[1,2]`\n| path | action |\n|---|---|\n| [] | record [] |\n| [1] | choose 1, record [1] |\n| [1,2] | choose 2, record [1,2] |\n| [1] | un-choose 2 |\n| [] | un-choose 1 |\n| [2] | choose 2, record [2] |\n",
    "exercises": [
      {
        "id": "backtracking-take",
        "title": "The 'take' branch",
        "prompt": "Counting subsets that sum to target. Fill the blank so the second recursive call **takes** a[i] (reducing the remaining target).",
        "hint": "Taking a[i] subtracts it from the remaining target.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    static int count = 0;\n    static void go(int[] a, int i, int rem) {\n        if (i == a.length) { if (rem == 0) count++; return; }\n        go(a, i + 1, rem);\n        ____\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt(); int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int target = sc.nextInt();\n        go(a, 0, target);\n        System.out.println(count);\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    static int count = 0;\n    static void go(int[] a, int i, int rem) {\n        if (i == a.length) { if (rem == 0) count++; return; }\n        go(a, i + 1, rem);\n        go(a, i + 1, rem - a[i]);\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt(); int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int target = sc.nextInt();\n        go(a, 0, target);\n        System.out.println(count);\n    }\n}\n",
        "tests": [
          {
            "input": "4\n1 2 3 4\n5",
            "output": "2"
          },
          {
            "input": "3\n2 3 5\n5",
            "output": "2"
          },
          {
            "input": "3\n1 2 3\n0",
            "output": "1"
          }
        ],
        "source_slug": "subset-sum-count"
      },
      {
        "id": "backtracking-base",
        "title": "The base case",
        "prompt": "Fill the blank so a completed choice counts only when the remaining target hit exactly zero.",
        "hint": "At the end of the array, success means nothing left to reach.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    static int count = 0;\n    static void go(int[] a, int i, int rem) {\n        if (i == a.length) { ____ return; }\n        go(a, i + 1, rem);\n        go(a, i + 1, rem - a[i]);\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt(); int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int target = sc.nextInt();\n        go(a, 0, target);\n        System.out.println(count);\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    static int count = 0;\n    static void go(int[] a, int i, int rem) {\n        if (i == a.length) { if (rem == 0) count++; return; }\n        go(a, i + 1, rem);\n        go(a, i + 1, rem - a[i]);\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt(); int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int target = sc.nextInt();\n        go(a, 0, target);\n        System.out.println(count);\n    }\n}\n",
        "tests": [
          {
            "input": "4\n1 2 3 4\n5",
            "output": "2"
          },
          {
            "input": "2\n1 2\n8",
            "output": "0"
          },
          {
            "input": "3\n1 2 3\n6",
            "output": "1"
          }
        ],
        "source_slug": "subset-sum-count"
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "pruning",
    "name": "Pruning",
    "category": "Recursion & DP",
    "what": "Cutting off branches of the search that cannot possibly succeed, so backtracking stays fast.",
    "deep": "The difference between a passing and a timing-out backtracker is usually pruning: sort inputs so you can 'break' once a candidate exceeds the target, skip duplicate choices to avoid repeated work, and check feasibility bounds before recursing. Good pruning shrinks an exponential tree dramatically.",
    "java": "Sort first; inside the loop `if (candidate > remaining) break;` and `if (i > start && a[i] == a[i-1]) continue;` to skip duplicates.",
    "language": "java",
    "lesson": "# Pruning\n\nA correct backtracker can still time out. **Pruning** cuts branches that can't yield a solution.\n\n```java\nArrays.sort(a);\nvoid go(int start, int remaining) {\n    if (remaining == 0) { count++; return; }\n    for (int i = start; i < a.length; i++) {\n        if (a[i] > remaining) break;      // sorted → nothing later fits\n        if (i > start && a[i] == a[i-1]) continue; // skip duplicate choice\n        go(i, remaining - a[i]);\n    }\n}\n```\n\n## Three standard cuts\n| Cut | Why it's safe |\n|---|---|\n| sort + `break` when candidate > remaining | everything after is even bigger |\n| skip `a[i]==a[i-1]` at the same depth | avoids generating a duplicate result |\n| bound check before recursing | prune impossible sub-trees early |\n",
    "exercises": [
      {
        "id": "pruning-break",
        "title": "Prune the sorted branch",
        "prompt": "Combination-sum counting on a sorted array. Fill the blank so the loop stops once a candidate is bigger than the remaining target.",
        "hint": "Sorted array — once a[i] exceeds rem, every later value does too.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    static int count = 0;\n    static void go(int[] a, int start, int rem) {\n        if (rem == 0) { count++; return; }\n        for (int i = start; i < a.length; i++) {\n            ____\n            go(a, i, rem - a[i]);\n        }\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt(); int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int target = sc.nextInt();\n        Arrays.sort(a);\n        go(a, 0, target);\n        System.out.println(count);\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    static int count = 0;\n    static void go(int[] a, int start, int rem) {\n        if (rem == 0) { count++; return; }\n        for (int i = start; i < a.length; i++) {\n            if (a[i] > rem) break;\n            go(a, i, rem - a[i]);\n        }\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt(); int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int target = sc.nextInt();\n        Arrays.sort(a);\n        go(a, 0, target);\n        System.out.println(count);\n    }\n}\n",
        "tests": [
          {
            "input": "4\n2 3 6 7\n7",
            "output": "2"
          },
          {
            "input": "3\n2 3 5\n8",
            "output": "3"
          },
          {
            "input": "2\n2 4\n6",
            "output": "2"
          }
        ],
        "source_slug": "combination-sum-count"
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "top_k",
    "name": "Top-K with a Heap",
    "category": "Data Structures",
    "what": "Keeping only the k best elements in a size-k heap so the answer stays available in O(log k) per item.",
    "deep": "For 'k largest / k most frequent / k closest' you never need to sort everything. A min-heap of size k holds the current k largest: push each element and, if the heap grows past k, pop the smallest. The root is then the k-th best. It streams in one pass with O(k) memory.",
    "java": "PriorityQueue<Integer> pq = new PriorityQueue<>(); for (int x : a){ pq.offer(x); if (pq.size() > k) pq.poll(); }  // pq.peek() = k-th largest",
    "language": "java",
    "lesson": "# Top-K with a Heap\n\n'Give me the k largest / most frequent / closest' never needs a full sort. Keep a **size-k min-heap**: the k best seen so far live inside it, and the smallest of them sits at the root ready to be evicted.\n\n```java\nPriorityQueue<Integer> pq = new PriorityQueue<>(); // min-heap\nfor (int x : nums) {\n    pq.offer(x);\n    if (pq.size() > k) pq.poll();   // drop the smallest\n}\nint kthLargest = pq.peek();          // root = k-th largest\n```\n\n## When to reach for this\nSignals: *'k largest'*, *'k most frequent'*, *'k closest'*, *'top k'*. Full sort is O(n log n); the heap is O(n log k) and streams.\n\n## Simulated solve — 2nd largest of `[3,2,1,5,6,4]`\n| x | heap (size ≤ 2) |\n|---|---|\n| 3 | [3] |\n| 2 | [2,3] |\n| 1 | [2,3] (1 popped) |\n| 5 | [3,5] |\n| 6 | [5,6] |\n| 4 | [5,6] → root **5** |\n",
    "exercises": [
      {
        "id": "top_k-evict",
        "title": "Evict past size k",
        "prompt": "Find the k-th largest with a size-k min-heap. Fill the blank so the heap drops its smallest once it exceeds k.",
        "hint": "When size exceeds k, the smallest can't be in the top k — poll it.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt(); int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int k = sc.nextInt();\n        PriorityQueue<Integer> pq = new PriorityQueue<>();\n        for (int x : a) {\n            pq.offer(x);\n            ____\n        }\n        System.out.println(pq.peek());\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt(); int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int k = sc.nextInt();\n        PriorityQueue<Integer> pq = new PriorityQueue<>();\n        for (int x : a) {\n            pq.offer(x);\n            if (pq.size() > k) pq.poll();\n        }\n        System.out.println(pq.peek());\n    }\n}\n",
        "tests": [
          {
            "input": "6 3 2 1 5 6 4 2",
            "output": "5"
          },
          {
            "input": "3 7 6 5 1",
            "output": "7"
          },
          {
            "input": "1 9 1",
            "output": "9"
          }
        ],
        "source_slug": "kth-largest-in-array"
      },
      {
        "id": "top_k-root",
        "title": "Read the answer",
        "prompt": "Fill the blank so the program prints the heap root — the k-th largest.",
        "hint": "The min of the k largest is the k-th largest — pq.peek().",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt(); int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int k = sc.nextInt();\n        PriorityQueue<Integer> pq = new PriorityQueue<>();\n        for (int x : a) {\n            pq.offer(x);\n            if (pq.size() > k) pq.poll();\n        }\n        ____\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt(); int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        int k = sc.nextInt();\n        PriorityQueue<Integer> pq = new PriorityQueue<>();\n        for (int x : a) {\n            pq.offer(x);\n            if (pq.size() > k) pq.poll();\n        }\n        System.out.println(pq.peek());\n    }\n}\n",
        "tests": [
          {
            "input": "6 3 2 1 5 6 4 2",
            "output": "5"
          },
          {
            "input": "4 4 3 2 1 2",
            "output": "3"
          },
          {
            "input": "4 2 5 6 1 2",
            "output": "5"
          }
        ],
        "source_slug": "kth-largest-in-array"
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "two_heaps",
    "name": "Two Heaps (Running Median)",
    "category": "Data Structures",
    "what": "Splitting data into a max-heap of the lower half and a min-heap of the upper half to read the median in O(1).",
    "deep": "Balance two heaps so the low half (max-heap) and high half (min-heap) differ in size by at most one. The median is the larger heap's top, or the average of both tops when sizes are equal. Insertion rebalances in O(log n). The same shape solves 'schedule to balance load' problems.",
    "java": "PriorityQueue<Integer> lo = new PriorityQueue<>(Collections.reverseOrder()), hi = new PriorityQueue<>(); insert into lo, push lo.poll() to hi, rebalance if hi bigger.",
    "language": "java",
    "lesson": "# Two Heaps — Running Median\n\nSplit the numbers into two halves and keep each in a heap facing the middle:\n\n```java\nPriorityQueue<Integer> lo = new PriorityQueue<>(Collections.reverseOrder()); // low half, max on top\nPriorityQueue<Integer> hi = new PriorityQueue<>();                            // high half, min on top\nvoid add(int x){\n    lo.offer(x);\n    hi.offer(lo.poll());              // funnel the biggest low into hi\n    if (hi.size() > lo.size()) lo.offer(hi.poll()); // rebalance\n}\ndouble median(){ return lo.size() > hi.size() ? lo.peek() : (lo.peek() + hi.peek()) / 2.0; }\n```\n\n## When to reach for this\nSignals: *'median of a stream'*, *'balance two groups'*, *'kth from each side'*. The invariant — `lo` never smaller than `hi`, sizes within one — is the whole trick.\n\n## Simulated solve — medians while adding 1,2,3\n| add | lo (max) | hi (min) | median |\n|---|---|---|---|\n| 1 | [1] | [] | 1.0 |\n| 2 | [1] | [2] | 1.5 |\n| 3 | [2,1] | [3] | 2.0 |\n",
    "exercises": [
      {
        "id": "two_heaps-balance",
        "title": "Rebalance the heaps",
        "prompt": "Running median with two heaps. Fill the blank so the heaps never differ in size by more than one.",
        "hint": "If the high heap grew larger, move its top back to the low heap.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        PriorityQueue<Integer> lo = new PriorityQueue<>(Collections.reverseOrder());\n        PriorityQueue<Integer> hi = new PriorityQueue<>();\n        for (int i = 0; i < n; i++) {\n            int x = sc.nextInt();\n            lo.offer(x);\n            hi.offer(lo.poll());\n            ____\n        }\n        double m = lo.size() > hi.size() ? (double) lo.peek() : (lo.peek() + hi.peek()) / 2.0;\n        System.out.println(String.format(java.util.Locale.US, \"%.1f\", m));\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        PriorityQueue<Integer> lo = new PriorityQueue<>(Collections.reverseOrder());\n        PriorityQueue<Integer> hi = new PriorityQueue<>();\n        for (int i = 0; i < n; i++) {\n            int x = sc.nextInt();\n            lo.offer(x);\n            hi.offer(lo.poll());\n            if (hi.size() > lo.size()) lo.offer(hi.poll());\n        }\n        double m = lo.size() > hi.size() ? (double) lo.peek() : (lo.peek() + hi.peek()) / 2.0;\n        System.out.println(String.format(java.util.Locale.US, \"%.1f\", m));\n    }\n}\n",
        "tests": [
          {
            "input": "3 1 2 3",
            "output": "2.0"
          },
          {
            "input": "4 1 2 3 4",
            "output": "2.5"
          },
          {
            "input": "1 5",
            "output": "5.0"
          }
        ],
        "source_slug": "median-from-stream"
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "heap_greedy",
    "name": "Greedy with a Heap",
    "category": "Data Structures",
    "what": "Repeatedly acting on the current extreme element, re-inserting updated state, to make a globally good sequence of choices.",
    "deep": "When each step should take the largest (or smallest) remaining item and then modify it, a heap keeps that extreme at your fingertips: last-stone-weight, reorganize-string, and task scheduling all pop the max, transform it, and push it back. Correctness rests on the greedy exchange argument that the local extreme is always safe to take.",
    "java": "A PriorityQueue keyed by the quantity you greedily extremize; loop poll → update → offer until done.",
    "language": "java",
    "lesson": "# Greedy with a Heap\n\nWhen every step should grab the current **extreme**, transform it, and put it back, a heap keeps that extreme O(log n) away.\n\n```java\nPriorityQueue<Integer> pq = new PriorityQueue<>(Collections.reverseOrder());\nfor (int s : stones) pq.offer(s);\nwhile (pq.size() > 1) {\n    int a = pq.poll(), b = pq.poll();   // two heaviest\n    if (a != b) pq.offer(a - b);          // push the remainder back\n}\n```\n\n## When to reach for this\nSignals: *'repeatedly take the largest/most-frequent'*, *'smash / merge the two biggest'*, *'schedule the busiest first'* — last-stone-weight, reorganize-string, task-scheduler.\n\n## Why the greedy is safe\nTaking the extreme now never blocks a better future: any solution can be rearranged to make the same extreme choice first without getting worse (an exchange argument).",
    "exercises": [],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "intervals",
    "name": "Interval Scheduling",
    "category": "Searching & Sorting",
    "what": "Reasoning about ranges [start, end] by sorting on the right endpoint and testing overlap with a[end] ≥ b[start].",
    "deep": "Interval problems fall into two moves: sort by START to merge or scan overlaps, or sort by END for greedy 'keep the most / fewest' results (activity selection, arrows, non-overlapping). A sweep line — process all endpoints in order, +1 on a start and −1 on an end — answers 'maximum concurrent' questions like minimum meeting rooms.",
    "java": "int[][] iv; Arrays.sort(iv, (a,b) -> a[0]-b[0]); // by start, or a[1]-b[1] for greedy by end. Overlap: a[1] >= b[0] (or > for point-touching rules).",
    "language": "java",
    "lesson": "# Interval Scheduling\n\nTwo sorts unlock almost every interval problem:\n\n- **Sort by start** → merge overlaps, scan for conflicts.\n- **Sort by end** → greedy 'keep the most non-overlapping' (activity selection, arrows, remove-to-disjoint).\n\n```java\nint[][] iv = /* pair up starts[i], ends[i] */;\nArrays.sort(iv, (a, b) -> a[0] - b[0]);        // by start\nList<int[]> merged = new ArrayList<>();\nfor (int[] cur : iv) {\n    if (!merged.isEmpty() && cur[0] <= merged.get(merged.size()-1)[1])\n        merged.get(merged.size()-1)[1] = Math.max(merged.get(merged.size()-1)[1], cur[1]);\n    else merged.add(cur);\n}\n```\n\n## When to reach for this\n| Signal | Sort by | Move |\n|---|---|---|\n| 'merge', 'can attend all' | start | extend / conflict-check |\n| 'max non-overlapping', 'fewest arrows' | end | greedy keep |\n| 'minimum rooms', 'max concurrent' | endpoints | sweep +1/−1 |\n\n## Simulated solve — merge `[1,3] [2,6] [8,10]`\n| interval | merged so far |\n|---|---|\n| [1,3] | [1,3] |\n| [2,6] | [1,6] (2 ≤ 3, extend) |\n| [8,10] | [1,6] [8,10] (8 > 6, new) |\n",
    "exercises": [
      {
        "id": "intervals-overlap",
        "title": "The overlap test",
        "prompt": "Meetings are sorted by start. Fill the blank so overlap is detected when a meeting starts before the previous one ends.",
        "hint": "Overlap: current start is before the previous end.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[][] iv = new int[n][2];\n        for (int i = 0; i < n; i++) { iv[i][0] = sc.nextInt(); iv[i][1] = sc.nextInt(); }\n        Arrays.sort(iv, (a, b) -> a[0] - b[0]);\n        boolean ok = true;\n        for (int i = 1; i < n; i++)\n            ____\n        System.out.println(ok ? \"true\" : \"false\");\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[][] iv = new int[n][2];\n        for (int i = 0; i < n; i++) { iv[i][0] = sc.nextInt(); iv[i][1] = sc.nextInt(); }\n        Arrays.sort(iv, (a, b) -> a[0] - b[0]);\n        boolean ok = true;\n        for (int i = 1; i < n; i++)\n            if (iv[i][0] < iv[i - 1][1]) ok = false;\n        System.out.println(ok ? \"true\" : \"false\");\n    }\n}\n",
        "tests": [
          {
            "input": "3\n0 30 5 10 15 20",
            "output": "false"
          },
          {
            "input": "2\n7 10 2 4",
            "output": "true"
          },
          {
            "input": "2\n1 2 2 3",
            "output": "true"
          }
        ],
        "source_slug": "can-attend-meetings"
      },
      {
        "id": "intervals-extend",
        "title": "Extend the merged interval",
        "prompt": "Merging sorted intervals. Fill the blank so an overlapping interval extends the current one to the farther end.",
        "hint": "Take the max of the two ends so the merged interval covers both.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[][] iv = new int[n][2];\n        for (int i = 0; i < n; i++) { iv[i][0] = sc.nextInt(); iv[i][1] = sc.nextInt(); }\n        Arrays.sort(iv, (a, b) -> a[0] - b[0]);\n        List<int[]> merged = new ArrayList<>();\n        for (int[] cur : iv) {\n            if (!merged.isEmpty() && cur[0] <= merged.get(merged.size() - 1)[1])\n                ____\n            else merged.add(new int[]{cur[0], cur[1]});\n        }\n        StringBuilder sb = new StringBuilder();\n        for (int[] m : merged) { if (sb.length() > 0) sb.append(' '); sb.append(m[0]).append(' ').append(m[1]); }\n        System.out.println(sb.toString());\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[][] iv = new int[n][2];\n        for (int i = 0; i < n; i++) { iv[i][0] = sc.nextInt(); iv[i][1] = sc.nextInt(); }\n        Arrays.sort(iv, (a, b) -> a[0] - b[0]);\n        List<int[]> merged = new ArrayList<>();\n        for (int[] cur : iv) {\n            if (!merged.isEmpty() && cur[0] <= merged.get(merged.size() - 1)[1])\n                merged.get(merged.size() - 1)[1] = Math.max(merged.get(merged.size() - 1)[1], cur[1]);\n            else merged.add(new int[]{cur[0], cur[1]});\n        }\n        StringBuilder sb = new StringBuilder();\n        for (int[] m : merged) { if (sb.length() > 0) sb.append(' '); sb.append(m[0]).append(' ').append(m[1]); }\n        System.out.println(sb.toString());\n    }\n}\n",
        "tests": [
          {
            "input": "4\n1 3 2 6 8 10 15 18",
            "output": "1 6 8 10 15 18"
          },
          {
            "input": "2\n1 4 4 5",
            "output": "1 5"
          },
          {
            "input": "1\n5 7",
            "output": "5 7"
          }
        ],
        "source_slug": "merge-intervals"
      },
      {
        "id": "intervals-sort",
        "title": "Sort by start",
        "prompt": "Fill the blank with the comparator that orders intervals by ascending start time.",
        "hint": "Compare the start fields a[0] and b[0].",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[][] iv = new int[n][2];\n        for (int i = 0; i < n; i++) { iv[i][0] = sc.nextInt(); iv[i][1] = sc.nextInt(); }\n        Arrays.sort(iv, ____);\n        List<int[]> merged = new ArrayList<>();\n        for (int[] cur : iv) {\n            if (!merged.isEmpty() && cur[0] <= merged.get(merged.size() - 1)[1])\n                merged.get(merged.size() - 1)[1] = Math.max(merged.get(merged.size() - 1)[1], cur[1]);\n            else merged.add(new int[]{cur[0], cur[1]});\n        }\n        StringBuilder sb = new StringBuilder();\n        for (int[] m : merged) { if (sb.length() > 0) sb.append(' '); sb.append(m[0]).append(' ').append(m[1]); }\n        System.out.println(sb.toString());\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[][] iv = new int[n][2];\n        for (int i = 0; i < n; i++) { iv[i][0] = sc.nextInt(); iv[i][1] = sc.nextInt(); }\n        Arrays.sort(iv, (a, b) -> a[0] - b[0]);\n        List<int[]> merged = new ArrayList<>();\n        for (int[] cur : iv) {\n            if (!merged.isEmpty() && cur[0] <= merged.get(merged.size() - 1)[1])\n                merged.get(merged.size() - 1)[1] = Math.max(merged.get(merged.size() - 1)[1], cur[1]);\n            else merged.add(new int[]{cur[0], cur[1]});\n        }\n        StringBuilder sb = new StringBuilder();\n        for (int[] m : merged) { if (sb.length() > 0) sb.append(' '); sb.append(m[0]).append(' ').append(m[1]); }\n        System.out.println(sb.toString());\n    }\n}\n",
        "tests": [
          {
            "input": "3\n8 10 1 3 2 6",
            "output": "1 6 8 10"
          },
          {
            "input": "2\n5 6 1 2",
            "output": "1 2 5 6"
          },
          {
            "input": "1\n1 9",
            "output": "1 9"
          }
        ],
        "source_slug": "merge-intervals"
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "design_ds",
    "name": "Design / Data-Structure Problems",
    "category": "Data Structures",
    "what": "Building a class with a required set of operations by choosing structures whose costs match the operation profile.",
    "deep": "'Design X' problems test whether you can pick the right combination of primitives: a hash map for O(1) lookup, a doubly-linked list for O(1) reordering (LRU), two stacks for a queue, a monotonic stack for spans, a heap for top-k feeds. The skill is mapping each required operation to a data structure that makes it cheap, and reasoning about amortized cost when one operation occasionally does more work.",
    "java": "Compose java.util building blocks: HashMap, ArrayDeque (stack/queue), LinkedHashMap (LRU-ish), PriorityQueue, and a hand-rolled doubly-linked list when you need O(1) node moves.",
    "language": "java",
    "lesson": "# Design / Data-Structure Problems\n\nA 'design' problem hands you a list of operations and asks for a class where each is fast. The whole game is **matching each operation to a structure that makes it cheap**.\n\n| Requirement | Reach for |\n|---|---|\n| O(1) key lookup | HashMap |\n| O(1) move-to-front / reorder | doubly-linked list |\n| FIFO from LIFO | two stacks (amortized O(1)) |\n| 'span' / 'next greater' | monotonic stack |\n| 'k most recent / frequent' | heap or freq buckets |\n| min alongside a stack | pair each value with the running min |\n\n## The classic: LRU cache\n```java\n// HashMap<key,node> for O(1) find + doubly-linked list for O(1) recency\nvoid get(int k){ if(map.has(k)){ moveToFront(node); return node.val; } return -1; }\nvoid put(int k,int v){ upsert; moveToFront(node); if(size>cap) removeTail(); }\n```\n\n## The I/O contract used here\nEvery design problem is driven by an **operation stream**: line 1 is the count `Q`, then one operation per line; your program prints a line only for the *query* operations. Parse the op name, dispatch, and buffer the outputs.",
    "exercises": [
      {
        "id": "design_ds-minpair",
        "title": "Pair each value with the min",
        "prompt": "Min Stack. Fill the blank so each pushed value stores the minimum of itself and the current top's min.",
        "hint": "The new min is min(x, previous min) — or x if the stack was empty.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int q = sc.nextInt();\n        Deque<int[]> st = new ArrayDeque<>();\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < q; i++) {\n            String op = sc.next();\n            if (op.equals(\"push\")) {\n                int x = sc.nextInt();\n                ____\n                st.push(new int[]{x, mn});\n            } else if (op.equals(\"pop\")) {\n                st.pop();\n            } else if (op.equals(\"top\")) {\n                sb.append(st.peek()[0]).append('\\n');\n            } else if (op.equals(\"getMin\")) {\n                sb.append(st.peek()[1]).append('\\n');\n            }\n        }\n        System.out.print(sb);\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int q = sc.nextInt();\n        Deque<int[]> st = new ArrayDeque<>();\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < q; i++) {\n            String op = sc.next();\n            if (op.equals(\"push\")) {\n                int x = sc.nextInt();\n                int mn = st.isEmpty() ? x : Math.min(st.peek()[1], x);\n                st.push(new int[]{x, mn});\n            } else if (op.equals(\"pop\")) {\n                st.pop();\n            } else if (op.equals(\"top\")) {\n                sb.append(st.peek()[0]).append('\\n');\n            } else if (op.equals(\"getMin\")) {\n                sb.append(st.peek()[1]).append('\\n');\n            }\n        }\n        System.out.print(sb);\n    }\n}\n",
        "tests": [
          {
            "input": "6\npush 2\npush 1\ngetMin\npop\ntop\ngetMin",
            "output": "1\n2\n2"
          },
          {
            "input": "3\npush 5\ntop\ngetMin",
            "output": "5\n5"
          },
          {
            "input": "4\npush 3\npush 4\ngetMin\ntop",
            "output": "3\n4"
          }
        ],
        "source_slug": "min-stack"
      },
      {
        "id": "design_ds-getmin",
        "title": "Read the stored min",
        "prompt": "Fill the blank so `getMin` prints the minimum stored alongside the current top.",
        "hint": "Index [1] of the top pair holds the running minimum.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int q = sc.nextInt();\n        Deque<int[]> st = new ArrayDeque<>();\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < q; i++) {\n            String op = sc.next();\n            if (op.equals(\"push\")) {\n                int x = sc.nextInt();\n                int mn = st.isEmpty() ? x : Math.min(st.peek()[1], x);\n                st.push(new int[]{x, mn});\n            } else if (op.equals(\"pop\")) {\n                st.pop();\n            } else if (op.equals(\"top\")) {\n                sb.append(st.peek()[0]).append('\\n');\n            } else if (op.equals(\"getMin\")) {\n                ____\n            }\n        }\n        System.out.print(sb);\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int q = sc.nextInt();\n        Deque<int[]> st = new ArrayDeque<>();\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < q; i++) {\n            String op = sc.next();\n            if (op.equals(\"push\")) {\n                int x = sc.nextInt();\n                int mn = st.isEmpty() ? x : Math.min(st.peek()[1], x);\n                st.push(new int[]{x, mn});\n            } else if (op.equals(\"pop\")) {\n                st.pop();\n            } else if (op.equals(\"top\")) {\n                sb.append(st.peek()[0]).append('\\n');\n            } else if (op.equals(\"getMin\")) {\n                sb.append(st.peek()[1]).append('\\n');\n            }\n        }\n        System.out.print(sb);\n    }\n}\n",
        "tests": [
          {
            "input": "4\npush 2\npush 1\ngetMin\ngetMin",
            "output": "1\n1"
          },
          {
            "input": "3\npush 9\npush 3\ngetMin",
            "output": "3"
          },
          {
            "input": "2\npush 4\ngetMin",
            "output": "4"
          }
        ],
        "source_slug": "min-stack"
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "union_find",
    "name": "Union-Find (DSU)",
    "category": "Graphs",
    "what": "A disjoint-set structure that merges groups and answers 'same group?' in near-constant time.",
    "deep": "Each element points to a parent; a set is a tree with a representative root. `find` follows parents to the root and, with path compression, flattens the tree; `union` links one root under another, and union-by-size/rank keeps trees shallow. Together they give near-O(1) amortized operations — the go-to for connectivity, cycle detection in undirected graphs, and Kruskal's MST.",
    "java": "int[] parent, size; find flattens: while(parent[x]!=x){ parent[x]=parent[parent[x]]; x=parent[x]; } union links smaller under larger.",
    "language": "java",
    "lesson": "# Union-Find (Disjoint Set Union)\n\nUnion-Find answers **'are these two in the same group?'** and **'merge these groups'** in near-constant time. Every element points at a parent; each group is a tree with a root that names it.\n\n```java\nint[] parent, size;\nint find(int x) {\n    while (parent[x] != x) { parent[x] = parent[parent[x]]; x = parent[x]; } // path compression\n    return x;\n}\nboolean union(int a, int b) {\n    int ra = find(a), rb = find(b);\n    if (ra == rb) return false;              // already together (a cycle!)\n    if (size[ra] < size[rb]) { int t = ra; ra = rb; rb = t; }\n    parent[rb] = ra; size[ra] += size[rb];    // union by size\n    return true;\n}\n```\n\n## When to reach for this\nSignals: *'connected components'*, *'provinces / friend circles'*, *'valid tree'*, *'redundant edge / cycle in an undirected graph'*, *'accounts merge'*, and Kruskal's MST.\n\n## Simulated solve — components of edges (0-1)(1-2)(3-4) on n=5\n| edge | action | components |\n|---|---|---|\n| start | 5 singletons | 5 |\n| 0-1 | union | 4 |\n| 1-2 | union | 3 |\n| 3-4 | union | 2 |\n\nA `union` that returns false (roots already equal) means you just found a **cycle**.",
    "exercises": [
      {
        "id": "union_find-find",
        "title": "Path compression",
        "prompt": "Union-Find counting components. Fill the blank so `find` flattens the tree while walking to the root.",
        "hint": "Point x at its grandparent to shorten the path.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    static int[] parent;\n    static int find(int x) {\n        while (parent[x] != x) { ____ x = parent[x]; }\n        return x;\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt(), m = sc.nextInt();\n        parent = new int[n];\n        for (int i = 0; i < n; i++) parent[i] = i;\n        int count = n;\n        for (int i = 0; i < m; i++) {\n            int u = sc.nextInt(), v = sc.nextInt();\n            int ru = find(u), rv = find(v);\n            if (ru != rv) { parent[ru] = rv; count--; }\n        }\n        System.out.println(count);\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    static int[] parent;\n    static int find(int x) {\n        while (parent[x] != x) { parent[x] = parent[parent[x]]; x = parent[x]; }\n        return x;\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt(), m = sc.nextInt();\n        parent = new int[n];\n        for (int i = 0; i < n; i++) parent[i] = i;\n        int count = n;\n        for (int i = 0; i < m; i++) {\n            int u = sc.nextInt(), v = sc.nextInt();\n            int ru = find(u), rv = find(v);\n            if (ru != rv) { parent[ru] = rv; count--; }\n        }\n        System.out.println(count);\n    }\n}\n",
        "tests": [
          {
            "input": "5 3\n0 1\n1 2\n3 4",
            "output": "2"
          },
          {
            "input": "4 3\n0 1\n1 2\n2 3",
            "output": "1"
          },
          {
            "input": "3 0",
            "output": "3"
          }
        ],
        "source_slug": "count-components"
      },
      {
        "id": "union_find-union",
        "title": "Merge two sets",
        "prompt": "Fill the blank so joining two different roots links one under the other and drops the component count.",
        "hint": "Only merge when the roots differ; then one fewer component.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    static int[] parent;\n    static int find(int x) {\n        while (parent[x] != x) { parent[x] = parent[parent[x]]; x = parent[x]; }\n        return x;\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt(), m = sc.nextInt();\n        parent = new int[n];\n        for (int i = 0; i < n; i++) parent[i] = i;\n        int count = n;\n        for (int i = 0; i < m; i++) {\n            int u = sc.nextInt(), v = sc.nextInt();\n            int ru = find(u), rv = find(v);\n            ____\n        }\n        System.out.println(count);\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    static int[] parent;\n    static int find(int x) {\n        while (parent[x] != x) { parent[x] = parent[parent[x]]; x = parent[x]; }\n        return x;\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt(), m = sc.nextInt();\n        parent = new int[n];\n        for (int i = 0; i < n; i++) parent[i] = i;\n        int count = n;\n        for (int i = 0; i < m; i++) {\n            int u = sc.nextInt(), v = sc.nextInt();\n            int ru = find(u), rv = find(v);\n            if (ru != rv) { parent[ru] = rv; count--; }\n        }\n        System.out.println(count);\n    }\n}\n",
        "tests": [
          {
            "input": "5 3\n0 1\n1 2\n3 4",
            "output": "2"
          },
          {
            "input": "4 4\n0 1\n1 2\n2 3\n3 0",
            "output": "1"
          },
          {
            "input": "6 2\n0 1\n2 3",
            "output": "4"
          }
        ],
        "source_slug": "count-components"
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "dijkstra",
    "name": "Dijkstra's Shortest Path",
    "category": "Graphs",
    "what": "Single-source shortest paths on non-negative weighted graphs using a min-heap of tentative distances.",
    "deep": "Dijkstra grows a set of finalized nodes: repeatedly pop the closest unfinalized node from a min-heap and relax its outgoing edges (if dist[x]+w < dist[y], update). Because weights are non-negative, once a node is popped its distance is final. It's the default for 'shortest / cheapest path with weights' and generalizes to grids (path with minimum effort).",
    "java": "PriorityQueue<long[]> pq keyed by distance; long[] dist filled with a large sentinel; skip stale pops with if (d > dist[x]) continue;.",
    "language": "java",
    "lesson": "# Dijkstra's Shortest Path\n\nDijkstra finds the shortest distance from one source to every node when **weights are non-negative**. Keep a min-heap of `(distance, node)`; the first time you pop a node its distance is final.\n\n```java\nlong[] dist = new long[n]; Arrays.fill(dist, Long.MAX_VALUE); dist[src] = 0;\nPriorityQueue<long[]> pq = new PriorityQueue<>((a,b) -> Long.compare(a[0], b[0]));\npq.offer(new long[]{0, src});\nwhile (!pq.isEmpty()) {\n    long[] cur = pq.poll(); long d = cur[0]; int x = (int) cur[1];\n    if (d > dist[x]) continue;                 // stale entry\n    for (int[] e : adj[x]) {                    // e = {to, weight}\n        if (d + e[1] < dist[e[0]]) { dist[e[0]] = d + e[1]; pq.offer(new long[]{dist[e[0]], e[0]}); }\n    }\n}\n```\n\n## When to reach for this\nSignals: *'shortest / cheapest path'*, *'minimum time to reach'*, weighted grids ('minimum effort'). Non-negative weights only — otherwise use Bellman-Ford.\n\n## Simulated solve — from 0, edges 0→1(4) 0→2(1) 2→1(2)\n| pop | dist after relax |\n|---|---|\n| (0,0) | [0, 4, 1] |\n| (1,2) | [0, 3, 1] (2→1 gives 3) |\n| (3,1) | final [0,3,1] |\n",
    "exercises": [
      {
        "id": "dijkstra-relax",
        "title": "Relax an edge",
        "prompt": "Dijkstra from a source. Fill the blank so a shorter route to a neighbour updates its distance and re-queues it.",
        "hint": "If d + weight beats the stored distance, update and push the neighbour.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt(), m = sc.nextInt(), src = sc.nextInt();\n        List<int[]>[] adj = new List[n];\n        for (int i = 0; i < n; i++) adj[i] = new ArrayList<>();\n        for (int i = 0; i < m; i++) { int u = sc.nextInt(), v = sc.nextInt(), w = sc.nextInt(); adj[u].add(new int[]{v, w}); }\n        long[] dist = new long[n];\n        Arrays.fill(dist, Long.MAX_VALUE);\n        dist[src] = 0;\n        PriorityQueue<long[]> pq = new PriorityQueue<>((a, b) -> Long.compare(a[0], b[0]));\n        pq.offer(new long[]{0, src});\n        while (!pq.isEmpty()) {\n            long[] cur = pq.poll(); long d = cur[0]; int x = (int) cur[1];\n            if (d > dist[x]) continue;\n            for (int[] e : adj[x]) {\n                ____\n            }\n        }\n        System.out.println(dist[n - 1] == Long.MAX_VALUE ? -1 : dist[n - 1]);\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt(), m = sc.nextInt(), src = sc.nextInt();\n        List<int[]>[] adj = new List[n];\n        for (int i = 0; i < n; i++) adj[i] = new ArrayList<>();\n        for (int i = 0; i < m; i++) { int u = sc.nextInt(), v = sc.nextInt(), w = sc.nextInt(); adj[u].add(new int[]{v, w}); }\n        long[] dist = new long[n];\n        Arrays.fill(dist, Long.MAX_VALUE);\n        dist[src] = 0;\n        PriorityQueue<long[]> pq = new PriorityQueue<>((a, b) -> Long.compare(a[0], b[0]));\n        pq.offer(new long[]{0, src});\n        while (!pq.isEmpty()) {\n            long[] cur = pq.poll(); long d = cur[0]; int x = (int) cur[1];\n            if (d > dist[x]) continue;\n            for (int[] e : adj[x]) {\n                if (d + e[1] < dist[e[0]]) { dist[e[0]] = d + e[1]; pq.offer(new long[]{dist[e[0]], e[0]}); }\n            }\n        }\n        System.out.println(dist[n - 1] == Long.MAX_VALUE ? -1 : dist[n - 1]);\n    }\n}\n",
        "tests": [
          {
            "input": "4 4 0\n0 1 4\n0 2 1\n2 1 2\n1 3 1",
            "output": "4"
          },
          {
            "input": "3 2 0\n0 1 5\n1 2 5",
            "output": "10"
          },
          {
            "input": "2 1 0\n0 1 7",
            "output": "7"
          }
        ],
        "source_slug": "network-delay-time"
      },
      {
        "id": "dijkstra-stale",
        "title": "Skip stale heap entries",
        "prompt": "Fill the blank so an outdated (already-improved) heap entry is ignored when popped.",
        "hint": "If the popped distance is worse than the best known, this entry is stale — skip it.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt(), m = sc.nextInt(), src = sc.nextInt();\n        List<int[]>[] adj = new List[n];\n        for (int i = 0; i < n; i++) adj[i] = new ArrayList<>();\n        for (int i = 0; i < m; i++) { int u = sc.nextInt(), v = sc.nextInt(), w = sc.nextInt(); adj[u].add(new int[]{v, w}); }\n        long[] dist = new long[n];\n        Arrays.fill(dist, Long.MAX_VALUE);\n        dist[src] = 0;\n        PriorityQueue<long[]> pq = new PriorityQueue<>((a, b) -> Long.compare(a[0], b[0]));\n        pq.offer(new long[]{0, src});\n        while (!pq.isEmpty()) {\n            long[] cur = pq.poll(); long d = cur[0]; int x = (int) cur[1];\n            ____\n            for (int[] e : adj[x]) {\n                if (d + e[1] < dist[e[0]]) { dist[e[0]] = d + e[1]; pq.offer(new long[]{dist[e[0]], e[0]}); }\n            }\n        }\n        System.out.println(dist[n - 1] == Long.MAX_VALUE ? -1 : dist[n - 1]);\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt(), m = sc.nextInt(), src = sc.nextInt();\n        List<int[]>[] adj = new List[n];\n        for (int i = 0; i < n; i++) adj[i] = new ArrayList<>();\n        for (int i = 0; i < m; i++) { int u = sc.nextInt(), v = sc.nextInt(), w = sc.nextInt(); adj[u].add(new int[]{v, w}); }\n        long[] dist = new long[n];\n        Arrays.fill(dist, Long.MAX_VALUE);\n        dist[src] = 0;\n        PriorityQueue<long[]> pq = new PriorityQueue<>((a, b) -> Long.compare(a[0], b[0]));\n        pq.offer(new long[]{0, src});\n        while (!pq.isEmpty()) {\n            long[] cur = pq.poll(); long d = cur[0]; int x = (int) cur[1];\n            if (d > dist[x]) continue;\n            for (int[] e : adj[x]) {\n                if (d + e[1] < dist[e[0]]) { dist[e[0]] = d + e[1]; pq.offer(new long[]{dist[e[0]], e[0]}); }\n            }\n        }\n        System.out.println(dist[n - 1] == Long.MAX_VALUE ? -1 : dist[n - 1]);\n    }\n}\n",
        "tests": [
          {
            "input": "4 4 0\n0 1 4\n0 2 1\n2 1 2\n1 3 1",
            "output": "4"
          },
          {
            "input": "3 3 0\n0 1 1\n0 2 5\n1 2 1",
            "output": "2"
          },
          {
            "input": "2 1 0\n0 1 3",
            "output": "3"
          }
        ],
        "source_slug": "network-delay-time"
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "bellman_ford",
    "name": "Bellman-Ford",
    "category": "Graphs",
    "what": "Shortest paths that tolerate negative weights or a hop limit by relaxing all edges V-1 (or k+1) times.",
    "deep": "Where Dijkstra fails (negative edges) or where you must bound the number of edges (at most k stops), Bellman-Ford helps: each round relaxes every edge once, so after r rounds you know the best path using at most r edges. Using a snapshot of the previous round's distances enforces the exact hop limit.",
    "java": "int[] dist; for (round) { int[] nd = dist.clone(); for each edge relax into nd; dist = nd; }",
    "language": "java",
    "lesson": "# Bellman-Ford\n\nWhen edges can be negative, or you must cap the number of hops, relax **all edges** repeatedly.\n\n```java\nint[] dist = new int[n]; Arrays.fill(dist, INF); dist[src] = 0;\nfor (int round = 0; round <= k; round++) {   // k+1 rounds = at most k stops\n    int[] nd = dist.clone();                  // snapshot: exactly one more edge\n    for (int[] e : edges)                      // e = {u, v, w}\n        if (dist[e[0]] + e[2] < nd[e[1]]) nd[e[1]] = dist[e[0]] + e[2];\n    dist = nd;\n}\n```\n\n## When to reach for this\nSignals: *'at most k stops'*, *'negative weights'*, *'detect a negative cycle'*. The `clone()` snapshot is what enforces the exact hop limit — without it, one round could chain many edges.",
    "exercises": [],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "mst",
    "name": "Minimum Spanning Tree",
    "category": "Graphs",
    "what": "The cheapest set of edges connecting all nodes, built greedily by Kruskal (sort + union) or Prim (grow a tree).",
    "deep": "Kruskal sorts edges ascending and adds each that joins two different components (Union-Find), stopping at n-1 edges. Prim grows one tree, always attaching the cheapest edge to a new node via a min-heap. On dense/complete graphs (points on a plane) Prim's O(n^2) avoids listing all edges.",
    "java": "Kruskal: sort edges, Union-Find to skip cycles. Prim: PriorityQueue of (cost, node), a boolean inTree[].",
    "language": "java",
    "lesson": "# Minimum Spanning Tree\n\nConnect every node for the least total edge weight. Two greedy builds:\n\n**Kruskal** — sort edges, add each that doesn't form a cycle (Union-Find):\n```java\nArrays.sort(edges, (a,b) -> a[2] - b[2]);\nfor (int[] e : edges) if (union(e[0], e[1])) total += e[2];  // skip cycles\n```\n\n**Prim** — grow one tree, always adding the cheapest edge to a new node (min-heap). Better on dense/complete graphs (e.g. connecting points on a plane).\n\n## When to reach for this\nSignals: *'connect all at minimum cost'*, *'minimum spanning tree'*, *'cheapest network'*. Kruskal pairs naturally with Union-Find; Prim with a heap.",
    "exercises": [],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "graph_cycle",
    "name": "Cycle Detection & Topological Sort",
    "category": "Graphs",
    "what": "Finding cycles (or a valid ordering) in directed graphs with DFS 3-coloring or Kahn's indegree algorithm.",
    "deep": "In a DIRECTED graph, DFS colors nodes white/gray/black; an edge to a gray (still-on-the-stack) node is a back-edge — a cycle. Equivalently, Kahn's algorithm repeatedly removes indegree-0 nodes; if some remain, a cycle exists. A graph with no cycle has a topological order (course schedule, build order, alien dictionary). In UNDIRECTED graphs, Union-Find or parent-aware DFS finds cycles.",
    "java": "Directed: int[] color (0/1/2) DFS. Kahn: int[] indeg, a queue of indegree-0 nodes, count popped == n means acyclic.",
    "language": "java",
    "lesson": "# Cycle Detection & Topological Sort\n\nIn a **directed** graph, color nodes as you DFS. An edge into a node still on the recursion stack is a **back edge** — a cycle.\n\n```java\nint[] color = new int[n];  // 0 white, 1 gray (on stack), 2 black\nboolean dfs(int x) {\n    color[x] = 1;\n    for (int y : adj[x]) {\n        if (color[y] == 1) return true;         // back edge → cycle\n        if (color[y] == 0 && dfs(y)) return true;\n    }\n    color[x] = 2; return false;\n}\n```\n\n## Kahn's topological sort (the acyclic case)\nRepeatedly remove indegree-0 nodes; if you emit all n, the graph is acyclic and you have a valid order (course schedule, build order, alien dictionary). Fewer than n → a cycle.\n\n## When to reach for this\nSignals: *'can you finish / order the tasks'*, *'prerequisites'*, *'build order'*, *'detect a cycle'*. Undirected graphs use Union-Find or parent-aware DFS instead.",
    "exercises": [
      {
        "id": "graph_cycle-back",
        "title": "Spot the back edge",
        "prompt": "Directed cycle detection with 3-coloring. Fill the blank so reaching a node still on the DFS stack reports a cycle.",
        "hint": "Gray (color 1) means the node is still on the current path — a back edge.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    static List<Integer>[] adj;\n    static int[] color;\n    static boolean dfs(int x) {\n        color[x] = 1;\n        for (int y : adj[x]) {\n            ____\n            if (color[y] == 0 && dfs(y)) return true;\n        }\n        color[x] = 2;\n        return false;\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt(), m = sc.nextInt();\n        adj = new List[n]; for (int i = 0; i < n; i++) adj[i] = new ArrayList<>();\n        for (int i = 0; i < m; i++) { int u = sc.nextInt(), v = sc.nextInt(); adj[u].add(v); }\n        color = new int[n];\n        boolean cyc = false;\n        for (int i = 0; i < n; i++) if (color[i] == 0 && dfs(i)) cyc = true;\n        System.out.println(cyc ? \"true\" : \"false\");\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    static List<Integer>[] adj;\n    static int[] color;\n    static boolean dfs(int x) {\n        color[x] = 1;\n        for (int y : adj[x]) {\n            if (color[y] == 1) return true;\n            if (color[y] == 0 && dfs(y)) return true;\n        }\n        color[x] = 2;\n        return false;\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt(), m = sc.nextInt();\n        adj = new List[n]; for (int i = 0; i < n; i++) adj[i] = new ArrayList<>();\n        for (int i = 0; i < m; i++) { int u = sc.nextInt(), v = sc.nextInt(); adj[u].add(v); }\n        color = new int[n];\n        boolean cyc = false;\n        for (int i = 0; i < n; i++) if (color[i] == 0 && dfs(i)) cyc = true;\n        System.out.println(cyc ? \"true\" : \"false\");\n    }\n}\n",
        "tests": [
          {
            "input": "3 3\n0 1\n1 2\n2 0",
            "output": "true"
          },
          {
            "input": "3 2\n0 1\n1 2",
            "output": "false"
          },
          {
            "input": "2 1\n0 0",
            "output": "true"
          }
        ],
        "source_slug": "detect-cycle-directed"
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "trie",
    "name": "Trie (Prefix Tree)",
    "category": "Data Structures",
    "what": "A tree where each edge is a character, so shared prefixes share a path — giving O(word length) insert and lookup.",
    "deep": "Each node has up to 26 children and an is-word flag. Words that share a prefix share the nodes along it, which is what makes tries shine at prefix queries: autocomplete, 'count words with prefix', word search with wildcards, and even maximum-XOR via a binary trie. The cost of any operation is the length of the word, independent of how many words are stored.",
    "java": "class Node { Node[] ch = new Node[26]; boolean end; } — index by c-'a'; insert walks/creates, search checks end, startsWith checks the path exists.",
    "language": "java",
    "lesson": "# Trie (Prefix Tree)\n\nA trie stores words along **paths of characters**. Words sharing a prefix share the nodes for that prefix, so every operation costs only the length of the word — never the size of the dictionary.\n\n```java\nclass Node { Node[] ch = new Node[26]; boolean end; }\nNode root = new Node();\nvoid insert(String w){\n    Node n = root;\n    for (char c : w.toCharArray()) {\n        int i = c - 'a';\n        if (n.ch[i] == null) n.ch[i] = new Node();  // create child\n        n = n.ch[i];\n    }\n    n.end = true;                                    // mark word end\n}\n```\n\n## search vs startsWith\n| Query | Succeeds when |\n|---|---|\n| `search(w)` | the path exists **and** the last node has `end == true` |\n| `startsWith(p)` | the path exists (end flag irrelevant) |\n\n## When to reach for this\nSignals: *'prefix'*, *'autocomplete / suggestions'*, *'dictionary of words'*, *'words with wildcard'*, and 'maximum XOR' (a binary trie of bits).\n\n## Simulated solve — insert 'app', 'apple', search 'app'\n| step | path |\n|---|---|\n| insert app | root-a-p-p (p.end=true) |\n| insert apple | reuse a-p-p, add l-e (e.end=true) |\n| search app | walk a-p-p, end==true → **true** |\n| search ap | walk a-p, end==false → **false** |\n",
    "exercises": [
      {
        "id": "trie-insert",
        "title": "Create the child",
        "prompt": "Trie insert. Fill the blank so a missing child node is created before descending into it.",
        "hint": "Add a new Node for c only if one doesn't already exist.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    static class Node { Map<Character,Node> ch = new HashMap<>(); boolean end; }\n    static Node root = new Node();\n    static void insert(String w) {\n        Node n = root;\n        for (char c : w.toCharArray()) {\n            ____\n            n = n.ch.get(c);\n        }\n        n.end = true;\n    }\n    static boolean search(String w) {\n        Node n = root;\n        for (char c : w.toCharArray()) {\n            if (!n.ch.containsKey(c)) return false;\n            n = n.ch.get(c);\n        }\n        return n.end;\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        for (int i = 0; i < n; i++) insert(sc.next());\n        int q = sc.nextInt();\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < q; i++) { sb.append(search(sc.next()) ? 1 : 0); if (i < q - 1) sb.append('\\n'); }\n        System.out.println(sb.toString());\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    static class Node { Map<Character,Node> ch = new HashMap<>(); boolean end; }\n    static Node root = new Node();\n    static void insert(String w) {\n        Node n = root;\n        for (char c : w.toCharArray()) {\n            n.ch.putIfAbsent(c, new Node());\n            n = n.ch.get(c);\n        }\n        n.end = true;\n    }\n    static boolean search(String w) {\n        Node n = root;\n        for (char c : w.toCharArray()) {\n            if (!n.ch.containsKey(c)) return false;\n            n = n.ch.get(c);\n        }\n        return n.end;\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        for (int i = 0; i < n; i++) insert(sc.next());\n        int q = sc.nextInt();\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < q; i++) { sb.append(search(sc.next()) ? 1 : 0); if (i < q - 1) sb.append('\\n'); }\n        System.out.println(sb.toString());\n    }\n}\n",
        "tests": [
          {
            "input": "3 cat car dog\n2 car cow",
            "output": "1\n0"
          },
          {
            "input": "1 a\n1 a",
            "output": "1"
          },
          {
            "input": "2 ab abc\n2 ab abcd",
            "output": "1\n0"
          }
        ],
        "source_slug": "implement-trie-ops"
      },
      {
        "id": "trie-search",
        "title": "Walk or fail",
        "prompt": "Trie search. Fill the blank so search returns false the moment a character path is missing.",
        "hint": "If the current node has no child for c, the word can't be present.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    static class Node { Map<Character,Node> ch = new HashMap<>(); boolean end; }\n    static Node root = new Node();\n    static void insert(String w) {\n        Node n = root;\n        for (char c : w.toCharArray()) {\n            n.ch.putIfAbsent(c, new Node());\n            n = n.ch.get(c);\n        }\n        n.end = true;\n    }\n    static boolean search(String w) {\n        Node n = root;\n        for (char c : w.toCharArray()) {\n            ____\n            n = n.ch.get(c);\n        }\n        return n.end;\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        for (int i = 0; i < n; i++) insert(sc.next());\n        int q = sc.nextInt();\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < q; i++) { sb.append(search(sc.next()) ? 1 : 0); if (i < q - 1) sb.append('\\n'); }\n        System.out.println(sb.toString());\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    static class Node { Map<Character,Node> ch = new HashMap<>(); boolean end; }\n    static Node root = new Node();\n    static void insert(String w) {\n        Node n = root;\n        for (char c : w.toCharArray()) {\n            n.ch.putIfAbsent(c, new Node());\n            n = n.ch.get(c);\n        }\n        n.end = true;\n    }\n    static boolean search(String w) {\n        Node n = root;\n        for (char c : w.toCharArray()) {\n            if (!n.ch.containsKey(c)) return false;\n            n = n.ch.get(c);\n        }\n        return n.end;\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        for (int i = 0; i < n; i++) insert(sc.next());\n        int q = sc.nextInt();\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < q; i++) { sb.append(search(sc.next()) ? 1 : 0); if (i < q - 1) sb.append('\\n'); }\n        System.out.println(sb.toString());\n    }\n}\n",
        "tests": [
          {
            "input": "2 cat car\n2 ca cat",
            "output": "0\n1"
          },
          {
            "input": "1 hi\n1 hix",
            "output": "0"
          },
          {
            "input": "2 a ab\n2 a ab",
            "output": "1\n1"
          }
        ],
        "source_slug": "implement-trie-ops"
      },
      {
        "id": "trie-end",
        "title": "Mark the word end",
        "prompt": "Fill the blank so the final node of an inserted word is flagged as a complete word.",
        "hint": "After walking the word, set end = true on the last node.",
        "language": "java",
        "kind": "drill",
        "difficulty": "",
        "starter": "import java.util.*;\npublic class Main {\n    static class Node { Map<Character,Node> ch = new HashMap<>(); boolean end; }\n    static Node root = new Node();\n    static void insert(String w) {\n        Node n = root;\n        for (char c : w.toCharArray()) {\n            n.ch.putIfAbsent(c, new Node());\n            n = n.ch.get(c);\n        }\n        ____\n    }\n    static boolean search(String w) {\n        Node n = root;\n        for (char c : w.toCharArray()) {\n            if (!n.ch.containsKey(c)) return false;\n            n = n.ch.get(c);\n        }\n        return n.end;\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        for (int i = 0; i < n; i++) insert(sc.next());\n        int q = sc.nextInt();\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < q; i++) { sb.append(search(sc.next()) ? 1 : 0); if (i < q - 1) sb.append('\\n'); }\n        System.out.println(sb.toString());\n    }\n}\n",
        "solution": "import java.util.*;\npublic class Main {\n    static class Node { Map<Character,Node> ch = new HashMap<>(); boolean end; }\n    static Node root = new Node();\n    static void insert(String w) {\n        Node n = root;\n        for (char c : w.toCharArray()) {\n            n.ch.putIfAbsent(c, new Node());\n            n = n.ch.get(c);\n        }\n        n.end = true;\n    }\n    static boolean search(String w) {\n        Node n = root;\n        for (char c : w.toCharArray()) {\n            if (!n.ch.containsKey(c)) return false;\n            n = n.ch.get(c);\n        }\n        return n.end;\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        for (int i = 0; i < n; i++) insert(sc.next());\n        int q = sc.nextInt();\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < q; i++) { sb.append(search(sc.next()) ? 1 : 0); if (i < q - 1) sb.append('\\n'); }\n        System.out.println(sb.toString());\n    }\n}\n",
        "tests": [
          {
            "input": "2 ca cat\n2 ca cab",
            "output": "1\n0"
          },
          {
            "input": "1 x\n1 x",
            "output": "1"
          },
          {
            "input": "2 go gone\n2 go gon",
            "output": "1\n0"
          }
        ],
        "source_slug": "implement-trie-ops"
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_variables",
    "name": "Variables — let & const",
    "category": "TS: Language Basics",
    "what": "Naming values with const (fixed binding) and let (reassignable), both block-scoped.",
    "deep": "A variable is a named box for a value. `const` locks the name to one value; `let` allows reassignment. Both live only inside the `{ }` block they are declared in. Reach for `const` by default and only switch to `let` when you truly need to reassign.",
    "java": "`const x = 5;` and `let y = 5;` replace Java's `final int x` / `int y`. No `var` (function-scoped, avoid it). Types are usually inferred, so `const x = 5` already has type `number`.",
    "language": "typescript",
    "lesson": "### Worked example\nThink of a variable as a labelled box. `const pi = 3.14` puts `3.14` in a box named `pi` and *locks the label* — you can never point `pi` at a different value. `let score = 0` makes a box you *can* refill: `score = score + 10`.\n\n```ts\nconst name = \"Ada\";      // fixed binding — reassigning is a compile error\nlet score = 0;           // reassignable\nscore = score + 10;      // fine\nconsole.log(name, score); // Ada 10\n```\n\n### Reach for const first\nDefault to `const`. It signals \"this never changes\", which makes code easier to read and catches accidental reassignment. Switch to `let` only when you genuinely need to update the variable (a running total, a loop counter).\n\n### Block scope\nA `const`/`let` exists only inside the nearest `{ }`:\n```ts\nif (true) {\n  const secret = 42;\n}\n// console.log(secret);  // error: secret is not defined out here\n```\n\n### Watch out for\n- `const` fixes the *binding*, not the contents: `const a = [1]; a.push(2);` is allowed — you changed the array, not which array `a` points to.\n- Never use `var` — it ignores block scope and causes subtle bugs.\n",
    "exercises": [
      {
        "id": "ts_variables-const",
        "title": "Declare a constant",
        "prompt": "The input holds one number. Replace `____` to declare a `const` named `n` holding that number so the program can print it.",
        "hint": "Number(input) turns the text into a real number; store it with const n = ... .",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\nconsole.log(n);\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\nconst n: number = Number(input);\nconsole.log(n);\n",
        "tests": [
          {
            "input": "42",
            "output": "42"
          },
          {
            "input": "7",
            "output": "7"
          },
          {
            "input": "-3",
            "output": "-3"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_variables-let",
        "title": "Reassign a running total",
        "prompt": "`total` starts at `n` and must be bumped by 10. Replace `____` to declare `total` with `let` (a `const` could not be reassigned on the next line).",
        "hint": "Only let allows the reassignment `total = total + 10` on the next line.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst n = Number(fs.readFileSync(0, \"utf8\").trim());\n____\ntotal = total + 10;\nconsole.log(total);\n",
        "solution": "import * as fs from \"fs\";\nconst n = Number(fs.readFileSync(0, \"utf8\").trim());\nlet total = n;\ntotal = total + 10;\nconsole.log(total);\n",
        "tests": [
          {
            "input": "5",
            "output": "15"
          },
          {
            "input": "0",
            "output": "10"
          },
          {
            "input": "-4",
            "output": "6"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_variables-swap",
        "title": "Swap two numbers",
        "prompt": "Two numbers `a` and `b` are read for you. Write the whole solution: swap them using a temporary variable and print them in swapped order, space-separated (`3 4` -> `4 3`).",
        "hint": "Save one value in a tmp box before overwriting it, or you lose it.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst [a, b] = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\n____\n",
        "solution": "import * as fs from \"fs\";\nconst [a, b] = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nlet first = a;\nlet second = b;\nconst tmp = first;\nfirst = second;\nsecond = tmp;\nconsole.log(first + \" \" + second);\n",
        "tests": [
          {
            "input": "3 4",
            "output": "4 3"
          },
          {
            "input": "1 2",
            "output": "2 1"
          },
          {
            "input": "9 9",
            "output": "9 9"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_variables-challenge",
        "title": "Two-number report",
        "prompt": "The input is two numbers `a b` on one line. Using `const`/`let` and arithmetic, print three lines: their sum, their difference (`a - b`), and their product.",
        "hint": "Read the two numbers, then compute and log each result on its own line.",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Intro",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\nconst [a, b] = input.split(/\\s+/).map(Number);\nconst sum = a + b;\nconst diff = a - b;\nconst product = a * b;\nconsole.log(sum);\nconsole.log(diff);\nconsole.log(product);\n",
        "tests": [
          {
            "input": "3 4",
            "output": "7\n-1\n12"
          },
          {
            "input": "10 5",
            "output": "15\n5\n50"
          },
          {
            "input": "-2 -3",
            "output": "-5\n1\n6"
          },
          {
            "input": "0 0",
            "output": "0\n0\n0"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_types",
    "name": "Primitive Types & Annotations",
    "category": "TS: Language Basics",
    "what": "The built-in value types (number, string, boolean, null, undefined) and how to annotate them.",
    "deep": "TypeScript adds a type to every value. The primitives you meet first are `number` (one type for ints and floats), `string`, `boolean`, plus `null` and `undefined`. You write a type after a colon — `let n: number` — but usually inference figures it out for you.",
    "java": "One `number` covers Java's int/long/double. `string` is lowercase (the type), `String` capitalized is something else. `boolean` holds `true`/`false`. There is no `char` — a single character is just a length-1 `string`.",
    "language": "typescript",
    "lesson": "### Worked example\nEvery value has a type. You can let TypeScript infer it, or write it after a colon:\n```ts\nlet count: number = 5;      // whole numbers AND decimals are both `number`\nlet price: number = 9.99;\nlet title: string = \"Poodcode\";\nlet done: boolean = false;  // true / false\nlet nothing: null = null;\nlet missing: undefined = undefined;\n```\n\n### The primitives you meet first\n| Type | Example | Notes |\n|------|---------|-------|\n| `number` | `42`, `3.5`, `-7` | one type for ints and floats |\n| `string` | `\"hi\"`, `` `hi ${x}` `` | text; no separate `char` type |\n| `boolean` | `true`, `false` | |\n| `null` | `null` | intentionally empty |\n| `undefined` | `undefined` | no value assigned yet |\n\n### From input to number\nInput arrives as text. Convert it explicitly:\n```ts\nconst n: number = Number(\"42\");   // 42, a real number\nconst s: string = String(42);     // \"42\"\n```\n\n### Watch out for\n- `number` can't hold arbitrarily huge integers exactly (beyond ~9 quadrillion); that's what `bigint` is for later.\n- `\"5\" + 1` is `\"51\"` (string concat) but `Number(\"5\") + 1` is `6`. Convert before doing math.\n",
    "exercises": [
      {
        "id": "ts_types-bool",
        "title": "Store a yes/no",
        "prompt": "Replace `____` so `isPositive` is a `boolean` that is `true` exactly when `n` is greater than 0.",
        "hint": "A comparison like n > 0 already produces a boolean.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst n = Number(fs.readFileSync(0, \"utf8\").trim());\n____\nconsole.log(isPositive);\n",
        "solution": "import * as fs from \"fs\";\nconst n = Number(fs.readFileSync(0, \"utf8\").trim());\nconst isPositive: boolean = n > 0;\nconsole.log(isPositive);\n",
        "tests": [
          {
            "input": "5",
            "output": "true"
          },
          {
            "input": "-2",
            "output": "false"
          },
          {
            "input": "0",
            "output": "false"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_types-convert",
        "title": "Text to number",
        "prompt": "The input arrives as text in `raw`. Replace `____` to convert it to a `number` named `value`, so `value + 1` does math (not string concatenation).",
        "hint": "Without Number(), \"41\" + 1 would be \"411\". Convert first.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst raw: string = fs.readFileSync(0, \"utf8\").trim();\n____\nconsole.log(value + 1);\n",
        "solution": "import * as fs from \"fs\";\nconst raw: string = fs.readFileSync(0, \"utf8\").trim();\nconst value: number = Number(raw);\nconsole.log(value + 1);\n",
        "tests": [
          {
            "input": "41",
            "output": "42"
          },
          {
            "input": "0",
            "output": "1"
          },
          {
            "input": "-1",
            "output": "0"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_types-rect",
        "title": "Rectangle area",
        "prompt": "Width and height are read into `w` and `h`. Write the solution: compute the area (a `number`) and print it.",
        "hint": "Area is width times height.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst [w, h] = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\n____\n",
        "solution": "import * as fs from \"fs\";\nconst [w, h] = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nconst area: number = w * h;\nconsole.log(area);\n",
        "tests": [
          {
            "input": "3 4",
            "output": "12"
          },
          {
            "input": "5 5",
            "output": "25"
          },
          {
            "input": "7 2",
            "output": "14"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_types-challenge",
        "title": "Coin counter",
        "prompt": "The input is four counts `quarters dimes nickels pennies`. Compute the total value in cents (25/10/5/1), then in dollars (cents / 100). Print the cents on line 1 and the dollars on line 2.",
        "hint": "cents is an integer; dividing by 100 gives a decimal `number` like 0.25.",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Intro",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\nconst [q, d, n, p] = input.split(/\\s+/).map(Number);\nconst cents: number = q * 25 + d * 10 + n * 5 + p;\nconst dollars: number = cents / 100;\nconsole.log(cents);\nconsole.log(dollars);\n",
        "tests": [
          {
            "input": "1 0 0 0",
            "output": "25\n0.25"
          },
          {
            "input": "0 0 0 0",
            "output": "0\n0"
          },
          {
            "input": "3 2 1 4",
            "output": "104\n1.04"
          },
          {
            "input": "4 0 0 0",
            "output": "100\n1"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_inference",
    "name": "Type Inference, any & unknown",
    "category": "TS: Language Basics",
    "what": "Letting TypeScript work out types for you, and the escape hatches any and unknown.",
    "deep": "When you initialize a variable, TypeScript infers its type — `const x = 3` is a `number` without you saying so. `any` turns checking off for a value (avoid it); `unknown` is the safe alternative that forces you to narrow before use.",
    "java": "Inference is like Java's `var`, but pervasive. `any` opts out of all checking (a foot-gun); prefer `unknown` and narrow with `typeof` before using the value.",
    "language": "typescript",
    "lesson": "### Worked example\nYou rarely need to write types — TypeScript infers them from the initializer:\n```ts\nconst x = 3;          // inferred: number\nconst label = \"hi\";   // inferred: string\nconst flags = [true]; // inferred: boolean[]\n```\nAnnotate only when there's no initializer, or to be intentionally stricter.\n\n### any vs unknown\nBoth mean \"type not known\", but they behave oppositely:\n```ts\nlet a: any = \"hello\";\na.foo.bar();          // compiles — checking is OFF. Dangerous.\n\nlet u: unknown = \"hello\";\n// u.toUpperCase();   // error: must narrow first\nif (typeof u === \"string\") {\n  console.log(u.toUpperCase()); // OK here — narrowed to string\n}\n```\n\n### Rule of thumb\n- Prefer inference; write a type when it clarifies intent.\n- Avoid `any` — it silently disables the safety you came for.\n- When you truly don't know a type (parsing, catch blocks), use `unknown` and narrow with a runtime check.\n\n### Watch out for\n- An empty `const arr = []` infers `any[]`; give it a type — `const arr: number[] = []`.\n",
    "exercises": [
      {
        "id": "ts_inference-sum",
        "title": "Let TypeScript infer",
        "prompt": "No type annotations needed here — TypeScript infers `number` from the values. Replace `____` to declare `total` as the sum of `a` and `b`.",
        "hint": "const total = a + b;  — its type is inferred as number.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst [a, b] = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\n____\nconsole.log(total);\n",
        "solution": "import * as fs from \"fs\";\nconst [a, b] = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nconst total = a + b;\nconsole.log(total);\n",
        "tests": [
          {
            "input": "3 4",
            "output": "7"
          },
          {
            "input": "10 -2",
            "output": "8"
          },
          {
            "input": "0 0",
            "output": "0"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_inference-unknown",
        "title": "Narrow an unknown",
        "prompt": "`raw` is typed `unknown`, so you must prove it is a string before calling `.toUpperCase()`. Replace `____` with the check that narrows it to a string.",
        "hint": "typeof raw === \"string\" tells TypeScript (and you) it is safe to call string methods.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst raw: unknown = fs.readFileSync(0, \"utf8\").trim();\nlet out = \"\";\nif (____) {\n  out = raw.toUpperCase();\n}\nconsole.log(out);\n",
        "solution": "import * as fs from \"fs\";\nconst raw: unknown = fs.readFileSync(0, \"utf8\").trim();\nlet out = \"\";\nif (typeof raw === \"string\") {\n  out = raw.toUpperCase();\n}\nconsole.log(out);\n",
        "tests": [
          {
            "input": "hello",
            "output": "HELLO"
          },
          {
            "input": "Poodcode",
            "output": "POODCODE"
          },
          {
            "input": "abc",
            "output": "ABC"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_inference-larger",
        "title": "Return the larger",
        "prompt": "Two numbers are read. Write the solution: store the larger of `a` and `b` in a variable (let inference give it a type) and print it.",
        "hint": "A ternary a > b ? a : b picks the larger value.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst [a, b] = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\n____\n",
        "solution": "import * as fs from \"fs\";\nconst [a, b] = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nconst larger = a > b ? a : b;\nconsole.log(larger);\n",
        "tests": [
          {
            "input": "3 4",
            "output": "4"
          },
          {
            "input": "9 2",
            "output": "9"
          },
          {
            "input": "5 5",
            "output": "5"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_inference-challenge",
        "title": "Min, max, range",
        "prompt": "The input is a line of numbers. Let TypeScript infer the types. Print the smallest on line 1, the largest on line 2, and the range (largest − smallest) on line 3.",
        "hint": "Math.min(...nums) and Math.max(...nums) spread the array as arguments.",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Intro",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\nconst nums = input.split(/\\s+/).map(Number);\nconst lo = Math.min(...nums);\nconst hi = Math.max(...nums);\nconsole.log(lo);\nconsole.log(hi);\nconsole.log(hi - lo);\n",
        "tests": [
          {
            "input": "3 1 4 1 5",
            "output": "1\n5\n4"
          },
          {
            "input": "7",
            "output": "7\n7\n0"
          },
          {
            "input": "-2 -5 -1",
            "output": "-5\n-1\n4"
          },
          {
            "input": "10 10",
            "output": "10\n10\n0"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_strings",
    "name": "Strings & Template Literals",
    "category": "TS: Language Basics",
    "what": "Building and inspecting text, and interpolating values with backtick templates.",
    "deep": "Strings are immutable sequences of characters. Backtick templates `` `Hi ${name}` `` splice values straight into text — clearer than `+` chains. Common tools: `.length`, `.toUpperCase()`, `.slice()`, `.includes()`, `.split()`.",
    "java": "`` `${a} and ${b}` `` replaces Java's string concatenation and `String.format`. Methods are camelCase and mostly match (`length` is a property, not `length()`). Indexing uses `s[i]` or `s.charAt(i)`.",
    "language": "typescript",
    "lesson": "### Worked example\nTemplate literals (backticks) splice values into text — clearer than gluing with `+`:\n```ts\nconst name = \"Ada\";\nconst age = 36;\nconsole.log(`${name} is ${age}`);   // Ada is 36\nconsole.log(name + \" is \" + age);   // same, but noisier\n```\n\n### Everyday string tools\n```ts\nconst s = \"Poodcode\";\ns.length;            // 8   (a property, not a call)\ns.toUpperCase();     // \"POODCODE\"\ns[0];                // \"P\"\ns.slice(0, 4);       // \"Pood\"   (start, end-exclusive)\ns.includes(\"code\");  // true\ns.indexOf(\"d\");      // 3\n\"a,b,c\".split(\",\");  // [\"a\",\"b\",\"c\"]\n\"  hi  \".trim();     // \"hi\"\n```\n\n### Strings are immutable\nMethods return NEW strings; the original never changes:\n```ts\nlet t = \"hi\";\nt.toUpperCase();      // returns \"HI\" but discards it\nconsole.log(t);       // still \"hi\"\nt = t.toUpperCase();  // keep the result\n```\n\n### Watch out for\n- `length` and `[i]` count UTF-16 units — fine for plain ASCII text.\n- To reverse a string: `[...s].reverse().join(\"\")`.\n",
    "exercises": [
      {
        "id": "ts_strings-template",
        "title": "Greet with a template",
        "prompt": "Replace `____` with a template literal that produces `Hi, <name>!` — for input `Ada`, print `Hi, Ada!`.",
        "hint": "Backticks let you embed ${name} straight into the text.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst name = fs.readFileSync(0, \"utf8\").trim();\nconsole.log(____);\n",
        "solution": "import * as fs from \"fs\";\nconst name = fs.readFileSync(0, \"utf8\").trim();\nconsole.log(`Hi, ${name}!`);\n",
        "tests": [
          {
            "input": "Ada",
            "output": "Hi, Ada!"
          },
          {
            "input": "Bob",
            "output": "Hi, Bob!"
          },
          {
            "input": "Grace Hopper",
            "output": "Hi, Grace Hopper!"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_strings-upper",
        "title": "Shout it",
        "prompt": "Replace `____` to print the input line in upper case.",
        "hint": "String methods are camelCase: s.toUpperCase().",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst s = fs.readFileSync(0, \"utf8\").trim();\nconsole.log(____);\n",
        "solution": "import * as fs from \"fs\";\nconst s = fs.readFileSync(0, \"utf8\").trim();\nconsole.log(s.toUpperCase());\n",
        "tests": [
          {
            "input": "hello",
            "output": "HELLO"
          },
          {
            "input": "MixEd",
            "output": "MIXED"
          },
          {
            "input": "abc def",
            "output": "ABC DEF"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_strings-length",
        "title": "How long?",
        "prompt": "Replace `____` to print the number of characters in `s`. Remember: `length` is a property, not a method call.",
        "hint": "s.length (no parentheses) gives the character count.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst s = fs.readFileSync(0, \"utf8\").trim();\nconsole.log(____);\n",
        "solution": "import * as fs from \"fs\";\nconst s = fs.readFileSync(0, \"utf8\").trim();\nconsole.log(s.length);\n",
        "tests": [
          {
            "input": "hello",
            "output": "5"
          },
          {
            "input": "a",
            "output": "1"
          },
          {
            "input": "Poodcode",
            "output": "8"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_strings-reverse",
        "title": "Reverse a word",
        "prompt": "Write the solution: reverse the input word and print it (`hello` -> `olleh`). Strings are immutable, so build a new one.",
        "hint": "Spread into an array of characters, reverse it, then .join(\"\").",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst s = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst s = fs.readFileSync(0, \"utf8\").trim();\nconst reversed = [...s].reverse().join(\"\");\nconsole.log(reversed);\n",
        "tests": [
          {
            "input": "hello",
            "output": "olleh"
          },
          {
            "input": "abc",
            "output": "cba"
          },
          {
            "input": "racecar",
            "output": "racecar"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_strings-challenge",
        "title": "Initials",
        "prompt": "The input is a name of one or more words. Print the uppercase initials joined together — `grace hopper` → `GH`.",
        "hint": "Split on spaces, take w[0] of each word, uppercase it, then join with \"\".",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Intro",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\nconst words = input.split(/\\s+/);\nconst initials = words.map(w => w[0].toUpperCase()).join(\"\");\nconsole.log(initials);\n",
        "tests": [
          {
            "input": "grace hopper",
            "output": "GH"
          },
          {
            "input": "ada",
            "output": "A"
          },
          {
            "input": "the quick brown fox",
            "output": "TQBF"
          },
          {
            "input": "john von neumann",
            "output": "JVN"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_operators",
    "name": "Operators & Expressions",
    "category": "TS: Language Basics",
    "what": "Arithmetic, comparison, logical operators, plus ?? (nullish) and ?. (optional chaining).",
    "deep": "Expressions combine values with operators. Beyond the usual `+ - * / %` and `< > === !==`, TypeScript has two everyday safety operators: `??` supplies a fallback only when the left side is null/undefined, and `?.` reads a property only if the object exists.",
    "java": "Always compare with `===`/`!==` (strict, no coercion), never `==`. `%` is remainder. `a ?? b` is like `a != null ? a : b`; `obj?.prop` short-circuits to `undefined` instead of throwing an NPE.",
    "language": "typescript",
    "lesson": "### Worked example\n```ts\n7 + 2;      // 9\n7 % 2;      // 1   (remainder — 7 is odd)\n7 / 2;      // 3.5 (real division, no truncation)\nMath.floor(7 / 2); // 3\n\n3 === 3;    // true   (strict equality — ALWAYS use ===)\n3 !== 4;    // true\n2 < 5 && 5 < 9; // true (logical and)\n```\n\n### The two safety operators\n```ts\n// ?? — fallback only when the left side is null OR undefined\nconst port = input ?? 8080;     // 0 and \"\" are kept; only null/undefined trigger the default\n\n// ?. — read a property only if the object exists\nconst city = user?.address?.city;  // undefined instead of a crash\n```\n\n### === vs ==\nAlways use `===` / `!==`. The loose `==` coerces types with surprising results (`0 == \"\"` is `true`). Strict equality has no surprises.\n\n### Watch out for\n- `??` differs from `||`: `0 || 5` is `5`, but `0 ?? 5` is `0`. Use `??` when 0 / \"\" / false are valid values.\n- `%` can be negative for negative inputs: `-7 % 3` is `-1`.\n",
    "exercises": [
      {
        "id": "ts_operators-mod",
        "title": "Remainder",
        "prompt": "Replace `____` to print the remainder of `n` divided by 2 (0 for even, 1 for odd).",
        "hint": "% is the remainder operator.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst n = Number(fs.readFileSync(0, \"utf8\").trim());\nconsole.log(____);\n",
        "solution": "import * as fs from \"fs\";\nconst n = Number(fs.readFileSync(0, \"utf8\").trim());\nconsole.log(n % 2);\n",
        "tests": [
          {
            "input": "7",
            "output": "1"
          },
          {
            "input": "10",
            "output": "0"
          },
          {
            "input": "3",
            "output": "1"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_operators-nullish",
        "title": "Default with ??",
        "prompt": "`value` is `undefined` when the input is empty. Replace `____` so `port` falls back to `\"8080\"` only when `value` is null/undefined.",
        "hint": "a ?? b gives b only when a is null or undefined.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst raw = fs.readFileSync(0, \"utf8\").trim();\nconst value: string | undefined = raw === \"\" ? undefined : raw;\nconst port = ____;\nconsole.log(port);\n",
        "solution": "import * as fs from \"fs\";\nconst raw = fs.readFileSync(0, \"utf8\").trim();\nconst value: string | undefined = raw === \"\" ? undefined : raw;\nconst port = value ?? \"8080\";\nconsole.log(port);\n",
        "tests": [
          {
            "input": "3000",
            "output": "3000"
          },
          {
            "input": "",
            "output": "8080"
          },
          {
            "input": "80",
            "output": "80"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_operators-ternary",
        "title": "Label the sign",
        "prompt": "Replace `____` with a chained ternary that yields `pos` when `n > 0`, `neg` when `n < 0`, and `zero` otherwise.",
        "hint": "cond1 ? a : cond2 ? b : c chains two decisions.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst n = Number(fs.readFileSync(0, \"utf8\").trim());\nconst label = ____;\nconsole.log(label);\n",
        "solution": "import * as fs from \"fs\";\nconst n = Number(fs.readFileSync(0, \"utf8\").trim());\nconst label = n > 0 ? \"pos\" : n < 0 ? \"neg\" : \"zero\";\nconsole.log(label);\n",
        "tests": [
          {
            "input": "5",
            "output": "pos"
          },
          {
            "input": "-3",
            "output": "neg"
          },
          {
            "input": "0",
            "output": "zero"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_operators-challenge",
        "title": "Operator trio",
        "prompt": "The input is two numbers `a b`. Print three lines: the remainder `a % b`, whether `a === b` (a boolean), and whether `a > b` (a boolean).",
        "hint": "A comparison like a === b evaluates to true or false — log it directly.",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Intro",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\nconst [a, b] = input.split(/\\s+/).map(Number);\nconsole.log(a % b);\nconsole.log(a === b);\nconsole.log(a > b);\n",
        "tests": [
          {
            "input": "7 2",
            "output": "1\nfalse\ntrue"
          },
          {
            "input": "4 4",
            "output": "0\ntrue\nfalse"
          },
          {
            "input": "3 10",
            "output": "3\nfalse\nfalse"
          },
          {
            "input": "9 3",
            "output": "0\nfalse\ntrue"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_conditionals",
    "name": "Conditionals & Truthiness",
    "category": "TS: Language Basics",
    "what": "Branching with if/else and switch, and which values count as true or false.",
    "deep": "`if` runs a block when a condition is truthy. Empty string, 0, null, undefined, and NaN are 'falsy'; everything else is 'truthy'. `switch` compares one value against many cases (remember `break`). Ternary `cond ? a : b` is an expression form.",
    "java": "`if`, `else if`, `switch` look like Java, but the condition need not be a boolean — any value is tested for truthiness. `switch` uses `===` and needs `break` between cases.",
    "language": "typescript",
    "lesson": "### Worked example\n```ts\nconst n = 7;\nif (n % 2 === 0) {\n  console.log(\"Even\");\n} else {\n  console.log(\"Odd\");        // prints Odd\n}\n```\n\n### Truthiness\n`if` accepts any value, not just booleans. These are **falsy**: `false`, `0`, `\"\"` (empty string), `null`, `undefined`, `NaN`. Everything else is **truthy**.\n```ts\nif (name) { /* runs when name is a non-empty string */ }\nif (list.length) { /* runs when the array is non-empty */ }\n```\n\n### switch\nCompare one value against many, with `===`. Don't forget `break`:\n```ts\nswitch (grade) {\n  case \"A\": console.log(\"Excellent\"); break;\n  case \"B\": console.log(\"Good\"); break;\n  default:  console.log(\"Keep going\");\n}\n```\n\n### Ternary — a conditional expression\n```ts\nconst label = n % 2 === 0 ? \"Even\" : \"Odd\";\n```\n\n### Watch out for\n- A missing `break` \"falls through\" to the next case — occasionally useful, usually a bug.\n- `if (x = 5)` (assignment) vs `if (x === 5)` (comparison) — use `===`.\n",
    "exercises": [
      {
        "id": "ts_conditionals-evenodd",
        "title": "Even or odd",
        "prompt": "Replace `____` with the condition that is true when `n` is even.",
        "hint": "A number is even when n % 2 === 0.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst n = Number(fs.readFileSync(0, \"utf8\").trim());\nif (____) {\n  console.log(\"Even\");\n} else {\n  console.log(\"Odd\");\n}\n",
        "solution": "import * as fs from \"fs\";\nconst n = Number(fs.readFileSync(0, \"utf8\").trim());\nif (n % 2 === 0) {\n  console.log(\"Even\");\n} else {\n  console.log(\"Odd\");\n}\n",
        "tests": [
          {
            "input": "4",
            "output": "Even"
          },
          {
            "input": "7",
            "output": "Odd"
          },
          {
            "input": "0",
            "output": "Even"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_conditionals-truthy",
        "title": "Non-empty?",
        "prompt": "Replace `____` with a truthy test on `trimmed.length` so the program prints `yes` for non-empty input and `no` for blank input.",
        "hint": "0 is falsy, so an empty string's length of 0 takes the else branch.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst s = fs.readFileSync(0, \"utf8\");\nconst trimmed = s.trim();\nif (____) {\n  console.log(\"yes\");\n} else {\n  console.log(\"no\");\n}\n",
        "solution": "import * as fs from \"fs\";\nconst s = fs.readFileSync(0, \"utf8\");\nconst trimmed = s.trim();\nif (trimmed.length) {\n  console.log(\"yes\");\n} else {\n  console.log(\"no\");\n}\n",
        "tests": [
          {
            "input": "hello",
            "output": "yes"
          },
          {
            "input": "",
            "output": "no"
          },
          {
            "input": "   ",
            "output": "no"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_conditionals-switch",
        "title": "Grade to word",
        "prompt": "Replace `____` with the `switch` header that branches on `grade`.",
        "hint": "switch (grade) { ... } compares grade against each case with ===.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst grade = fs.readFileSync(0, \"utf8\").trim();\n____\n  case \"A\":\n    console.log(\"Excellent\");\n    break;\n  case \"B\":\n    console.log(\"Good\");\n    break;\n  default:\n    console.log(\"Keep going\");\n}\n",
        "solution": "import * as fs from \"fs\";\nconst grade = fs.readFileSync(0, \"utf8\").trim();\nswitch (grade) {\n  case \"A\":\n    console.log(\"Excellent\");\n    break;\n  case \"B\":\n    console.log(\"Good\");\n    break;\n  default:\n    console.log(\"Keep going\");\n}\n",
        "tests": [
          {
            "input": "A",
            "output": "Excellent"
          },
          {
            "input": "B",
            "output": "Good"
          },
          {
            "input": "C",
            "output": "Keep going"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_conditionals-challenge",
        "title": "Letter grade",
        "prompt": "The input is a single score (0–100). Print its letter grade with an if/else ladder: 90+ → A, 80+ → B, 70+ → C, 60+ → D, otherwise F.",
        "hint": "Check the highest threshold first; each `else if` handles the next band down.",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Intro",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\nconst score = Number(input);\nlet grade: string;\nif (score >= 90) grade = \"A\";\nelse if (score >= 80) grade = \"B\";\nelse if (score >= 70) grade = \"C\";\nelse if (score >= 60) grade = \"D\";\nelse grade = \"F\";\nconsole.log(grade);\n",
        "tests": [
          {
            "input": "95",
            "output": "A"
          },
          {
            "input": "82",
            "output": "B"
          },
          {
            "input": "70",
            "output": "C"
          },
          {
            "input": "60",
            "output": "D"
          },
          {
            "input": "40",
            "output": "F"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_loops",
    "name": "Loops",
    "category": "TS: Language Basics",
    "what": "Repeating work with for, for…of, for…in, while and do…while.",
    "deep": "`for (let i…)` counts with an index; `for…of` walks the *values* of an array; `for…in` walks the *keys* of an object. `while` repeats until a condition fails. `break` stops the loop; `continue` skips to the next turn.",
    "java": "`for…of` is Java's enhanced for (`for (x : arr)`). Do NOT use `for…in` on arrays — it yields string keys, not elements. Classic `for (let i = 0; i < n; i++)` still works for index math.",
    "language": "typescript",
    "lesson": "### Worked example\nSum an array three equivalent ways:\n```ts\nconst nums = [10, 20, 30];\n\nlet a = 0;\nfor (let i = 0; i < nums.length; i++) a += nums[i];  // index loop\n\nlet b = 0;\nfor (const x of nums) b += x;                         // for…of: values\n\nconsole.log(a, b);  // 60 60\n```\n\n### Which loop when\n| Loop | Gives you | Use for |\n|------|-----------|---------|\n| `for (let i…)` | an index `i` | index math, counting |\n| `for…of` | each **value** | arrays, strings, Sets |\n| `for…in` | each **key** | object properties |\n| `while` | — | repeat until a condition fails |\n\n### break & continue\n```ts\nfor (const x of nums) {\n  if (x < 0) continue;   // skip negatives\n  if (x > 100) break;    // stop entirely\n  console.log(x);\n}\n```\n\n### Watch out for\n- **Never** use `for…in` on an array — it iterates string keys (\"0\",\"1\",…), not elements. Use `for…of`.\n- Off-by-one: `i < n` visits indexes 0..n-1; `i <= n` overruns.\n",
    "exercises": [
      {
        "id": "ts_loops-sum",
        "title": "Sum with for…of",
        "prompt": "Replace `____` with a `for…of` loop header that walks each value of `nums` as `x`.",
        "hint": "for (const x of nums) gives you each element, not its index.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nlet total = 0;\n____\n  total += x;\n}\nconsole.log(total);\n",
        "solution": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nlet total = 0;\nfor (const x of nums) {\n  total += x;\n}\nconsole.log(total);\n",
        "tests": [
          {
            "input": "1 2 3",
            "output": "6"
          },
          {
            "input": "10 20",
            "output": "30"
          },
          {
            "input": "5",
            "output": "5"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_loops-count",
        "title": "Count evens",
        "prompt": "Replace `____` with the loop condition that keeps `i` a valid index of `nums`.",
        "hint": "Valid indexes run 0 .. nums.length - 1, so the guard is i < nums.length.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nlet count = 0;\nfor (let i = 0; ____; i++) {\n  if (nums[i] % 2 === 0) count++;\n}\nconsole.log(count);\n",
        "solution": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nlet count = 0;\nfor (let i = 0; i < nums.length; i++) {\n  if (nums[i] % 2 === 0) count++;\n}\nconsole.log(count);\n",
        "tests": [
          {
            "input": "1 2 3 4",
            "output": "2"
          },
          {
            "input": "2 4 6",
            "output": "3"
          },
          {
            "input": "1 3 5",
            "output": "0"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_loops-firstover",
        "title": "First over threshold",
        "prompt": "Line 1 is a threshold `t`; line 2 is a list of numbers. Write the solution: print the first number strictly greater than `t`, or `-1` if none. Use `break` to stop early.",
        "hint": "Default the answer to -1, then break as soon as you find one bigger than t.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst lines = fs.readFileSync(0, \"utf8\").trim().split(/\\r?\\n/);\nconst t = Number(lines[0]);\nconst nums = lines[1].split(/\\s+/).map(Number);\n____\n",
        "solution": "import * as fs from \"fs\";\nconst lines = fs.readFileSync(0, \"utf8\").trim().split(/\\r?\\n/);\nconst t = Number(lines[0]);\nconst nums = lines[1].split(/\\s+/).map(Number);\nlet answer = -1;\nfor (const x of nums) {\n  if (x > t) {\n    answer = x;\n    break;\n  }\n}\nconsole.log(answer);\n",
        "tests": [
          {
            "input": "5\n1 3 7 2",
            "output": "7"
          },
          {
            "input": "10\n1 2 3",
            "output": "-1"
          },
          {
            "input": "0\n-1 -2 5",
            "output": "5"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_loops-challenge",
        "title": "FizzBuzz",
        "prompt": "The input is a number `n`. Print the numbers 1..n one per line, but print `Fizz` for multiples of 3, `Buzz` for multiples of 5, and `FizzBuzz` for multiples of both.",
        "hint": "Test the both-case (i % 15 === 0) first, or 15 prints Fizz instead of FizzBuzz.",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Intro",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\nconst n = Number(input);\nfor (let i = 1; i <= n; i++) {\n  if (i % 15 === 0) console.log(\"FizzBuzz\");\n  else if (i % 3 === 0) console.log(\"Fizz\");\n  else if (i % 5 === 0) console.log(\"Buzz\");\n  else console.log(i);\n}\n",
        "tests": [
          {
            "input": "5",
            "output": "1\n2\nFizz\n4\nBuzz"
          },
          {
            "input": "3",
            "output": "1\n2\nFizz"
          },
          {
            "input": "1",
            "output": "1"
          },
          {
            "input": "15",
            "output": "1\n2\nFizz\n4\nBuzz\nFizz\n7\n8\nFizz\nBuzz\n11\nFizz\n13\n14\nFizzBuzz"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_functions",
    "name": "Functions & Arrow Functions",
    "category": "TS: Functions & Types",
    "what": "Declaring reusable logic with function declarations, expressions, and arrow syntax.",
    "deep": "A function packages steps behind a name. You annotate parameter types and (optionally) a return type: `function add(a: number, b: number): number`. Arrow functions `(a, b) => a + b` are a compact form used constantly for callbacks.",
    "java": "Return type goes *after* the parameters. Arrow functions `(x) => x * 2` are like Java lambdas but also usable as top-level functions. A `: void` return means 'returns nothing'.",
    "language": "typescript",
    "lesson": "### Worked example\n```ts\n// declaration — hoisted, named\nfunction add(a: number, b: number): number {\n  return a + b;\n}\n\n// arrow function — compact, great for callbacks\nconst double = (x: number): number => x * 2;\n\nconsole.log(add(2, 3), double(10));  // 5 20\n```\n\n### Anatomy\n- Parameters get types: `(a: number, b: number)`.\n- The return type goes after the `)`: `: number`. It's usually inferred, so you can omit it.\n- A function that returns nothing has return type `void`.\n\n### Arrow function shorthands\n```ts\nconst square = (x: number) => x * x;   // single expression: implicit return\nconst greet = (name: string): void => { console.log(`Hi ${name}`); };\n```\n\n### Functions are values\nYou can pass them around and store them — the foundation of array methods and callbacks:\n```ts\nconst ops = [add, (a: number, b: number) => a - b];\nconsole.log(ops[1](9, 4));  // 5\n```\n\n### Watch out for\n- No implicit return with braces: `(x) => { x * 2 }` returns `undefined`; drop the braces or add `return`.\n",
    "exercises": [
      {
        "id": "ts_functions-add",
        "title": "Write add()",
        "prompt": "Replace `____` with the body of `add` so it returns the sum of its two parameters.",
        "hint": "return x + y;",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst [a, b] = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nfunction add(x: number, y: number): number {\n  ____\n}\nconsole.log(add(a, b));\n",
        "solution": "import * as fs from \"fs\";\nconst [a, b] = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nfunction add(x: number, y: number): number {\n  return x + y;\n}\nconsole.log(add(a, b));\n",
        "tests": [
          {
            "input": "2 3",
            "output": "5"
          },
          {
            "input": "10 -4",
            "output": "6"
          },
          {
            "input": "0 0",
            "output": "0"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_functions-arrow",
        "title": "Double it (arrow)",
        "prompt": "Replace `____` with an arrow function that takes a `number` and returns it doubled.",
        "hint": "(x: number): number => x * 2 — a single expression, so no braces or return.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst n = Number(fs.readFileSync(0, \"utf8\").trim());\nconst double = ____;\nconsole.log(double(n));\n",
        "solution": "import * as fs from \"fs\";\nconst n = Number(fs.readFileSync(0, \"utf8\").trim());\nconst double = (x: number): number => x * 2;\nconsole.log(double(n));\n",
        "tests": [
          {
            "input": "5",
            "output": "10"
          },
          {
            "input": "0",
            "output": "0"
          },
          {
            "input": "-3",
            "output": "-6"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_functions-callback",
        "title": "Apply the chosen op",
        "prompt": "The input is an operator (`+` or `*`) and two numbers. Write the solution: pick the matching function and apply it, printing the result.",
        "hint": "Functions are values — store the chosen one in fn, then call fn(a, b).",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\nconst op = parts[0];\nconst a = Number(parts[1]);\nconst b = Number(parts[2]);\nconst add = (x: number, y: number): number => x + y;\nconst mul = (x: number, y: number): number => x * y;\n____\n",
        "solution": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\nconst op = parts[0];\nconst a = Number(parts[1]);\nconst b = Number(parts[2]);\nconst add = (x: number, y: number): number => x + y;\nconst mul = (x: number, y: number): number => x * y;\nconst fn = op === \"+\" ? add : mul;\nconsole.log(fn(a, b));\n",
        "tests": [
          {
            "input": "+ 3 4",
            "output": "7"
          },
          {
            "input": "* 3 4",
            "output": "12"
          },
          {
            "input": "+ 10 -2",
            "output": "8"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_functions-challenge",
        "title": "Count primes",
        "prompt": "The input is a number `n`. Write an `isPrime` helper function, then count how many integers in 2..n are prime and print the count.",
        "hint": "isPrime(x): false below 2, otherwise no divisor d with d*d <= x divides it.",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Easy",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\nconst n = Number(input);\nfunction isPrime(x: number): boolean {\n  if (x < 2) return false;\n  for (let d = 2; d * d <= x; d++) if (x % d === 0) return false;\n  return true;\n}\nlet count = 0;\nfor (let i = 2; i <= n; i++) if (isPrime(i)) count++;\nconsole.log(count);\n",
        "tests": [
          {
            "input": "10",
            "output": "4"
          },
          {
            "input": "1",
            "output": "0"
          },
          {
            "input": "2",
            "output": "1"
          },
          {
            "input": "20",
            "output": "8"
          },
          {
            "input": "0",
            "output": "0"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_params",
    "name": "Optional, Default & Rest Parameters",
    "category": "TS: Functions & Types",
    "what": "Flexible parameter lists: optional (?), defaults (=), and variadic rest (…).",
    "deep": "A `?` marks a parameter optional (it may be `undefined`). A default `= value` fills in when the argument is omitted. A rest parameter `...nums: number[]` collects any number of trailing arguments into an array.",
    "java": "`...nums: number[]` is Java's varargs `int... nums`. Defaults (`p = 10`) and optionals (`p?`) have no direct Java equivalent — they remove most overloads. Optional params must come after required ones.",
    "language": "typescript",
    "lesson": "### Worked example\n```ts\nfunction greet(name: string, greeting: string = \"Hello\"): string {\n  return `${greeting}, ${name}!`;\n}\ngreet(\"Ada\");            // \"Hello, Ada!\"   (default used)\ngreet(\"Ada\", \"Hi\");      // \"Hi, Ada!\"\n```\n\n### Three flavours\n```ts\nfunction f(required: number, optional?: number, withDefault = 10) { … }\n\nfunction sum(...nums: number[]): number {     // rest: collects the rest into an array\n  return nums.reduce((a, b) => a + b, 0);\n}\nsum(1, 2, 3, 4);  // 10\n```\n\n### Rules of order\nRequired parameters come first, then optional/defaulted ones, then a single rest parameter last:\n```ts\nfunction log(level: string, ...messages: string[]) { … }\n```\n\n### Optional means \"maybe undefined\"\n```ts\nfunction area(w: number, h?: number): number {\n  return w * (h ?? w);   // square when h is omitted\n}\n```\n\n### Watch out for\n- You can't put a required parameter after an optional one.\n- A default value makes the parameter optional automatically — don't add `?` too.\n",
    "exercises": [
      {
        "id": "ts_params-default",
        "title": "A default parameter",
        "prompt": "Replace `____` to declare `greeting` with a default value of `\"Hello\"`, so calling `greet(name)` still works.",
        "hint": "A default is written param: type = value, e.g. greeting: string = \"Hello\".",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst name = fs.readFileSync(0, \"utf8\").trim();\nfunction greet(name: string, ____): string {\n  return `${greeting}, ${name}!`;\n}\nconsole.log(greet(name));\n",
        "solution": "import * as fs from \"fs\";\nconst name = fs.readFileSync(0, \"utf8\").trim();\nfunction greet(name: string, greeting: string = \"Hello\"): string {\n  return `${greeting}, ${name}!`;\n}\nconsole.log(greet(name));\n",
        "tests": [
          {
            "input": "Ada",
            "output": "Hello, Ada!"
          },
          {
            "input": "Bob",
            "output": "Hello, Bob!"
          },
          {
            "input": "Cid",
            "output": "Hello, Cid!"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_params-rest",
        "title": "Sum any count",
        "prompt": "Replace `____` to declare a rest parameter `xs` that collects all the numbers passed in.",
        "hint": "A rest parameter uses ... and collects arguments into an array: ...xs: number[].",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nfunction sum(____): number {\n  return xs.reduce((a, b) => a + b, 0);\n}\nconsole.log(sum(...nums));\n",
        "solution": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nfunction sum(...xs: number[]): number {\n  return xs.reduce((a, b) => a + b, 0);\n}\nconsole.log(sum(...nums));\n",
        "tests": [
          {
            "input": "1 2 3 4",
            "output": "10"
          },
          {
            "input": "5",
            "output": "5"
          },
          {
            "input": "10 20 30",
            "output": "60"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_params-optional",
        "title": "Rectangle or square",
        "prompt": "One or two numbers are given. Write the solution: an `area(w, h?)` function that squares `w` when `h` is omitted, then print the area for the given input.",
        "hint": "Mark h optional with h?, then use h ?? w so a missing h becomes w (a square).",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\n____\n",
        "solution": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nfunction area(w: number, h?: number): number {\n  return w * (h ?? w);\n}\nconst result = parts.length === 2 ? area(parts[0], parts[1]) : area(parts[0]);\nconsole.log(result);\n",
        "tests": [
          {
            "input": "3 4",
            "output": "12"
          },
          {
            "input": "5",
            "output": "25"
          },
          {
            "input": "2 6",
            "output": "12"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_params-challenge",
        "title": "Clamp to a range",
        "prompt": "The input is a line of numbers. Write a `clamp(x, lo = 0, hi = 100)` using default parameters, apply it to every number, and print the clamped values space-separated.",
        "hint": "Call clamp(x) with just one argument — the defaults supply lo and hi.",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Easy",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\nconst nums = input.split(/\\s+/).map(Number);\nfunction clamp(x: number, lo: number = 0, hi: number = 100): number {\n  if (x < lo) return lo;\n  if (x > hi) return hi;\n  return x;\n}\nconsole.log(nums.map(x => clamp(x)).join(\" \"));\n",
        "tests": [
          {
            "input": "50 -5 150 100",
            "output": "50 0 100 100"
          },
          {
            "input": "0",
            "output": "0"
          },
          {
            "input": "-1 -2 -3",
            "output": "0 0 0"
          },
          {
            "input": "200 99",
            "output": "100 99"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_unions",
    "name": "Union & Literal Types",
    "category": "TS: Functions & Types",
    "what": "Types that allow several shapes (A | B) and exact-value literal types.",
    "deep": "A union `string | number` says a value is one of several types. A literal type like `\"N\" | \"S\" | \"E\" | \"W\"` restricts a value to an exact set of strings — a lightweight, type-safe alternative to enums.",
    "java": "There is no Java equivalent to `\"red\" | \"green\"` — it constrains a plain string to a fixed set at compile time. Unions replace many small class hierarchies. You must narrow a union before using type-specific methods.",
    "language": "typescript",
    "lesson": "### Worked example\nA union lets a value be one of several types:\n```ts\nfunction describe(x: string | number): string {\n  if (typeof x === \"number\") return `number ${x}`;\n  return `string of length ${x.length}`;\n}\ndescribe(42);      // \"number 42\"\ndescribe(\"hi\");    // \"string of length 2\"\n```\n\n### Literal types — an exact set of values\n```ts\ntype Direction = \"N\" | \"S\" | \"E\" | \"W\";\nfunction step(d: Direction): string {\n  return d === \"N\" ? \"up\" : d === \"S\" ? \"down\" : \"sideways\";\n}\n```\nNow `step(\"X\")` is a compile error — only the four strings are allowed. This is the type-safe replacement for magic strings and (often) enums.\n\n### Combining them\n```ts\ntype Status = \"loading\" | \"done\" | number;  // strings OR a numeric code\n```\n\n### Watch out for\n- Before calling a method that only one member has, you must **narrow** (see Type Narrowing) — TypeScript won't let you call `.length` on a `string | number` directly.\n- Literal unions are compile-time only; at runtime they're just plain strings/numbers.\n",
    "exercises": [
      {
        "id": "ts_unions-direction",
        "title": "Direction to word",
        "prompt": "`Direction` is a literal union of the four compass letters. Replace `____` with a chained ternary mapping `N/S/E/W` to `north/south/east/west`.",
        "hint": "Chain ternaries: dir === \"N\" ? \"north\" : dir === \"S\" ? ... .",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst d = fs.readFileSync(0, \"utf8\").trim();\ntype Direction = \"N\" | \"S\" | \"E\" | \"W\";\nfunction toWord(dir: Direction): string {\n  return ____;\n}\nconsole.log(toWord(d as Direction));\n",
        "solution": "import * as fs from \"fs\";\nconst d = fs.readFileSync(0, \"utf8\").trim();\ntype Direction = \"N\" | \"S\" | \"E\" | \"W\";\nfunction toWord(dir: Direction): string {\n  return dir === \"N\" ? \"north\" : dir === \"S\" ? \"south\" : dir === \"E\" ? \"east\" : \"west\";\n}\nconsole.log(toWord(d as Direction));\n",
        "tests": [
          {
            "input": "N",
            "output": "north"
          },
          {
            "input": "W",
            "output": "west"
          },
          {
            "input": "S",
            "output": "south"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_unions-strnum",
        "title": "string | number",
        "prompt": "`value` is built as a `string | number` (a number when the input starts with `n`). Replace `____` with the expression that builds it: `Number(parts[1])` for the `n` case, else the raw string.",
        "hint": "Pick the number for the n case, otherwise keep the string.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\nconst value: string | number = ____;\nlet out: number;\nif (typeof value === \"number\") {\n  out = value;\n} else {\n  out = value.length;\n}\nconsole.log(out);\n",
        "solution": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\nconst value: string | number = parts[0] === \"n\" ? Number(parts[1]) : parts[1];\nlet out: number;\nif (typeof value === \"number\") {\n  out = value;\n} else {\n  out = value.length;\n}\nconsole.log(out);\n",
        "tests": [
          {
            "input": "n 42",
            "output": "42"
          },
          {
            "input": "s hello",
            "output": "5"
          },
          {
            "input": "n -3",
            "output": "-3"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_unions-status",
        "title": "Status to message",
        "prompt": "`Status` is `\"ok\" | \"notfound\" | number`. Write the solution: if it is a number print `code <n>`, if `ok` print `all good`, otherwise print `missing`.",
        "hint": "Handle the number member first with typeof, then the two string literals.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst raw = fs.readFileSync(0, \"utf8\").trim();\ntype Status = \"ok\" | \"notfound\" | number;\nconst status: Status = /^-?\\d+$/.test(raw) ? Number(raw) : (raw as \"ok\" | \"notfound\");\nlet message: string;\n____\nconsole.log(message);\n",
        "solution": "import * as fs from \"fs\";\nconst raw = fs.readFileSync(0, \"utf8\").trim();\ntype Status = \"ok\" | \"notfound\" | number;\nconst status: Status = /^-?\\d+$/.test(raw) ? Number(raw) : (raw as \"ok\" | \"notfound\");\nlet message: string;\nif (typeof status === \"number\") {\n  message = `code ${status}`;\n} else if (status === \"ok\") {\n  message = \"all good\";\n} else {\n  message = \"missing\";\n}\nconsole.log(message);\n",
        "tests": [
          {
            "input": "ok",
            "output": "all good"
          },
          {
            "input": "notfound",
            "output": "missing"
          },
          {
            "input": "404",
            "output": "code 404"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_unions-challenge",
        "title": "Robot walk",
        "prompt": "The input is a string of moves drawn from the union `\"N\" | \"S\" | \"E\" | \"W\"`. Start at (0, 0); N/S change y by +1/−1 and E/W change x by +1/−1. Print the final position as `x y`.",
        "hint": "Map each direction letter to a (dx, dy) step, then accumulate x and y.",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Easy",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\ntype Dir = \"N\" | \"S\" | \"E\" | \"W\";\nfunction delta(d: Dir): [number, number] {\n  if (d === \"N\") return [0, 1];\n  if (d === \"S\") return [0, -1];\n  if (d === \"E\") return [1, 0];\n  return [-1, 0];\n}\nlet x = 0, y = 0;\nfor (const ch of input.split(\"\")) {\n  const [dx, dy] = delta(ch as Dir);\n  x += dx;\n  y += dy;\n}\nconsole.log(x + \" \" + y);\n",
        "tests": [
          {
            "input": "N",
            "output": "0 1"
          },
          {
            "input": "NNEE",
            "output": "2 2"
          },
          {
            "input": "NSEW",
            "output": "0 0"
          },
          {
            "input": "EEESSS",
            "output": "3 -3"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_aliases",
    "name": "Type Aliases & Interfaces",
    "category": "TS: Functions & Types",
    "what": "Naming a type with `type`, and describing object shapes with `interface`.",
    "deep": "`type ID = string | number` gives a name to any type. `interface Point { x: number; y: number }` describes the shape an object must have. Both let you write the shape once and reuse it; interfaces are the usual choice for object shapes.",
    "java": "An `interface` here just lists fields an object must have (no implementation, no `implements` needed — shape matching is structural). `type` can name unions and primitives too, which Java interfaces cannot.",
    "language": "typescript",
    "lesson": "### Worked example\n```ts\ntype ID = string | number;          // name any type\ninterface Point { x: number; y: number; }   // describe an object shape\n\nconst p: Point = { x: 3, y: 4 };\nfunction dist(a: Point): number {\n  return Math.sqrt(a.x * a.x + a.y * a.y);\n}\nconsole.log(dist(p));  // 5\n```\n\n### interface vs type\n- Use `interface` for object shapes — it's the convention and can be extended.\n- Use `type` for unions, primitives, tuples, and aliases — things an interface can't name.\n```ts\ninterface User { name: string; age: number; }\ntype Pair = [number, number];\ntype Nullable = string | null;\n```\n\n### Optional & readonly fields\n```ts\ninterface Config {\n  host: string;\n  port?: number;       // may be omitted\n  readonly id: string; // can't be reassigned after creation\n}\n```\n\n### Structural typing\nAn object fits an interface if it *has the right shape* — no `implements` needed:\n```ts\nfunction print(pt: Point) { console.log(`${pt.x},${pt.y}`); }\nprint({ x: 1, y: 2 });   // fine — shape matches\n```\n\n### Watch out for\n- Interfaces describe shape at compile time only; they vanish at runtime.\n",
    "exercises": [
      {
        "id": "ts_aliases-point",
        "title": "Distance from origin",
        "prompt": "`Point` is an interface with `x` and `y`. Replace `____` with the body of `dist` (Pythagoras from the origin).",
        "hint": "Distance from the origin is sqrt(x*x + y*y).",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst [x, y] = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\ninterface Point { x: number; y: number; }\nfunction dist(p: Point): number {\n  ____\n}\nconsole.log(dist({ x, y }));\n",
        "solution": "import * as fs from \"fs\";\nconst [x, y] = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\ninterface Point { x: number; y: number; }\nfunction dist(p: Point): number {\n  return Math.sqrt(p.x * p.x + p.y * p.y);\n}\nconsole.log(dist({ x, y }));\n",
        "tests": [
          {
            "input": "3 4",
            "output": "5"
          },
          {
            "input": "0 0",
            "output": "0"
          },
          {
            "input": "6 8",
            "output": "10"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_aliases-build",
        "title": "Build to the shape",
        "prompt": "`User` requires `name` and `age`. Replace `____` with the object literal that satisfies the interface using the two input tokens.",
        "hint": "age must be a number, so wrap parts[1] in Number(...).",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\ninterface User { name: string; age: number; }\nconst u: User = ____;\nconsole.log(`${u.name} is ${u.age}`);\n",
        "solution": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\ninterface User { name: string; age: number; }\nconst u: User = { name: parts[0], age: Number(parts[1]) };\nconsole.log(`${u.name} is ${u.age}`);\n",
        "tests": [
          {
            "input": "Ada 36",
            "output": "Ada is 36"
          },
          {
            "input": "Bob 40",
            "output": "Bob is 40"
          },
          {
            "input": "Cid 7",
            "output": "Cid is 7"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_aliases-points",
        "title": "Sum of point coordinates",
        "prompt": "The input is pairs of numbers. Write the solution: build a `Point[]` (each `{x, y}`) and print the sum of every coordinate.",
        "hint": "Step by 2 to read each x,y pair; then add up x + y for every point.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\ntype Point = { x: number; y: number };\n____\n",
        "solution": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\ntype Point = { x: number; y: number };\nconst points: Point[] = [];\nfor (let i = 0; i < nums.length; i += 2) {\n  points.push({ x: nums[i], y: nums[i + 1] });\n}\nlet total = 0;\nfor (const p of points) {\n  total += p.x + p.y;\n}\nconsole.log(total);\n",
        "tests": [
          {
            "input": "1 2 3 4",
            "output": "10"
          },
          {
            "input": "0 0 5 5",
            "output": "10"
          },
          {
            "input": "2 3",
            "output": "5"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_aliases-challenge",
        "title": "Rectangle areas",
        "prompt": "Each input line is `w h` for one rectangle. Define an interface `Rect`, build a typed array of them, print each area on its own line, then a final line `total <sum-of-areas>`.",
        "hint": "Parse each line into a { w, h } object, then loop to print areas and sum them.",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Easy",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\ninterface Rect { w: number; h: number; }\nconst rects: Rect[] = input.split(\"\\n\").map(line => {\n  const [w, h] = line.trim().split(/\\s+/).map(Number);\n  return { w, h };\n});\nlet total = 0;\nfor (const r of rects) {\n  const area = r.w * r.h;\n  total += area;\n  console.log(area);\n}\nconsole.log(\"total \" + total);\n",
        "tests": [
          {
            "input": "3 4",
            "output": "12\ntotal 12"
          },
          {
            "input": "2 2\n5 5",
            "output": "4\n25\ntotal 29"
          },
          {
            "input": "1 1\n1 1\n1 1",
            "output": "1\n1\n1\ntotal 3"
          },
          {
            "input": "10 2",
            "output": "20\ntotal 20"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_narrowing",
    "name": "Type Narrowing",
    "category": "TS: Functions & Types",
    "what": "Convincing TypeScript (and yourself) which specific type a value has right now.",
    "deep": "When a value could be several types, you narrow it with a runtime check: `typeof x === \"number\"`, `\"key\" in obj`, a truthiness test, or `Array.isArray(x)`. Inside the checked branch TypeScript knows the exact type — and so does your logic.",
    "java": "`typeof x === \"string\"` plays the role of `instanceof` for primitives. Narrowing is how you safely 'unpack' a union — the runtime check and the compile-time type refinement happen together.",
    "language": "typescript",
    "lesson": "### Worked example\nA union isn't usable until you prove which type it is *right now*:\n```ts\nfunction len(x: string | string[]): number {\n  if (typeof x === \"string\") {\n    return x.length;        // here x is a string\n  }\n  return x.length;          // here x is string[]\n}\n```\n\n### The narrowing toolkit\n| Check | Narrows to |\n|-------|-----------|\n| `typeof x === \"number\"` | primitive types |\n| `Array.isArray(x)` | arrays |\n| `\"key\" in obj` | objects that have that key |\n| `if (x)` / `if (x != null)` | rules out falsy / null+undefined |\n| `x === \"N\"` | a specific literal |\n\n### Discriminated unions (a taste)\nGive each shape a common tag, then switch on it:\n```ts\ntype Shape =\n  | { kind: \"circle\"; r: number }\n  | { kind: \"square\"; side: number };\n\nfunction area(s: Shape): number {\n  if (s.kind === \"circle\") return Math.PI * s.r * s.r;\n  return s.side * s.side;\n}\n```\n\n### Watch out for\n- After narrowing inside an `if`, the refinement only holds *inside* that branch.\n- `typeof null` is `\"object\"` — guard null with `x != null`, not `typeof`.\n",
    "exercises": [
      {
        "id": "ts_narrowing-typeof",
        "title": "Narrow with typeof",
        "prompt": "`value` is `string | number`. Replace `____` with the `typeof` check that is true when it is a number.",
        "hint": "typeof value === \"number\" lets you safely do value * 2 inside the branch.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\nconst value: string | number = parts[0] === \"num\" ? Number(parts[1]) : parts[1];\nlet result: string;\nif (____) {\n  result = `double ${value * 2}`;\n} else {\n  result = `chars ${value.length}`;\n}\nconsole.log(result);\n",
        "solution": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\nconst value: string | number = parts[0] === \"num\" ? Number(parts[1]) : parts[1];\nlet result: string;\nif (typeof value === \"number\") {\n  result = `double ${value * 2}`;\n} else {\n  result = `chars ${value.length}`;\n}\nconsole.log(result);\n",
        "tests": [
          {
            "input": "num 5",
            "output": "double 10"
          },
          {
            "input": "str hello",
            "output": "chars 5"
          },
          {
            "input": "num -3",
            "output": "double -6"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_narrowing-in",
        "title": "Narrow with in",
        "prompt": "`shape` has either a `radius` or a `side`. Replace `____` with the `in` check that is true when the object has a `radius`.",
        "hint": "\"radius\" in shape is true only for the circle variant.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\ntype Shape = { radius: number } | { side: number };\nconst shape: Shape = parts[0] === \"circle\" ? { radius: Number(parts[1]) } : { side: Number(parts[1]) };\nlet out: number;\nif (____) {\n  out = shape.radius * 2;\n} else {\n  out = shape.side * 4;\n}\nconsole.log(out);\n",
        "solution": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\ntype Shape = { radius: number } | { side: number };\nconst shape: Shape = parts[0] === \"circle\" ? { radius: Number(parts[1]) } : { side: Number(parts[1]) };\nlet out: number;\nif (\"radius\" in shape) {\n  out = shape.radius * 2;\n} else {\n  out = shape.side * 4;\n}\nconsole.log(out);\n",
        "tests": [
          {
            "input": "circle 3",
            "output": "6"
          },
          {
            "input": "square 5",
            "output": "20"
          },
          {
            "input": "circle 10",
            "output": "20"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_narrowing-discriminated",
        "title": "Area by kind",
        "prompt": "`Shape` is a discriminated union tagged by `kind`. Write the solution: compute a circle's area as `3*r*r` and a square's as `side*side`, then print it.",
        "hint": "Switch on s.kind; inside each branch TypeScript knows which fields exist.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\ntype Shape =\n  | { kind: \"circle\"; r: number }\n  | { kind: \"square\"; side: number };\nconst s: Shape = parts[0] === \"circle\"\n  ? { kind: \"circle\", r: Number(parts[1]) }\n  : { kind: \"square\", side: Number(parts[1]) };\nlet area: number;\n____\nconsole.log(area);\n",
        "solution": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\ntype Shape =\n  | { kind: \"circle\"; r: number }\n  | { kind: \"square\"; side: number };\nconst s: Shape = parts[0] === \"circle\"\n  ? { kind: \"circle\", r: Number(parts[1]) }\n  : { kind: \"square\", side: Number(parts[1]) };\nlet area: number;\nif (s.kind === \"circle\") {\n  area = 3 * s.r * s.r;\n} else {\n  area = s.side * s.side;\n}\nconsole.log(area);\n",
        "tests": [
          {
            "input": "circle 2",
            "output": "12"
          },
          {
            "input": "square 5",
            "output": "25"
          },
          {
            "input": "circle 10",
            "output": "300"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_narrowing-challenge",
        "title": "Numbers vs words",
        "prompt": "The input is a line of space-separated tokens, mixing numbers and words. Sum the tokens that are numeric and count the ones that are not. Print `<sum> <wordCount>`.",
        "hint": "Number(t) is NaN for a non-numeric token — that check narrows it to a word.",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Easy",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\nconst tokens = input.split(/\\s+/);\nlet sum = 0;\nlet words = 0;\nfor (const t of tokens) {\n  if (t === \"\") continue;\n  const n = Number(t);\n  if (!Number.isNaN(n)) sum += n;\n  else words++;\n}\nconsole.log(sum + \" \" + words);\n",
        "tests": [
          {
            "input": "1 cat 2 dog 3",
            "output": "6 2"
          },
          {
            "input": "hello world",
            "output": "0 2"
          },
          {
            "input": "10 20 30",
            "output": "60 0"
          },
          {
            "input": "5 5 5 x",
            "output": "15 1"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_arrays",
    "name": "Arrays & Tuples",
    "category": "TS: Data Structures",
    "what": "Ordered, growable lists (number[]) and fixed-shape tuples ([number, string]).",
    "deep": "An array `number[]` is an ordered, resizable list — push/pop at the end, index with `arr[i]`, length via `arr.length`. A tuple `[number, string]` is a fixed-length array where each position has its own type.",
    "java": "Arrays here are dynamic like `ArrayList` — `.push()` grows them, no fixed size. `number[]` and `Array<number>` are the same type. Tuples `[a, b]` have no Java analogue; they type a fixed-position pair.",
    "language": "typescript",
    "lesson": "### Worked example\n```ts\nconst nums: number[] = [10, 20, 30];\nnums.push(40);           // add to end -> [10,20,30,40]\nnums[0];                 // 10\nnums.length;             // 4\nconst last = nums.pop(); // remove & return end -> 40\n```\n\n### Making & reshaping\n```ts\nconst zeros: number[] = new Array(3).fill(0);   // [0,0,0]\nconst copy = [...nums];                          // shallow copy (spread)\nconst joined = nums.join(\"-\");                   // \"10-20-30\"\nconst part = nums.slice(1, 3);                   // [20,30] (end-exclusive)\n```\n\n### Tuples — fixed shape, per-position types\n```ts\nconst point: [number, number] = [3, 4];\nconst entry: [string, number] = [\"age\", 36];\nconst [x, y] = point;   // destructuring -> x=3, y=4\n```\n\n### Watch out for\n- Reading past the end gives `undefined`, not an error: `nums[99]` is `undefined`.\n- `number[]` and `Array<number>` are the same type — pick one style.\n- Arrays are reference values: `const b = a` shares the same array; copy with `[...a]`.\n",
    "exercises": [
      {
        "id": "ts_arrays-push",
        "title": "Collect and count",
        "prompt": "Replace `____` to push each parsed number onto `nums`, so its length is printed.",
        "hint": "arr.push(x) appends x to the end.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst raw = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\nconst nums: number[] = [];\nfor (const r of raw) {\n  ____\n}\nconsole.log(nums.length);\n",
        "solution": "import * as fs from \"fs\";\nconst raw = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\nconst nums: number[] = [];\nfor (const r of raw) {\n  nums.push(Number(r));\n}\nconsole.log(nums.length);\n",
        "tests": [
          {
            "input": "1 2 3",
            "output": "3"
          },
          {
            "input": "5",
            "output": "1"
          },
          {
            "input": "9 8 7 6",
            "output": "4"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_arrays-last",
        "title": "Last element",
        "prompt": "Replace `____` with the expression for the last element of `nums`.",
        "hint": "The last index is length - 1.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nconst last = ____;\nconsole.log(last);\n",
        "solution": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nconst last = nums[nums.length - 1];\nconsole.log(last);\n",
        "tests": [
          {
            "input": "1 2 3",
            "output": "3"
          },
          {
            "input": "42",
            "output": "42"
          },
          {
            "input": "5 9",
            "output": "9"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_arrays-tuple",
        "title": "Swap a tuple",
        "prompt": "`pair` is a `[number, number]` tuple. Replace `____` with the swapped tuple `[pair[1], pair[0]]`.",
        "hint": "A tuple is just a fixed-length array; put the second element first.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst [a, b] = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nconst pair: [number, number] = [a, b];\nconst swapped: [number, number] = ____;\nconsole.log(swapped.join(\" \"));\n",
        "solution": "import * as fs from \"fs\";\nconst [a, b] = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nconst pair: [number, number] = [a, b];\nconst swapped: [number, number] = [pair[1], pair[0]];\nconsole.log(swapped.join(\" \"));\n",
        "tests": [
          {
            "input": "3 4",
            "output": "4 3"
          },
          {
            "input": "1 2",
            "output": "2 1"
          },
          {
            "input": "7 7",
            "output": "7 7"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_arrays-middle",
        "title": "Drop the ends",
        "prompt": "Write the solution: print all elements except the first and last, space-separated. (Input always has at least three numbers.)",
        "hint": "slice(1, length - 1) keeps everything between the ends (end index is exclusive).",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\n____\n",
        "solution": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nconst middle = nums.slice(1, nums.length - 1);\nconsole.log(middle.join(\" \"));\n",
        "tests": [
          {
            "input": "1 2 3 4 5",
            "output": "2 3 4"
          },
          {
            "input": "10 20 30",
            "output": "20"
          },
          {
            "input": "5 6 7 8",
            "output": "6 7"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_arrays-challenge",
        "title": "Rotate right by k",
        "prompt": "Line 1 is a list of numbers; line 2 is `k`. Rotate the array right by k positions (wrap around) and print it space-separated. `1 2 3 4 5` with k=2 → `4 5 1 2 3`.",
        "hint": "Take the last k with slice(len-k), then the rest, and spread them into one array.",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Easy",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\nconst lines = input.split(\"\\n\");\nconst arr = lines[0].trim().split(/\\s+/).map(Number);\nconst k = Number(lines[1]) % arr.length;\nconst rotated = [...arr.slice(arr.length - k), ...arr.slice(0, arr.length - k)];\nconsole.log(rotated.join(\" \"));\n",
        "tests": [
          {
            "input": "1 2 3 4 5\n2",
            "output": "4 5 1 2 3"
          },
          {
            "input": "1 2 3\n0",
            "output": "1 2 3"
          },
          {
            "input": "1 2 3\n3",
            "output": "1 2 3"
          },
          {
            "input": "1 2\n1",
            "output": "2 1"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_array_methods",
    "name": "Array Methods",
    "category": "TS: Data Structures",
    "what": "Transforming lists with map, filter, reduce, find, some/every and sort.",
    "deep": "Array methods express loops declaratively: `map` transforms every element, `filter` keeps the ones matching a test, `reduce` folds a list to one value, `find` returns the first match, `some`/`every` answer yes/no. Each takes a callback.",
    "java": "These are the Stream operations without the `.stream()`/`.collect()` ceremony — called straight on the array and returning a new array. Note `sort` mutates in place and sorts *as strings* by default; pass `(a, b) => a - b` for numbers.",
    "language": "typescript",
    "lesson": "### Worked example\n```ts\nconst nums = [1, 2, 3, 4, 5];\n\nnums.map(x => x * x);            // [1,4,9,16,25]   transform each\nnums.filter(x => x % 2 === 0);  // [2,4]            keep matches\nnums.reduce((a, x) => a + x, 0);// 15               fold to one value\nnums.find(x => x > 3);          // 4                first match (or undefined)\nnums.some(x => x > 4);          // true\nnums.every(x => x > 0);         // true\n```\n\n### Chaining\nEach method returns a new value, so they compose:\n```ts\nconst total = nums\n  .filter(x => x % 2 === 1)   // [1,3,5]\n  .map(x => x * 10)           // [10,30,50]\n  .reduce((a, x) => a + x, 0);// 90\n```\n\n### reduce, demystified\n`reduce(fn, start)` carries an accumulator: start with `start`, then for each element compute `fn(acc, element)`.\n```ts\nconst max = nums.reduce((m, x) => (x > m ? x : m), nums[0]);  // 5\n```\n\n### Watch out for\n- `sort()` sorts **as strings** by default: `[10,2,1].sort()` -> `[1,10,2]`. For numbers pass `(a, b) => a - b`.\n- `sort` and `reverse` **mutate** the array in place; copy first (`[...a].sort(...)`) if you need the original.\n- `map`/`filter` return new arrays; `forEach` returns nothing (use it only for side effects).\n",
    "exercises": [
      {
        "id": "ts_array_methods-map",
        "title": "Square each",
        "prompt": "Replace `____` with a `map` call that squares every element.",
        "hint": "map transforms each element: nums.map(x => x * x).",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nconst squares = ____;\nconsole.log(squares.join(\" \"));\n",
        "solution": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nconst squares = nums.map(x => x * x);\nconsole.log(squares.join(\" \"));\n",
        "tests": [
          {
            "input": "1 2 3",
            "output": "1 4 9"
          },
          {
            "input": "5",
            "output": "25"
          },
          {
            "input": "2 4",
            "output": "4 16"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_array_methods-filter",
        "title": "Keep evens",
        "prompt": "Replace `____` with a `filter` call that keeps only even numbers.",
        "hint": "filter keeps elements whose callback returns true.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nconst evens = ____;\nconsole.log(evens.join(\" \"));\n",
        "solution": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nconst evens = nums.filter(x => x % 2 === 0);\nconsole.log(evens.join(\" \"));\n",
        "tests": [
          {
            "input": "2 4 6",
            "output": "2 4 6"
          },
          {
            "input": "1 2 3 4",
            "output": "2 4"
          },
          {
            "input": "10 15 20",
            "output": "10 20"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_array_methods-reduce",
        "title": "Sum with reduce",
        "prompt": "Replace `____` with a `reduce` call that sums the array, starting from 0.",
        "hint": "reduce((acc, x) => acc + x, 0) folds the list to a single sum.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nconst total = ____;\nconsole.log(total);\n",
        "solution": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nconst total = nums.reduce((a, x) => a + x, 0);\nconsole.log(total);\n",
        "tests": [
          {
            "input": "1 2 3",
            "output": "6"
          },
          {
            "input": "10 20",
            "output": "30"
          },
          {
            "input": "5",
            "output": "5"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_array_methods-pipeline",
        "title": "Sum of squared odds",
        "prompt": "Write the solution: keep the odd numbers, square them, and sum the result — using a `filter`/`map`/`reduce` chain.",
        "hint": "Chain the three methods; each returns a new array for the next.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\n____\n",
        "solution": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nconst answer = nums\n  .filter(x => x % 2 === 1)\n  .map(x => x * x)\n  .reduce((a, x) => a + x, 0);\nconsole.log(answer);\n",
        "tests": [
          {
            "input": "1 2 3 4",
            "output": "10"
          },
          {
            "input": "2 4 6",
            "output": "0"
          },
          {
            "input": "1 3 5",
            "output": "35"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_array_methods-challenge",
        "title": "Number stats",
        "prompt": "The input is a line of numbers. Using array methods (no manual index loops), print the sum on line 1, the count of even numbers on line 2, and the maximum on line 3.",
        "hint": "reduce folds to the sum/max; filter(...).length counts the evens.",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Easy",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\nconst nums = input.split(/\\s+/).map(Number);\nconst sum = nums.reduce((a, x) => a + x, 0);\nconst evens = nums.filter(x => x % 2 === 0).length;\nconst max = nums.reduce((m, x) => (x > m ? x : m), nums[0]);\nconsole.log(sum);\nconsole.log(evens);\nconsole.log(max);\n",
        "tests": [
          {
            "input": "1 2 3 4 5",
            "output": "15\n2\n5"
          },
          {
            "input": "2 4 6",
            "output": "12\n3\n6"
          },
          {
            "input": "-1 -2 -3",
            "output": "-6\n1\n-1"
          },
          {
            "input": "7",
            "output": "7\n0\n7"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_objects",
    "name": "Objects",
    "category": "TS: Data Structures",
    "what": "Key–value records with typed fields, optional properties, and index signatures.",
    "deep": "An object groups named fields: `{ name: \"Ada\", age: 36 }`. Fields can be optional (`age?: number`), read-only (`readonly id`), and an index signature `{ [k: string]: number }` types a map-like bag of arbitrary keys.",
    "java": "Object literals are like anonymous POJOs typed by shape. Access with `obj.field` or `obj[\"field\"]`. `Object.keys(o)`, `Object.values(o)`, `Object.entries(o)` iterate them — handy when using an object as a small map.",
    "language": "typescript",
    "lesson": "### Worked example\n```ts\ninterface User { name: string; age: number; admin?: boolean; }\n\nconst u: User = { name: \"Ada\", age: 36 };\nconsole.log(u.name);      // Ada\nu.age = 37;               // update a field\nconsole.log(u.admin);     // undefined (optional, absent)\n```\n\n### Access & iterate\n```ts\nu[\"name\"];                    // bracket access\nObject.keys(u);               // [\"name\",\"age\"]\nObject.values(u);             // [\"Ada\", 37]\nfor (const [k, v] of Object.entries(u)) {\n  console.log(`${k}=${v}`);\n}\n```\n\n### Index signatures — object as a bag of keys\n```ts\nconst counts: { [word: string]: number } = {};\ncounts[\"hi\"] = (counts[\"hi\"] ?? 0) + 1;\n```\n\n### Copy & merge with spread\n```ts\nconst patched = { ...u, age: 40 };   // new object, age overridden\n```\n\n### Watch out for\n- Objects are references: `const b = a` shares the same object; spread `{...a}` for a shallow copy.\n- Reading a missing key gives `undefined` — combine with `??` to supply a default.\n",
    "exercises": [
      {
        "id": "ts_objects-access",
        "title": "Read a field",
        "prompt": "Replace `____` to print the `name` field of `user`.",
        "hint": "Access object fields with a dot: user.name.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\nconst user = { name: parts[0], age: Number(parts[1]) };\nconsole.log(____);\n",
        "solution": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\nconst user = { name: parts[0], age: Number(parts[1]) };\nconsole.log(user.name);\n",
        "tests": [
          {
            "input": "Ada 36",
            "output": "Ada"
          },
          {
            "input": "Bob 40",
            "output": "Bob"
          },
          {
            "input": "Cid 7",
            "output": "Cid"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_objects-count",
        "title": "Count with an index signature",
        "prompt": "`counts` maps each word to how many times it appears. Replace `____` with the line that increments the count for `w` (defaulting a missing count to 0).",
        "hint": "counts[w] = (counts[w] ?? 0) + 1 — the ?? handles the first time you see w.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst words = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\nconst counts: { [w: string]: number } = {};\nfor (const w of words) {\n  ____\n}\nconsole.log(counts[words[0]]);\n",
        "solution": "import * as fs from \"fs\";\nconst words = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\nconst counts: { [w: string]: number } = {};\nfor (const w of words) {\n  counts[w] = (counts[w] ?? 0) + 1;\n}\nconsole.log(counts[words[0]]);\n",
        "tests": [
          {
            "input": "a b a c a",
            "output": "3"
          },
          {
            "input": "hi hi",
            "output": "2"
          },
          {
            "input": "x y z",
            "output": "1"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_objects-entries",
        "title": "Sum object values",
        "prompt": "The input is `key value` pairs. Write the solution: build an object from them, then sum all its values with `Object.entries`.",
        "hint": "Object.entries(obj) yields [key, value] pairs you can destructure.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\nconst obj: { [k: string]: number } = {};\nfor (let i = 0; i < parts.length; i += 2) {\n  obj[parts[i]] = Number(parts[i + 1]);\n}\n____\n",
        "solution": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\nconst obj: { [k: string]: number } = {};\nfor (let i = 0; i < parts.length; i += 2) {\n  obj[parts[i]] = Number(parts[i + 1]);\n}\nlet total = 0;\nfor (const [k, v] of Object.entries(obj)) {\n  total += v;\n}\nconsole.log(total);\n",
        "tests": [
          {
            "input": "a 1 b 2 c 3",
            "output": "6"
          },
          {
            "input": "x 10 y 20",
            "output": "30"
          },
          {
            "input": "solo 5",
            "output": "5"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_objects-challenge",
        "title": "Most common word",
        "prompt": "The input is a line of words. Count them using an object with an index signature, then print the most frequent word and its count as `<word> <count>` (on a tie, the word that reached the top count first).",
        "hint": "Tally into counts[w], then scan the words keeping the first that beats the max.",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Easy",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\nconst words = input.split(/\\s+/).filter(w => w !== \"\");\nconst counts: { [w: string]: number } = {};\nfor (const w of words) counts[w] = (counts[w] ?? 0) + 1;\nlet best = words[0];\nlet bestCount = 0;\nfor (const w of words) {\n  if (counts[w] > bestCount) {\n    best = w;\n    bestCount = counts[w];\n  }\n}\nconsole.log(best + \" \" + bestCount);\n",
        "tests": [
          {
            "input": "a b a c a",
            "output": "a 3"
          },
          {
            "input": "x y y",
            "output": "y 2"
          },
          {
            "input": "one",
            "output": "one 1"
          },
          {
            "input": "cat dog cat dog",
            "output": "cat 2"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_maps_sets",
    "name": "Map & Set",
    "category": "TS: Data Structures",
    "what": "Real hash structures: Map for key→value, Set for unique membership.",
    "deep": "`Map<K, V>` stores key→value pairs with any key type and preserves insertion order (`.set`, `.get`, `.has`, `.size`). `Set<T>` stores unique values (`.add`, `.has`). Both beat plain objects when keys aren't strings or order/size matters.",
    "java": "`Map`/`Set` mirror `HashMap`/`HashSet` (well, `LinkedHashMap` — order is kept). Iterate a Map with `for (const [k, v] of map)`. Use `map.get(k) ?? 0` for the getOrDefault pattern when counting.",
    "language": "typescript",
    "lesson": "### Worked example\n```ts\nconst ages = new Map<string, number>();\nages.set(\"Ada\", 36);\nages.set(\"Bob\", 40);\nages.get(\"Ada\");     // 36\nages.has(\"Cid\");     // false\nages.size;           // 2\n\nfor (const [name, age] of ages) {\n  console.log(`${name}:${age}`);\n}\n```\n\n### Set — unique values\n```ts\nconst seen = new Set<number>();\nseen.add(1); seen.add(1); seen.add(2);\nseen.has(1);         // true\nseen.size;           // 2\nconst unique = [...new Set([3, 3, 1, 3])];  // [3,1]\n```\n\n### Counting pattern (very common)\n```ts\nconst freq = new Map<string, number>();\nfor (const w of words) {\n  freq.set(w, (freq.get(w) ?? 0) + 1);\n}\n```\n\n### Map vs plain object\nPrefer `Map` when: keys aren't strings, you add/remove a lot, or you need `.size` and reliable iteration order. Prefer a plain object for fixed, string-keyed records.\n\n### Watch out for\n- `map.get(k)` returns `V | undefined` — handle the miss (often with `?? 0`).\n- A `Set` uses `===` for equality, so two different objects with the same fields count as distinct.\n",
    "exercises": [
      {
        "id": "ts_maps_sets-set",
        "title": "Count distinct",
        "prompt": "Replace `____` to add each value to the `Set`, so its `size` (the distinct count) is printed.",
        "hint": "A Set ignores duplicates, so seen.add(x) then seen.size gives the distinct count.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nconst seen = new Set<number>();\nfor (const x of nums) {\n  ____\n}\nconsole.log(seen.size);\n",
        "solution": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nconst seen = new Set<number>();\nfor (const x of nums) {\n  seen.add(x);\n}\nconsole.log(seen.size);\n",
        "tests": [
          {
            "input": "1 2 2 3",
            "output": "3"
          },
          {
            "input": "5 5 5",
            "output": "1"
          },
          {
            "input": "1 2 3 4",
            "output": "4"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_maps_sets-map",
        "title": "Highest frequency",
        "prompt": "`freq` counts each word. Replace `____` with the line that increments `w`'s count in the `Map` (defaulting a miss to 0).",
        "hint": "freq.set(w, (freq.get(w) ?? 0) + 1) is the Map counting pattern.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst words = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\nconst freq = new Map<string, number>();\nfor (const w of words) {\n  ____\n}\nlet best = 0;\nfor (const c of freq.values()) {\n  if (c > best) best = c;\n}\nconsole.log(best);\n",
        "solution": "import * as fs from \"fs\";\nconst words = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\nconst freq = new Map<string, number>();\nfor (const w of words) {\n  freq.set(w, (freq.get(w) ?? 0) + 1);\n}\nlet best = 0;\nfor (const c of freq.values()) {\n  if (c > best) best = c;\n}\nconsole.log(best);\n",
        "tests": [
          {
            "input": "a b a a",
            "output": "3"
          },
          {
            "input": "x y z",
            "output": "1"
          },
          {
            "input": "hi hi bye",
            "output": "2"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_maps_sets-firstdup",
        "title": "First repeated value",
        "prompt": "Write the solution: print the first value that appears a second time, or `-1` if every value is unique. Use a `Set` to remember what you've seen.",
        "hint": "Check seen.has(x) BEFORE adding x; the first hit is your answer.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\n____\n",
        "solution": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nconst seen = new Set<number>();\nlet answer = -1;\nfor (const x of nums) {\n  if (seen.has(x)) {\n    answer = x;\n    break;\n  }\n  seen.add(x);\n}\nconsole.log(answer);\n",
        "tests": [
          {
            "input": "1 2 3 2 1",
            "output": "2"
          },
          {
            "input": "1 2 3",
            "output": "-1"
          },
          {
            "input": "5 5",
            "output": "5"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_maps_sets-challenge",
        "title": "First unique number",
        "prompt": "The input is a line of numbers. Using a Map to count occurrences, print the first number that appears exactly once. If every number repeats, print `none`.",
        "hint": "Count first, then scan in original order for the first value with count === 1.",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Easy",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\nconst nums = input.split(/\\s+/).map(Number);\nconst freq = new Map<number, number>();\nfor (const n of nums) freq.set(n, (freq.get(n) ?? 0) + 1);\nlet ans = \"none\";\nfor (const n of nums) {\n  if (freq.get(n) === 1) {\n    ans = String(n);\n    break;\n  }\n}\nconsole.log(ans);\n",
        "tests": [
          {
            "input": "1 2 2 3 3",
            "output": "1"
          },
          {
            "input": "4 4 5",
            "output": "5"
          },
          {
            "input": "1 1 2 2",
            "output": "none"
          },
          {
            "input": "9",
            "output": "9"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_enums",
    "name": "Enums & Literal Unions",
    "category": "TS: Data Structures",
    "what": "Fixed sets of named constants — via enum, or the more common literal-union pattern.",
    "deep": "An `enum Dir { N, S, E, W }` names a fixed set of options. In modern TypeScript the lighter-weight pattern is a literal union `type Dir = \"N\" | \"S\" | \"E\" | \"W\"` plus plain string values — no runtime cost and easier to serialize.",
    "java": "`enum` looks like Java's, but many codebases prefer the union-of-string-literals form instead. (Note: bare `enum` needs a compile step; the string-union pattern is what runs in a type-stripping runtime, so the drills use that.)",
    "language": "typescript",
    "lesson": "### Worked example — the pattern that runs everywhere\nThe modern, lightweight way to model a fixed set is a **literal union** plus plain values:\n```ts\ntype Direction = \"N\" | \"E\" | \"S\" | \"W\";\n\nfunction turnRight(d: Direction): Direction {\n  const order: Direction[] = [\"N\", \"E\", \"S\", \"W\"];\n  return order[(order.indexOf(d) + 1) % 4];\n}\nconsole.log(turnRight(\"N\"));  // E\n```\nNo runtime object is created, the values serialize as themselves, and invalid strings are compile errors.\n\n### The classic enum\nTypeScript also has a dedicated `enum`:\n```ts\nenum Direction { N, E, S, W }   // N=0, E=1, S=2, W=3\nenum Color { Red = \"RED\", Green = \"GREEN\" }\n```\nIt creates a real runtime object mapping names to values. It's fine, but many teams prefer the union pattern because it's simpler and lighter.\n\n> Note: a bare `enum` needs a compile/transform step, so it won't run in a pure type-stripping runtime. The drills below use the string-union pattern, which runs everywhere.\n\n### const objects as enums\n```ts\nconst Status = { Loading: \"loading\", Done: \"done\" } as const;\ntype Status = typeof Status[keyof typeof Status];   // \"loading\" | \"done\"\n```\n\n### Watch out for\n- Prefer the union pattern unless you specifically need reverse mappings.\n",
    "exercises": [
      {
        "id": "ts_enums-next",
        "title": "Next direction",
        "prompt": "`order` lists the compass directions clockwise. Replace `____` with the expression for the next direction after `idx` (wrapping around with `% 4`).",
        "hint": "(idx + 1) % 4 wraps from the last direction back to the first.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst d = fs.readFileSync(0, \"utf8\").trim();\ntype Direction = \"N\" | \"E\" | \"S\" | \"W\";\nconst order: Direction[] = [\"N\", \"E\", \"S\", \"W\"];\nconst idx = order.indexOf(d as Direction);\nconst next = ____;\nconsole.log(next);\n",
        "solution": "import * as fs from \"fs\";\nconst d = fs.readFileSync(0, \"utf8\").trim();\ntype Direction = \"N\" | \"E\" | \"S\" | \"W\";\nconst order: Direction[] = [\"N\", \"E\", \"S\", \"W\"];\nconst idx = order.indexOf(d as Direction);\nconst next = order[(idx + 1) % 4];\nconsole.log(next);\n",
        "tests": [
          {
            "input": "N",
            "output": "E"
          },
          {
            "input": "W",
            "output": "N"
          },
          {
            "input": "S",
            "output": "W"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_enums-const",
        "title": "Label from a const map",
        "prompt": "`Status` is a frozen object mapping codes to labels. Replace `____` with the lookup that returns the label for `code`.",
        "hint": "Index the object with the key: Status[code as Key].",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst code = fs.readFileSync(0, \"utf8\").trim();\nconst Status = { L: \"loading\", D: \"done\", E: \"error\" } as const;\ntype Key = keyof typeof Status;\nconst label = ____;\nconsole.log(label);\n",
        "solution": "import * as fs from \"fs\";\nconst code = fs.readFileSync(0, \"utf8\").trim();\nconst Status = { L: \"loading\", D: \"done\", E: \"error\" } as const;\ntype Key = keyof typeof Status;\nconst label = Status[code as Key];\nconsole.log(label);\n",
        "tests": [
          {
            "input": "L",
            "output": "loading"
          },
          {
            "input": "D",
            "output": "done"
          },
          {
            "input": "E",
            "output": "error"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_enums-traffic",
        "title": "Traffic light action",
        "prompt": "`Light` is the literal union `red | yellow | green`. Write the solution: a function returning `stop` / `slow` / `go` respectively, then print the action.",
        "hint": "Handle red and yellow explicitly; green is the remaining case.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst color = fs.readFileSync(0, \"utf8\").trim();\ntype Light = \"red\" | \"yellow\" | \"green\";\n____\nconsole.log(action(color as Light));\n",
        "solution": "import * as fs from \"fs\";\nconst color = fs.readFileSync(0, \"utf8\").trim();\ntype Light = \"red\" | \"yellow\" | \"green\";\nfunction action(c: Light): string {\n  if (c === \"red\") return \"stop\";\n  if (c === \"yellow\") return \"slow\";\n  return \"go\";\n}\nconsole.log(action(color as Light));\n",
        "tests": [
          {
            "input": "red",
            "output": "stop"
          },
          {
            "input": "green",
            "output": "go"
          },
          {
            "input": "yellow",
            "output": "slow"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_enums-challenge",
        "title": "Compass turns",
        "prompt": "Start facing `N`. The input is a string of turns: `R` turns clockwise (N→E→S→W), `L` turns counter-clockwise. Print the final direction. Model directions with a literal-union array.",
        "hint": "Index into [\"N\",\"E\",\"S\",\"W\"]; R is +1 mod 4, L is +3 mod 4 (same as −1).",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Easy",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\ntype Dir = \"N\" | \"E\" | \"S\" | \"W\";\nconst order: Dir[] = [\"N\", \"E\", \"S\", \"W\"];\nlet idx = 0;\nfor (const t of input.split(\"\")) {\n  if (t === \"R\") idx = (idx + 1) % 4;\n  else if (t === \"L\") idx = (idx + 3) % 4;\n}\nconsole.log(order[idx]);\n",
        "tests": [
          {
            "input": "R",
            "output": "E"
          },
          {
            "input": "RR",
            "output": "S"
          },
          {
            "input": "L",
            "output": "W"
          },
          {
            "input": "RRRR",
            "output": "N"
          },
          {
            "input": "RRL",
            "output": "E"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_generics",
    "name": "Generics (Basics)",
    "category": "TS: Composition & Reuse",
    "what": "Type variables that let one function or type work for many element types safely.",
    "deep": "A generic `<T>` is a placeholder type filled in per call: `function first<T>(a: T[]): T` returns the array's element type, whatever it is. A constraint `<T extends { length: number }>` limits what T can be while keeping the link.",
    "java": "`<T>` works exactly like Java generics — `function id<T>(x: T): T`. Constraints use `extends` (`<T extends number>`). At runtime the type is erased (as in Java), so generics are a compile-time guarantee only.",
    "language": "typescript",
    "lesson": "### Worked example\nA generic keeps the *link* between input and output types:\n```ts\nfunction first<T>(arr: T[]): T {\n  return arr[0];\n}\nfirst([1, 2, 3]);        // T = number  -> 1\nfirst([\"a\", \"b\"]);       // T = string  -> \"a\"\n```\nThe `<T>` is a type variable filled in per call — so `first` works for any element type without losing type safety.\n\n### Constraints — limit T while keeping the link\n```ts\nfunction longest<T extends { length: number }>(a: T, b: T): T {\n  return a.length >= b.length ? a : b;\n}\nlongest([1, 2, 3], [1]);      // arrays have .length -> ok\nlongest(\"hello\", \"hi\");       // strings too\n```\n\n### Generic pairs and containers\n```ts\nfunction pair<A, B>(a: A, b: B): [A, B] {\n  return [a, b];\n}\nconst p = pair(\"age\", 36);   // [string, number]\n```\n\n### Watch out for\n- Types are erased at runtime — a generic can't inspect `T` (no `new T()`, no `T === number`).\n- Don't over-constrain: only add `extends` when you actually use the capability.\n",
    "exercises": [
      {
        "id": "ts_generics-first",
        "title": "Generic first element",
        "prompt": "Replace `____` with the header of a generic function `first` taking a `T[]` and returning a `T`.",
        "hint": "The type variable goes in angle brackets: function first<T>(arr: T[]): T.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\n____\n  return arr[0];\n}\nconsole.log(first(nums));\n",
        "solution": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nfunction first<T>(arr: T[]): T {\n  return arr[0];\n}\nconsole.log(first(nums));\n",
        "tests": [
          {
            "input": "3 1 2",
            "output": "3"
          },
          {
            "input": "9",
            "output": "9"
          },
          {
            "input": "5 6 7",
            "output": "5"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_generics-last",
        "title": "Generic last element",
        "prompt": "Replace `____` with the body that returns the last element of the generic array.",
        "hint": "The last index is arr.length - 1.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nfunction last<T>(arr: T[]): T {\n  ____\n}\nconsole.log(last(nums));\n",
        "solution": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nfunction last<T>(arr: T[]): T {\n  return arr[arr.length - 1];\n}\nconsole.log(last(nums));\n",
        "tests": [
          {
            "input": "3 1 2",
            "output": "2"
          },
          {
            "input": "9",
            "output": "9"
          },
          {
            "input": "5 6 7",
            "output": "7"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_generics-longest",
        "title": "Longer of two lists",
        "prompt": "Two space-separated lists are given (one per line). Write the solution: a constrained generic `longest<T extends { length: number }>` that returns the longer one, then print its length. On a tie, return the first.",
        "hint": "The constraint { length: number } lets you compare .length; use >= so ties keep x.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst lines = fs.readFileSync(0, \"utf8\").split(/\\r?\\n/);\nconst a = lines[0].trim().split(/\\s+/);\nconst b = lines[1].trim().split(/\\s+/);\n____\n",
        "solution": "import * as fs from \"fs\";\nconst lines = fs.readFileSync(0, \"utf8\").split(/\\r?\\n/);\nconst a = lines[0].trim().split(/\\s+/);\nconst b = lines[1].trim().split(/\\s+/);\nfunction longest<T extends { length: number }>(x: T, y: T): T {\n  return x.length >= y.length ? x : y;\n}\nconsole.log(longest(a, b).length);\n",
        "tests": [
          {
            "input": "1 2 3\n4 5",
            "output": "3"
          },
          {
            "input": "1\n2 3 4 5",
            "output": "4"
          },
          {
            "input": "1 2\n3 4",
            "output": "2"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_generics-challenge",
        "title": "Generic de-duplicate",
        "prompt": "Write a generic `unique<T>(arr: T[]): T[]` that keeps the first occurrence of each value in order. Apply it to the input tokens; print the unique tokens space-separated on line 1 and how many there are on line 2.",
        "hint": "Track seen values in a Set<T>; push only the first time you see each one.",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Medium",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\nfunction unique<T>(arr: T[]): T[] {\n  const out: T[] = [];\n  const seen = new Set<T>();\n  for (const x of arr) {\n    if (!seen.has(x)) {\n      seen.add(x);\n      out.push(x);\n    }\n  }\n  return out;\n}\nconst toks = input.split(/\\s+/).filter(t => t !== \"\");\nconst u = unique(toks);\nconsole.log(u.join(\" \"));\nconsole.log(u.length);\n",
        "tests": [
          {
            "input": "a b a c b",
            "output": "a b c\n3"
          },
          {
            "input": "1 1 1",
            "output": "1\n1"
          },
          {
            "input": "x y z",
            "output": "x y z\n3"
          },
          {
            "input": "dog cat dog",
            "output": "dog cat\n2"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_classes",
    "name": "Classes",
    "category": "TS: Composition & Reuse",
    "what": "Blueprints with fields, a constructor, methods, access modifiers, and inheritance.",
    "deep": "A `class` bundles data (fields) with behavior (methods). The `constructor` initializes fields; `public`/`private`/`readonly` control access; `extends` inherits and `super(...)` calls the parent. Create instances with `new`.",
    "java": "Very close to Java: `class`, `extends`, `super`, `new`. Declare each field and assign it in the constructor (the shorthand `constructor(public x)` needs a compile step, so spell fields out). Methods drop the `public` keyword by default.",
    "language": "typescript",
    "lesson": "### Worked example\n```ts\nclass Counter {\n  private count: number;         // declare the field\n  constructor(start: number) {\n    this.count = start;          // assign it\n  }\n  increment(): void { this.count++; }\n  get value(): number { return this.count; }\n}\n\nconst c = new Counter(10);\nc.increment();\nconsole.log(c.value);   // 11\n```\n\n### Access modifiers\n- `public` (default): visible everywhere.\n- `private`: only inside the class.\n- `readonly`: assignable once (in the constructor), then fixed.\n\n### Inheritance\n```ts\nclass Animal {\n  name: string;\n  constructor(name: string) { this.name = name; }\n  speak(): string { return `${this.name} makes a sound`; }\n}\nclass Dog extends Animal {\n  speak(): string { return `${this.name} barks`; }   // override\n}\nconsole.log(new Dog(\"Rex\").speak());  // Rex barks\n```\nCall the parent constructor with `super(...)`, and a parent method with `super.method()`.\n\n> Note: the shorthand `constructor(public count: number)` needs a compile step, so these drills declare fields explicitly and assign them in the constructor — the form that runs in a type-stripping runtime.\n\n### Watch out for\n- You must call `super(...)` before using `this` in a subclass constructor.\n",
    "exercises": [
      {
        "id": "ts_classes-field",
        "title": "Initialize a field",
        "prompt": "Replace `____` with the constructor line that stores `start` into the instance's `count` field.",
        "hint": "Assign the parameter to the field with this.count = start;.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst n = Number(fs.readFileSync(0, \"utf8\").trim());\nclass Counter {\n  count: number;\n  constructor(start: number) {\n    ____\n  }\n  increment(): void {\n    this.count++;\n  }\n}\nconst c = new Counter(0);\nfor (let i = 0; i < n; i++) {\n  c.increment();\n}\nconsole.log(c.count);\n",
        "solution": "import * as fs from \"fs\";\nconst n = Number(fs.readFileSync(0, \"utf8\").trim());\nclass Counter {\n  count: number;\n  constructor(start: number) {\n    this.count = start;\n  }\n  increment(): void {\n    this.count++;\n  }\n}\nconst c = new Counter(0);\nfor (let i = 0; i < n; i++) {\n  c.increment();\n}\nconsole.log(c.count);\n",
        "tests": [
          {
            "input": "3",
            "output": "3"
          },
          {
            "input": "0",
            "output": "0"
          },
          {
            "input": "5",
            "output": "5"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_classes-method",
        "title": "A method that computes",
        "prompt": "Replace `____` with the body of the `area()` method.",
        "hint": "Inside a method, read fields via this: this.width * this.height.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst [w, h] = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nclass Rect {\n  width: number;\n  height: number;\n  constructor(w: number, h: number) {\n    this.width = w;\n    this.height = h;\n  }\n  area(): number {\n    ____\n  }\n}\nconsole.log(new Rect(w, h).area());\n",
        "solution": "import * as fs from \"fs\";\nconst [w, h] = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nclass Rect {\n  width: number;\n  height: number;\n  constructor(w: number, h: number) {\n    this.width = w;\n    this.height = h;\n  }\n  area(): number {\n    return this.width * this.height;\n  }\n}\nconsole.log(new Rect(w, h).area());\n",
        "tests": [
          {
            "input": "3 4",
            "output": "12"
          },
          {
            "input": "5 5",
            "output": "25"
          },
          {
            "input": "2 7",
            "output": "14"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_classes-inherit",
        "title": "Override a method",
        "prompt": "`Dog` should extend `Animal` and override `speak()` to say `<name> barks`. Write the `Dog` subclass.",
        "hint": "extends Animal, then redefine speak() to return the barking message.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\nclass Animal {\n  name: string;\n  constructor(name: string) {\n    this.name = name;\n  }\n  speak(): string {\n    return `${this.name} makes a sound`;\n  }\n}\n____\nconst animal = parts[0] === \"dog\" ? new Dog(parts[1]) : new Animal(parts[1]);\nconsole.log(animal.speak());\n",
        "solution": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\nclass Animal {\n  name: string;\n  constructor(name: string) {\n    this.name = name;\n  }\n  speak(): string {\n    return `${this.name} makes a sound`;\n  }\n}\nclass Dog extends Animal {\n  speak(): string {\n    return `${this.name} barks`;\n  }\n}\nconst animal = parts[0] === \"dog\" ? new Dog(parts[1]) : new Animal(parts[1]);\nconsole.log(animal.speak());\n",
        "tests": [
          {
            "input": "dog Rex",
            "output": "Rex barks"
          },
          {
            "input": "cat Tom",
            "output": "Tom makes a sound"
          },
          {
            "input": "dog Max",
            "output": "Max barks"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_classes-challenge",
        "title": "Bank account",
        "prompt": "Line 1 is a starting balance. Each later line is `deposit <n>` or `withdraw <n>`. Model an `Account` class; a withdrawal larger than the balance is rejected (counted, not applied). Print `<finalBalance> <rejectedCount>`.",
        "hint": "Keep balance and a rejected counter as private fields; guard withdraw before subtracting.",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Medium",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\nclass Account {\n  private balance: number;\n  private rejected: number;\n  constructor(start: number) {\n    this.balance = start;\n    this.rejected = 0;\n  }\n  deposit(amount: number): void { this.balance += amount; }\n  withdraw(amount: number): void {\n    if (amount > this.balance) this.rejected++;\n    else this.balance -= amount;\n  }\n  report(): string { return this.balance + \" \" + this.rejected; }\n}\nconst lines = input.split(\"\\n\");\nconst acc = new Account(Number(lines[0]));\nfor (let i = 1; i < lines.length; i++) {\n  const [op, amt] = lines[i].trim().split(/\\s+/);\n  if (op === \"deposit\") acc.deposit(Number(amt));\n  else if (op === \"withdraw\") acc.withdraw(Number(amt));\n}\nconsole.log(acc.report());\n",
        "tests": [
          {
            "input": "100\ndeposit 50\nwithdraw 30",
            "output": "120 0"
          },
          {
            "input": "100\nwithdraw 200",
            "output": "100 1"
          },
          {
            "input": "0\ndeposit 10\nwithdraw 5\nwithdraw 100",
            "output": "5 1"
          },
          {
            "input": "50",
            "output": "50 0"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_this_accessors",
    "name": "this, Getters & Setters",
    "category": "TS: Composition & Reuse",
    "what": "Referring to the current instance and exposing computed properties via get/set.",
    "deep": "Inside a method `this` is the current instance. A getter `get area()` looks like a field to callers but runs code; a setter `set width(v)` intercepts assignment. Arrow-function methods capture the surrounding `this`, avoiding a classic pitfall.",
    "java": "`this` matches Java. Getters/setters use the `get`/`set` keywords and are accessed *without* parentheses — `rect.area`, not `rect.area()`. Beware: a plain method passed as a callback can lose its `this`; an arrow field keeps it.",
    "language": "typescript",
    "lesson": "### Worked example\n```ts\nclass Rect {\n  width: number;\n  height: number;\n  constructor(w: number, h: number) {\n    this.width = w;\n    this.height = h;\n  }\n  get area(): number {          // getter — accessed WITHOUT ()\n    return this.width * this.height;\n  }\n  set square(side: number) {    // setter — runs on assignment\n    this.width = side;\n    this.height = side;\n  }\n}\n\nconst r = new Rect(3, 4);\nconsole.log(r.area);   // 12   (no parentheses!)\nr.square = 5;\nconsole.log(r.area);   // 25\n```\n\n### this\nInside a method, `this` is the instance the method was called on. It's how methods read and update their own fields.\n\n### The lost-this pitfall\nPassing a method as a callback can detach it from its object:\n```ts\nclass Greeter {\n  name = \"Ada\";\n  hi = () => `Hi ${this.name}`;   // arrow field captures `this` — safe\n}\n```\nAn **arrow-function field** captures the surrounding `this`, so it keeps working as a callback.\n\n### Watch out for\n- Getters/setters are used like properties (`r.area`), not called like methods (`r.area()`).\n",
    "exercises": [
      {
        "id": "ts_this_accessors-getter",
        "title": "Use a getter",
        "prompt": "`area` is a getter, so it is read like a property. Replace `____` to print `r.area` (no parentheses!).",
        "hint": "A getter is accessed like a field: r.area, not r.area().",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst [w, h] = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nclass Rect {\n  width: number;\n  height: number;\n  constructor(w: number, h: number) {\n    this.width = w;\n    this.height = h;\n  }\n  get area(): number {\n    return this.width * this.height;\n  }\n}\nconst r = new Rect(w, h);\nconsole.log(____);\n",
        "solution": "import * as fs from \"fs\";\nconst [w, h] = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nclass Rect {\n  width: number;\n  height: number;\n  constructor(w: number, h: number) {\n    this.width = w;\n    this.height = h;\n  }\n  get area(): number {\n    return this.width * this.height;\n  }\n}\nconst r = new Rect(w, h);\nconsole.log(r.area);\n",
        "tests": [
          {
            "input": "3 4",
            "output": "12"
          },
          {
            "input": "5 5",
            "output": "25"
          },
          {
            "input": "2 7",
            "output": "14"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_this_accessors-setter",
        "title": "A setter makes a square",
        "prompt": "Assigning to `square` should set both sides. Replace `____` with the assignment `b.square = side;` that triggers the setter.",
        "hint": "A setter runs on assignment: b.square = side; sets both width and height.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst side = Number(fs.readFileSync(0, \"utf8\").trim());\nclass Box {\n  width: number;\n  height: number;\n  constructor() {\n    this.width = 0;\n    this.height = 0;\n  }\n  set square(s: number) {\n    this.width = s;\n    this.height = s;\n  }\n  get area(): number {\n    return this.width * this.height;\n  }\n}\nconst b = new Box();\n____\nconsole.log(b.area);\n",
        "solution": "import * as fs from \"fs\";\nconst side = Number(fs.readFileSync(0, \"utf8\").trim());\nclass Box {\n  width: number;\n  height: number;\n  constructor() {\n    this.width = 0;\n    this.height = 0;\n  }\n  set square(s: number) {\n    this.width = s;\n    this.height = s;\n  }\n  get area(): number {\n    return this.width * this.height;\n  }\n}\nconst b = new Box();\nb.square = side;\nconsole.log(b.area);\n",
        "tests": [
          {
            "input": "5",
            "output": "25"
          },
          {
            "input": "3",
            "output": "9"
          },
          {
            "input": "10",
            "output": "100"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_this_accessors-account",
        "title": "Deposit into an account",
        "prompt": "Write the solution: a `deposit(amount)` method that adds to `this.balance`, so after depositing every input amount the final balance prints.",
        "hint": "Inside deposit, update the field: this.balance += amount;.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst amounts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nclass Account {\n  balance: number;\n  constructor() {\n    this.balance = 0;\n  }\n  ____\n}\nconst acc = new Account();\nfor (const a of amounts) {\n  acc.deposit(a);\n}\nconsole.log(acc.balance);\n",
        "solution": "import * as fs from \"fs\";\nconst amounts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nclass Account {\n  balance: number;\n  constructor() {\n    this.balance = 0;\n  }\n  deposit(amount: number): void {\n    this.balance += amount;\n  }\n}\nconst acc = new Account();\nfor (const a of amounts) {\n  acc.deposit(a);\n}\nconsole.log(acc.balance);\n",
        "tests": [
          {
            "input": "10 20 30",
            "output": "60"
          },
          {
            "input": "5",
            "output": "5"
          },
          {
            "input": "100 -40 10",
            "output": "70"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_this_accessors-challenge",
        "title": "Celsius to Fahrenheit",
        "prompt": "The input is a line of Celsius temperatures. Build a `Temp` class with a `fahrenheit` getter (c × 9 / 5 + 32) and print each temperature's Fahrenheit value space-separated.",
        "hint": "A getter is read like a property: t.fahrenheit (no parentheses).",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Medium",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\nclass Temp {\n  celsius: number;\n  constructor(c: number) { this.celsius = c; }\n  get fahrenheit(): number { return this.celsius * 9 / 5 + 32; }\n}\nconst out = input.split(/\\s+/).map(s => {\n  const t = new Temp(Number(s));\n  return t.fahrenheit;\n});\nconsole.log(out.join(\" \"));\n",
        "tests": [
          {
            "input": "0",
            "output": "32"
          },
          {
            "input": "100",
            "output": "212"
          },
          {
            "input": "37",
            "output": "98.6"
          },
          {
            "input": "-40",
            "output": "-40"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_modules",
    "name": "Modules (import / export)",
    "category": "TS: Composition & Reuse",
    "what": "Splitting code across files with export (publish) and import (consume).",
    "deep": "Each file is a module. `export` publishes a function/type/value; another file pulls it in with `import { thing } from \"./file\"`. There are named exports (`export function f`) and one `export default` per file. Imports are how real projects stay organized.",
    "java": "`import { X } from \"./util\"` is like Java's `import`, but you choose exactly what to expose with `export` (no `public` per file — nothing is visible unless exported). `export default` is the single 'main' export of a file.",
    "language": "typescript",
    "lesson": "### Worked example\nReal projects split code across files. One file **exports**; another **imports**.\n```ts\n// math.ts\nexport function add(a: number, b: number): number { return a + b; }\nexport const PI = 3.14159;\n\n// main.ts\nimport { add, PI } from \"./math\";\nconsole.log(add(2, 3), PI);   // 5 3.14159\n```\n\n### Named vs default exports\n```ts\n// util.ts\nexport function helper() { … }        // named — import { helper }\nexport default function main() { … }  // one default per file\n\n// consumer.ts\nimport main, { helper } from \"./util\";   // default first, then named in braces\n```\n\n### Rename & namespace imports\n```ts\nimport { add as plus } from \"./math\";\nimport * as math from \"./math\";\nmath.add(1, 2);\n```\n\n### Why it matters\nNothing in a file is visible to other files unless you `export` it — modules give you explicit, per-symbol encapsulation. This is how large codebases stay organized and avoid name clashes.\n\n### Watch out for\n- Import paths to your own files start with `./` or `../`; bare names (`\"fs\"`) mean packages.\n- A file with no `import`/`export` is treated as a plain script, not a module.\n",
    "exercises": [
      {
        "id": "ts_modules-export",
        "title": "Export a function",
        "prompt": "In a real project `add` would live in its own module. Replace `____` with the `export` keyword so `add` is published (it still runs here in one file).",
        "hint": "Prefix the declaration with export: export function add(...) { ... }.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst [a, b] = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\n____\n  return x + y;\n}\nconsole.log(add(a, b));\n",
        "solution": "import * as fs from \"fs\";\nconst [a, b] = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nexport function add(x: number, y: number): number {\n  return x + y;\n}\nconsole.log(add(a, b));\n",
        "tests": [
          {
            "input": "2 3",
            "output": "5"
          },
          {
            "input": "10 -4",
            "output": "6"
          },
          {
            "input": "0 0",
            "output": "0"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_modules-use",
        "title": "Use an exported constant",
        "prompt": "`TAX` is exported above. Replace `____` with the expression that applies the tax: `price * TAX`, rounded down.",
        "hint": "Multiply price by the exported constant TAX.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst price = Number(fs.readFileSync(0, \"utf8\").trim());\nexport const TAX = 2;\nconst total = Math.floor(____);\nconsole.log(total);\n",
        "solution": "import * as fs from \"fs\";\nconst price = Number(fs.readFileSync(0, \"utf8\").trim());\nexport const TAX = 2;\nconst total = Math.floor(price * TAX);\nconsole.log(total);\n",
        "tests": [
          {
            "input": "10",
            "output": "20"
          },
          {
            "input": "7",
            "output": "14"
          },
          {
            "input": "0",
            "output": "0"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_modules-default",
        "title": "A default export",
        "prompt": "Each file may have one `export default`. Write the solution: export `main` as the default and call it, printing the doubled input.",
        "hint": "export default function main(...) marks the file's single main export.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst n = Number(fs.readFileSync(0, \"utf8\").trim());\n____\n",
        "solution": "import * as fs from \"fs\";\nconst n = Number(fs.readFileSync(0, \"utf8\").trim());\nexport default function main(x: number): number {\n  return x * 2;\n}\nconsole.log(main(n));\n",
        "tests": [
          {
            "input": "5",
            "output": "10"
          },
          {
            "input": "0",
            "output": "0"
          },
          {
            "input": "-3",
            "output": "-6"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_modules-challenge",
        "title": "Exported geometry helpers",
        "prompt": "The input is `w h`. Declare and `export` two functions — `area(w, h)` and `perimeter(w, h)` — then use them to print the rectangle's area on line 1 and perimeter on line 2.",
        "hint": "`export function` publishes a function; here you both export and call them in one file.",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Easy",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\nexport function area(w: number, h: number): number { return w * h; }\nexport function perimeter(w: number, h: number): number { return 2 * (w + h); }\nconst [w, h] = input.split(/\\s+/).map(Number);\nconsole.log(area(w, h));\nconsole.log(perimeter(w, h));\n",
        "tests": [
          {
            "input": "3 4",
            "output": "12\n14"
          },
          {
            "input": "5 5",
            "output": "25\n20"
          },
          {
            "input": "1 1",
            "output": "1\n4"
          },
          {
            "input": "10 2",
            "output": "20\n24"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_nullish",
    "name": "null, undefined & Strict Checks",
    "category": "TS: Robustness",
    "what": "Handling missing values safely with strictNullChecks, ??, and ?..",
    "deep": "`undefined` means 'no value assigned'; `null` means 'intentionally empty'. Under strict checks a `string | undefined` must be handled before use. `x ?? fallback` supplies a default; `obj?.field` reads safely; a guard `if (x != null)` narrows away the empty case.",
    "java": "This is TypeScript's answer to the NullPointerException: the type system forces you to deal with maybe-missing values. `x ?? d` = default-if-nullish, `x?.y` = safe access. `x != null` rules out BOTH null and undefined at once.",
    "language": "typescript",
    "lesson": "### Worked example\n```ts\nfunction greet(name: string | undefined): string {\n  return `Hi ${name ?? \"friend\"}`;   // fallback when missing\n}\ngreet(\"Ada\");        // \"Hi Ada\"\ngreet(undefined);    // \"Hi friend\"\n```\n\n### undefined vs null\n- `undefined`: no value has been assigned yet (missing property, unset variable).\n- `null`: an intentional \"empty\" you set on purpose.\nBoth are ruled out at once by `x != null` (note: loose `!=` here is the idiomatic exception).\n\n### The three safety tools\n```ts\nconst port = config.port ?? 8080;   // ?? default when null/undefined\nconst city = user?.address?.city;   // ?. safe read, short-circuits to undefined\nif (value != null) {                // guard narrows to the non-empty type\n  value.toFixed(2);\n}\n```\n\n### Strict null checks\nWith strict checks on (the default here), a `string | undefined` *cannot* be used as a `string` until you handle the `undefined` case — the compiler forces the safety that prevents \"cannot read property of undefined\" crashes.\n\n### Watch out for\n- `??` vs `||`: use `??` when `0`, `\"\"`, or `false` are valid values you want to keep.\n- `obj?.method()` skips the call entirely when `obj` is nullish (returns `undefined`).\n",
    "exercises": [
      {
        "id": "ts_nullish-default",
        "title": "Fallback with ??",
        "prompt": "`name` may be `undefined` (empty input). Replace `____` so the greeting falls back to `friend` when it is missing.",
        "hint": "name ?? \"friend\" uses the fallback only when name is null/undefined.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst raw = fs.readFileSync(0, \"utf8\").trim();\nconst name: string | undefined = raw === \"\" ? undefined : raw;\nconsole.log(`Hi ${____}`);\n",
        "solution": "import * as fs from \"fs\";\nconst raw = fs.readFileSync(0, \"utf8\").trim();\nconst name: string | undefined = raw === \"\" ? undefined : raw;\nconsole.log(`Hi ${name ?? \"friend\"}`);\n",
        "tests": [
          {
            "input": "Ada",
            "output": "Hi Ada"
          },
          {
            "input": "",
            "output": "Hi friend"
          },
          {
            "input": "Bob",
            "output": "Hi Bob"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_nullish-guard",
        "title": "Guard the missing case",
        "prompt": "`maybe` is `number | undefined`. Replace `____` with the guard that rules out the missing case so `.toFixed(1)` is safe.",
        "hint": "x != null rules out BOTH null and undefined in one check.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst raw = fs.readFileSync(0, \"utf8\").trim();\nconst maybe: number | undefined = raw === \"\" ? undefined : Number(raw);\nlet out: string;\nif (____) {\n  out = maybe.toFixed(1);\n} else {\n  out = \"none\";\n}\nconsole.log(out);\n",
        "solution": "import * as fs from \"fs\";\nconst raw = fs.readFileSync(0, \"utf8\").trim();\nconst maybe: number | undefined = raw === \"\" ? undefined : Number(raw);\nlet out: string;\nif (maybe != null) {\n  out = maybe.toFixed(1);\n} else {\n  out = \"none\";\n}\nconsole.log(out);\n",
        "tests": [
          {
            "input": "3",
            "output": "3.0"
          },
          {
            "input": "",
            "output": "none"
          },
          {
            "input": "10",
            "output": "10.0"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_nullish-chain",
        "title": "Safe access with ?.",
        "prompt": "The city may be absent. Write the solution: use optional chaining to read `user.address?.city`, falling back to `unknown`, and print it.",
        "hint": "user.address?.city is undefined when address is missing; ?? supplies the fallback.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst raw = fs.readFileSync(0, \"utf8\").trim();\nconst user: { address?: { city: string } } = raw === \"\" ? {} : { address: { city: raw } };\n____\n",
        "solution": "import * as fs from \"fs\";\nconst raw = fs.readFileSync(0, \"utf8\").trim();\nconst user: { address?: { city: string } } = raw === \"\" ? {} : { address: { city: raw } };\nconst city = user.address?.city ?? \"unknown\";\nconsole.log(city);\n",
        "tests": [
          {
            "input": "Paris",
            "output": "Paris"
          },
          {
            "input": "",
            "output": "unknown"
          },
          {
            "input": "Tokyo",
            "output": "Tokyo"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_nullish-challenge",
        "title": "Resolve config",
        "prompt": "Each input line is `key value` for one of `host`, `port`, `mode` (any may be missing). Read them into a Map, then print `<host> <port> <mode>`, supplying defaults `localhost` / `8080` / `prod` with `??` when a key is absent.",
        "hint": "map.get(k) is undefined when the key is missing — `?? default` fills it in.",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Medium",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\nconst map = new Map<string, string>();\nfor (const line of input.split(\"\\n\")) {\n  const [k, v] = line.trim().split(/\\s+/);\n  if (k) map.set(k, v);\n}\nconst host = map.get(\"host\") ?? \"localhost\";\nconst port = map.get(\"port\") ?? \"8080\";\nconst mode = map.get(\"mode\") ?? \"prod\";\nconsole.log(host + \" \" + port + \" \" + mode);\n",
        "tests": [
          {
            "input": "host example.com",
            "output": "example.com 8080 prod"
          },
          {
            "input": "port 3000\nmode dev",
            "output": "localhost 3000 dev"
          },
          {
            "input": "",
            "output": "localhost 8080 prod"
          },
          {
            "input": "host a\nport 1\nmode b",
            "output": "a 1 b"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_errors",
    "name": "Error Handling",
    "category": "TS: Robustness",
    "what": "Signaling and recovering from failures with throw, try/catch/finally.",
    "deep": "`throw new Error(\"message\")` signals a failure. A `try { … } catch (e) { … }` block runs the risky code and handles the error; `finally` always runs. Because anything can be thrown, the caught value is `unknown` — narrow it before reading `.message`.",
    "java": "`throw`/`try`/`catch`/`finally` mirror Java, but there are no checked exceptions and no `throws` clause. The catch binding is untyped (`unknown`) — use `e instanceof Error` before touching `e.message`.",
    "language": "typescript",
    "lesson": "### Worked example\n```ts\nfunction parsePositive(s: string): number {\n  const n = Number(s);\n  if (Number.isNaN(n)) throw new Error(\"not a number\");\n  if (n <= 0) throw new Error(\"must be positive\");\n  return n;\n}\n\ntry {\n  console.log(parsePositive(\"abc\"));\n} catch (e) {\n  console.log(\"failed:\", e instanceof Error ? e.message : \"unknown\");\n}\n// failed: not a number\n```\n\n### try / catch / finally\n```ts\ntry {\n  risky();\n} catch (e) {\n  // handle the failure\n} finally {\n  // ALWAYS runs — cleanup goes here\n}\n```\n\n### The catch value is unknown\nYou can throw anything, so `e` is typed `unknown`. Narrow before reading it:\n```ts\ncatch (e) {\n  if (e instanceof Error) console.log(e.message);\n  else console.log(String(e));\n}\n```\n\n### Throwing well\n`throw new Error(\"clear message\")` — prefer `Error` (or a subclass) over throwing bare strings, so callers get a `.message` and a stack trace.\n\n### Watch out for\n- `finally` runs even if you `return` or `throw` inside `try`.\n- Don't swallow errors silently — at least log them.\n",
    "exercises": [
      {
        "id": "ts_errors-throw",
        "title": "Throw on bad input",
        "prompt": "Replace `____` with a `throw` that raises an `Error` when the number is not positive.",
        "hint": "throw new Error(\"...\") signals the failure the catch block handles.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst n = Number(fs.readFileSync(0, \"utf8\").trim());\nfunction check(x: number): number {\n  if (x <= 0) ____\n  return x;\n}\ntry {\n  console.log(check(n));\n} catch (e) {\n  console.log(e instanceof Error ? e.message : \"unknown\");\n}\n",
        "solution": "import * as fs from \"fs\";\nconst n = Number(fs.readFileSync(0, \"utf8\").trim());\nfunction check(x: number): number {\n  if (x <= 0) throw new Error(\"must be positive\");\n  return x;\n}\ntry {\n  console.log(check(n));\n} catch (e) {\n  console.log(e instanceof Error ? e.message : \"unknown\");\n}\n",
        "tests": [
          {
            "input": "5",
            "output": "5"
          },
          {
            "input": "-1",
            "output": "must be positive"
          },
          {
            "input": "0",
            "output": "must be positive"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_errors-catch",
        "title": "Narrow the caught value",
        "prompt": "The caught value `e` is `unknown`. Replace `____` with the check that lets you safely read `e.message`.",
        "hint": "e instanceof Error narrows unknown so e.message is available.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst raw = fs.readFileSync(0, \"utf8\").trim();\ntry {\n  const n = Number(raw);\n  if (Number.isNaN(n)) throw new Error(\"not a number\");\n  console.log(n * 2);\n} catch (e) {\n  console.log(____ ? e.message : \"unknown\");\n}\n",
        "solution": "import * as fs from \"fs\";\nconst raw = fs.readFileSync(0, \"utf8\").trim();\ntry {\n  const n = Number(raw);\n  if (Number.isNaN(n)) throw new Error(\"not a number\");\n  console.log(n * 2);\n} catch (e) {\n  console.log(e instanceof Error ? e.message : \"unknown\");\n}\n",
        "tests": [
          {
            "input": "21",
            "output": "42"
          },
          {
            "input": "abc",
            "output": "not a number"
          },
          {
            "input": "0",
            "output": "0"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_errors-finally",
        "title": "Parse with try/catch/finally",
        "prompt": "Write the solution: parse the input; print the number if valid or `invalid` if not; and ALWAYS print `done` afterward using `finally`.",
        "hint": "finally runs no matter what, so put the 'done' print there.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst raw = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst raw = fs.readFileSync(0, \"utf8\").trim();\ntry {\n  const n = Number(raw);\n  if (Number.isNaN(n)) throw new Error(\"bad\");\n  console.log(n);\n} catch (e) {\n  console.log(\"invalid\");\n} finally {\n  console.log(\"done\");\n}\n",
        "tests": [
          {
            "input": "7",
            "output": "7\ndone"
          },
          {
            "input": "oops",
            "output": "invalid\ndone"
          },
          {
            "input": "0",
            "output": "0\ndone"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_errors-challenge",
        "title": "Safe sum",
        "prompt": "The input is a line of tokens. Write a `toNumber` that throws on a non-numeric token. In a try/catch, sum the valid numbers and count the failures. Print `<sum> <failures>`.",
        "hint": "Wrap each toNumber call in try/catch; the catch branch just increments failures.",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Medium",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\nfunction toNumber(s: string): number {\n  const n = Number(s);\n  if (Number.isNaN(n)) throw new Error(\"bad number: \" + s);\n  return n;\n}\nlet sum = 0;\nlet failures = 0;\nfor (const t of input.split(/\\s+/)) {\n  if (t === \"\") continue;\n  try {\n    sum += toNumber(t);\n  } catch (e) {\n    failures++;\n  }\n}\nconsole.log(sum + \" \" + failures);\n",
        "tests": [
          {
            "input": "1 2 x 3",
            "output": "6 1"
          },
          {
            "input": "a b c",
            "output": "0 3"
          },
          {
            "input": "10 20",
            "output": "30 0"
          },
          {
            "input": "5 ? 5 !",
            "output": "10 2"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_utility_types",
    "name": "Utility Types",
    "category": "TS: Robustness",
    "what": "Built-in type transformers: Partial, Readonly, Pick, Record.",
    "deep": "Utility types reshape existing types so you don't repeat yourself: `Partial<T>` makes every field optional, `Readonly<T>` makes them read-only, `Pick<T, K>` selects some fields, and `Record<K, V>` builds an object type from keys to a value type.",
    "java": "There is no Java analogue — these are compile-time functions over types. `Record<string, number>` types a string-keyed map; `Partial<User>` is a User where every field may be absent (great for update payloads).",
    "language": "typescript",
    "lesson": "### Worked example\nUtility types transform an existing type so you don't repeat its fields:\n```ts\ninterface User {\n  id: string;\n  name: string;\n  age: number;\n}\n\ntype UserUpdate = Partial<User>;          // every field optional\ntype FrozenUser = Readonly<User>;         // every field read-only\ntype NameCard  = Pick<User, \"name\">;      // { name: string }\ntype ById      = Record<string, User>;    // { [id: string]: User }\n```\n\n### What each one does\n| Utility | Effect |\n|---------|--------|\n| `Partial<T>` | all fields optional — great for update/patch payloads |\n| `Required<T>` | all fields required (the inverse) |\n| `Readonly<T>` | all fields read-only |\n| `Pick<T, K>` | keep only fields `K` |\n| `Omit<T, K>` | drop fields `K` |\n| `Record<K, V>` | build an object type from keys `K` to values `V` |\n\n### In practice\n```ts\nfunction update(user: User, patch: Partial<User>): User {\n  return { ...user, ...patch };\n}\nupdate(u, { age: 41 });   // only the fields you pass are changed\n```\n\n### Watch out for\n- These are **compile-time only** — at runtime the objects are ordinary objects; nothing enforces `Readonly` except the type checker.\n- `Record<string, number>` is the idiomatic type for a string-keyed count map.\n",
    "exercises": [
      {
        "id": "ts_utility_types-partial",
        "title": "Apply a Partial patch",
        "prompt": "`patch` is typed `Partial<User>` (every field optional). Replace `____` with the spread merge that applies the patch over `user`.",
        "hint": "Spread the base then the patch: { ...user, ...patch } — later fields win.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\ninterface User { name: string; age: number; }\nconst user: User = { name: \"Ada\", age: 36 };\nconst patch: Partial<User> = { age: Number(parts[0]) };\nconst updated: User = ____;\nconsole.log(`${updated.name} ${updated.age}`);\n",
        "solution": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\ninterface User { name: string; age: number; }\nconst user: User = { name: \"Ada\", age: 36 };\nconst patch: Partial<User> = { age: Number(parts[0]) };\nconst updated: User = { ...user, ...patch };\nconsole.log(`${updated.name} ${updated.age}`);\n",
        "tests": [
          {
            "input": "40",
            "output": "Ada 40"
          },
          {
            "input": "18",
            "output": "Ada 18"
          },
          {
            "input": "99",
            "output": "Ada 99"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_utility_types-record",
        "title": "A Record count map",
        "prompt": "`counts` is typed `Record<string, number>`. Replace `____` with the increment line for word `w`.",
        "hint": "Record<string, number> is just a string-keyed number map; increment as usual.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst words = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\nconst counts: Record<string, number> = {};\nfor (const w of words) {\n  ____\n}\nconsole.log(counts[words[0]]);\n",
        "solution": "import * as fs from \"fs\";\nconst words = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\nconst counts: Record<string, number> = {};\nfor (const w of words) {\n  counts[w] = (counts[w] ?? 0) + 1;\n}\nconsole.log(counts[words[0]]);\n",
        "tests": [
          {
            "input": "a b a a",
            "output": "3"
          },
          {
            "input": "hi",
            "output": "1"
          },
          {
            "input": "x x y",
            "output": "2"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_utility_types-pick",
        "title": "Pick a subset",
        "prompt": "`NameOnly` uses `Pick<User, \"name\">`. Write the solution: build a `NameOnly` from the full user and print its name.",
        "hint": "Pick<User, \"name\"> keeps only the name field, so build { name: user.name }.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\ninterface User { name: string; age: number; }\nconst user: User = { name: parts[0], age: Number(parts[1]) };\ntype NameOnly = Pick<User, \"name\">;\n____\n",
        "solution": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\ninterface User { name: string; age: number; }\nconst user: User = { name: parts[0], age: Number(parts[1]) };\ntype NameOnly = Pick<User, \"name\">;\nconst card: NameOnly = { name: user.name };\nconsole.log(card.name);\n",
        "tests": [
          {
            "input": "Ada 36",
            "output": "Ada"
          },
          {
            "input": "Bob 40",
            "output": "Bob"
          },
          {
            "input": "Cid 7",
            "output": "Cid"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_utility_types-challenge",
        "title": "Apply patches",
        "prompt": "Line 1 is a base user `name age`. Each later line is a patch `name <value>` or `age <value>`. Apply them in order using a `Partial<User>` and spread, then print the final `name age`.",
        "hint": "Each patch is a Partial<User>; `{ ...user, ...patch }` overrides just its field.",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Medium",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\ninterface User { name: string; age: number; }\nconst lines = input.split(\"\\n\");\nconst [name0, age0] = lines[0].trim().split(/\\s+/);\nlet user: User = { name: name0, age: Number(age0) };\nfor (let i = 1; i < lines.length; i++) {\n  const [field, value] = lines[i].trim().split(/\\s+/);\n  const patch: Partial<User> = field === \"age\"\n    ? { age: Number(value) }\n    : { name: value };\n  user = { ...user, ...patch };\n}\nconsole.log(user.name + \" \" + user.age);\n",
        "tests": [
          {
            "input": "Ada 36",
            "output": "Ada 36"
          },
          {
            "input": "Ada 36\nage 40",
            "output": "Ada 40"
          },
          {
            "input": "Ada 36\nname Bob\nage 41",
            "output": "Bob 41"
          },
          {
            "input": "X 1\nname Y",
            "output": "Y 1"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_async",
    "name": "Promises & async/await",
    "category": "TS: Robustness",
    "what": "Working with values that arrive later using Promise, async and await.",
    "deep": "A `Promise<T>` is a value that will exist later. An `async` function always returns a Promise; inside it `await p` pauses until the promise resolves and hands you the value. This makes asynchronous code read top-to-bottom like ordinary code.",
    "java": "A `Promise<T>` is like a `CompletableFuture<T>`. `await` unwraps it without callbacks — the function suspends, not the thread. Mark a function `async` to use `await` inside it; the function's result is itself a Promise.",
    "language": "typescript",
    "lesson": "### Worked example\n```ts\nfunction later(value: number): Promise<number> {\n  return new Promise(resolve => setTimeout(() => resolve(value), 10));\n}\n\nasync function main(): Promise<void> {\n  const x = await later(21);   // pause until it resolves\n  console.log(x * 2);          // 42\n}\nmain();\n```\n\n### The shape of async code\n- A `Promise<T>` is a value that will exist *later* (or fail).\n- An `async` function always returns a `Promise`.\n- `await p` inside an `async` function suspends until `p` resolves, then yields the value — so asynchronous code reads top-to-bottom.\n\n### Sequential vs parallel\n```ts\nconst a = await slow();   // wait...\nconst b = await slow();   // ...then wait again (sequential)\n\nconst [c, d] = await Promise.all([slow(), slow()]);  // both at once (parallel)\n```\n\n### Errors in async code\n`await` throws on rejection, so use ordinary `try/catch`:\n```ts\ntry {\n  const data = await fetchThing();\n} catch (e) {\n  console.log(\"failed\");\n}\n```\n\n### Watch out for\n- `await` only works inside an `async` function (or at the top level of a module).\n- Forgetting `await` gives you the *Promise*, not the value — a very common bug.\n",
    "exercises": [
      {
        "id": "ts_async-await",
        "title": "Await a promise",
        "prompt": "`later(n)` resolves to `n` after a tick. Replace `____` to `await` it inside the async function.",
        "hint": "await pauses until the promise resolves: const x = await later(n);.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst n = Number(fs.readFileSync(0, \"utf8\").trim());\nfunction later(value: number): Promise<number> {\n  return new Promise(resolve => setTimeout(() => resolve(value), 5));\n}\nasync function main(): Promise<void> {\n  ____\n  console.log(x * 2);\n}\nmain();\n",
        "solution": "import * as fs from \"fs\";\nconst n = Number(fs.readFileSync(0, \"utf8\").trim());\nfunction later(value: number): Promise<number> {\n  return new Promise(resolve => setTimeout(() => resolve(value), 5));\n}\nasync function main(): Promise<void> {\n  const x = await later(n);\n  console.log(x * 2);\n}\nmain();\n",
        "tests": [
          {
            "input": "21",
            "output": "42"
          },
          {
            "input": "0",
            "output": "0"
          },
          {
            "input": "-3",
            "output": "-6"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_async-all",
        "title": "Await in parallel",
        "prompt": "Replace `____` with `Promise.all` over the two promises so both resolve before you sum them.",
        "hint": "Promise.all([p1, p2]) resolves to an array once both are done.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst [a, b] = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nfunction later(value: number): Promise<number> {\n  return new Promise(resolve => setTimeout(() => resolve(value), 5));\n}\nasync function main(): Promise<void> {\n  const [x, y] = ____;\n  console.log(x + y);\n}\nmain();\n",
        "solution": "import * as fs from \"fs\";\nconst [a, b] = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nfunction later(value: number): Promise<number> {\n  return new Promise(resolve => setTimeout(() => resolve(value), 5));\n}\nasync function main(): Promise<void> {\n  const [x, y] = await Promise.all([later(a), later(b)]);\n  console.log(x + y);\n}\nmain();\n",
        "tests": [
          {
            "input": "2 3",
            "output": "5"
          },
          {
            "input": "10 20",
            "output": "30"
          },
          {
            "input": "0 0",
            "output": "0"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_async-trycatch",
        "title": "Handle a rejection",
        "prompt": "`fetchValue` rejects when the input is negative. Write the async solution: await it, print the value on success, or `failed` on rejection using try/catch.",
        "hint": "await throws on rejection, so wrap it in try/catch just like synchronous errors.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst n = Number(fs.readFileSync(0, \"utf8\").trim());\nfunction fetchValue(x: number): Promise<number> {\n  return new Promise((resolve, reject) => {\n    if (x < 0) reject(new Error(\"negative\"));\n    else resolve(x);\n  });\n}\nasync function main(): Promise<void> {\n  ____\n}\nmain();\n",
        "solution": "import * as fs from \"fs\";\nconst n = Number(fs.readFileSync(0, \"utf8\").trim());\nfunction fetchValue(x: number): Promise<number> {\n  return new Promise((resolve, reject) => {\n    if (x < 0) reject(new Error(\"negative\"));\n    else resolve(x);\n  });\n}\nasync function main(): Promise<void> {\n  try {\n    const v = await fetchValue(n);\n    console.log(v);\n  } catch (e) {\n    console.log(\"failed\");\n  }\n}\nmain();\n",
        "tests": [
          {
            "input": "7",
            "output": "7"
          },
          {
            "input": "-1",
            "output": "failed"
          },
          {
            "input": "0",
            "output": "0"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_async-challenge",
        "title": "Async square-sum",
        "prompt": "The input is a line of numbers. Write a `square(n)` that returns a `Promise<number>`, then in an `async` function `await` each square and print the total sum.",
        "hint": "await square(n) inside the loop unwraps the Promise to a plain number to add up.",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Medium",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\nfunction square(n: number): Promise<number> {\n  return new Promise(resolve => resolve(n * n));\n}\nasync function main(): Promise<void> {\n  const nums = input.split(/\\s+/).map(Number);\n  let total = 0;\n  for (const n of nums) {\n    total += await square(n);\n  }\n  console.log(total);\n}\nawait main();\n",
        "tests": [
          {
            "input": "1 2 3",
            "output": "14"
          },
          {
            "input": "0",
            "output": "0"
          },
          {
            "input": "2 2 2",
            "output": "12"
          },
          {
            "input": "-3 4",
            "output": "25"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_destructuring",
    "name": "Destructuring, Spread & Rest",
    "category": "TS: Language Basics",
    "what": "Pulling values out of arrays/objects by shape, and copying/merging them with the ... operator.",
    "deep": "Destructuring unpacks a structure in one line: `const [a, b] = arr` or `const { name } = user`. The spread `...` copies an array/object into a new one (`[...a, ...b]`, `{ ...user, age: 40 }`), and rest `...` gathers leftovers (`const [first, ...rest] = arr`). This syntax appears in almost every real TypeScript file.",
    "java": "There is no Java analogue — one line replaces a pile of `arr[0]`/`getX()` calls. Spread is the idiomatic immutable copy/merge (like `List.copyOf` + edits), and rest gathers the remaining elements into a new array.",
    "language": "typescript",
    "lesson": "### Worked example\nDestructuring unpacks a structure in one line:\n```ts\nconst [a, b] = [3, 4];          // a = 3, b = 4\nconst { name, age } = { name: \"Ada\", age: 36 };\nconsole.log(a, b, name, age);   // 3 4 Ada 36\n```\n\n### Array vs object\n```ts\nconst [first, second] = [10, 20];         // by position\nconst { x, y } = { x: 1, y: 2 };           // by key name\nconst { y: height } = { y: 99 };           // rename while unpacking -> height = 99\nconst [head, ...tail] = [1, 2, 3, 4];      // head = 1, tail = [2, 3, 4]\n```\n\n### Spread — copy and merge\nThe `...` operator expands a structure into a new one:\n```ts\nconst a = [1, 2];\nconst b = [3, 4];\nconst both = [...a, ...b];                 // [1, 2, 3, 4]\n\nconst user = { name: \"Ada\", age: 36 };\nconst older = { ...user, age: 37 };        // new object; user is untouched\n```\n\n### Defaults while destructuring\n```ts\nconst { port = 8080 } = config;            // 8080 when port is missing\nconst [p = 0, q = 0] = [5];                // p = 5, q = 0\n```\n\n### Watch out for\n- Spread makes a **shallow** copy — nested objects are still shared.\n- `{ ...a, ...b }` lets later keys win, which is exactly how you 'patch' an object.\n",
    "exercises": [
      {
        "id": "ts_destructuring-array",
        "title": "Unpack an array",
        "prompt": "Replace `____` to destructure the first two elements of `nums` into `a` and `b`.",
        "hint": "const [a, b] = nums pulls out the first two elements by position.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\n____\nconsole.log(a + b);\n",
        "solution": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nconst [a, b] = nums;\nconsole.log(a + b);\n",
        "tests": [
          {
            "input": "3 4",
            "output": "7"
          },
          {
            "input": "10 20",
            "output": "30"
          },
          {
            "input": "5 6",
            "output": "11"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_destructuring-object",
        "title": "Unpack an object",
        "prompt": "Replace `____` to destructure `name` and `age` out of `user` by key.",
        "hint": "Object destructuring matches by key: const { name, age } = user.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\nconst user = { name: parts[0], age: Number(parts[1]) };\n____\nconsole.log(`${name} ${age}`);\n",
        "solution": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\nconst user = { name: parts[0], age: Number(parts[1]) };\nconst { name, age } = user;\nconsole.log(`${name} ${age}`);\n",
        "tests": [
          {
            "input": "Ada 36",
            "output": "Ada 36"
          },
          {
            "input": "Bob 40",
            "output": "Bob 40"
          },
          {
            "input": "Cid 7",
            "output": "Cid 7"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_destructuring-spread",
        "title": "Merge with spread",
        "prompt": "Two lists are read (one per line). Replace `____` with the spread expression that concatenates them into one array.",
        "hint": "[...a, ...b] expands both arrays into a new combined array.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst lines = fs.readFileSync(0, \"utf8\").split(/\\r?\\n/);\nconst a = lines[0].trim().split(/\\s+/).map(Number);\nconst b = lines[1].trim().split(/\\s+/).map(Number);\nconst merged = ____;\nconsole.log(merged.join(\" \"));\n",
        "solution": "import * as fs from \"fs\";\nconst lines = fs.readFileSync(0, \"utf8\").split(/\\r?\\n/);\nconst a = lines[0].trim().split(/\\s+/).map(Number);\nconst b = lines[1].trim().split(/\\s+/).map(Number);\nconst merged = [...a, ...b];\nconsole.log(merged.join(\" \"));\n",
        "tests": [
          {
            "input": "1 2\n3 4",
            "output": "1 2 3 4"
          },
          {
            "input": "5\n6 7",
            "output": "5 6 7"
          },
          {
            "input": "1\n2",
            "output": "1 2"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_destructuring-rest",
        "title": "Sum all but the first",
        "prompt": "Write the solution: use a rest pattern to split off the first element, then print the sum of the remaining ones.",
        "hint": "const [first, ...rest] = nums gathers everything after the first into rest.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\n____\n",
        "solution": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nconst [first, ...rest] = nums;\nconst total = rest.reduce((a, x) => a + x, 0);\nconsole.log(total);\n",
        "tests": [
          {
            "input": "1 2 3 4",
            "output": "9"
          },
          {
            "input": "10",
            "output": "0"
          },
          {
            "input": "5 5 5",
            "output": "10"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_destructuring-challenge",
        "title": "Path length",
        "prompt": "The input is a flat list of coordinates `x1 y1 x2 y2 …` describing a path. Walk consecutive points and sum the Manhattan distance (|dx| + |dy|) of each step. Print the total path length.",
        "hint": "Destructure each pair of points, add |x−px| + |y−py| to a running total.",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Easy",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\nconst nums = input.split(/\\s+/).map(Number);\nlet total = 0;\nfor (let i = 2; i < nums.length; i += 2) {\n  const [px, py, x, y] = [nums[i - 2], nums[i - 1], nums[i], nums[i + 1]];\n  total += Math.abs(x - px) + Math.abs(y - py);\n}\nconsole.log(total);\n",
        "tests": [
          {
            "input": "0 0 3 4",
            "output": "7"
          },
          {
            "input": "0 0 1 0 1 1",
            "output": "2"
          },
          {
            "input": "0 0 0 0",
            "output": "0"
          },
          {
            "input": "0 0 -3 0 -3 -4",
            "output": "7"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_assertions",
    "name": "Type Assertions (as)",
    "category": "TS: Functions & Types",
    "what": "Telling the compiler a value's type when you know more than it does — value as Type.",
    "deep": "When you know a value's type better than TypeScript can infer (a `JSON.parse` result, a value from a union, a DOM lookup), `value as Type` asserts it. It is a compile-time claim only — it does NOT convert or check anything at runtime, so a wrong assertion becomes a bug. Related: `expr!` asserts non-null, and `as const` freezes a literal's type.",
    "java": "`x as Foo` looks like a Java cast but does NOT check at runtime (no ClassCastException) — it is a pure compile-time promise, so use it sparingly and only when you are certain. `expr!` is 'trust me, not null'.",
    "language": "typescript",
    "lesson": "### Worked example\nSometimes you know a value's type better than TypeScript can. `as` tells it so:\n```ts\nconst raw: unknown = JSON.parse('{\"count\":5}');\nconst data = raw as { count: number };\nconsole.log(data.count);   // 5\n```\n\n### It is a compile-time claim only\nAn assertion does **not** convert or check anything at runtime — it just silences the type checker. If you assert wrongly, you get a runtime bug with no cast error:\n```ts\nconst n = \"hello\" as unknown as number;   // compiles, but n is really a string\n```\nSo reach for `as` only when you are genuinely certain, and prefer narrowing (`typeof`, `in`) when you can check instead.\n\n### The non-null assertion `!`\n```ts\nconst el = list.find(x => x > 10)!;   // 'I promise this isn't undefined'\n```\nUse it only when you truly know the value exists — otherwise a real `undefined` slips through.\n\n### `as const`\n```ts\nconst dir = \"N\" as const;   // type is the literal \"N\", not string\n```\n\n### Watch out for\n- `x as Foo` is not a Java cast — there is no runtime check, so a wrong assertion fails silently later.\n- Prefer a real runtime check (narrowing) over an assertion whenever one is possible.\n",
    "exercises": [
      {
        "id": "ts_assertions-parse",
        "title": "Use an asserted shape",
        "prompt": "`data` was parsed and asserted as `{ count: number }`. Replace `____` with the expression that doubles its `count`.",
        "hint": "The assertion lets you read data.count as a number; multiply it by 2.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst raw = fs.readFileSync(0, \"utf8\").trim();\nconst data = JSON.parse(raw) as { count: number };\nconsole.log(____);\n",
        "solution": "import * as fs from \"fs\";\nconst raw = fs.readFileSync(0, \"utf8\").trim();\nconst data = JSON.parse(raw) as { count: number };\nconsole.log(data.count * 2);\n",
        "tests": [
          {
            "input": "{\"count\":5}",
            "output": "10"
          },
          {
            "input": "{\"count\":0}",
            "output": "0"
          },
          {
            "input": "{\"count\":21}",
            "output": "42"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_assertions-nonnull",
        "title": "Trust a found value",
        "prompt": "`find` may return `undefined`, but here a match always exists, so a non-null assertion `!` is used. Replace `____` with the `find` call that locates the first value over 10.",
        "hint": "nums.find(x => x > 10) returns the first match; the ! after it asserts it isn't undefined.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nconst firstBig = ____!;\nconsole.log(firstBig * 2);\n",
        "solution": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nconst firstBig = nums.find(x => x > 10)!;\nconsole.log(firstBig * 2);\n",
        "tests": [
          {
            "input": "5 15 3",
            "output": "30"
          },
          {
            "input": "20 1",
            "output": "40"
          },
          {
            "input": "11 12",
            "output": "22"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_assertions-array",
        "title": "Parse and sum a JSON array",
        "prompt": "The input is a JSON array of numbers. Write the solution: parse it (asserting `number[]`) and print the sum.",
        "hint": "JSON.parse(raw) as number[] gives a typed array you can reduce.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst raw = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst raw = fs.readFileSync(0, \"utf8\").trim();\nconst nums = JSON.parse(raw) as number[];\nconst total = nums.reduce((a, x) => a + x, 0);\nconsole.log(total);\n",
        "tests": [
          {
            "input": "[1,2,3]",
            "output": "6"
          },
          {
            "input": "[10,20]",
            "output": "30"
          },
          {
            "input": "[5]",
            "output": "5"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_assertions-challenge",
        "title": "First adult",
        "prompt": "The input is a JSON array of people like `[{\"name\":\"Ada\",\"age\":36}]`. Parse it and assert the shape with `as`, then print the name of the first person aged 18 or older, or `none` if there is none.",
        "hint": "`JSON.parse(input) as Person[]` tells TypeScript the parsed value's shape.",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Medium",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\ninterface Person { name: string; age: number; }\nconst people = JSON.parse(input) as Person[];\nconst adult = people.find(p => p.age >= 18);\nif (adult) {\n  console.log(adult.name);\n} else {\n  console.log(\"none\");\n}\n",
        "tests": [
          {
            "input": "[{\"name\":\"Kid\",\"age\":10},{\"name\":\"Ada\",\"age\":36}]",
            "output": "Ada"
          },
          {
            "input": "[{\"name\":\"A\",\"age\":18}]",
            "output": "A"
          },
          {
            "input": "[{\"name\":\"B\",\"age\":5}]",
            "output": "none"
          },
          {
            "input": "[]",
            "output": "none"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_compose",
    "name": "Extending & Combining Types",
    "category": "TS: Functions & Types",
    "what": "Building bigger shapes from smaller ones with interface extends and intersection (&).",
    "deep": "Reuse shapes instead of repeating fields: `interface B extends A` adds fields to `A`, and `type C = A & B` (an intersection) requires everything from both. This is how you compose small, focused types into the exact shape a function needs — the type-level equivalent of building with blocks.",
    "java": "`interface B extends A` matches Java. The intersection `A & B` has no direct Java equivalent — it means 'has all members of A AND B at once', the way implementing two interfaces does, but as a plain type you can name and pass around.",
    "language": "typescript",
    "lesson": "### Worked example\nBuild bigger shapes out of smaller ones instead of repeating fields.\n```ts\ninterface Named { name: string; }\ninterface Person extends Named {   // adds to Named\n  age: number;\n}\nconst p: Person = { name: \"Ada\", age: 36 };   // needs BOTH fields\n```\n\n### Intersection types (&)\n`A & B` means 'has everything from A **and** B':\n```ts\ntype Point = { x: number; y: number };\ntype Labeled = { label: string };\ntype LabeledPoint = Point & Labeled;\n\nconst lp: LabeledPoint = { x: 1, y: 2, label: \"origin\" };\n```\n\n### extends vs &\n- `interface B extends A` — the interface way to add fields to a shape.\n- `type C = A & B` — the `type` way; also works on unions and primitives.\nBoth produce a shape requiring all the combined members.\n\n### Composing many small types\n```ts\ntype Timestamps = { createdAt: number; updatedAt: number };\ntype User = { id: string; name: string } & Timestamps;\n```\nSmall, focused pieces snap together into exactly the shape a function needs.\n\n### Watch out for\n- Intersection requires **all** members at once — `Point & Labeled` needs `x`, `y`, *and* `label`.\n- Don't confuse `&` (must satisfy both) with `|` (a union — may be either).\n",
    "exercises": [
      {
        "id": "ts_compose-extends",
        "title": "Satisfy an extended interface",
        "prompt": "`Person extends Named`, so it needs BOTH `name` and `age`. Replace `____` with the object literal that satisfies `Person`.",
        "hint": "An extended interface needs the inherited field (name) AND the new one (age).",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\ninterface Named { name: string; }\ninterface Person extends Named { age: number; }\nconst p: Person = ____;\nconsole.log(`${p.name} ${p.age}`);\n",
        "solution": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\ninterface Named { name: string; }\ninterface Person extends Named { age: number; }\nconst p: Person = { name: parts[0], age: Number(parts[1]) };\nconsole.log(`${p.name} ${p.age}`);\n",
        "tests": [
          {
            "input": "Ada 36",
            "output": "Ada 36"
          },
          {
            "input": "Bob 40",
            "output": "Bob 40"
          },
          {
            "input": "Cid 7",
            "output": "Cid 7"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_compose-intersection",
        "title": "Build an intersection type",
        "prompt": "`Employee` is `Person & { salary: number }`. Replace `____` with the object literal that has every required field.",
        "hint": "An intersection needs all members of both parts: name AND salary.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\ntype Person = { name: string };\ntype Employee = Person & { salary: number };\nconst e: Employee = ____;\nconsole.log(`${e.name} earns ${e.salary}`);\n",
        "solution": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\ntype Person = { name: string };\ntype Employee = Person & { salary: number };\nconst e: Employee = { name: parts[0], salary: Number(parts[1]) };\nconsole.log(`${e.name} earns ${e.salary}`);\n",
        "tests": [
          {
            "input": "Ada 100",
            "output": "Ada earns 100"
          },
          {
            "input": "Bob 50",
            "output": "Bob earns 50"
          },
          {
            "input": "Cid 7",
            "output": "Cid earns 7"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_compose-circle",
        "title": "Compose then compute",
        "prompt": "`Circle extends Point` (adds a radius). Write the solution: build the circle from the three inputs and print its area as `3 * r * r`.",
        "hint": "Circle needs x, y (from Point) and r; area uses the radius: 3 * r * r.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\ninterface Point { x: number; y: number; }\ninterface Circle extends Point { r: number; }\n____\n",
        "solution": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\ninterface Point { x: number; y: number; }\ninterface Circle extends Point { r: number; }\nconst c: Circle = { x: parts[0], y: parts[1], r: parts[2] };\nconst area = 3 * c.r * c.r;\nconsole.log(area);\n",
        "tests": [
          {
            "input": "0 0 2",
            "output": "12"
          },
          {
            "input": "1 1 10",
            "output": "300"
          },
          {
            "input": "5 5 1",
            "output": "3"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_compose-challenge",
        "title": "Labeled point",
        "prompt": "The input is `label x y`. Using an intersection type `Point & Labeled`, build one object holding all three, then print `<label>: (<x>, <y>)` on line 1 and `|x| + |y|` on line 2.",
        "hint": "Point & Labeled requires x, y, AND label together in one object.",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Medium",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\ntype Point = { x: number; y: number };\ntype Labeled = { label: string };\ntype LabeledPoint = Point & Labeled;\nconst [label, xs, ys] = input.split(/\\s+/);\nconst lp: LabeledPoint = { label, x: Number(xs), y: Number(ys) };\nconsole.log(lp.label + \": (\" + lp.x + \", \" + lp.y + \")\");\nconsole.log(Math.abs(lp.x) + Math.abs(lp.y));\n",
        "tests": [
          {
            "input": "origin 0 0",
            "output": "origin: (0, 0)\n0"
          },
          {
            "input": "A 3 4",
            "output": "A: (3, 4)\n7"
          },
          {
            "input": "p -1 -2",
            "output": "p: (-1, -2)\n3"
          },
          {
            "input": "home 5 5",
            "output": "home: (5, 5)\n10"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_json",
    "name": "JSON: parse & stringify",
    "category": "TS: Data Structures",
    "what": "Converting between JSON text and live objects with JSON.parse and JSON.stringify.",
    "deep": "`JSON.parse(text)` turns a JSON string into a live value; `JSON.stringify(value)` turns a value back into a JSON string. This is how programs read config, talk to APIs, and save state. Because `JSON.parse` returns `any`, you typically assert or validate the shape before trusting it.",
    "java": "This is built in — no Jackson/Gson needed. `JSON.stringify(obj)` serializes in field-insertion order with no spaces; `JSON.parse` gives you a plain object typed `any`, so pair it with a type assertion or a runtime check.",
    "language": "typescript",
    "lesson": "### Worked example\n```ts\nconst text = '{\"name\":\"Ada\",\"age\":36}';\nconst user = JSON.parse(text);        // live object\nconsole.log(user.name);               // Ada\n\nconst back = JSON.stringify(user);    // '{\"name\":\"Ada\",\"age\":36}'\nconsole.log(back);\n```\n\n### The two directions\n| Call | In | Out |\n|------|----|-----|\n| `JSON.parse(text)` | JSON string | a live value (`any`) |\n| `JSON.stringify(value)` | any value | a JSON string |\n\n### Typing the parsed result\n`JSON.parse` returns `any`, so it is a prime spot for an assertion (or a runtime check):\n```ts\nconst data = JSON.parse(text) as { name: string; age: number };\n```\n\n### Round-trip: read, change, write\n```ts\nconst user = JSON.parse(text) as { name: string; age: number };\nuser.age = user.age + 1;\nconsole.log(JSON.stringify(user));    // '{\"name\":\"Ada\",\"age\":37}'\n```\n\n### Watch out for\n- `JSON.stringify` outputs fields in insertion order with no spaces: `{\"a\":1,\"b\":2}`.\n- Only JSON-safe data survives: functions, `undefined`, and `Map`/`Set` are dropped or need custom handling.\n- `JSON.parse` throws on malformed text — wrap it in `try/catch` for untrusted input.\n",
    "exercises": [
      {
        "id": "ts_json-parse",
        "title": "Parse and read",
        "prompt": "Replace `____` with the call that turns the JSON text `raw` into a live object.",
        "hint": "JSON.parse(raw) converts the JSON string into an object you can read fields from.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst raw = fs.readFileSync(0, \"utf8\").trim();\nconst obj = ____;\nconsole.log(obj.name);\n",
        "solution": "import * as fs from \"fs\";\nconst raw = fs.readFileSync(0, \"utf8\").trim();\nconst obj = JSON.parse(raw);\nconsole.log(obj.name);\n",
        "tests": [
          {
            "input": "{\"name\":\"Ada\"}",
            "output": "Ada"
          },
          {
            "input": "{\"name\":\"Bob\",\"age\":3}",
            "output": "Bob"
          },
          {
            "input": "{\"name\":\"Cid\"}",
            "output": "Cid"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_json-stringify",
        "title": "Serialize to JSON",
        "prompt": "Replace `____` with the call that turns the `user` object into a JSON string.",
        "hint": "JSON.stringify(user) emits the fields in insertion order with no spaces.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\nconst user = { name: parts[0], age: Number(parts[1]) };\nconsole.log(____);\n",
        "solution": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\nconst user = { name: parts[0], age: Number(parts[1]) };\nconsole.log(JSON.stringify(user));\n",
        "tests": [
          {
            "input": "Ada 36",
            "output": "{\"name\":\"Ada\",\"age\":36}"
          },
          {
            "input": "Bob 40",
            "output": "{\"name\":\"Bob\",\"age\":40}"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_json-roundtrip",
        "title": "Read, change, write",
        "prompt": "The input is a JSON user. Write the solution: parse it, add 1 to its `age`, and print the object back as JSON.",
        "hint": "Parse to an object, mutate the field, then stringify the whole object back.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst raw = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst raw = fs.readFileSync(0, \"utf8\").trim();\nconst user = JSON.parse(raw) as { name: string; age: number };\nuser.age = user.age + 1;\nconsole.log(JSON.stringify(user));\n",
        "tests": [
          {
            "input": "{\"name\":\"Ada\",\"age\":36}",
            "output": "{\"name\":\"Ada\",\"age\":37}"
          },
          {
            "input": "{\"name\":\"Bob\",\"age\":9}",
            "output": "{\"name\":\"Bob\",\"age\":10}"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_json-challenge",
        "title": "Inventory total",
        "prompt": "The input is a JSON array of items like `[{\"name\":\"a\",\"qty\":2,\"price\":3}]`. Parse it and print the total value — the sum of `qty × price` across all items.",
        "hint": "JSON.parse gives you the array; reduce over qty * price starting from 0.",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Easy",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\ninterface Item { name: string; qty: number; price: number; }\nconst items = JSON.parse(input) as Item[];\nconst total = items.reduce((sum, it) => sum + it.qty * it.price, 0);\nconsole.log(total);\n",
        "tests": [
          {
            "input": "[{\"name\":\"a\",\"qty\":2,\"price\":3}]",
            "output": "6"
          },
          {
            "input": "[]",
            "output": "0"
          },
          {
            "input": "[{\"name\":\"x\",\"qty\":1,\"price\":10},{\"name\":\"y\",\"qty\":3,\"price\":2}]",
            "output": "16"
          },
          {
            "input": "[{\"name\":\"z\",\"qty\":0,\"price\":99}]",
            "output": "0"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_immutability",
    "name": "readonly & Immutability",
    "category": "TS: Robustness",
    "what": "Preventing change: readonly fields, as const, Object.freeze, and immutable updates via spread.",
    "deep": "Immutability means never mutating existing data — you build a new value instead. `readonly` fields and `readonly T[]` forbid reassignment at compile time; `Object.freeze(obj)` enforces it at runtime; and the everyday pattern is an immutable update with spread: `{ ...user, age: 40 }` leaves the original untouched. Immutable data is easier to reason about and avoids a whole class of bugs.",
    "java": "`readonly` is like `final` on a field, but also applies to arrays (`readonly number[]`). `const` fixes the binding; `readonly`/`Object.freeze` fix the contents — two different guarantees. Spreading to a new object is the idiomatic 'copy with changes'.",
    "language": "typescript",
    "lesson": "### Worked example\nImmutability means you never change existing data — you build a new value instead.\n```ts\nconst user = { name: \"Ada\", age: 36 };\nconst older = { ...user, age: 37 };   // new object\nconsole.log(user.age, older.age);     // 36 37  (original untouched)\n```\n\n### readonly — compile-time protection\n```ts\ninterface Config {\n  readonly id: string;        // cannot be reassigned after creation\n}\nconst nums: readonly number[] = [1, 2, 3];\n// nums.push(4);              // error: push doesn't exist on a readonly array\n```\n\n### const vs readonly\n- `const` fixes the **binding** — you can't point the name at a new value, but you can still mutate the contents.\n- `readonly` / `Object.freeze` fix the **contents**.\n```ts\nconst arr = [1, 2];\narr.push(3);        // allowed! const only locks the binding\n```\n\n### Object.freeze — runtime protection\n```ts\nconst config = Object.freeze({ limit: 10 });\n// config.limit = 20;   // throws in strict mode; silently ignored otherwise\n```\n\n### Immutable update patterns\n```ts\nconst next = [...arr, 99];               // append without mutating\nconst patched = { ...obj, done: true };  // change one field, keep the rest\n```\n\n### Watch out for\n- `const` alone does **not** make an object immutable — its fields can still change.\n- `Object.freeze` is shallow: nested objects remain mutable.\n",
    "exercises": [
      {
        "id": "ts_immutability-update",
        "title": "Immutable update",
        "prompt": "Replace `____` with a spread that makes a NEW object like `user` but with `age` one higher — leaving `user` unchanged.",
        "hint": "{ ...user, age: user.age + 1 } copies user then overrides age; the original stays put.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\nconst user = { name: parts[0], age: Number(parts[1]) };\nconst older = ____;\nconsole.log(`${user.age} ${older.age}`);\n",
        "solution": "import * as fs from \"fs\";\nconst parts = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\nconst user = { name: parts[0], age: Number(parts[1]) };\nconst older = { ...user, age: user.age + 1 };\nconsole.log(`${user.age} ${older.age}`);\n",
        "tests": [
          {
            "input": "Ada 36",
            "output": "36 37"
          },
          {
            "input": "Bob 9",
            "output": "9 10"
          },
          {
            "input": "Cid 0",
            "output": "0 1"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_immutability-append",
        "title": "Immutable append",
        "prompt": "Replace `____` with a spread that builds a NEW array: every element of `nums` followed by `99`.",
        "hint": "[...nums, 99] spreads the originals into a new array and adds 99 — nums itself is unchanged.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nconst extended = ____;\nconsole.log(`${nums.length} ${extended.length}`);\n",
        "solution": "import * as fs from \"fs\";\nconst nums = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nconst extended = [...nums, 99];\nconsole.log(`${nums.length} ${extended.length}`);\n",
        "tests": [
          {
            "input": "1 2 3",
            "output": "3 4"
          },
          {
            "input": "5",
            "output": "1 2"
          },
          {
            "input": "7 8 9 10",
            "output": "4 5"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_immutability-freeze",
        "title": "Freeze at runtime",
        "prompt": "Write the solution: freeze a config object so an attempted change can't take effect, then print its `limit` (which stays the input value).",
        "hint": "Object.freeze locks the object; the write is rejected, so limit keeps its original value.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst n = Number(fs.readFileSync(0, \"utf8\").trim());\n____\n",
        "solution": "import * as fs from \"fs\";\nconst n = Number(fs.readFileSync(0, \"utf8\").trim());\nconst config = Object.freeze({ limit: n });\ntry {\n  (config as { limit: number }).limit = 999;\n} catch (e) {\n  // frozen objects reject writes in strict mode\n}\nconsole.log(config.limit);\n",
        "tests": [
          {
            "input": "5",
            "output": "5"
          },
          {
            "input": "42",
            "output": "42"
          },
          {
            "input": "0",
            "output": "0"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_immutability-challenge",
        "title": "Immutable ledger",
        "prompt": "Line 1 is a starting balance; each later line is a delta. Build a NEW state object for every step (never mutate the old one) using spread, keeping a history array. Print `<finalBalance> <numberOfStates>`.",
        "hint": "`state = { ...state, balance: ... }` makes a fresh object; push each new state to history.",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Medium",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\ninterface State { balance: number; steps: number; }\nconst lines = input.split(\"\\n\");\nlet state: State = { balance: Number(lines[0]), steps: 0 };\nconst history: State[] = [state];\nfor (let i = 1; i < lines.length; i++) {\n  const delta = Number(lines[i]);\n  state = { ...state, balance: state.balance + delta, steps: state.steps + 1 };\n  history.push(state);\n}\nconsole.log(state.balance + \" \" + history.length);\n",
        "tests": [
          {
            "input": "100\n10\n-20",
            "output": "90 3"
          },
          {
            "input": "0",
            "output": "0 1"
          },
          {
            "input": "5\n5\n5\n5",
            "output": "20 4"
          },
          {
            "input": "50\n-50",
            "output": "0 2"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_number_math",
    "name": "Numbers & Math",
    "category": "TS: Language Basics",
    "what": "Rounding, integer division, absolute value, and the Math toolbox — plus NaN and Infinity.",
    "deep": "There is one `number` type for ints and floats, so `7 / 2` is `3.5`. When you need a whole number you round explicitly: `Math.floor`, `Math.round`, `Math.ceil`, or `Math.trunc`. The `Math` object also gives you `abs`, `min`, `max`, `pow`, and `sqrt`. Watch for `NaN` (from a failed `Number(...)`) and `Infinity` (from dividing by zero).",
    "java": "`/` never truncates — `7 / 2` is `3.5`, not `3`. Use `Math.floor(a / b)` for Java-style integer division. `Math.floor/round/ceil` match Java's Math; `x % 0` is `NaN` (not an exception) and `x / 0` is `Infinity`. Check a parse with `Number.isNaN(n)`, never `n === NaN` (which is always false).",
    "language": "typescript",
    "lesson": "### Worked example\nOne `number` type means division keeps the fraction:\n```ts\n7 / 2;             // 3.5   (NOT 3)\nMath.floor(7 / 2); // 3     integer division\nMath.trunc(-7 / 2);// -3    toward zero\n-7 % 3;            // -1    remainder keeps the sign of the left operand\n```\n\n### Rounding\n```ts\nMath.floor(3.7);  // 3   down\nMath.ceil(3.2);   // 4   up\nMath.round(3.5);  // 4   nearest (.5 rounds up)\nMath.trunc(3.9);  // 3   drop the fraction\n```\n\n### The Math toolbox\n```ts\nMath.abs(-5);        // 5\nMath.min(3, 9, 1);   // 1\nMath.max(3, 9, 1);   // 9\nMath.pow(2, 10);     // 1024   (or 2 ** 10)\nMath.sqrt(144);      // 12\n```\nSpread an array into `min`/`max`: `Math.max(...nums)`.\n\n### NaN and Infinity\n```ts\nNumber(\"oops\");      // NaN   — a failed conversion\nNumber.isNaN(Number(\"oops\")); // true  — the safe test\n1 / 0;               // Infinity\n```\n\n### Watch out for\n- `Math.round(-2.5)` is `-2` (rounds toward +∞), not `-3`.\n- `n === NaN` is **always false**. Use `Number.isNaN(n)`.\n- For big whole numbers beyond ~9 quadrillion, reach for `bigint`.\n",
    "exercises": [
      {
        "id": "ts_number_math-intdiv",
        "title": "Integer division",
        "prompt": "The input is two numbers `a b`. Replace `____` to print the integer part of `a / b` (round down with `Math.floor`).",
        "hint": "Plain a / b keeps the fraction; Math.floor rounds it down to an integer.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst [a, b] = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nconsole.log(____);\n",
        "solution": "import * as fs from \"fs\";\nconst [a, b] = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/).map(Number);\nconsole.log(Math.floor(a / b));\n",
        "tests": [
          {
            "input": "7 2",
            "output": "3"
          },
          {
            "input": "10 3",
            "output": "3"
          },
          {
            "input": "9 3",
            "output": "3"
          },
          {
            "input": "1 2",
            "output": "0"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_number_math-round",
        "title": "Round three ways",
        "prompt": "The input is one decimal number. Replace `____` to print its `Math.round`, then `Math.floor`, then `Math.ceil`, each on its own line.",
        "hint": "round = nearest, floor = down, ceil = up.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst x = Number(fs.readFileSync(0, \"utf8\").trim());\n____\n",
        "solution": "import * as fs from \"fs\";\nconst x = Number(fs.readFileSync(0, \"utf8\").trim());\nconsole.log(Math.round(x));\nconsole.log(Math.floor(x));\nconsole.log(Math.ceil(x));\n",
        "tests": [
          {
            "input": "3.5",
            "output": "4\n3\n4"
          },
          {
            "input": "2.1",
            "output": "2\n2\n3"
          },
          {
            "input": "7",
            "output": "7\n7\n7"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_number_math-challenge",
        "title": "Digit sum",
        "prompt": "The input is one integer (possibly negative). Print two numbers separated by a space: the sum of its digits and how many digits it has. Use `% 10` and `Math.floor(n / 10)`.",
        "hint": "Peel the last digit with n % 10, then drop it with Math.floor(n / 10) until n is 0.",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Intro",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\nlet n = Math.abs(Number(input));\nlet sum = 0;\nlet digits = 0;\nif (n === 0) digits = 1;\nwhile (n > 0) {\n  sum += n % 10;\n  n = Math.floor(n / 10);\n  digits++;\n}\nconsole.log(sum + \" \" + digits);\n",
        "tests": [
          {
            "input": "123",
            "output": "6 3"
          },
          {
            "input": "0",
            "output": "0 1"
          },
          {
            "input": "-45",
            "output": "9 2"
          },
          {
            "input": "1000",
            "output": "1 4"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "ts_string_methods",
    "name": "String Methods",
    "category": "TS: Language Basics",
    "what": "The everyday string toolbox: slice, indexOf/includes, replace/replaceAll, padStart, repeat, and split/join.",
    "deep": "Strings are immutable, so every method returns a NEW string. `slice(start, end)` cuts a substring, `indexOf`/`includes` search, `replaceAll` swaps text, `padStart`/`padEnd` align, `repeat` duplicates, and `split`/`join` convert to and from arrays. Chaining them is how you clean and reshape text.",
    "java": "Methods are camelCase and mostly match Java's `String` (`indexOf`, `replace`, `split`). `slice` allows negative indices (from the end) — handier than `substring`. `padStart(5, \"0\")` replaces `String.format(\"%05d\", n)`, and `\"ab\".repeat(3)` replaces a manual loop.",
    "language": "typescript",
    "lesson": "### Worked example\nEvery method returns a new string — the original is untouched:\n```ts\nconst s = \"Poodcode\";\ns.slice(0, 4);        // \"Pood\"    (start, end-exclusive)\ns.slice(-4);          // \"code\"    negative counts from the end\ns.indexOf(\"code\");    // 4         (-1 if not found)\ns.includes(\"ood\");    // true\ns.toUpperCase();      // \"POODCODE\"\n```\n\n### Reshaping text\n```ts\n\"a-b-c\".split(\"-\");            // [\"a\",\"b\",\"c\"]\n[\"a\",\"b\",\"c\"].join(\"/\");      // \"a/b/c\"\n\"  hi  \".trim();               // \"hi\"\n\"5\".padStart(3, \"0\");          // \"005\"\n\"ab\".repeat(3);                // \"ababab\"\n\"cat cat\".replaceAll(\"cat\", \"dog\"); // \"dog dog\"\n```\n\n### Chaining\n```ts\nconst clean = \"  Hello, World  \"\n  .trim()                 // \"Hello, World\"\n  .toLowerCase()          // \"hello, world\"\n  .replaceAll(\",\", \"\");   // \"hello world\"\n```\n\n### Counting occurrences\nA quick trick: `text.split(needle).length - 1` counts how many times `needle` appears.\n\n### Watch out for\n- `replace` swaps only the FIRST match; use `replaceAll` for every match.\n- `slice` and `substring` differ on negative indices — prefer `slice`.\n- Indexing past the end (`s[99]`) gives `undefined`, not an error.\n",
    "exercises": [
      {
        "id": "ts_string_methods-pad",
        "title": "Zero-pad a number",
        "prompt": "The input is a number. Replace `____` to print it as a string padded on the left with `0` to a width of 5 — `42` → `00042`.",
        "hint": "padStart(5, \"0\") adds leading zeros until the string is 5 characters wide.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst n = fs.readFileSync(0, \"utf8\").trim();\nconsole.log(____);\n",
        "solution": "import * as fs from \"fs\";\nconst n = fs.readFileSync(0, \"utf8\").trim();\nconsole.log(n.padStart(5, \"0\"));\n",
        "tests": [
          {
            "input": "42",
            "output": "00042"
          },
          {
            "input": "7",
            "output": "00007"
          },
          {
            "input": "12345",
            "output": "12345"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_string_methods-count",
        "title": "Count a character",
        "prompt": "The input is a word then a single character, like `banana a`. Replace `____` to print how many times the character appears in the word (use the split-length trick).",
        "hint": "Splitting on the character yields one more piece than there are occurrences.",
        "language": "typescript",
        "kind": "drill",
        "difficulty": "",
        "starter": "import * as fs from \"fs\";\nconst [word, ch] = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\nconsole.log(____);\n",
        "solution": "import * as fs from \"fs\";\nconst [word, ch] = fs.readFileSync(0, \"utf8\").trim().split(/\\s+/);\nconsole.log(word.split(ch).length - 1);\n",
        "tests": [
          {
            "input": "banana a",
            "output": "3"
          },
          {
            "input": "hello l",
            "output": "2"
          },
          {
            "input": "abc z",
            "output": "0"
          }
        ],
        "source_slug": ""
      },
      {
        "id": "ts_string_methods-challenge",
        "title": "Censor a word",
        "prompt": "Line 1 is a sentence; line 2 is a word to censor. Replace every occurrence of the word with a run of `*` the same length, and print the result. Use `repeat` and `split`/`join` (or `replaceAll`).",
        "hint": "\"*\".repeat(word.length) builds the mask; split(word).join(mask) swaps every occurrence.",
        "language": "typescript",
        "kind": "challenge",
        "difficulty": "Easy",
        "starter": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\n____\n",
        "solution": "import * as fs from \"fs\";\nconst input = fs.readFileSync(0, \"utf8\").trim();\nconst lines = input.split(\"\\n\");\nconst text = lines[0];\nconst word = lines[1].trim();\nconst stars = \"*\".repeat(word.length);\nconsole.log(text.split(word).join(stars));\n",
        "tests": [
          {
            "input": "the cat sat\ncat",
            "output": "the *** sat"
          },
          {
            "input": "aaa\na",
            "output": "***"
          },
          {
            "input": "hello world\nworld",
            "output": "hello *****"
          },
          {
            "input": "no match here\nxyz",
            "output": "no match here"
          }
        ],
        "source_slug": ""
      }
    ],
    "cards": [],
    "quiz": [],
    "practice": []
  },
  {
    "key": "jp_coding_basics",
    "name": "Coding Basics — プログラミングの基礎",
    "category": "JP: Coding Basics",
    "what": "The everyday words for code, running it, variables, functions and I/O.",
    "deep": "Start here. These are the words that show up in every conversation about code regardless of language — writing it, running it, and the pieces every program is built from (variables, values, functions, types).",
    "java": "The **Reading** column gives the kana pronunciation and a romaji gloss so you can say the word out loud. Loanwords written in katakana already *are* their reading, so only the romaji is shown. Example sentences are polite (です・ます) form — exactly how you'd hear them in a standup or an interview.",
    "language": "japanese",
    "lesson": "| 用語 (Term) | 読み方 (Reading) | 意味 (Meaning) | 例文 (Example) |\n|---|---|---|---|\n| プログラミング | puroguramingu | programming | 毎日プログラミングを練習しています。 — *I practice programming every day.* |\n| コード | kōdo | code | このコードを読んでください。 — *Please read this code.* |\n| プログラム | puroguramu | program | プログラムを実行します。 — *I'll run the program.* |\n| 開発 | かいはつ (kaihatsu) | development | 新しいアプリを開発しています。 — *I'm developing a new app.* |\n| 実装 | じっそう (jissō) | implementation | この機能を実装してください。 — *Please implement this feature.* |\n| 機能 | きのう (kinō) | feature / function | この機能はまだできていません。 — *This feature isn't done yet.* |\n| 変数 | へんすう (hensū) | variable | 変数に値を代入します。 — *We assign a value to a variable.* |\n| 値 | あたい (atai) | value | この変数の値は10です。 — *This variable's value is 10.* |\n| 代入 | だいにゅう (dainyū) | assignment | 変数xに5を代入します。 — *Assign 5 to variable x.* |\n| 関数 | かんすう (kansū) | function | この関数は合計を返します。 — *This function returns the sum.* |\n| 引数 | ひきすう (hikisū) | argument / parameter | 関数に引数を渡します。 — *We pass an argument to the function.* |\n| 戻り値 | もどりち (modorichi) | return value | 戻り値は整数です。 — *The return value is an integer.* |\n| 返す | かえす (kaesu) | to return (a value) | 結果を返してください。 — *Please return the result.* |\n| 呼び出す | よびだす (yobidasu) | to call / invoke | この関数を呼び出します。 — *We call this function.* |\n| 型 | かた (kata) | type | この変数の型は文字列です。 — *This variable's type is string.* |\n| 整数 | せいすう (seisū) | integer | 整数を入力してください。 — *Please enter an integer.* |\n| 文字列 | もじれつ (mojiretsu) | string | 二つの文字列を連結します。 — *We concatenate the two strings.* |\n| 真偽値 | しんぎち (shingichi) | boolean | 真偽値はtrueかfalseです。 — *A boolean is true or false.* |\n| 条件分岐 | じょうけんぶんき (jōken bunki) | conditional branch | 条件分岐でif文を使います。 — *We use an if statement for branching.* |\n| 繰り返し | くりかえし (kurikaeshi) | loop / iteration | 繰り返しで配列を処理します。 — *We process the array with a loop.* |\n| 処理 | しょり (shori) | processing | このデータを処理します。 — *We process this data.* |\n| 実行 | じっこう (jikkō) | execution / run | プログラムを実行しました。 — *I ran the program.* |\n| コンパイル | konpairu | compile | ソースコードをコンパイルします。 — *We compile the source code.* |\n| コメント | komento | comment | コードにコメントを書きます。 — *I write comments in the code.* |\n| 入力 | にゅうりょく (nyūryoku) | input | 数字を入力します。 — *We input a number.* |\n| 出力 | しゅつりょく (shutsuryoku) | output | 結果を出力します。 — *We output the result.* |\n",
    "exercises": [],
    "cards": [
      {
        "front": "プログラミング",
        "reading": "puroguramingu",
        "meaning": "programming",
        "example_ja": "毎日プログラミングを練習しています。",
        "example_en": "I practice programming every day."
      },
      {
        "front": "コード",
        "reading": "kōdo",
        "meaning": "code",
        "example_ja": "このコードを読んでください。",
        "example_en": "Please read this code."
      },
      {
        "front": "プログラム",
        "reading": "puroguramu",
        "meaning": "program",
        "example_ja": "プログラムを実行します。",
        "example_en": "I'll run the program."
      },
      {
        "front": "開発",
        "reading": "かいはつ (kaihatsu)",
        "meaning": "development",
        "example_ja": "新しいアプリを開発しています。",
        "example_en": "I'm developing a new app."
      },
      {
        "front": "実装",
        "reading": "じっそう (jissō)",
        "meaning": "implementation",
        "example_ja": "この機能を実装してください。",
        "example_en": "Please implement this feature."
      },
      {
        "front": "機能",
        "reading": "きのう (kinō)",
        "meaning": "feature / function",
        "example_ja": "この機能はまだできていません。",
        "example_en": "This feature isn't done yet."
      },
      {
        "front": "変数",
        "reading": "へんすう (hensū)",
        "meaning": "variable",
        "example_ja": "変数に値を代入します。",
        "example_en": "We assign a value to a variable."
      },
      {
        "front": "値",
        "reading": "あたい (atai)",
        "meaning": "value",
        "example_ja": "この変数の値は10です。",
        "example_en": "This variable's value is 10."
      },
      {
        "front": "代入",
        "reading": "だいにゅう (dainyū)",
        "meaning": "assignment",
        "example_ja": "変数xに5を代入します。",
        "example_en": "Assign 5 to variable x."
      },
      {
        "front": "関数",
        "reading": "かんすう (kansū)",
        "meaning": "function",
        "example_ja": "この関数は合計を返します。",
        "example_en": "This function returns the sum."
      },
      {
        "front": "引数",
        "reading": "ひきすう (hikisū)",
        "meaning": "argument / parameter",
        "example_ja": "関数に引数を渡します。",
        "example_en": "We pass an argument to the function."
      },
      {
        "front": "戻り値",
        "reading": "もどりち (modorichi)",
        "meaning": "return value",
        "example_ja": "戻り値は整数です。",
        "example_en": "The return value is an integer."
      },
      {
        "front": "返す",
        "reading": "かえす (kaesu)",
        "meaning": "to return (a value)",
        "example_ja": "結果を返してください。",
        "example_en": "Please return the result."
      },
      {
        "front": "呼び出す",
        "reading": "よびだす (yobidasu)",
        "meaning": "to call / invoke",
        "example_ja": "この関数を呼び出します。",
        "example_en": "We call this function."
      },
      {
        "front": "型",
        "reading": "かた (kata)",
        "meaning": "type",
        "example_ja": "この変数の型は文字列です。",
        "example_en": "This variable's type is string."
      },
      {
        "front": "整数",
        "reading": "せいすう (seisū)",
        "meaning": "integer",
        "example_ja": "整数を入力してください。",
        "example_en": "Please enter an integer."
      },
      {
        "front": "文字列",
        "reading": "もじれつ (mojiretsu)",
        "meaning": "string",
        "example_ja": "二つの文字列を連結します。",
        "example_en": "We concatenate the two strings."
      },
      {
        "front": "真偽値",
        "reading": "しんぎち (shingichi)",
        "meaning": "boolean",
        "example_ja": "真偽値はtrueかfalseです。",
        "example_en": "A boolean is true or false."
      },
      {
        "front": "条件分岐",
        "reading": "じょうけんぶんき (jōken bunki)",
        "meaning": "conditional branch",
        "example_ja": "条件分岐でif文を使います。",
        "example_en": "We use an if statement for branching."
      },
      {
        "front": "繰り返し",
        "reading": "くりかえし (kurikaeshi)",
        "meaning": "loop / iteration",
        "example_ja": "繰り返しで配列を処理します。",
        "example_en": "We process the array with a loop."
      },
      {
        "front": "処理",
        "reading": "しょり (shori)",
        "meaning": "processing",
        "example_ja": "このデータを処理します。",
        "example_en": "We process this data."
      },
      {
        "front": "実行",
        "reading": "じっこう (jikkō)",
        "meaning": "execution / run",
        "example_ja": "プログラムを実行しました。",
        "example_en": "I ran the program."
      },
      {
        "front": "コンパイル",
        "reading": "konpairu",
        "meaning": "compile",
        "example_ja": "ソースコードをコンパイルします。",
        "example_en": "We compile the source code."
      },
      {
        "front": "コメント",
        "reading": "komento",
        "meaning": "comment",
        "example_ja": "コードにコメントを書きます。",
        "example_en": "I write comments in the code."
      },
      {
        "front": "入力",
        "reading": "にゅうりょく (nyūryoku)",
        "meaning": "input",
        "example_ja": "数字を入力します。",
        "example_en": "We input a number."
      },
      {
        "front": "出力",
        "reading": "しゅつりょく (shutsuryoku)",
        "meaning": "output",
        "example_ja": "結果を出力します。",
        "example_en": "We output the result."
      }
    ],
    "quiz": [],
    "practice": []
  },
  {
    "key": "jp_java_lang",
    "name": "Java Language — Java言語",
    "category": "JP: Java Language",
    "what": "Classes, methods, objects, inheritance and the vocabulary of Java itself.",
    "deep": "Java's building blocks have standard Japanese names. Note that many are katakana loanwords (クラス, メソッド, オブジェクト) while the OOP concepts tend to be kanji compounds (継承, 多態性, 抽象).",
    "java": "The **Reading** column gives the kana pronunciation and a romaji gloss so you can say the word out loud. Loanwords written in katakana already *are* their reading, so only the romaji is shown. Example sentences are polite (です・ます) form — exactly how you'd hear them in a standup or an interview.",
    "language": "japanese",
    "lesson": "| 用語 (Term) | 読み方 (Reading) | 意味 (Meaning) | 例文 (Example) |\n|---|---|---|---|\n| クラス | kurasu | class | 新しいクラスを作ります。 — *We create a new class.* |\n| メソッド | mesoddo | method | このメソッドを呼び出します。 — *We call this method.* |\n| オブジェクト | obujekuto | object | オブジェクトを生成します。 — *We instantiate an object.* |\n| インスタンス | insutansu | instance | クラスのインスタンスを作ります。 — *We create an instance of the class.* |\n| フィールド | fīrudo | field (member variable) | このクラスには三つのフィールドがあります。 — *This class has three fields.* |\n| コンストラクタ | konsutorakuta | constructor | コンストラクタで初期化します。 — *We initialize in the constructor.* |\n| 継承 | けいしょう (keishō) | inheritance | このクラスは親クラスを継承します。 — *This class inherits from the parent class.* |\n| 親クラス | おやクラス (oya kurasu) | parent / superclass | 親クラスのメソッドを使います。 — *We use the parent class's method.* |\n| インターフェース | intāfēsu | interface | インターフェースを実装します。 — *We implement the interface.* |\n| 抽象クラス | ちゅうしょうクラス (chūshō kurasu) | abstract class | 抽象クラスはインスタンス化できません。 — *An abstract class can't be instantiated.* |\n| カプセル化 | カプセルか (kapuseru-ka) | encapsulation | カプセル化でデータを守ります。 — *Encapsulation protects the data.* |\n| 多態性 | たたいせい (tataisei) | polymorphism | 多態性で同じメソッド名を使えます。 — *Polymorphism lets us reuse the same method name.* |\n| オーバーライド | ōbāraido | override | メソッドをオーバーライドします。 — *We override the method.* |\n| ジェネリクス | jenerikusu | generics | ジェネリクスで型を指定します。 — *We specify the type with generics.* |\n| 修飾子 | しゅうしょくし (shūshokushi) | (access) modifier | privateは修飾子です。 — *`private` is a modifier.* |\n| 静的 | せいてき (seiteki) | static | 静的メソッドはクラスに属します。 — *A static method belongs to the class.* |\n| パッケージ | pakkēji | package | このクラスは同じパッケージにあります。 — *This class is in the same package.* |\n| インポート | inpōto | import | ライブラリをインポートします。 — *We import the library.* |\n| アノテーション | anotēshon | annotation | @Overrideはアノテーションです。 — *`@Override` is an annotation.* |\n| ライブラリ | raiburari | library | 標準ライブラリを使います。 — *We use the standard library.* |\n| フレームワーク | furēmuwāku | framework | このフレームワークは便利です。 — *This framework is convenient.* |\n| 定数 | ていすう (teisū) | constant | 定数はfinalで宣言します。 — *We declare a constant with `final`.* |\n| 宣言 | せんげん (sengen) | declaration | 変数を宣言します。 — *We declare a variable.* |\n| 初期化 | しょきか (shokika) | initialization | リストを初期化します。 — *We initialize the list.* |\n| 継承関係 | けいしょうかんけい (keishō kankei) | inheritance relationship | 二つのクラスは継承関係にあります。 — *The two classes have an inheritance relationship.* |\n| 親子関係 | おやこかんけい (oyako kankei) | parent-child relationship | クラスの親子関係を確認します。 — *We check the class parent-child relationship.* |\n",
    "exercises": [],
    "cards": [
      {
        "front": "クラス",
        "reading": "kurasu",
        "meaning": "class",
        "example_ja": "新しいクラスを作ります。",
        "example_en": "We create a new class."
      },
      {
        "front": "メソッド",
        "reading": "mesoddo",
        "meaning": "method",
        "example_ja": "このメソッドを呼び出します。",
        "example_en": "We call this method."
      },
      {
        "front": "オブジェクト",
        "reading": "obujekuto",
        "meaning": "object",
        "example_ja": "オブジェクトを生成します。",
        "example_en": "We instantiate an object."
      },
      {
        "front": "インスタンス",
        "reading": "insutansu",
        "meaning": "instance",
        "example_ja": "クラスのインスタンスを作ります。",
        "example_en": "We create an instance of the class."
      },
      {
        "front": "フィールド",
        "reading": "fīrudo",
        "meaning": "field (member variable)",
        "example_ja": "このクラスには三つのフィールドがあります。",
        "example_en": "This class has three fields."
      },
      {
        "front": "コンストラクタ",
        "reading": "konsutorakuta",
        "meaning": "constructor",
        "example_ja": "コンストラクタで初期化します。",
        "example_en": "We initialize in the constructor."
      },
      {
        "front": "継承",
        "reading": "けいしょう (keishō)",
        "meaning": "inheritance",
        "example_ja": "このクラスは親クラスを継承します。",
        "example_en": "This class inherits from the parent class."
      },
      {
        "front": "親クラス",
        "reading": "おやクラス (oya kurasu)",
        "meaning": "parent / superclass",
        "example_ja": "親クラスのメソッドを使います。",
        "example_en": "We use the parent class's method."
      },
      {
        "front": "インターフェース",
        "reading": "intāfēsu",
        "meaning": "interface",
        "example_ja": "インターフェースを実装します。",
        "example_en": "We implement the interface."
      },
      {
        "front": "抽象クラス",
        "reading": "ちゅうしょうクラス (chūshō kurasu)",
        "meaning": "abstract class",
        "example_ja": "抽象クラスはインスタンス化できません。",
        "example_en": "An abstract class can't be instantiated."
      },
      {
        "front": "カプセル化",
        "reading": "カプセルか (kapuseru-ka)",
        "meaning": "encapsulation",
        "example_ja": "カプセル化でデータを守ります。",
        "example_en": "Encapsulation protects the data."
      },
      {
        "front": "多態性",
        "reading": "たたいせい (tataisei)",
        "meaning": "polymorphism",
        "example_ja": "多態性で同じメソッド名を使えます。",
        "example_en": "Polymorphism lets us reuse the same method name."
      },
      {
        "front": "オーバーライド",
        "reading": "ōbāraido",
        "meaning": "override",
        "example_ja": "メソッドをオーバーライドします。",
        "example_en": "We override the method."
      },
      {
        "front": "ジェネリクス",
        "reading": "jenerikusu",
        "meaning": "generics",
        "example_ja": "ジェネリクスで型を指定します。",
        "example_en": "We specify the type with generics."
      },
      {
        "front": "修飾子",
        "reading": "しゅうしょくし (shūshokushi)",
        "meaning": "(access) modifier",
        "example_ja": "privateは修飾子です。",
        "example_en": "`private` is a modifier."
      },
      {
        "front": "静的",
        "reading": "せいてき (seiteki)",
        "meaning": "static",
        "example_ja": "静的メソッドはクラスに属します。",
        "example_en": "A static method belongs to the class."
      },
      {
        "front": "パッケージ",
        "reading": "pakkēji",
        "meaning": "package",
        "example_ja": "このクラスは同じパッケージにあります。",
        "example_en": "This class is in the same package."
      },
      {
        "front": "インポート",
        "reading": "inpōto",
        "meaning": "import",
        "example_ja": "ライブラリをインポートします。",
        "example_en": "We import the library."
      },
      {
        "front": "アノテーション",
        "reading": "anotēshon",
        "meaning": "annotation",
        "example_ja": "@Overrideはアノテーションです。",
        "example_en": "`@Override` is an annotation."
      },
      {
        "front": "ライブラリ",
        "reading": "raiburari",
        "meaning": "library",
        "example_ja": "標準ライブラリを使います。",
        "example_en": "We use the standard library."
      },
      {
        "front": "フレームワーク",
        "reading": "furēmuwāku",
        "meaning": "framework",
        "example_ja": "このフレームワークは便利です。",
        "example_en": "This framework is convenient."
      },
      {
        "front": "定数",
        "reading": "ていすう (teisū)",
        "meaning": "constant",
        "example_ja": "定数はfinalで宣言します。",
        "example_en": "We declare a constant with `final`."
      },
      {
        "front": "宣言",
        "reading": "せんげん (sengen)",
        "meaning": "declaration",
        "example_ja": "変数を宣言します。",
        "example_en": "We declare a variable."
      },
      {
        "front": "初期化",
        "reading": "しょきか (shokika)",
        "meaning": "initialization",
        "example_ja": "リストを初期化します。",
        "example_en": "We initialize the list."
      },
      {
        "front": "継承関係",
        "reading": "けいしょうかんけい (keishō kankei)",
        "meaning": "inheritance relationship",
        "example_ja": "二つのクラスは継承関係にあります。",
        "example_en": "The two classes have an inheritance relationship."
      },
      {
        "front": "親子関係",
        "reading": "おやこかんけい (oyako kankei)",
        "meaning": "parent-child relationship",
        "example_ja": "クラスの親子関係を確認します。",
        "example_en": "We check the class parent-child relationship."
      }
    ],
    "quiz": [],
    "practice": []
  },
  {
    "key": "jp_control_flow",
    "name": "Control Flow & Operators — 制御構文と演算子",
    "category": "JP: Control Flow",
    "what": "Conditions, loops, comparisons and the operator vocabulary.",
    "deep": "Once you can name a variable, you need to name what you do with it: test a condition, branch, loop, break out, compare two values. These words come up constantly when you talk through your solution out loud.",
    "java": "The **Reading** column gives the kana pronunciation and a romaji gloss so you can say the word out loud. Loanwords written in katakana already *are* their reading, so only the romaji is shown. Example sentences are polite (です・ます) form — exactly how you'd hear them in a standup or an interview.",
    "language": "japanese",
    "lesson": "| 用語 (Term) | 読み方 (Reading) | 意味 (Meaning) | 例文 (Example) |\n|---|---|---|---|\n| 条件 | じょうけん (jōken) | condition | 条件が真なら実行します。 — *If the condition is true, it runs.* |\n| 分岐 | ぶんき (bunki) | branch | ここでコードが分岐します。 — *The code branches here.* |\n| 真 | しん (shin) | true | 条件は真です。 — *The condition is true.* |\n| 偽 | ぎ (gi) | false | 結果は偽でした。 — *The result was false.* |\n| 比較 | ひかく (hikaku) | comparison | 二つの値を比較します。 — *We compare the two values.* |\n| 演算子 | えんざんし (enzanshi) | operator | プラスは算術演算子です。 — *Plus is an arithmetic operator.* |\n| 論理演算 | ろんりえんざん (ronri enzan) | logical operation | 論理演算でANDを使います。 — *We use AND in a logical operation.* |\n| 反復 | はんぷく (hanpuku) | iteration | for文で反復します。 — *We iterate with a for loop.* |\n| 再帰 | さいき (saiki) | recursion | この関数は再帰を使います。 — *This function uses recursion.* |\n| 無限ループ | むげんループ (mugen rūpu) | infinite loop | 無限ループに注意してください。 — *Watch out for infinite loops.* |\n| 抜ける | ぬける (nukeru) | to break / exit a loop | breakでループを抜けます。 — *We exit the loop with `break`.* |\n| 続ける | つづける (tsuzukeru) | to continue | continueで次の反復に進みます。 — *We move to the next iteration with `continue`.* |\n| 場合 | ばあい (baai) | case | この場合、xは0です。 — *In this case, x is 0.* |\n| 三項演算子 | さんこうえんざんし (sankō enzanshi) | ternary operator | 三項演算子で短く書けます。 — *We can write it shorter with the ternary operator.* |\n| 剰余 | じょうよ (jōyo) | remainder / modulo | 剰余で偶数かどうか判定します。 — *We check for even numbers with the remainder.* |\n| 加算 | かさん (kasan) | addition | 二つの数を加算します。 — *We add the two numbers.* |\n| 判定 | はんてい (hantei) | check / determination | 素数かどうか判定します。 — *We check whether it's prime.* |\n| 等しい | ひとしい (hitoshii) | equal | 二つの値が等しいか確認します。 — *We check if the two values are equal.* |\n| 以上 | いじょう (ijō) | greater than or equal | xが5以上なら実行します。 — *Run if x is 5 or more.* |\n| 以下 | いか (ika) | less than or equal | yが10以下か確認します。 — *We check if y is 10 or less.* |\n| 初期値 | しょきち (shokichi) | initial value | カウンタの初期値は0です。 — *The counter's initial value is 0.* |\n| 真偽 | しんぎ (shingi) | true-or-false | 条件の真偽を確認します。 — *We check the truth of the condition.* |\n",
    "exercises": [],
    "cards": [
      {
        "front": "条件",
        "reading": "じょうけん (jōken)",
        "meaning": "condition",
        "example_ja": "条件が真なら実行します。",
        "example_en": "If the condition is true, it runs."
      },
      {
        "front": "分岐",
        "reading": "ぶんき (bunki)",
        "meaning": "branch",
        "example_ja": "ここでコードが分岐します。",
        "example_en": "The code branches here."
      },
      {
        "front": "真",
        "reading": "しん (shin)",
        "meaning": "true",
        "example_ja": "条件は真です。",
        "example_en": "The condition is true."
      },
      {
        "front": "偽",
        "reading": "ぎ (gi)",
        "meaning": "false",
        "example_ja": "結果は偽でした。",
        "example_en": "The result was false."
      },
      {
        "front": "比較",
        "reading": "ひかく (hikaku)",
        "meaning": "comparison",
        "example_ja": "二つの値を比較します。",
        "example_en": "We compare the two values."
      },
      {
        "front": "演算子",
        "reading": "えんざんし (enzanshi)",
        "meaning": "operator",
        "example_ja": "プラスは算術演算子です。",
        "example_en": "Plus is an arithmetic operator."
      },
      {
        "front": "論理演算",
        "reading": "ろんりえんざん (ronri enzan)",
        "meaning": "logical operation",
        "example_ja": "論理演算でANDを使います。",
        "example_en": "We use AND in a logical operation."
      },
      {
        "front": "反復",
        "reading": "はんぷく (hanpuku)",
        "meaning": "iteration",
        "example_ja": "for文で反復します。",
        "example_en": "We iterate with a for loop."
      },
      {
        "front": "再帰",
        "reading": "さいき (saiki)",
        "meaning": "recursion",
        "example_ja": "この関数は再帰を使います。",
        "example_en": "This function uses recursion."
      },
      {
        "front": "無限ループ",
        "reading": "むげんループ (mugen rūpu)",
        "meaning": "infinite loop",
        "example_ja": "無限ループに注意してください。",
        "example_en": "Watch out for infinite loops."
      },
      {
        "front": "抜ける",
        "reading": "ぬける (nukeru)",
        "meaning": "to break / exit a loop",
        "example_ja": "breakでループを抜けます。",
        "example_en": "We exit the loop with `break`."
      },
      {
        "front": "続ける",
        "reading": "つづける (tsuzukeru)",
        "meaning": "to continue",
        "example_ja": "continueで次の反復に進みます。",
        "example_en": "We move to the next iteration with `continue`."
      },
      {
        "front": "場合",
        "reading": "ばあい (baai)",
        "meaning": "case",
        "example_ja": "この場合、xは0です。",
        "example_en": "In this case, x is 0."
      },
      {
        "front": "三項演算子",
        "reading": "さんこうえんざんし (sankō enzanshi)",
        "meaning": "ternary operator",
        "example_ja": "三項演算子で短く書けます。",
        "example_en": "We can write it shorter with the ternary operator."
      },
      {
        "front": "剰余",
        "reading": "じょうよ (jōyo)",
        "meaning": "remainder / modulo",
        "example_ja": "剰余で偶数かどうか判定します。",
        "example_en": "We check for even numbers with the remainder."
      },
      {
        "front": "加算",
        "reading": "かさん (kasan)",
        "meaning": "addition",
        "example_ja": "二つの数を加算します。",
        "example_en": "We add the two numbers."
      },
      {
        "front": "判定",
        "reading": "はんてい (hantei)",
        "meaning": "check / determination",
        "example_ja": "素数かどうか判定します。",
        "example_en": "We check whether it's prime."
      },
      {
        "front": "等しい",
        "reading": "ひとしい (hitoshii)",
        "meaning": "equal",
        "example_ja": "二つの値が等しいか確認します。",
        "example_en": "We check if the two values are equal."
      },
      {
        "front": "以上",
        "reading": "いじょう (ijō)",
        "meaning": "greater than or equal",
        "example_ja": "xが5以上なら実行します。",
        "example_en": "Run if x is 5 or more."
      },
      {
        "front": "以下",
        "reading": "いか (ika)",
        "meaning": "less than or equal",
        "example_ja": "yが10以下か確認します。",
        "example_en": "We check if y is 10 or less."
      },
      {
        "front": "初期値",
        "reading": "しょきち (shokichi)",
        "meaning": "initial value",
        "example_ja": "カウンタの初期値は0です。",
        "example_en": "The counter's initial value is 0."
      },
      {
        "front": "真偽",
        "reading": "しんぎ (shingi)",
        "meaning": "true-or-false",
        "example_ja": "条件の真偽を確認します。",
        "example_en": "We check the truth of the condition."
      }
    ],
    "quiz": [],
    "practice": []
  },
  {
    "key": "jp_data_structures",
    "name": "Data Structures & Algorithms — データ構造とアルゴリズム",
    "category": "JP: Data Structures",
    "what": "Arrays, lists, trees, graphs, sorting, search and complexity.",
    "deep": "This is the heart of a technical interview in Japanese. You'll be asked to pick a データ構造 (data structure), describe an アルゴリズム (algorithm), and state its 計算量 (complexity). These are the exact words for that.",
    "java": "The **Reading** column gives the kana pronunciation and a romaji gloss so you can say the word out loud. Loanwords written in katakana already *are* their reading, so only the romaji is shown. Example sentences are polite (です・ます) form — exactly how you'd hear them in a standup or an interview.",
    "language": "japanese",
    "lesson": "| 用語 (Term) | 読み方 (Reading) | 意味 (Meaning) | 例文 (Example) |\n|---|---|---|---|\n| データ構造 | dēta kōzō | data structure | 適切なデータ構造を選びます。 — *We choose the right data structure.* |\n| アルゴリズム | arugorizumu | algorithm | 効率の良いアルゴリズムを考えます。 — *We think of an efficient algorithm.* |\n| 配列 | はいれつ (hairetsu) | array | 配列を昇順に並べます。 — *We arrange the array in ascending order.* |\n| 連結リスト | れんけつリスト (renketsu risuto) | linked list | 連結リストにノードを追加します。 — *We add a node to the linked list.* |\n| スタック | sutakku | stack | スタックは後入れ先出しです。 — *A stack is last-in, first-out.* |\n| キュー | kyū | queue | キューは先入れ先出しです。 — *A queue is first-in, first-out.* |\n| 木構造 | きこうぞう (ki kōzō) | tree (structure) | 木構造を再帰で探索します。 — *We traverse the tree recursively.* |\n| 二分木 | にぶんぎ (nibungi) | binary tree | 二分木には左と右の子があります。 — *A binary tree has left and right children.* |\n| 二分探索木 | にぶんたんさくぎ (nibun tansakugi) | binary search tree | 二分探索木は整列されています。 — *A binary search tree is kept sorted.* |\n| グラフ | gurafu | graph | グラフを幅優先で探索します。 — *We traverse the graph breadth-first.* |\n| ノード | nōdo | node | 各ノードは値を持ちます。 — *Each node holds a value.* |\n| 頂点 | ちょうてん (chōten) | vertex | グラフには頂点と辺があります。 — *A graph has vertices and edges.* |\n| 辺 | へん (hen) | edge | 二つの頂点を辺でつなぎます。 — *We connect two vertices with an edge.* |\n| ハッシュマップ | hasshu mappu | hash map | ハッシュマップで高速に検索します。 — *We search quickly with a hash map.* |\n| 連想配列 | れんそうはいれつ (rensō hairetsu) | associative array (map) | 連想配列はキーと値を持ちます。 — *An associative array has keys and values.* |\n| 探索 | たんさく (tansaku) | search | 二分探索で要素を探します。 — *We find the element with binary search.* |\n| 整列 | せいれつ (seiretsu) / ソート | sorting | 配列をソートします。 — *We sort the array.* |\n| 昇順 | しょうじゅん (shōjun) | ascending order | 昇順に並べ替えます。 — *We sort in ascending order.* |\n| 降順 | こうじゅん (kōjun) | descending order | 降順に並べ替えます。 — *We sort in descending order.* |\n| 計算量 | けいさんりょう (keisanryō) | computational complexity | このアルゴリズムの計算量はO(n)です。 — *This algorithm's complexity is O(n).* |\n| 時間計算量 | じかんけいさんりょう (jikan keisanryō) | time complexity | 時間計算量を見積もります。 — *We estimate the time complexity.* |\n| 空間計算量 | くうかんけいさんりょう (kūkan keisanryō) | space complexity | 空間計算量を減らします。 — *We reduce the space complexity.* |\n| 効率 | こうりつ (kōritsu) | efficiency | コードの効率を上げます。 — *We improve the code's efficiency.* |\n| 要素 | ようそ (yōso) | element | リストの要素を数えます。 — *We count the list's elements.* |\n| 添字 | そえじ (soeji) | index (subscript) | 添字で配列にアクセスします。 — *We access the array by index.* |\n| 再帰呼び出し | さいきよびだし (saiki yobidashi) | recursive call | 再帰呼び出しで木をたどります。 — *We traverse the tree with recursive calls.* |\n| 幅優先探索 | はばゆうせんたんさく (haba yūsen tansaku) | breadth-first search (BFS) | 幅優先探索で最短経路を求めます。 — *We find the shortest path with BFS.* |\n| 深さ優先探索 | ふかさゆうせんたんさく (fukasa yūsen tansaku) | depth-first search (DFS) | 深さ優先探索で全経路を調べます。 — *We examine all paths with DFS.* |\n| 動的計画法 | どうてきけいかくほう (dōteki keikakuhō) | dynamic programming | 動的計画法で最適解を求めます。 — *We find the optimal solution with DP.* |\n",
    "exercises": [],
    "cards": [
      {
        "front": "データ構造",
        "reading": "dēta kōzō",
        "meaning": "data structure",
        "example_ja": "適切なデータ構造を選びます。",
        "example_en": "We choose the right data structure."
      },
      {
        "front": "アルゴリズム",
        "reading": "arugorizumu",
        "meaning": "algorithm",
        "example_ja": "効率の良いアルゴリズムを考えます。",
        "example_en": "We think of an efficient algorithm."
      },
      {
        "front": "配列",
        "reading": "はいれつ (hairetsu)",
        "meaning": "array",
        "example_ja": "配列を昇順に並べます。",
        "example_en": "We arrange the array in ascending order."
      },
      {
        "front": "連結リスト",
        "reading": "れんけつリスト (renketsu risuto)",
        "meaning": "linked list",
        "example_ja": "連結リストにノードを追加します。",
        "example_en": "We add a node to the linked list."
      },
      {
        "front": "スタック",
        "reading": "sutakku",
        "meaning": "stack",
        "example_ja": "スタックは後入れ先出しです。",
        "example_en": "A stack is last-in, first-out."
      },
      {
        "front": "キュー",
        "reading": "kyū",
        "meaning": "queue",
        "example_ja": "キューは先入れ先出しです。",
        "example_en": "A queue is first-in, first-out."
      },
      {
        "front": "木構造",
        "reading": "きこうぞう (ki kōzō)",
        "meaning": "tree (structure)",
        "example_ja": "木構造を再帰で探索します。",
        "example_en": "We traverse the tree recursively."
      },
      {
        "front": "二分木",
        "reading": "にぶんぎ (nibungi)",
        "meaning": "binary tree",
        "example_ja": "二分木には左と右の子があります。",
        "example_en": "A binary tree has left and right children."
      },
      {
        "front": "二分探索木",
        "reading": "にぶんたんさくぎ (nibun tansakugi)",
        "meaning": "binary search tree",
        "example_ja": "二分探索木は整列されています。",
        "example_en": "A binary search tree is kept sorted."
      },
      {
        "front": "グラフ",
        "reading": "gurafu",
        "meaning": "graph",
        "example_ja": "グラフを幅優先で探索します。",
        "example_en": "We traverse the graph breadth-first."
      },
      {
        "front": "ノード",
        "reading": "nōdo",
        "meaning": "node",
        "example_ja": "各ノードは値を持ちます。",
        "example_en": "Each node holds a value."
      },
      {
        "front": "頂点",
        "reading": "ちょうてん (chōten)",
        "meaning": "vertex",
        "example_ja": "グラフには頂点と辺があります。",
        "example_en": "A graph has vertices and edges."
      },
      {
        "front": "辺",
        "reading": "へん (hen)",
        "meaning": "edge",
        "example_ja": "二つの頂点を辺でつなぎます。",
        "example_en": "We connect two vertices with an edge."
      },
      {
        "front": "ハッシュマップ",
        "reading": "hasshu mappu",
        "meaning": "hash map",
        "example_ja": "ハッシュマップで高速に検索します。",
        "example_en": "We search quickly with a hash map."
      },
      {
        "front": "連想配列",
        "reading": "れんそうはいれつ (rensō hairetsu)",
        "meaning": "associative array (map)",
        "example_ja": "連想配列はキーと値を持ちます。",
        "example_en": "An associative array has keys and values."
      },
      {
        "front": "探索",
        "reading": "たんさく (tansaku)",
        "meaning": "search",
        "example_ja": "二分探索で要素を探します。",
        "example_en": "We find the element with binary search."
      },
      {
        "front": "整列",
        "reading": "せいれつ (seiretsu) / ソート",
        "meaning": "sorting",
        "example_ja": "配列をソートします。",
        "example_en": "We sort the array."
      },
      {
        "front": "昇順",
        "reading": "しょうじゅん (shōjun)",
        "meaning": "ascending order",
        "example_ja": "昇順に並べ替えます。",
        "example_en": "We sort in ascending order."
      },
      {
        "front": "降順",
        "reading": "こうじゅん (kōjun)",
        "meaning": "descending order",
        "example_ja": "降順に並べ替えます。",
        "example_en": "We sort in descending order."
      },
      {
        "front": "計算量",
        "reading": "けいさんりょう (keisanryō)",
        "meaning": "computational complexity",
        "example_ja": "このアルゴリズムの計算量はO(n)です。",
        "example_en": "This algorithm's complexity is O(n)."
      },
      {
        "front": "時間計算量",
        "reading": "じかんけいさんりょう (jikan keisanryō)",
        "meaning": "time complexity",
        "example_ja": "時間計算量を見積もります。",
        "example_en": "We estimate the time complexity."
      },
      {
        "front": "空間計算量",
        "reading": "くうかんけいさんりょう (kūkan keisanryō)",
        "meaning": "space complexity",
        "example_ja": "空間計算量を減らします。",
        "example_en": "We reduce the space complexity."
      },
      {
        "front": "効率",
        "reading": "こうりつ (kōritsu)",
        "meaning": "efficiency",
        "example_ja": "コードの効率を上げます。",
        "example_en": "We improve the code's efficiency."
      },
      {
        "front": "要素",
        "reading": "ようそ (yōso)",
        "meaning": "element",
        "example_ja": "リストの要素を数えます。",
        "example_en": "We count the list's elements."
      },
      {
        "front": "添字",
        "reading": "そえじ (soeji)",
        "meaning": "index (subscript)",
        "example_ja": "添字で配列にアクセスします。",
        "example_en": "We access the array by index."
      },
      {
        "front": "再帰呼び出し",
        "reading": "さいきよびだし (saiki yobidashi)",
        "meaning": "recursive call",
        "example_ja": "再帰呼び出しで木をたどります。",
        "example_en": "We traverse the tree with recursive calls."
      },
      {
        "front": "幅優先探索",
        "reading": "はばゆうせんたんさく (haba yūsen tansaku)",
        "meaning": "breadth-first search (BFS)",
        "example_ja": "幅優先探索で最短経路を求めます。",
        "example_en": "We find the shortest path with BFS."
      },
      {
        "front": "深さ優先探索",
        "reading": "ふかさゆうせんたんさく (fukasa yūsen tansaku)",
        "meaning": "depth-first search (DFS)",
        "example_ja": "深さ優先探索で全経路を調べます。",
        "example_en": "We examine all paths with DFS."
      },
      {
        "front": "動的計画法",
        "reading": "どうてきけいかくほう (dōteki keikakuhō)",
        "meaning": "dynamic programming",
        "example_ja": "動的計画法で最適解を求めます。",
        "example_en": "We find the optimal solution with DP."
      }
    ],
    "quiz": [],
    "practice": []
  },
  {
    "key": "jp_errors_debug",
    "name": "Errors & Debugging — エラーとデバッグ",
    "category": "JP: Errors & Debugging",
    "what": "Bugs, exceptions, stack traces and the words for fixing things.",
    "deep": "When something breaks, you'll need to describe it fast: what エラー (error) came up, its 原因 (cause), and how you 修正 (fixed) it. Verbs matter here — 直す (to fix), 再現する (to reproduce), 確認する (to check).",
    "java": "The **Reading** column gives the kana pronunciation and a romaji gloss so you can say the word out loud. Loanwords written in katakana already *are* their reading, so only the romaji is shown. Example sentences are polite (です・ます) form — exactly how you'd hear them in a standup or an interview.",
    "language": "japanese",
    "lesson": "| 用語 (Term) | 読み方 (Reading) | 意味 (Meaning) | 例文 (Example) |\n|---|---|---|---|\n| エラー | erā | error | エラーが発生しました。 — *An error occurred.* |\n| バグ | bagu | bug | このバグを直します。 — *I'll fix this bug.* |\n| デバッグ | debaggu | debugging | コードをデバッグします。 — *We debug the code.* |\n| 例外 | れいがい (reigai) | exception | 例外がスローされました。 — *An exception was thrown.* |\n| 例外処理 | れいがいしょり (reigai shori) | exception handling | try-catchで例外処理をします。 — *We handle exceptions with try-catch.* |\n| 発生 | はっせい (hassei) | occurrence | 特定の入力でエラーが発生します。 — *An error occurs on certain input.* |\n| 修正 | しゅうせい (shūsei) | fix / correction | バグを修正しました。 — *I fixed the bug.* |\n| 直す | なおす (naosu) | to fix | この問題を直してください。 — *Please fix this problem.* |\n| 原因 | げんいん (gen'in) | cause | エラーの原因を調べます。 — *We investigate the cause of the error.* |\n| 動作 | どうさ (dōsa) | behavior / operation | プログラムは正しく動作します。 — *The program works correctly.* |\n| 再現 | さいげん (saigen) | reproduce | そのバグを再現できません。 — *I can't reproduce that bug.* |\n| ヌルポインタ例外 | ヌルポインタれいがい (nuru pointa reigai) | null pointer exception | ヌルポインタ例外に注意してください。 — *Watch out for null pointer exceptions.* |\n| スタックトレース | sutakku torēsu | stack trace | スタックトレースを確認します。 — *We check the stack trace.* |\n| ログ | rogu | log | ログを出力してデバッグします。 — *We debug by printing logs.* |\n| ブレークポイント | burēkupointo | breakpoint | この行にブレークポイントを設定します。 — *We set a breakpoint on this line.* |\n| 例外を投げる | れいがいをなげる (reigai o nageru) | to throw an exception | 不正な入力なら例外を投げます。 — *If the input is invalid, we throw an exception.* |\n| キャッチ | kyacchi | to catch | 例外をキャッチして処理します。 — *We catch and handle the exception.* |\n| 警告 | けいこく (keikoku) | warning | コンパイラが警告を出します。 — *The compiler gives a warning.* |\n| 想定外 | そうていがい (sōteigai) | unexpected | 想定外の値が入りました。 — *An unexpected value came in.* |\n| 確認する | かくにんする (kakunin suru) | to check / confirm | 出力を確認します。 — *We check the output.* |\n| 試す | ためす (tamesu) | to try / test out | もう一度試してください。 — *Please try it again.* |\n",
    "exercises": [],
    "cards": [
      {
        "front": "エラー",
        "reading": "erā",
        "meaning": "error",
        "example_ja": "エラーが発生しました。",
        "example_en": "An error occurred."
      },
      {
        "front": "バグ",
        "reading": "bagu",
        "meaning": "bug",
        "example_ja": "このバグを直します。",
        "example_en": "I'll fix this bug."
      },
      {
        "front": "デバッグ",
        "reading": "debaggu",
        "meaning": "debugging",
        "example_ja": "コードをデバッグします。",
        "example_en": "We debug the code."
      },
      {
        "front": "例外",
        "reading": "れいがい (reigai)",
        "meaning": "exception",
        "example_ja": "例外がスローされました。",
        "example_en": "An exception was thrown."
      },
      {
        "front": "例外処理",
        "reading": "れいがいしょり (reigai shori)",
        "meaning": "exception handling",
        "example_ja": "try-catchで例外処理をします。",
        "example_en": "We handle exceptions with try-catch."
      },
      {
        "front": "発生",
        "reading": "はっせい (hassei)",
        "meaning": "occurrence",
        "example_ja": "特定の入力でエラーが発生します。",
        "example_en": "An error occurs on certain input."
      },
      {
        "front": "修正",
        "reading": "しゅうせい (shūsei)",
        "meaning": "fix / correction",
        "example_ja": "バグを修正しました。",
        "example_en": "I fixed the bug."
      },
      {
        "front": "直す",
        "reading": "なおす (naosu)",
        "meaning": "to fix",
        "example_ja": "この問題を直してください。",
        "example_en": "Please fix this problem."
      },
      {
        "front": "原因",
        "reading": "げんいん (gen'in)",
        "meaning": "cause",
        "example_ja": "エラーの原因を調べます。",
        "example_en": "We investigate the cause of the error."
      },
      {
        "front": "動作",
        "reading": "どうさ (dōsa)",
        "meaning": "behavior / operation",
        "example_ja": "プログラムは正しく動作します。",
        "example_en": "The program works correctly."
      },
      {
        "front": "再現",
        "reading": "さいげん (saigen)",
        "meaning": "reproduce",
        "example_ja": "そのバグを再現できません。",
        "example_en": "I can't reproduce that bug."
      },
      {
        "front": "ヌルポインタ例外",
        "reading": "ヌルポインタれいがい (nuru pointa reigai)",
        "meaning": "null pointer exception",
        "example_ja": "ヌルポインタ例外に注意してください。",
        "example_en": "Watch out for null pointer exceptions."
      },
      {
        "front": "スタックトレース",
        "reading": "sutakku torēsu",
        "meaning": "stack trace",
        "example_ja": "スタックトレースを確認します。",
        "example_en": "We check the stack trace."
      },
      {
        "front": "ログ",
        "reading": "rogu",
        "meaning": "log",
        "example_ja": "ログを出力してデバッグします。",
        "example_en": "We debug by printing logs."
      },
      {
        "front": "ブレークポイント",
        "reading": "burēkupointo",
        "meaning": "breakpoint",
        "example_ja": "この行にブレークポイントを設定します。",
        "example_en": "We set a breakpoint on this line."
      },
      {
        "front": "例外を投げる",
        "reading": "れいがいをなげる (reigai o nageru)",
        "meaning": "to throw an exception",
        "example_ja": "不正な入力なら例外を投げます。",
        "example_en": "If the input is invalid, we throw an exception."
      },
      {
        "front": "キャッチ",
        "reading": "kyacchi",
        "meaning": "to catch",
        "example_ja": "例外をキャッチして処理します。",
        "example_en": "We catch and handle the exception."
      },
      {
        "front": "警告",
        "reading": "けいこく (keikoku)",
        "meaning": "warning",
        "example_ja": "コンパイラが警告を出します。",
        "example_en": "The compiler gives a warning."
      },
      {
        "front": "想定外",
        "reading": "そうていがい (sōteigai)",
        "meaning": "unexpected",
        "example_ja": "想定外の値が入りました。",
        "example_en": "An unexpected value came in."
      },
      {
        "front": "確認する",
        "reading": "かくにんする (kakunin suru)",
        "meaning": "to check / confirm",
        "example_ja": "出力を確認します。",
        "example_en": "We check the output."
      },
      {
        "front": "試す",
        "reading": "ためす (tamesu)",
        "meaning": "to try / test out",
        "example_ja": "もう一度試してください。",
        "example_en": "Please try it again."
      }
    ],
    "quiz": [],
    "practice": []
  },
  {
    "key": "jp_dev_tools",
    "name": "Dev Tools, Git & Testing — 開発ツール・Git・テスト",
    "category": "JP: Dev Tools & Git",
    "what": "Repositories, commits, branches, builds, tests and dependencies.",
    "deep": "Daily engineering runs on these. Git verbs are katakana loanwords (コミット, プッシュ, マージ), while process words tend to be kanji (変更, 差分, 依存関係, 設定).",
    "java": "The **Reading** column gives the kana pronunciation and a romaji gloss so you can say the word out loud. Loanwords written in katakana already *are* their reading, so only the romaji is shown. Example sentences are polite (です・ます) form — exactly how you'd hear them in a standup or an interview.",
    "language": "japanese",
    "lesson": "| 用語 (Term) | 読み方 (Reading) | 意味 (Meaning) | 例文 (Example) |\n|---|---|---|---|\n| リポジトリ | ripojitori | repository | リポジトリをクローンします。 — *We clone the repository.* |\n| コミット | komitto | commit | 変更をコミットします。 — *We commit the changes.* |\n| プッシュ | pusshu | push | リモートにプッシュします。 — *We push to the remote.* |\n| プル | puru | pull | 最新の変更をプルします。 — *We pull the latest changes.* |\n| マージ | māji | merge | ブランチをマージします。 — *We merge the branch.* |\n| ブランチ | buranchi | branch | 新しいブランチを作ります。 — *We create a new branch.* |\n| 変更 | へんこう (henkō) | change | この変更をレビューします。 — *We review this change.* |\n| 差分 | さぶん (sabun) | diff / difference | コミット前に差分を確認します。 — *We check the diff before committing.* |\n| 競合 | きょうごう (kyōgō) | (merge) conflict | マージで競合が起きました。 — *A conflict happened during the merge.* |\n| 解決 | かいけつ (kaiketsu) | resolve | 競合を解決します。 — *We resolve the conflict.* |\n| ビルド | birudo | build | プロジェクトをビルドします。 — *We build the project.* |\n| デプロイ | depuroi | deploy | サーバーにデプロイします。 — *We deploy to the server.* |\n| テスト | tesuto | test | テストを書いて実行します。 — *We write and run the tests.* |\n| 単体テスト | たんたいテスト (tantai tesuto) | unit test | この関数に単体テストを追加します。 — *We add a unit test for this function.* |\n| 依存関係 | いぞんかんけい (izon kankei) | dependency | 依存関係をインストールします。 — *We install the dependencies.* |\n| 環境 | かんきょう (kankyō) | environment | 開発環境を用意します。 — *We set up the development environment.* |\n| 設定 | せってい (settei) | configuration / settings | 設定ファイルを編集します。 — *We edit the config file.* |\n| 導入 | どうにゅう (dōnyū) | adopt / introduce | このツールを導入します。 — *We adopt this tool.* |\n| 更新 | こうしん (kōshin) | update | ライブラリを最新版に更新します。 — *We update the library to the latest version.* |\n| コードレビュー | kōdo rebyū | code review | コードレビューをお願いします。 — *Please review my code.* |\n| バージョン管理 | バージョンかんり (bājon kanri) | version control | Gitでバージョン管理します。 — *We manage versions with Git.* |\n| 反映 | はんえい (han'ei) | reflect / apply | 変更を本番環境に反映します。 — *We apply the changes to production.* |\n",
    "exercises": [],
    "cards": [
      {
        "front": "リポジトリ",
        "reading": "ripojitori",
        "meaning": "repository",
        "example_ja": "リポジトリをクローンします。",
        "example_en": "We clone the repository."
      },
      {
        "front": "コミット",
        "reading": "komitto",
        "meaning": "commit",
        "example_ja": "変更をコミットします。",
        "example_en": "We commit the changes."
      },
      {
        "front": "プッシュ",
        "reading": "pusshu",
        "meaning": "push",
        "example_ja": "リモートにプッシュします。",
        "example_en": "We push to the remote."
      },
      {
        "front": "プル",
        "reading": "puru",
        "meaning": "pull",
        "example_ja": "最新の変更をプルします。",
        "example_en": "We pull the latest changes."
      },
      {
        "front": "マージ",
        "reading": "māji",
        "meaning": "merge",
        "example_ja": "ブランチをマージします。",
        "example_en": "We merge the branch."
      },
      {
        "front": "ブランチ",
        "reading": "buranchi",
        "meaning": "branch",
        "example_ja": "新しいブランチを作ります。",
        "example_en": "We create a new branch."
      },
      {
        "front": "変更",
        "reading": "へんこう (henkō)",
        "meaning": "change",
        "example_ja": "この変更をレビューします。",
        "example_en": "We review this change."
      },
      {
        "front": "差分",
        "reading": "さぶん (sabun)",
        "meaning": "diff / difference",
        "example_ja": "コミット前に差分を確認します。",
        "example_en": "We check the diff before committing."
      },
      {
        "front": "競合",
        "reading": "きょうごう (kyōgō)",
        "meaning": "(merge) conflict",
        "example_ja": "マージで競合が起きました。",
        "example_en": "A conflict happened during the merge."
      },
      {
        "front": "解決",
        "reading": "かいけつ (kaiketsu)",
        "meaning": "resolve",
        "example_ja": "競合を解決します。",
        "example_en": "We resolve the conflict."
      },
      {
        "front": "ビルド",
        "reading": "birudo",
        "meaning": "build",
        "example_ja": "プロジェクトをビルドします。",
        "example_en": "We build the project."
      },
      {
        "front": "デプロイ",
        "reading": "depuroi",
        "meaning": "deploy",
        "example_ja": "サーバーにデプロイします。",
        "example_en": "We deploy to the server."
      },
      {
        "front": "テスト",
        "reading": "tesuto",
        "meaning": "test",
        "example_ja": "テストを書いて実行します。",
        "example_en": "We write and run the tests."
      },
      {
        "front": "単体テスト",
        "reading": "たんたいテスト (tantai tesuto)",
        "meaning": "unit test",
        "example_ja": "この関数に単体テストを追加します。",
        "example_en": "We add a unit test for this function."
      },
      {
        "front": "依存関係",
        "reading": "いぞんかんけい (izon kankei)",
        "meaning": "dependency",
        "example_ja": "依存関係をインストールします。",
        "example_en": "We install the dependencies."
      },
      {
        "front": "環境",
        "reading": "かんきょう (kankyō)",
        "meaning": "environment",
        "example_ja": "開発環境を用意します。",
        "example_en": "We set up the development environment."
      },
      {
        "front": "設定",
        "reading": "せってい (settei)",
        "meaning": "configuration / settings",
        "example_ja": "設定ファイルを編集します。",
        "example_en": "We edit the config file."
      },
      {
        "front": "導入",
        "reading": "どうにゅう (dōnyū)",
        "meaning": "adopt / introduce",
        "example_ja": "このツールを導入します。",
        "example_en": "We adopt this tool."
      },
      {
        "front": "更新",
        "reading": "こうしん (kōshin)",
        "meaning": "update",
        "example_ja": "ライブラリを最新版に更新します。",
        "example_en": "We update the library to the latest version."
      },
      {
        "front": "コードレビュー",
        "reading": "kōdo rebyū",
        "meaning": "code review",
        "example_ja": "コードレビューをお願いします。",
        "example_en": "Please review my code."
      },
      {
        "front": "バージョン管理",
        "reading": "バージョンかんり (bājon kanri)",
        "meaning": "version control",
        "example_ja": "Gitでバージョン管理します。",
        "example_en": "We manage versions with Git."
      },
      {
        "front": "反映",
        "reading": "はんえい (han'ei)",
        "meaning": "reflect / apply",
        "example_ja": "変更を本番環境に反映します。",
        "example_en": "We apply the changes to production."
      }
    ],
    "quiz": [],
    "practice": []
  },
  {
    "key": "jp_interview",
    "name": "Interview & Workplace — 面接と職場",
    "category": "JP: Interview & Workplace",
    "what": "Phrases for a technical interview and everyday engineering-team talk.",
    "deep": "Beyond the tech words, you need the conversation itself: solving a 問題 (problem), explaining your 考え方 (approach), talking about 要件 (requirements) and 締め切り (deadlines). These carry a whole interview.",
    "java": "The **Reading** column gives the kana pronunciation and a romaji gloss so you can say the word out loud. Loanwords written in katakana already *are* their reading, so only the romaji is shown. Example sentences are polite (です・ます) form — exactly how you'd hear them in a standup or an interview.",
    "language": "japanese",
    "lesson": "| 用語 (Term) | 読み方 (Reading) | 意味 (Meaning) | 例文 (Example) |\n|---|---|---|---|\n| 面接 | めんせつ (mensetsu) | interview | 明日、技術面接があります。 — *I have a technical interview tomorrow.* |\n| 技術面接 | ぎじゅつめんせつ (gijutsu mensetsu) | technical interview | 技術面接でアルゴリズムを聞かれます。 — *In a technical interview, they ask about algorithms.* |\n| 問題 | もんだい (mondai) | problem | この問題を解いてください。 — *Please solve this problem.* |\n| 解く | とく (toku) | to solve | コーディング問題を解きます。 — *We solve a coding problem.* |\n| 解答 | かいとう (kaitō) | answer / solution | 解答を説明してください。 — *Please explain your solution.* |\n| 説明する | せつめいする (setsumei suru) | to explain | まず考え方を説明します。 — *First I'll explain my approach.* |\n| 要件 | ようけん (yōken) | requirement | 要件を確認します。 — *We confirm the requirements.* |\n| 仕様 | しよう (shiyō) | specification | 仕様書をよく読みます。 — *We read the spec carefully.* |\n| 設計 | せっけい (sekkei) | design | システムを設計します。 — *We design the system.* |\n| 実装する | じっそうする (jissō suru) | to implement | この機能を実装します。 — *We implement this feature.* |\n| 最適化 | さいてきか (saitekika) | optimization | コードを最適化します。 — *We optimize the code.* |\n| 改善 | かいぜん (kaizen) | improvement | パフォーマンスを改善します。 — *We improve the performance.* |\n| 締め切り | しめきり (shimekiri) | deadline | 締め切りは金曜日です。 — *The deadline is Friday.* |\n| 納期 | のうき (nōki) | delivery deadline | 納期に間に合わせます。 — *We'll make the delivery deadline.* |\n| 会議 | かいぎ (kaigi) | meeting | 朝に短い会議があります。 — *There's a short meeting in the morning.* |\n| 進捗 | しんちょく (shinchoku) | progress | 毎日、進捗を報告します。 — *I report progress every day.* |\n| 質問 | しつもん (shitsumon) | question | 一つ質問してもいいですか。 — *May I ask one question?* |\n| 考え方 | かんがえかた (kangaekata) | approach / way of thinking | まず考え方を整理します。 — *First, let me organize my approach.* |\n| 担当 | たんとう (tantō) | in charge of | 私がこの機能を担当します。 — *I'm in charge of this feature.* |\n| 期待 | きたい (kitai) | expectation | コードは期待通りに動きます。 — *The code works as expected.* |\n| 報告 | ほうこく (hōkoku) | report | 結果を上司に報告します。 — *I report the results to my manager.* |\n| お疲れ様です | おつかれさまです (otsukaresama desu) | (standard workplace greeting) | お疲れ様です、レビューありがとうございます。 — *Thanks for your work — and thanks for the review.* |\n",
    "exercises": [],
    "cards": [
      {
        "front": "面接",
        "reading": "めんせつ (mensetsu)",
        "meaning": "interview",
        "example_ja": "明日、技術面接があります。",
        "example_en": "I have a technical interview tomorrow."
      },
      {
        "front": "技術面接",
        "reading": "ぎじゅつめんせつ (gijutsu mensetsu)",
        "meaning": "technical interview",
        "example_ja": "技術面接でアルゴリズムを聞かれます。",
        "example_en": "In a technical interview, they ask about algorithms."
      },
      {
        "front": "問題",
        "reading": "もんだい (mondai)",
        "meaning": "problem",
        "example_ja": "この問題を解いてください。",
        "example_en": "Please solve this problem."
      },
      {
        "front": "解く",
        "reading": "とく (toku)",
        "meaning": "to solve",
        "example_ja": "コーディング問題を解きます。",
        "example_en": "We solve a coding problem."
      },
      {
        "front": "解答",
        "reading": "かいとう (kaitō)",
        "meaning": "answer / solution",
        "example_ja": "解答を説明してください。",
        "example_en": "Please explain your solution."
      },
      {
        "front": "説明する",
        "reading": "せつめいする (setsumei suru)",
        "meaning": "to explain",
        "example_ja": "まず考え方を説明します。",
        "example_en": "First I'll explain my approach."
      },
      {
        "front": "要件",
        "reading": "ようけん (yōken)",
        "meaning": "requirement",
        "example_ja": "要件を確認します。",
        "example_en": "We confirm the requirements."
      },
      {
        "front": "仕様",
        "reading": "しよう (shiyō)",
        "meaning": "specification",
        "example_ja": "仕様書をよく読みます。",
        "example_en": "We read the spec carefully."
      },
      {
        "front": "設計",
        "reading": "せっけい (sekkei)",
        "meaning": "design",
        "example_ja": "システムを設計します。",
        "example_en": "We design the system."
      },
      {
        "front": "実装する",
        "reading": "じっそうする (jissō suru)",
        "meaning": "to implement",
        "example_ja": "この機能を実装します。",
        "example_en": "We implement this feature."
      },
      {
        "front": "最適化",
        "reading": "さいてきか (saitekika)",
        "meaning": "optimization",
        "example_ja": "コードを最適化します。",
        "example_en": "We optimize the code."
      },
      {
        "front": "改善",
        "reading": "かいぜん (kaizen)",
        "meaning": "improvement",
        "example_ja": "パフォーマンスを改善します。",
        "example_en": "We improve the performance."
      },
      {
        "front": "締め切り",
        "reading": "しめきり (shimekiri)",
        "meaning": "deadline",
        "example_ja": "締め切りは金曜日です。",
        "example_en": "The deadline is Friday."
      },
      {
        "front": "納期",
        "reading": "のうき (nōki)",
        "meaning": "delivery deadline",
        "example_ja": "納期に間に合わせます。",
        "example_en": "We'll make the delivery deadline."
      },
      {
        "front": "会議",
        "reading": "かいぎ (kaigi)",
        "meaning": "meeting",
        "example_ja": "朝に短い会議があります。",
        "example_en": "There's a short meeting in the morning."
      },
      {
        "front": "進捗",
        "reading": "しんちょく (shinchoku)",
        "meaning": "progress",
        "example_ja": "毎日、進捗を報告します。",
        "example_en": "I report progress every day."
      },
      {
        "front": "質問",
        "reading": "しつもん (shitsumon)",
        "meaning": "question",
        "example_ja": "一つ質問してもいいですか。",
        "example_en": "May I ask one question?"
      },
      {
        "front": "考え方",
        "reading": "かんがえかた (kangaekata)",
        "meaning": "approach / way of thinking",
        "example_ja": "まず考え方を整理します。",
        "example_en": "First, let me organize my approach."
      },
      {
        "front": "担当",
        "reading": "たんとう (tantō)",
        "meaning": "in charge of",
        "example_ja": "私がこの機能を担当します。",
        "example_en": "I'm in charge of this feature."
      },
      {
        "front": "期待",
        "reading": "きたい (kitai)",
        "meaning": "expectation",
        "example_ja": "コードは期待通りに動きます。",
        "example_en": "The code works as expected."
      },
      {
        "front": "報告",
        "reading": "ほうこく (hōkoku)",
        "meaning": "report",
        "example_ja": "結果を上司に報告します。",
        "example_en": "I report the results to my manager."
      },
      {
        "front": "お疲れ様です",
        "reading": "おつかれさまです (otsukaresama desu)",
        "meaning": "(standard workplace greeting)",
        "example_ja": "お疲れ様です、レビューありがとうございます。",
        "example_en": "Thanks for your work — and thanks for the review."
      }
    ],
    "quiz": [],
    "practice": []
  },
  {
    "key": "jp_ts_basics",
    "name": "TypeScript Basics — TypeScriptの基礎",
    "category": "JP: TypeScript Basics",
    "what": "TypeScript itself: type annotations, inference, compiling, variables.",
    "deep": "TypeScript adds types on top of JavaScript. These are the words for the language itself — 型注釈 (annotation), 型推論 (inference), コンパイル — and the basic building blocks you declare and initialize.",
    "java": "The **Reading** column gives the kana pronunciation and a romaji gloss so you can say the word out loud. Loanwords written in katakana already *are* their reading, so only the romaji is shown. Example sentences are polite (です・ます) form — exactly how you'd hear them in a standup or an interview.",
    "language": "japanese",
    "lesson": "| 用語 (Term) | 読み方 (Reading) | 意味 (Meaning) | 例文 (Example) |\n|---|---|---|---|\n| TypeScript | TypeScript | TypeScript | TypeScriptはJavaScriptに型を追加した言語です。 — *TypeScript is a language that adds types to JavaScript.* |\n| JavaScript | JavaScript | JavaScript | TypeScriptは最終的にJavaScriptに変換されます。 — *TypeScript is ultimately converted to JavaScript.* |\n| 型注釈 | かたちゅうしゃく (kata chūshaku) | type annotation | 変数に型注釈を付けます。 — *We add a type annotation to the variable.* |\n| 型推論 | かたすいろん (kata suiron) | type inference | 型推論で型を自動的に決めます。 — *Type inference determines the type automatically.* |\n| コンパイル | konpairu | compile | TypeScriptをコンパイルします。 — *We compile the TypeScript.* |\n| トランスパイル | toransupairu | transpile | tscがTypeScriptをJavaScriptにトランスパイルします。 — *tsc transpiles TypeScript to JavaScript.* |\n| 静的型付け | せいてきかたづけ (seiteki katazuke) | static typing | TypeScriptは静的型付けの言語です。 — *TypeScript is a statically typed language.* |\n| 型安全 | かたあんぜん (kata anzen) | type safety | 型安全なコードを書きます。 — *We write type-safe code.* |\n| 変数 | へんすう (hensū) | variable | letで変数を宣言します。 — *We declare a variable with `let`.* |\n| 定数 | ていすう (teisū) | constant | constで定数を宣言します。 — *We declare a constant with `const`.* |\n| 再代入 | さいだいにゅう (sai dainyū) | reassignment | constは再代入できません。 — *A `const` cannot be reassigned.* |\n| スコープ | sukōpu | scope | 変数のスコープに注意します。 — *We pay attention to the variable's scope.* |\n| ブロックスコープ | burokku sukōpu | block scope | letはブロックスコープを持ちます。 — *`let` has block scope.* |\n| 宣言 | せんげん (sengen) | declaration | 型と一緒に変数を宣言します。 — *We declare the variable together with its type.* |\n| 初期化 | しょきか (shokika) | initialization | 変数を初期化します。 — *We initialize the variable.* |\n| プリミティブ型 | プリミティブがた (purimitibu gata) | primitive type | numberはプリミティブ型です。 — *`number` is a primitive type.* |\n| 数値 | すうち (sūchi) | number | numberは数値を表します。 — *`number` represents a numeric value.* |\n| 文字列 | もじれつ (mojiretsu) | string | stringは文字列を表します。 — *`string` represents text.* |\n| 真偽値 | しんぎち (shingichi) | boolean | booleanはtrueかfalseです。 — *A boolean is true or false.* |\n| テンプレートリテラル | tenpurēto riteraru | template literal | テンプレートリテラルで文字列を組み立てます。 — *We build strings with template literals.* |\n| 実行環境 | じっこうかんきょう (jikkō kankyō) | runtime environment | Node.jsはJavaScriptの実行環境です。 — *Node.js is a JavaScript runtime.* |\n| 開発環境 | かいはつかんきょう (kaihatsu kankyō) | development environment | 開発環境を整えます。 — *We set up the development environment.* |\n| 構文 | こうぶん (kōbun) | syntax | TypeScriptの構文を学びます。 — *We learn TypeScript's syntax.* |\n| 拡張子 | かくちょうし (kakuchōshi) | file extension | TypeScriptの拡張子は.tsです。 — *The TypeScript extension is `.ts`.* |\n",
    "exercises": [],
    "cards": [
      {
        "front": "TypeScript",
        "reading": "TypeScript",
        "meaning": "TypeScript",
        "example_ja": "TypeScriptはJavaScriptに型を追加した言語です。",
        "example_en": "TypeScript is a language that adds types to JavaScript."
      },
      {
        "front": "JavaScript",
        "reading": "JavaScript",
        "meaning": "JavaScript",
        "example_ja": "TypeScriptは最終的にJavaScriptに変換されます。",
        "example_en": "TypeScript is ultimately converted to JavaScript."
      },
      {
        "front": "型注釈",
        "reading": "かたちゅうしゃく (kata chūshaku)",
        "meaning": "type annotation",
        "example_ja": "変数に型注釈を付けます。",
        "example_en": "We add a type annotation to the variable."
      },
      {
        "front": "型推論",
        "reading": "かたすいろん (kata suiron)",
        "meaning": "type inference",
        "example_ja": "型推論で型を自動的に決めます。",
        "example_en": "Type inference determines the type automatically."
      },
      {
        "front": "コンパイル",
        "reading": "konpairu",
        "meaning": "compile",
        "example_ja": "TypeScriptをコンパイルします。",
        "example_en": "We compile the TypeScript."
      },
      {
        "front": "トランスパイル",
        "reading": "toransupairu",
        "meaning": "transpile",
        "example_ja": "tscがTypeScriptをJavaScriptにトランスパイルします。",
        "example_en": "tsc transpiles TypeScript to JavaScript."
      },
      {
        "front": "静的型付け",
        "reading": "せいてきかたづけ (seiteki katazuke)",
        "meaning": "static typing",
        "example_ja": "TypeScriptは静的型付けの言語です。",
        "example_en": "TypeScript is a statically typed language."
      },
      {
        "front": "型安全",
        "reading": "かたあんぜん (kata anzen)",
        "meaning": "type safety",
        "example_ja": "型安全なコードを書きます。",
        "example_en": "We write type-safe code."
      },
      {
        "front": "変数",
        "reading": "へんすう (hensū)",
        "meaning": "variable",
        "example_ja": "letで変数を宣言します。",
        "example_en": "We declare a variable with `let`."
      },
      {
        "front": "定数",
        "reading": "ていすう (teisū)",
        "meaning": "constant",
        "example_ja": "constで定数を宣言します。",
        "example_en": "We declare a constant with `const`."
      },
      {
        "front": "再代入",
        "reading": "さいだいにゅう (sai dainyū)",
        "meaning": "reassignment",
        "example_ja": "constは再代入できません。",
        "example_en": "A `const` cannot be reassigned."
      },
      {
        "front": "スコープ",
        "reading": "sukōpu",
        "meaning": "scope",
        "example_ja": "変数のスコープに注意します。",
        "example_en": "We pay attention to the variable's scope."
      },
      {
        "front": "ブロックスコープ",
        "reading": "burokku sukōpu",
        "meaning": "block scope",
        "example_ja": "letはブロックスコープを持ちます。",
        "example_en": "`let` has block scope."
      },
      {
        "front": "宣言",
        "reading": "せんげん (sengen)",
        "meaning": "declaration",
        "example_ja": "型と一緒に変数を宣言します。",
        "example_en": "We declare the variable together with its type."
      },
      {
        "front": "初期化",
        "reading": "しょきか (shokika)",
        "meaning": "initialization",
        "example_ja": "変数を初期化します。",
        "example_en": "We initialize the variable."
      },
      {
        "front": "プリミティブ型",
        "reading": "プリミティブがた (purimitibu gata)",
        "meaning": "primitive type",
        "example_ja": "numberはプリミティブ型です。",
        "example_en": "`number` is a primitive type."
      },
      {
        "front": "数値",
        "reading": "すうち (sūchi)",
        "meaning": "number",
        "example_ja": "numberは数値を表します。",
        "example_en": "`number` represents a numeric value."
      },
      {
        "front": "文字列",
        "reading": "もじれつ (mojiretsu)",
        "meaning": "string",
        "example_ja": "stringは文字列を表します。",
        "example_en": "`string` represents text."
      },
      {
        "front": "真偽値",
        "reading": "しんぎち (shingichi)",
        "meaning": "boolean",
        "example_ja": "booleanはtrueかfalseです。",
        "example_en": "A boolean is true or false."
      },
      {
        "front": "テンプレートリテラル",
        "reading": "tenpurēto riteraru",
        "meaning": "template literal",
        "example_ja": "テンプレートリテラルで文字列を組み立てます。",
        "example_en": "We build strings with template literals."
      },
      {
        "front": "実行環境",
        "reading": "じっこうかんきょう (jikkō kankyō)",
        "meaning": "runtime environment",
        "example_ja": "Node.jsはJavaScriptの実行環境です。",
        "example_en": "Node.js is a JavaScript runtime."
      },
      {
        "front": "開発環境",
        "reading": "かいはつかんきょう (kaihatsu kankyō)",
        "meaning": "development environment",
        "example_ja": "開発環境を整えます。",
        "example_en": "We set up the development environment."
      },
      {
        "front": "構文",
        "reading": "こうぶん (kōbun)",
        "meaning": "syntax",
        "example_ja": "TypeScriptの構文を学びます。",
        "example_en": "We learn TypeScript's syntax."
      },
      {
        "front": "拡張子",
        "reading": "かくちょうし (kakuchōshi)",
        "meaning": "file extension",
        "example_ja": "TypeScriptの拡張子は.tsです。",
        "example_en": "The TypeScript extension is `.ts`."
      }
    ],
    "quiz": [],
    "practice": []
  },
  {
    "key": "jp_ts_types",
    "name": "Types & Interfaces — 型とインターフェース",
    "category": "JP: TS Types",
    "what": "The type system: interfaces, unions, generics, guards, assertions.",
    "deep": "The heart of TypeScript. You describe the shape of data with インターフェース and 型エイリアス, combine types with ユニオン型 / 交差型, and make them reusable with ジェネリクス. This is where interviews about TypeScript live.",
    "java": "The **Reading** column gives the kana pronunciation and a romaji gloss so you can say the word out loud. Loanwords written in katakana already *are* their reading, so only the romaji is shown. Example sentences are polite (です・ます) form — exactly how you'd hear them in a standup or an interview.",
    "language": "japanese",
    "lesson": "| 用語 (Term) | 読み方 (Reading) | 意味 (Meaning) | 例文 (Example) |\n|---|---|---|---|\n| 型 | かた (kata) | type | すべての値に型があります。 — *Every value has a type.* |\n| 型エイリアス | かたエイリアス (kata eiriasu) | type alias | typeで型エイリアスを作ります。 — *We make a type alias with `type`.* |\n| インターフェース | intāfēsu | interface | インターフェースでオブジェクトの形を定義します。 — *We define an object's shape with an interface.* |\n| ユニオン型 | ユニオンがた (yunion gata) | union type | ユニオン型で複数の型を許可します。 — *A union type allows several types.* |\n| 交差型 | こうさがた (kōsa gata) | intersection type | 交差型で型を組み合わせます。 — *We combine types with an intersection type.* |\n| ジェネリクス | jenerikusu | generics | ジェネリクスで再利用できる型を書きます。 — *We write reusable types with generics.* |\n| 型引数 | かたひきすう (kata hikisū) | type parameter | 関数に型引数を渡します。 — *We pass a type parameter to the function.* |\n| 型ガード | かたガード (kata gādo) | type guard | 型ガードで型を絞り込みます。 — *We narrow the type with a type guard.* |\n| 型アサーション | かたアサーション (kata asāshon) | type assertion | asで型アサーションを行います。 — *We do a type assertion with `as`.* |\n| リテラル型 | リテラルがた (riteraru gata) | literal type | リテラル型で特定の値だけを許可します。 — *A literal type allows only specific values.* |\n| タプル | tapuru | tuple | タプルは長さと型が固定です。 — *A tuple has a fixed length and element types.* |\n| 列挙型 | れっきょがた (rekkyo gata) | enum | 列挙型で定数をまとめます。 — *We group constants with an enum.* |\n| 読み取り専用 | よみとりせんよう (yomitori sen'yō) | readonly | 読み取り専用のプロパティは変更できません。 — *A readonly property can't be changed.* |\n| 任意 | にんい (nin'i) | optional | 任意のプロパティは省略できます。 — *An optional property can be omitted.* |\n| null許容 | ヌルきょよう (nuru kyoyō) | nullable | null許容の値はnullになり得ます。 — *A nullable value may be null.* |\n| any型 | エニーがた (enī gata) | the any type | any型は型チェックを無効にします。 — *The `any` type disables type checking.* |\n| unknown型 | アンノウンがた (announ gata) | the unknown type | unknown型は使う前に型を確認します。 — *The `unknown` type must be checked before use.* |\n| プロパティ | puropati | property | オブジェクトのプロパティにアクセスします。 — *We access the object's property.* |\n| 型定義 | かたていぎ (kata teigi) | type definition | ライブラリの型定義を読み込みます。 — *We load the library's type definitions.* |\n| 構造的型付け | こうぞうてきかたづけ (kōzōteki katazuke) | structural typing | TypeScriptは構造的型付けを採用しています。 — *TypeScript uses structural typing.* |\n| 網羅性チェック | もうらせいチェック (mōrasei chekku) | exhaustiveness check | switch文で網羅性チェックをします。 — *We do an exhaustiveness check in a switch.* |\n| 継承 | けいしょう (keishō) | inheritance (extends) | インターフェースは他のインターフェースを継承できます。 — *An interface can extend another interface.* |\n| 型変換 | かたへんかん (kata henkan) | type conversion | 文字列を数値に型変換します。 — *We convert the string to a number.* |\n| 省略可能 | しょうりゃくかのう (shōryaku kanō) | optional / omittable | この引数は省略可能です。 — *This argument is optional.* |\n| 定義 | ていぎ (teigi) | definition | 型の定義を確認します。 — *We check the type definition.* |\n| 制約 | せいやく (seiyaku) | constraint | ジェネリクスに制約を付けます。 — *We add a constraint to the generic.* |\n",
    "exercises": [],
    "cards": [
      {
        "front": "型",
        "reading": "かた (kata)",
        "meaning": "type",
        "example_ja": "すべての値に型があります。",
        "example_en": "Every value has a type."
      },
      {
        "front": "型エイリアス",
        "reading": "かたエイリアス (kata eiriasu)",
        "meaning": "type alias",
        "example_ja": "typeで型エイリアスを作ります。",
        "example_en": "We make a type alias with `type`."
      },
      {
        "front": "インターフェース",
        "reading": "intāfēsu",
        "meaning": "interface",
        "example_ja": "インターフェースでオブジェクトの形を定義します。",
        "example_en": "We define an object's shape with an interface."
      },
      {
        "front": "ユニオン型",
        "reading": "ユニオンがた (yunion gata)",
        "meaning": "union type",
        "example_ja": "ユニオン型で複数の型を許可します。",
        "example_en": "A union type allows several types."
      },
      {
        "front": "交差型",
        "reading": "こうさがた (kōsa gata)",
        "meaning": "intersection type",
        "example_ja": "交差型で型を組み合わせます。",
        "example_en": "We combine types with an intersection type."
      },
      {
        "front": "ジェネリクス",
        "reading": "jenerikusu",
        "meaning": "generics",
        "example_ja": "ジェネリクスで再利用できる型を書きます。",
        "example_en": "We write reusable types with generics."
      },
      {
        "front": "型引数",
        "reading": "かたひきすう (kata hikisū)",
        "meaning": "type parameter",
        "example_ja": "関数に型引数を渡します。",
        "example_en": "We pass a type parameter to the function."
      },
      {
        "front": "型ガード",
        "reading": "かたガード (kata gādo)",
        "meaning": "type guard",
        "example_ja": "型ガードで型を絞り込みます。",
        "example_en": "We narrow the type with a type guard."
      },
      {
        "front": "型アサーション",
        "reading": "かたアサーション (kata asāshon)",
        "meaning": "type assertion",
        "example_ja": "asで型アサーションを行います。",
        "example_en": "We do a type assertion with `as`."
      },
      {
        "front": "リテラル型",
        "reading": "リテラルがた (riteraru gata)",
        "meaning": "literal type",
        "example_ja": "リテラル型で特定の値だけを許可します。",
        "example_en": "A literal type allows only specific values."
      },
      {
        "front": "タプル",
        "reading": "tapuru",
        "meaning": "tuple",
        "example_ja": "タプルは長さと型が固定です。",
        "example_en": "A tuple has a fixed length and element types."
      },
      {
        "front": "列挙型",
        "reading": "れっきょがた (rekkyo gata)",
        "meaning": "enum",
        "example_ja": "列挙型で定数をまとめます。",
        "example_en": "We group constants with an enum."
      },
      {
        "front": "読み取り専用",
        "reading": "よみとりせんよう (yomitori sen'yō)",
        "meaning": "readonly",
        "example_ja": "読み取り専用のプロパティは変更できません。",
        "example_en": "A readonly property can't be changed."
      },
      {
        "front": "任意",
        "reading": "にんい (nin'i)",
        "meaning": "optional",
        "example_ja": "任意のプロパティは省略できます。",
        "example_en": "An optional property can be omitted."
      },
      {
        "front": "null許容",
        "reading": "ヌルきょよう (nuru kyoyō)",
        "meaning": "nullable",
        "example_ja": "null許容の値はnullになり得ます。",
        "example_en": "A nullable value may be null."
      },
      {
        "front": "any型",
        "reading": "エニーがた (enī gata)",
        "meaning": "the any type",
        "example_ja": "any型は型チェックを無効にします。",
        "example_en": "The `any` type disables type checking."
      },
      {
        "front": "unknown型",
        "reading": "アンノウンがた (announ gata)",
        "meaning": "the unknown type",
        "example_ja": "unknown型は使う前に型を確認します。",
        "example_en": "The `unknown` type must be checked before use."
      },
      {
        "front": "プロパティ",
        "reading": "puropati",
        "meaning": "property",
        "example_ja": "オブジェクトのプロパティにアクセスします。",
        "example_en": "We access the object's property."
      },
      {
        "front": "型定義",
        "reading": "かたていぎ (kata teigi)",
        "meaning": "type definition",
        "example_ja": "ライブラリの型定義を読み込みます。",
        "example_en": "We load the library's type definitions."
      },
      {
        "front": "構造的型付け",
        "reading": "こうぞうてきかたづけ (kōzōteki katazuke)",
        "meaning": "structural typing",
        "example_ja": "TypeScriptは構造的型付けを採用しています。",
        "example_en": "TypeScript uses structural typing."
      },
      {
        "front": "網羅性チェック",
        "reading": "もうらせいチェック (mōrasei chekku)",
        "meaning": "exhaustiveness check",
        "example_ja": "switch文で網羅性チェックをします。",
        "example_en": "We do an exhaustiveness check in a switch."
      },
      {
        "front": "継承",
        "reading": "けいしょう (keishō)",
        "meaning": "inheritance (extends)",
        "example_ja": "インターフェースは他のインターフェースを継承できます。",
        "example_en": "An interface can extend another interface."
      },
      {
        "front": "型変換",
        "reading": "かたへんかん (kata henkan)",
        "meaning": "type conversion",
        "example_ja": "文字列を数値に型変換します。",
        "example_en": "We convert the string to a number."
      },
      {
        "front": "省略可能",
        "reading": "しょうりゃくかのう (shōryaku kanō)",
        "meaning": "optional / omittable",
        "example_ja": "この引数は省略可能です。",
        "example_en": "This argument is optional."
      },
      {
        "front": "定義",
        "reading": "ていぎ (teigi)",
        "meaning": "definition",
        "example_ja": "型の定義を確認します。",
        "example_en": "We check the type definition."
      },
      {
        "front": "制約",
        "reading": "せいやく (seiyaku)",
        "meaning": "constraint",
        "example_ja": "ジェネリクスに制約を付けます。",
        "example_en": "We add a constraint to the generic."
      }
    ],
    "quiz": [],
    "practice": []
  },
  {
    "key": "jp_ts_functions",
    "name": "Functions & Async — 関数と非同期",
    "category": "JP: TS Functions",
    "what": "Functions, arrow functions, callbacks, Promises and async/await.",
    "deep": "Functions and asynchronous code are where a lot of TypeScript work happens. Learn 非同期 (async), プロミス (Promise), 待機 (await), and the words for arguments, return values and callbacks.",
    "java": "The **Reading** column gives the kana pronunciation and a romaji gloss so you can say the word out loud. Loanwords written in katakana already *are* their reading, so only the romaji is shown. Example sentences are polite (です・ます) form — exactly how you'd hear them in a standup or an interview.",
    "language": "japanese",
    "lesson": "| 用語 (Term) | 読み方 (Reading) | 意味 (Meaning) | 例文 (Example) |\n|---|---|---|---|\n| 関数 | かんすう (kansū) | function | 関数を定義します。 — *We define a function.* |\n| アロー関数 | アローかんすう (arō kansū) | arrow function | アロー関数で短く書けます。 — *We can write it shorter with an arrow function.* |\n| 引数 | ひきすう (hikisū) | argument | 関数に引数を渡します。 — *We pass an argument to the function.* |\n| 仮引数 | かりひきすう (kari hikisū) | parameter | 仮引数に型を付けます。 — *We type the parameter.* |\n| 戻り値 | もどりち (modorichi) | return value | 関数の戻り値の型を指定します。 — *We specify the function's return type.* |\n| コールバック | kōrubakku | callback | 関数にコールバックを渡します。 — *We pass a callback to the function.* |\n| 高階関数 | こうかいかんすう (kōkai kansū) | higher-order function | mapは高階関数です。 — *`map` is a higher-order function.* |\n| 無名関数 | むめいかんすう (mumei kansū) | anonymous function | 無名関数をその場で渡します。 — *We pass an anonymous function inline.* |\n| 非同期 | ひどうき (hidōki) | asynchronous | 非同期でデータを取得します。 — *We fetch the data asynchronously.* |\n| 同期 | どうき (dōki) | synchronous | この処理は同期的に実行されます。 — *This runs synchronously.* |\n| プロミス | puromisu | Promise | プロミスは非同期の結果を表します。 — *A Promise represents an async result.* |\n| 待機 | たいき (taiki) | await / waiting | awaitで結果を待機します。 — *We await the result with `await`.* |\n| 解決 | かいけつ (kaiketsu) | resolve | プロミスが値で解決します。 — *The promise resolves with a value.* |\n| 拒否 | きょひ (kyohi) | reject | エラーでプロミスが拒否されます。 — *The promise is rejected with an error.* |\n| 副作用 | ふくさよう (fukusayō) | side effect | この関数は副作用がありません。 — *This function has no side effects.* |\n| 純粋関数 | じゅんすいかんすう (junsui kansū) | pure function | 純粋関数はテストしやすいです。 — *Pure functions are easy to test.* |\n| デフォルト引数 | デフォルトひきすう (deforuto hikisū) | default parameter | デフォルト引数を設定します。 — *We set a default parameter.* |\n| 残余引数 | ざんよひきすう (zan'yo hikisū) | rest parameter | 残余引数で複数の値を受け取ります。 — *We receive multiple values with a rest parameter.* |\n| クロージャ | kurōja | closure | クロージャは外側の変数を覚えています。 — *A closure remembers outer variables.* |\n| 再帰 | さいき (saiki) | recursion | この関数は再帰を使います。 — *This function uses recursion.* |\n| 並行処理 | へいこうしょり (heikō shori) | concurrency | 並行処理で複数の要求を扱います。 — *We handle multiple requests with concurrency.* |\n| 例外処理 | れいがいしょり (reigai shori) | exception handling | try-catchで例外処理をします。 — *We handle exceptions with try-catch.* |\n| 呼び出し | よびだし (yobidashi) | call / invocation | 関数の呼び出しを確認します。 — *We check the function call.* |\n| 遅延評価 | ちえんひょうか (chien hyōka) | lazy evaluation | 遅延評価で計算を後回しにします。 — *Lazy evaluation defers the computation.* |\n",
    "exercises": [],
    "cards": [
      {
        "front": "関数",
        "reading": "かんすう (kansū)",
        "meaning": "function",
        "example_ja": "関数を定義します。",
        "example_en": "We define a function."
      },
      {
        "front": "アロー関数",
        "reading": "アローかんすう (arō kansū)",
        "meaning": "arrow function",
        "example_ja": "アロー関数で短く書けます。",
        "example_en": "We can write it shorter with an arrow function."
      },
      {
        "front": "引数",
        "reading": "ひきすう (hikisū)",
        "meaning": "argument",
        "example_ja": "関数に引数を渡します。",
        "example_en": "We pass an argument to the function."
      },
      {
        "front": "仮引数",
        "reading": "かりひきすう (kari hikisū)",
        "meaning": "parameter",
        "example_ja": "仮引数に型を付けます。",
        "example_en": "We type the parameter."
      },
      {
        "front": "戻り値",
        "reading": "もどりち (modorichi)",
        "meaning": "return value",
        "example_ja": "関数の戻り値の型を指定します。",
        "example_en": "We specify the function's return type."
      },
      {
        "front": "コールバック",
        "reading": "kōrubakku",
        "meaning": "callback",
        "example_ja": "関数にコールバックを渡します。",
        "example_en": "We pass a callback to the function."
      },
      {
        "front": "高階関数",
        "reading": "こうかいかんすう (kōkai kansū)",
        "meaning": "higher-order function",
        "example_ja": "mapは高階関数です。",
        "example_en": "`map` is a higher-order function."
      },
      {
        "front": "無名関数",
        "reading": "むめいかんすう (mumei kansū)",
        "meaning": "anonymous function",
        "example_ja": "無名関数をその場で渡します。",
        "example_en": "We pass an anonymous function inline."
      },
      {
        "front": "非同期",
        "reading": "ひどうき (hidōki)",
        "meaning": "asynchronous",
        "example_ja": "非同期でデータを取得します。",
        "example_en": "We fetch the data asynchronously."
      },
      {
        "front": "同期",
        "reading": "どうき (dōki)",
        "meaning": "synchronous",
        "example_ja": "この処理は同期的に実行されます。",
        "example_en": "This runs synchronously."
      },
      {
        "front": "プロミス",
        "reading": "puromisu",
        "meaning": "Promise",
        "example_ja": "プロミスは非同期の結果を表します。",
        "example_en": "A Promise represents an async result."
      },
      {
        "front": "待機",
        "reading": "たいき (taiki)",
        "meaning": "await / waiting",
        "example_ja": "awaitで結果を待機します。",
        "example_en": "We await the result with `await`."
      },
      {
        "front": "解決",
        "reading": "かいけつ (kaiketsu)",
        "meaning": "resolve",
        "example_ja": "プロミスが値で解決します。",
        "example_en": "The promise resolves with a value."
      },
      {
        "front": "拒否",
        "reading": "きょひ (kyohi)",
        "meaning": "reject",
        "example_ja": "エラーでプロミスが拒否されます。",
        "example_en": "The promise is rejected with an error."
      },
      {
        "front": "副作用",
        "reading": "ふくさよう (fukusayō)",
        "meaning": "side effect",
        "example_ja": "この関数は副作用がありません。",
        "example_en": "This function has no side effects."
      },
      {
        "front": "純粋関数",
        "reading": "じゅんすいかんすう (junsui kansū)",
        "meaning": "pure function",
        "example_ja": "純粋関数はテストしやすいです。",
        "example_en": "Pure functions are easy to test."
      },
      {
        "front": "デフォルト引数",
        "reading": "デフォルトひきすう (deforuto hikisū)",
        "meaning": "default parameter",
        "example_ja": "デフォルト引数を設定します。",
        "example_en": "We set a default parameter."
      },
      {
        "front": "残余引数",
        "reading": "ざんよひきすう (zan'yo hikisū)",
        "meaning": "rest parameter",
        "example_ja": "残余引数で複数の値を受け取ります。",
        "example_en": "We receive multiple values with a rest parameter."
      },
      {
        "front": "クロージャ",
        "reading": "kurōja",
        "meaning": "closure",
        "example_ja": "クロージャは外側の変数を覚えています。",
        "example_en": "A closure remembers outer variables."
      },
      {
        "front": "再帰",
        "reading": "さいき (saiki)",
        "meaning": "recursion",
        "example_ja": "この関数は再帰を使います。",
        "example_en": "This function uses recursion."
      },
      {
        "front": "並行処理",
        "reading": "へいこうしょり (heikō shori)",
        "meaning": "concurrency",
        "example_ja": "並行処理で複数の要求を扱います。",
        "example_en": "We handle multiple requests with concurrency."
      },
      {
        "front": "例外処理",
        "reading": "れいがいしょり (reigai shori)",
        "meaning": "exception handling",
        "example_ja": "try-catchで例外処理をします。",
        "example_en": "We handle exceptions with try-catch."
      },
      {
        "front": "呼び出し",
        "reading": "よびだし (yobidashi)",
        "meaning": "call / invocation",
        "example_ja": "関数の呼び出しを確認します。",
        "example_en": "We check the function call."
      },
      {
        "front": "遅延評価",
        "reading": "ちえんひょうか (chien hyōka)",
        "meaning": "lazy evaluation",
        "example_ja": "遅延評価で計算を後回しにします。",
        "example_en": "Lazy evaluation defers the computation."
      }
    ],
    "quiz": [],
    "practice": []
  },
  {
    "key": "jp_ts_objects",
    "name": "Objects, Modules & Tooling — オブジェクト・モジュール・ツール",
    "category": "JP: TS Objects & Tooling",
    "what": "Objects, destructuring, immutability, modules, packages and build tools.",
    "deep": "Structuring and moving data around: オブジェクト and プロパティ, 分割代入 (destructuring), スプレッド構文 (spread), and how code is organized into モジュール and packages, then built with a バンドラー.",
    "java": "The **Reading** column gives the kana pronunciation and a romaji gloss so you can say the word out loud. Loanwords written in katakana already *are* their reading, so only the romaji is shown. Example sentences are polite (です・ます) form — exactly how you'd hear them in a standup or an interview.",
    "language": "japanese",
    "lesson": "| 用語 (Term) | 読み方 (Reading) | 意味 (Meaning) | 例文 (Example) |\n|---|---|---|---|\n| オブジェクト | obujekuto | object | オブジェクトにデータをまとめます。 — *We group data in an object.* |\n| プロパティ | puropati | property | オブジェクトにプロパティを追加します。 — *We add a property to the object.* |\n| キー | kī | key | オブジェクトのキーで値を取り出します。 — *We get the value by the object's key.* |\n| 値 | あたい (atai) | value | キーに対応する値を更新します。 — *We update the value for the key.* |\n| 配列 | はいれつ (hairetsu) | array | 配列に要素を追加します。 — *We add an element to the array.* |\n| 分割代入 | ぶんかつだいにゅう (bunkatsu dainyū) | destructuring | 分割代入でプロパティを取り出します。 — *We extract properties with destructuring.* |\n| スプレッド構文 | スプレッドこうぶん (supureddo kōbun) | spread syntax | スプレッド構文で配列を複製します。 — *We copy an array with spread syntax.* |\n| 不変 | ふへん (fuhen) | immutable | 不変のデータは変更しません。 — *We don't change immutable data.* |\n| 可変 | かへん (kahen) | mutable | 可変のオブジェクトは変更できます。 — *A mutable object can be changed.* |\n| 参照 | さんしょう (sanshō) | reference | オブジェクトは参照で渡されます。 — *Objects are passed by reference.* |\n| 複製 | ふくせい (fukusei) | copy / clone | オブジェクトを複製します。 — *We clone the object.* |\n| モジュール | mojūru | module | コードをモジュールに分けます。 — *We split code into modules.* |\n| インポート | inpōto | import | 別のファイルから関数をインポートします。 — *We import a function from another file.* |\n| エクスポート | ekusupōto | export | 関数をエクスポートします。 — *We export the function.* |\n| 名前空間 | なまえくうかん (namae kūkan) | namespace | 名前空間で名前の衝突を防ぎます。 — *A namespace prevents name collisions.* |\n| パッケージ | pakkēji | package | npmでパッケージをインストールします。 — *We install a package with npm.* |\n| 依存関係 | いぞんかんけい (izon kankei) | dependency | プロジェクトの依存関係を管理します。 — *We manage the project's dependencies.* |\n| バンドラー | bandorā | bundler | バンドラーがファイルを一つにまとめます。 — *A bundler combines the files into one.* |\n| ビルド | birudo | build | プロジェクトをビルドします。 — *We build the project.* |\n| 反復処理 | はんぷくしょり (hanpuku shori) | iteration | 配列を反復処理します。 — *We iterate over the array.* |\n| マップ | mappu | Map | マップはキーと値の対を保持します。 — *A Map holds key-value pairs.* |\n| セット | setto | Set | セットは重複を許しません。 — *A Set does not allow duplicates.* |\n| JSON形式 | JSONけいしき (JSON keishiki) | JSON format | データをJSON形式で保存します。 — *We save the data in JSON format.* |\n| 直列化 | ちょくれつか (chokuretsuka) | serialization | オブジェクトを直列化します。 — *We serialize the object.* |\n| 検証 | けんしょう (kenshō) | validation | 入力データを検証します。 — *We validate the input data.* |\n| 設定ファイル | せっていファイル (settei fairu) | config file | tsconfigは設定ファイルです。 — *`tsconfig` is a config file.* |\n",
    "exercises": [],
    "cards": [
      {
        "front": "オブジェクト",
        "reading": "obujekuto",
        "meaning": "object",
        "example_ja": "オブジェクトにデータをまとめます。",
        "example_en": "We group data in an object."
      },
      {
        "front": "プロパティ",
        "reading": "puropati",
        "meaning": "property",
        "example_ja": "オブジェクトにプロパティを追加します。",
        "example_en": "We add a property to the object."
      },
      {
        "front": "キー",
        "reading": "kī",
        "meaning": "key",
        "example_ja": "オブジェクトのキーで値を取り出します。",
        "example_en": "We get the value by the object's key."
      },
      {
        "front": "値",
        "reading": "あたい (atai)",
        "meaning": "value",
        "example_ja": "キーに対応する値を更新します。",
        "example_en": "We update the value for the key."
      },
      {
        "front": "配列",
        "reading": "はいれつ (hairetsu)",
        "meaning": "array",
        "example_ja": "配列に要素を追加します。",
        "example_en": "We add an element to the array."
      },
      {
        "front": "分割代入",
        "reading": "ぶんかつだいにゅう (bunkatsu dainyū)",
        "meaning": "destructuring",
        "example_ja": "分割代入でプロパティを取り出します。",
        "example_en": "We extract properties with destructuring."
      },
      {
        "front": "スプレッド構文",
        "reading": "スプレッドこうぶん (supureddo kōbun)",
        "meaning": "spread syntax",
        "example_ja": "スプレッド構文で配列を複製します。",
        "example_en": "We copy an array with spread syntax."
      },
      {
        "front": "不変",
        "reading": "ふへん (fuhen)",
        "meaning": "immutable",
        "example_ja": "不変のデータは変更しません。",
        "example_en": "We don't change immutable data."
      },
      {
        "front": "可変",
        "reading": "かへん (kahen)",
        "meaning": "mutable",
        "example_ja": "可変のオブジェクトは変更できます。",
        "example_en": "A mutable object can be changed."
      },
      {
        "front": "参照",
        "reading": "さんしょう (sanshō)",
        "meaning": "reference",
        "example_ja": "オブジェクトは参照で渡されます。",
        "example_en": "Objects are passed by reference."
      },
      {
        "front": "複製",
        "reading": "ふくせい (fukusei)",
        "meaning": "copy / clone",
        "example_ja": "オブジェクトを複製します。",
        "example_en": "We clone the object."
      },
      {
        "front": "モジュール",
        "reading": "mojūru",
        "meaning": "module",
        "example_ja": "コードをモジュールに分けます。",
        "example_en": "We split code into modules."
      },
      {
        "front": "インポート",
        "reading": "inpōto",
        "meaning": "import",
        "example_ja": "別のファイルから関数をインポートします。",
        "example_en": "We import a function from another file."
      },
      {
        "front": "エクスポート",
        "reading": "ekusupōto",
        "meaning": "export",
        "example_ja": "関数をエクスポートします。",
        "example_en": "We export the function."
      },
      {
        "front": "名前空間",
        "reading": "なまえくうかん (namae kūkan)",
        "meaning": "namespace",
        "example_ja": "名前空間で名前の衝突を防ぎます。",
        "example_en": "A namespace prevents name collisions."
      },
      {
        "front": "パッケージ",
        "reading": "pakkēji",
        "meaning": "package",
        "example_ja": "npmでパッケージをインストールします。",
        "example_en": "We install a package with npm."
      },
      {
        "front": "依存関係",
        "reading": "いぞんかんけい (izon kankei)",
        "meaning": "dependency",
        "example_ja": "プロジェクトの依存関係を管理します。",
        "example_en": "We manage the project's dependencies."
      },
      {
        "front": "バンドラー",
        "reading": "bandorā",
        "meaning": "bundler",
        "example_ja": "バンドラーがファイルを一つにまとめます。",
        "example_en": "A bundler combines the files into one."
      },
      {
        "front": "ビルド",
        "reading": "birudo",
        "meaning": "build",
        "example_ja": "プロジェクトをビルドします。",
        "example_en": "We build the project."
      },
      {
        "front": "反復処理",
        "reading": "はんぷくしょり (hanpuku shori)",
        "meaning": "iteration",
        "example_ja": "配列を反復処理します。",
        "example_en": "We iterate over the array."
      },
      {
        "front": "マップ",
        "reading": "mappu",
        "meaning": "Map",
        "example_ja": "マップはキーと値の対を保持します。",
        "example_en": "A Map holds key-value pairs."
      },
      {
        "front": "セット",
        "reading": "setto",
        "meaning": "Set",
        "example_ja": "セットは重複を許しません。",
        "example_en": "A Set does not allow duplicates."
      },
      {
        "front": "JSON形式",
        "reading": "JSONけいしき (JSON keishiki)",
        "meaning": "JSON format",
        "example_ja": "データをJSON形式で保存します。",
        "example_en": "We save the data in JSON format."
      },
      {
        "front": "直列化",
        "reading": "ちょくれつか (chokuretsuka)",
        "meaning": "serialization",
        "example_ja": "オブジェクトを直列化します。",
        "example_en": "We serialize the object."
      },
      {
        "front": "検証",
        "reading": "けんしょう (kenshō)",
        "meaning": "validation",
        "example_ja": "入力データを検証します。",
        "example_en": "We validate the input data."
      },
      {
        "front": "設定ファイル",
        "reading": "せっていファイル (settei fairu)",
        "meaning": "config file",
        "example_ja": "tsconfigは設定ファイルです。",
        "example_en": "`tsconfig` is a config file."
      }
    ],
    "quiz": [],
    "practice": []
  },
  {
    "key": "jp_web_frontend",
    "name": "Web & Frontend — Webとフロントエンド",
    "category": "JP: Web & Frontend",
    "what": "Where TypeScript runs: components, state, requests, servers and auth.",
    "deep": "The surrounding world TypeScript is written for: フロントエンド / バックエンド, コンポーネント and 状態 (state), and the request/response cycle with a server and database.",
    "java": "The **Reading** column gives the kana pronunciation and a romaji gloss so you can say the word out loud. Loanwords written in katakana already *are* their reading, so only the romaji is shown. Example sentences are polite (です・ます) form — exactly how you'd hear them in a standup or an interview.",
    "language": "japanese",
    "lesson": "| 用語 (Term) | 読み方 (Reading) | 意味 (Meaning) | 例文 (Example) |\n|---|---|---|---|\n| フロントエンド | furonto endo | frontend | フロントエンドの開発を担当します。 — *I'm in charge of frontend development.* |\n| バックエンド | bakku endo | backend | バックエンドとAPIでやり取りします。 — *We communicate with the backend via an API.* |\n| コンポーネント | konpōnento | component | UIをコンポーネントに分けます。 — *We split the UI into components.* |\n| 状態 | じょうたい (jōtai) | state | コンポーネントの状態を管理します。 — *We manage the component's state.* |\n| 描画 | びょうが (byōga) | render | 画面を描画します。 — *We render the screen.* |\n| 再描画 | さいびょうが (sai byōga) | re-render | 状態が変わると再描画されます。 — *It re-renders when the state changes.* |\n| イベント | ibento | event | クリックイベントを処理します。 — *We handle the click event.* |\n| 要求 | ようきゅう (yōkyū) | request | サーバーに要求を送ります。 — *We send a request to the server.* |\n| 応答 | おうとう (ōtō) | response | サーバーからの応答を待ちます。 — *We wait for the server's response.* |\n| 取得 | しゅとく (shutoku) | fetch / get | APIからデータを取得します。 — *We fetch data from the API.* |\n| 更新 | こうしん (kōshin) | update | 画面を最新の状態に更新します。 — *We update the screen to the latest state.* |\n| 読み込み | よみこみ (yomikomi) | loading | データの読み込み中です。 — *The data is loading.* |\n| 表示 | ひょうじ (hyōji) | display | 結果を画面に表示します。 — *We display the result on the screen.* |\n| 入力欄 | にゅうりょくらん (nyūryoku ran) | input field | 入力欄に名前を入れます。 — *We type a name into the input field.* |\n| ボタン | botan | button | ボタンを押すと送信されます。 — *Pressing the button submits it.* |\n| 画面 | がめん (gamen) | screen | 画面のレイアウトを整えます。 — *We arrange the screen layout.* |\n| サーバー | sābā | server | サーバーを起動します。 — *We start the server.* |\n| データベース | dētabēsu | database | データベースに保存します。 — *We save it to the database.* |\n| 認証 | にんしょう (ninshō) | authentication | ユーザー認証を実装します。 — *We implement user authentication.* |\n| 権限 | けんげん (kengen) | permission | 管理者の権限が必要です。 — *Admin permission is required.* |\n| 通信 | つうしん (tsūshin) | communication | サーバーと通信します。 — *We communicate with the server.* |\n| 非同期通信 | ひどうきつうしん (hidōki tsūshin) | async communication | 非同期通信でページを更新します。 — *We update the page with async communication.* |\n| 再利用 | さいりよう (sairiyō) | reuse | コンポーネントを再利用します。 — *We reuse the component.* |\n| 端末 | たんまつ (tanmatsu) | terminal | 端末でコマンドを実行します。 — *We run commands in the terminal.* |\n",
    "exercises": [],
    "cards": [
      {
        "front": "フロントエンド",
        "reading": "furonto endo",
        "meaning": "frontend",
        "example_ja": "フロントエンドの開発を担当します。",
        "example_en": "I'm in charge of frontend development."
      },
      {
        "front": "バックエンド",
        "reading": "bakku endo",
        "meaning": "backend",
        "example_ja": "バックエンドとAPIでやり取りします。",
        "example_en": "We communicate with the backend via an API."
      },
      {
        "front": "コンポーネント",
        "reading": "konpōnento",
        "meaning": "component",
        "example_ja": "UIをコンポーネントに分けます。",
        "example_en": "We split the UI into components."
      },
      {
        "front": "状態",
        "reading": "じょうたい (jōtai)",
        "meaning": "state",
        "example_ja": "コンポーネントの状態を管理します。",
        "example_en": "We manage the component's state."
      },
      {
        "front": "描画",
        "reading": "びょうが (byōga)",
        "meaning": "render",
        "example_ja": "画面を描画します。",
        "example_en": "We render the screen."
      },
      {
        "front": "再描画",
        "reading": "さいびょうが (sai byōga)",
        "meaning": "re-render",
        "example_ja": "状態が変わると再描画されます。",
        "example_en": "It re-renders when the state changes."
      },
      {
        "front": "イベント",
        "reading": "ibento",
        "meaning": "event",
        "example_ja": "クリックイベントを処理します。",
        "example_en": "We handle the click event."
      },
      {
        "front": "要求",
        "reading": "ようきゅう (yōkyū)",
        "meaning": "request",
        "example_ja": "サーバーに要求を送ります。",
        "example_en": "We send a request to the server."
      },
      {
        "front": "応答",
        "reading": "おうとう (ōtō)",
        "meaning": "response",
        "example_ja": "サーバーからの応答を待ちます。",
        "example_en": "We wait for the server's response."
      },
      {
        "front": "取得",
        "reading": "しゅとく (shutoku)",
        "meaning": "fetch / get",
        "example_ja": "APIからデータを取得します。",
        "example_en": "We fetch data from the API."
      },
      {
        "front": "更新",
        "reading": "こうしん (kōshin)",
        "meaning": "update",
        "example_ja": "画面を最新の状態に更新します。",
        "example_en": "We update the screen to the latest state."
      },
      {
        "front": "読み込み",
        "reading": "よみこみ (yomikomi)",
        "meaning": "loading",
        "example_ja": "データの読み込み中です。",
        "example_en": "The data is loading."
      },
      {
        "front": "表示",
        "reading": "ひょうじ (hyōji)",
        "meaning": "display",
        "example_ja": "結果を画面に表示します。",
        "example_en": "We display the result on the screen."
      },
      {
        "front": "入力欄",
        "reading": "にゅうりょくらん (nyūryoku ran)",
        "meaning": "input field",
        "example_ja": "入力欄に名前を入れます。",
        "example_en": "We type a name into the input field."
      },
      {
        "front": "ボタン",
        "reading": "botan",
        "meaning": "button",
        "example_ja": "ボタンを押すと送信されます。",
        "example_en": "Pressing the button submits it."
      },
      {
        "front": "画面",
        "reading": "がめん (gamen)",
        "meaning": "screen",
        "example_ja": "画面のレイアウトを整えます。",
        "example_en": "We arrange the screen layout."
      },
      {
        "front": "サーバー",
        "reading": "sābā",
        "meaning": "server",
        "example_ja": "サーバーを起動します。",
        "example_en": "We start the server."
      },
      {
        "front": "データベース",
        "reading": "dētabēsu",
        "meaning": "database",
        "example_ja": "データベースに保存します。",
        "example_en": "We save it to the database."
      },
      {
        "front": "認証",
        "reading": "にんしょう (ninshō)",
        "meaning": "authentication",
        "example_ja": "ユーザー認証を実装します。",
        "example_en": "We implement user authentication."
      },
      {
        "front": "権限",
        "reading": "けんげん (kengen)",
        "meaning": "permission",
        "example_ja": "管理者の権限が必要です。",
        "example_en": "Admin permission is required."
      },
      {
        "front": "通信",
        "reading": "つうしん (tsūshin)",
        "meaning": "communication",
        "example_ja": "サーバーと通信します。",
        "example_en": "We communicate with the server."
      },
      {
        "front": "非同期通信",
        "reading": "ひどうきつうしん (hidōki tsūshin)",
        "meaning": "async communication",
        "example_ja": "非同期通信でページを更新します。",
        "example_en": "We update the page with async communication."
      },
      {
        "front": "再利用",
        "reading": "さいりよう (sairiyō)",
        "meaning": "reuse",
        "example_ja": "コンポーネントを再利用します。",
        "example_en": "We reuse the component."
      },
      {
        "front": "端末",
        "reading": "たんまつ (tanmatsu)",
        "meaning": "terminal",
        "example_ja": "端末でコマンドを実行します。",
        "example_en": "We run commands in the terminal."
      }
    ],
    "quiz": [],
    "practice": []
  },
  {
    "key": "alg_what_is",
    "name": "What Is an Algorithm?",
    "category": "Algo: Foundations",
    "what": "A precise recipe that turns input into the right output — and the two yardsticks we judge it by.",
    "deep": "An algorithm is a finite, unambiguous sequence of steps that transforms an input into a desired output. Every step must be well-defined and the whole thing must eventually stop. We judge an algorithm on two axes: is it correct (does it produce the right answer for every valid input?) and is it efficient (how does its time and memory grow as the input grows?). Correctness comes first — a fast wrong answer is worthless — but among correct solutions we prefer the one that scales.",
    "java": "**Correctness** — right answer on *every* valid input, edge cases included.\n\n**Efficiency** — how time & memory grow with input size *n*.\n\nA good solution is *correct first, efficient second*.",
    "language": "algorithms",
    "lesson": "An **algorithm** is a finite list of unambiguous steps that turns an *input*\ninto the correct *output*. Three properties make it an algorithm rather than\njust \"some code\":\n\n1. **Finiteness** — it terminates after a finite number of steps.\n2. **Definiteness** — every step is precisely defined; no \"do the obvious thing\".\n3. **Effectiveness** — each step is simple enough to actually be carried out.\n\n## The two yardsticks\n\nEvery algorithm is judged on two independent axes:\n\n- **Correctness** — does it give the right answer for *every* valid input,\n  including the awkward edge cases (empty input, one element, all-equal,\n  negatives, the largest allowed value)?\n- **Efficiency** — how do its running time and memory use *grow* as the input\n  grows? This is what [Big-O notation](#) captures.\n\n> Correctness always wins ties. A blazing-fast function that's wrong on empty\n> input is worse than a slow one that's always right. Get it correct, *then*\n> make it fast.\n\n## A tiny example — is a number even?\n\n```text\nfunction isEven(n):\n    return (n mod 2) == 0\n```\n\nTrace it on a few inputs to convince yourself it's correct:\n\n| Input n | n mod 2 | Output |\n|---|---|---|\n| 4 | 0 | true |\n| 7 | 1 | false |\n| 0 | 0 | true |\n| -3 | depends on language's mod | ⚠️ careful |\n\nThat last row is the whole game: an algorithm is only correct once you've\npinned down the awkward cases. In some languages `-3 mod 2` is `-1`, not `1` —\nso `== 0` still works here, but *thinking about it* is the habit that separates\na correct solution from a lucky one.\n\n## The engineering loop\n\nWhen you meet a problem, work in this order:\n\n1. **Understand** the exact input, output, and constraints.\n2. Find *a* correct approach (even a brute-force one).\n3. **Analyze** its cost.\n4. **Improve** the cost only if the constraints demand it.\n\nThe rest of this track gives you the vocabulary for steps 3 and 4, then a\ntoolbox of techniques for step 4.\n\n### Key takeaways\n- An algorithm = finite + definite + effective steps from input to output.\n- Judge it on **correctness** and **efficiency**, in that priority order.\n- Edge cases are part of correctness, not an afterthought.\n",
    "exercises": [],
    "cards": [],
    "quiz": [
      {
        "question": "Which pair of properties do we use to judge an algorithm?",
        "options": [
          "Length and readability",
          "Correctness and efficiency",
          "Language and framework",
          "Number of lines and comments"
        ],
        "answer": 1,
        "explanation": "We ask two things of every algorithm: does it produce the right answer for all valid inputs (correctness), and how well does it scale (efficiency)?"
      },
      {
        "question": "You have two solutions: A is very fast but crashes on empty input; B is slower but always correct. Which is preferable as a starting point?",
        "options": [
          "A, because speed matters most",
          "B, because correctness comes before efficiency",
          "Whichever is shorter",
          "They are equally good"
        ],
        "answer": 1,
        "explanation": "A wrong-but-fast answer is worthless. Correctness is the first requirement; you optimize only among solutions that are already correct."
      },
      {
        "question": "Which of these disqualifies a procedure from being an algorithm?",
        "options": [
          "It uses a loop",
          "It might never terminate on some inputs",
          "It calls another function",
          "It uses more than O(1) memory"
        ],
        "answer": 1,
        "explanation": "An algorithm must be finite — it has to terminate. A procedure that can loop forever on some input is not an algorithm."
      },
      {
        "question": "Why do we care about how cost grows with input size n rather than the raw time in seconds?",
        "options": [
          "Seconds are hard to spell",
          "Raw time depends on the machine, language, and load; growth rate is intrinsic to the algorithm",
          "Growth rate is always smaller",
          "Seconds only matter for sorting"
        ],
        "answer": 1,
        "explanation": "Wall-clock time varies with hardware and language. The *growth rate* (Big-O) is a property of the algorithm itself, so it's what we reason about."
      }
    ],
    "practice": [
      {
        "slug": "even-or-odd",
        "note": "The isEven example above, as a runnable problem."
      },
      {
        "slug": "larger-of-two",
        "note": "The simplest 'correct on every input' warm-up — mind the equal case."
      },
      {
        "slug": "print-greeting",
        "note": "Input in, exact output out: the whole shape of an algorithm."
      }
    ]
  },
  {
    "key": "alg_big_o",
    "name": "Big-O Notation",
    "category": "Algo: Foundations",
    "what": "The language for how an algorithm's cost grows — drop the constants, keep the shape.",
    "deep": "Big-O describes the growth rate of an algorithm's cost as the input size n grows toward infinity. We keep only the fastest-growing term and drop constant factors, because for large n those dominate everything else: 3n + 100 and n/2 are both O(n). The point isn't to predict exact runtimes — it's to compare how algorithms *scale* so you can tell an O(n) solution from an O(n^2) one before you ever run them.",
    "java": "Keep the **dominant term**, drop **constants & lower terms**.\n\n`3n^2 + 5n + 9  →  O(n^2)`\n\nRanking (best→worst): `O(1) < O(log n) < O(n) < O(n log n) < O(n^2) < O(2^n) < O(n!)`",
    "language": "algorithms",
    "lesson": "**Big-O notation** answers one question: *as the input gets big, how fast does\nthe work grow?* It deliberately throws away detail so you can compare\nalgorithms without a stopwatch.\n\nTwo rules produce every Big-O you'll write:\n\n1. **Drop constant factors.** `500n` and `n` both scale linearly → `O(n)`.\n2. **Keep only the dominant term.** For large *n*, `n^2` dwarfs `n`, which\n   dwarfs `1`. So `n^2 + 3n + 7 → O(n^2)`.\n\nWhy is this fair? Because we care about *large* inputs. At n = 1,000,000 an\nO(n^2) algorithm does a trillion steps while an O(n) one does a million — the\nconstants are irrelevant next to that gap.\n\n## The growth-rate ladder\n\nFrom best to worst, the classes you meet constantly:\n\n| Big-O | Name | Doubling n does what? | Typical source |\n|---|---|---|---|\n| O(1) | constant | nothing | array index, hash lookup |\n| O(log n) | logarithmic | +1 step | binary search, balanced tree |\n| O(n) | linear | doubles | one pass over the data |\n| O(n log n) | linearithmic | a bit more than doubles | good sorting |\n| O(n^2) | quadratic | ×4 | nested loop over all pairs |\n| O(2^n) | exponential | squares | trying every subset |\n| O(n!) | factorial | explodes | trying every ordering |\n\n## Feel the difference\n\nHow many steps for each class as *n* grows:\n\n| n | O(log n) | O(n) | O(n log n) | O(n^2) | O(2^n) |\n|---|---|---|---|---|---|\n| 10 | 3 | 10 | 33 | 100 | 1,024 |\n| 100 | 7 | 100 | 664 | 10,000 | huge |\n| 1,000 | 10 | 1,000 | 9,966 | 1,000,000 | astronomical |\n\nThe lesson: an O(n^2) algorithm is fine at n = 100 and hopeless at n = 100,000.\nReading the constraint on *n* in a problem often tells you which complexity\nclass you need to hit.\n\n## O, Θ, Ω in one breath\n\n- **O(f)** — an *upper* bound: grows *no faster* than f (the common one).\n- **Ω(f)** — a *lower* bound: grows *at least* as fast as f.\n- **Θ(f)** — both: grows *exactly* like f.\n\nIn interviews \"Big-O\" almost always means the worst-case upper bound, Θ in\nspirit. Say \"O(n)\" and you'll be understood.\n\n### Common pitfalls\n- **Constants inside the same class don't matter, but the *class* does.** Two\n  passes is still O(n); a nested pass is O(n^2).\n- `O(log n)` has no base — `log₂` and `log₁₀` differ by a constant factor, which\n  Big-O drops.\n- Adding sequential phases *adds* (take the max: `O(n) + O(n log n) = O(n log n)`);\n  nesting loops *multiplies* (`O(n) · O(n) = O(n^2)`).\n\n### Key takeaways\n- Drop constants, keep the dominant term.\n- Memorize the ladder `1 < log n < n < n log n < n^2 < 2^n < n!`.\n- The constraint on *n* hints at the complexity class you must reach.\n",
    "exercises": [],
    "cards": [],
    "quiz": [
      {
        "question": "Simplify: 4n^2 + 100n + 3000 in Big-O.",
        "options": [
          "O(n)",
          "O(n^2)",
          "O(4n^2)",
          "O(n^2 + n)"
        ],
        "answer": 1,
        "explanation": "Keep only the dominant term (n^2) and drop constant factors and lower-order terms → O(n^2)."
      },
      {
        "question": "An algorithm makes two separate full passes over an array of size n, one after the other. Its complexity is:",
        "options": [
          "O(n^2)",
          "O(2n) which is O(n)",
          "O(n log n)",
          "O(1)"
        ],
        "answer": 1,
        "explanation": "Sequential phases add: n + n = 2n, and constant factors drop, so it's O(n). Nesting (not sequencing) would give O(n^2)."
      },
      {
        "question": "Which growth rate is best (scales best) for large n?",
        "options": [
          "O(n^2)",
          "O(n log n)",
          "O(log n)",
          "O(n)"
        ],
        "answer": 2,
        "explanation": "Order is O(log n) < O(n) < O(n log n) < O(n^2). Logarithmic grows slowest, so it scales best."
      },
      {
        "question": "You combine an O(n) step followed by an O(n log n) sort. Total complexity?",
        "options": [
          "O(n^2 log n)",
          "O(n log n)",
          "O(n)",
          "O(log n)"
        ],
        "answer": 1,
        "explanation": "Sequential steps add, and the sum is dominated by the larger term: O(n) + O(n log n) = O(n log n)."
      },
      {
        "question": "Doubling n roughly quadruples the running time. Which class is this?",
        "options": [
          "O(log n)",
          "O(n)",
          "O(n^2)",
          "O(2^n)"
        ],
        "answer": 2,
        "explanation": "For O(n^2), replacing n with 2n gives (2n)^2 = 4n^2 — four times the work. That ×4 fingerprint means quadratic."
      },
      {
        "question": "The problem says n can be up to 200,000 and the time limit is tight. Which target complexity is the safe aim?",
        "options": [
          "O(n^2) — about 4×10^10 operations",
          "O(n log n) or better",
          "O(2^n)",
          "O(n!)"
        ],
        "answer": 1,
        "explanation": "At n = 2×10^5, O(n^2) is ~4×10^10 operations — far too slow. O(n log n) (~3.5 million) comfortably fits, so aim there or better."
      }
    ],
    "practice": [
      {
        "slug": "array-sum",
        "note": "One pass — the canonical O(n)."
      },
      {
        "slug": "binary-search-first",
        "note": "O(log n): each step halves the search space."
      },
      {
        "slug": "contains-duplicate",
        "note": "Compare the O(n^2) all-pairs idea with the O(n) hashing idea."
      }
    ]
  },
  {
    "key": "alg_analyzing",
    "name": "Analyzing Loops & Recursion",
    "category": "Algo: Foundations",
    "what": "Read code and derive its Big-O — from nested loops to amortized costs.",
    "deep": "Deriving an algorithm's complexity is a mechanical skill once you know the rules: a loop that runs n times is O(n); nesting multiplies; a loop that halves its range each step is O(log n). Recursion costs (number of calls) × (work per call). And some operations look expensive but are cheap on average — an amortized cost — like appending to a dynamic array that occasionally doubles. This lesson turns 'staring at code' into a repeatable counting procedure.",
    "java": "**Sequential** → add. **Nested** → multiply. **Halving** → log n.\n\nRecursion cost = (number of calls) × (work per call).\n\n**Amortized** = average per operation over a worst-case sequence.",
    "language": "algorithms",
    "lesson": "Complexity analysis is just *counting the steps as a function of n*. Here's the\nwhole toolkit.\n\n## Rule 1 — a simple loop is linear\n\n```text\nfor i in 0 .. n-1:      # runs n times\n    do O(1) work\n```\n→ **O(n)**. The body's constant work times n iterations.\n\n## Rule 2 — nested loops multiply\n\n```text\nfor i in 0 .. n-1:          # n times\n    for j in 0 .. n-1:      #   n times each\n        do O(1) work\n```\n→ **O(n · n) = O(n^2)**. Each extra nested level over the full range multiplies\nby another n.\n\nWatch the *bounds*, though. This one is **not** n^2:\n\n```text\nfor i in 0 .. n-1:\n    for j in i+1 .. n-1:   # shrinks: n-1, n-2, ... 1\n        do O(1) work\n```\nThe inner loop runs `(n-1) + (n-2) + ... + 1 = n(n-1)/2` times → still\n**O(n^2)**, because n²/2 drops its constant. All-pairs work is quadratic even\nwhen you only touch each pair once.\n\n## Rule 3 — halving the range is logarithmic\n\n```text\nwhile n > 1:\n    n = n / 2      # 16 → 8 → 4 → 2 → 1\n```\nHalving until you hit 1 takes about **log₂ n** steps → **O(log n)**. This is the\nheartbeat of [binary search](#).\n\n## Rule 4 — recursion = calls × work-per-call\n\nCount how many times the function is invoked, and how much work each invocation\ndoes *outside* its recursive calls.\n\n```text\nfunction sum(list, i):\n    if i == length(list): return 0\n    return list[i] + sum(list, i+1)\n```\nn calls, O(1) each → **O(n)** time. But also **O(n) space** — n stack frames are\nalive at once (see [Recursion & the Call Stack](#)).\n\nA recurrence like `T(n) = 2·T(n/2) + O(n)` (two half-size calls plus linear\nmerging) solves to **O(n log n)** — that's merge sort. The\n[Recurrences & Master Theorem](#) lesson makes this systematic.\n\n## Best / worst / average case\n\nThe same algorithm can have different costs depending on the *input*, not just\nits size:\n\n| Case | Meaning | Linear search example |\n|---|---|---|\n| Best | luckiest input | O(1) — target is first |\n| Worst | unluckiest input | O(n) — target is last / absent |\n| Average | expected over inputs | O(n) — on average n/2 checks |\n\nBy default \"the complexity\" means **worst case** unless stated otherwise.\n\n## Amortized cost — cheap on average\n\nSome single operations are occasionally expensive but rare enough to be cheap\n*per operation over a long run*. Appending to a dynamic array is the classic:\nusually O(1), but when it's full it doubles (an O(n) copy). Because doublings\nare rare, the **amortized** cost of append is **O(1)**.\n\n| Append # | Capacity | Cost |\n|---|---|---|\n| 1 | 1 → grow | O(1) |\n| 2 | 2 → grow | O(1)+copy |\n| 3–4 | 4 | O(1) |\n| 5–8 | 8 | O(1) |\n\nTotal work for n appends is ~2n → **O(1) amortized each**.\n\n### Common pitfalls\n- A nested loop whose inner bound *shrinks* is still O(n^2), not O(n).\n- `log` sneaks in whenever a quantity is repeatedly halved or doubled.\n- Don't confuse *amortized* (average over a sequence, worst-case-safe) with\n  *average-case* (expected over random inputs).\n\n### Key takeaways\n- Sequential adds, nested multiplies, halving gives log n.\n- Recursion cost = calls × work-per-call; mind the stack space too.\n- \"Complexity\" defaults to worst case; some cheap-looking ops are only cheap\n  *amortized*.\n",
    "exercises": [],
    "cards": [],
    "quiz": [
      {
        "question": "What is the time complexity of two nested loops that each run from 0 to n-1?",
        "options": [
          "O(n)",
          "O(2n)",
          "O(n^2)",
          "O(log n)"
        ],
        "answer": 2,
        "explanation": "Nesting multiplies: n iterations × n iterations = n^2 → O(n^2)."
      },
      {
        "question": "The inner loop runs from i+1 to n-1 (shrinking as i grows). Overall complexity?",
        "options": [
          "O(n) — because the inner loop shrinks",
          "O(n^2) — the total is n(n-1)/2, still quadratic",
          "O(n log n)",
          "O(log n)"
        ],
        "answer": 1,
        "explanation": "Summing the shrinking bounds gives n(n-1)/2 ≈ n²/2. Dropping the constant, that's still O(n^2) — all-pairs work."
      },
      {
        "question": "A loop that repeatedly halves n until it reaches 1 runs in:",
        "options": [
          "O(n)",
          "O(n/2)",
          "O(log n)",
          "O(1)"
        ],
        "answer": 2,
        "explanation": "Halving 16→8→4→2→1 takes log₂ n steps, so it's O(log n)."
      },
      {
        "question": "For the recurrence T(n) = 2·T(n/2) + O(n), the total time is:",
        "options": [
          "O(n)",
          "O(n log n)",
          "O(n^2)",
          "O(log n)"
        ],
        "answer": 1,
        "explanation": "Two half-size subproblems plus linear work per level, over log n levels of n total work, gives O(n log n) — the merge-sort recurrence."
      },
      {
        "question": "Appending to a dynamic array that doubles when full has which amortized cost per append?",
        "options": [
          "O(n)",
          "O(log n)",
          "O(1)",
          "O(n^2)"
        ],
        "answer": 2,
        "explanation": "Occasional O(n) doublings are rare enough that n appends cost ~2n total, i.e. O(1) amortized each."
      },
      {
        "question": "Unless stated otherwise, 'the time complexity' of an algorithm refers to its:",
        "options": [
          "Best case",
          "Average case",
          "Worst case",
          "Amortized case"
        ],
        "answer": 2,
        "explanation": "By convention, an unqualified complexity is the worst case — the guarantee that holds for every input of size n."
      }
    ],
    "practice": [
      {
        "slug": "contains-duplicate",
        "note": "Reason about the nested-loop O(n^2) vs. the hashing O(n)."
      },
      {
        "slug": "binary-search-first",
        "note": "Prove to yourself the halving loop is O(log n)."
      },
      {
        "slug": "array-sum",
        "note": "The textbook single O(n) pass."
      }
    ]
  },
  {
    "key": "alg_space",
    "name": "Space Complexity & Tradeoffs",
    "category": "Algo: Foundations",
    "what": "Count the extra memory an algorithm needs — and trade memory for speed on purpose.",
    "deep": "Space complexity measures the *extra* memory an algorithm uses as a function of n, beyond the input itself. A hash set of everything seen is O(n) space; a couple of index variables are O(1) — 'in place'. Recursion quietly costs O(depth) in stack frames. The deep idea is the time–space tradeoff: you can very often buy speed with memory (a lookup table, a cache, a precomputed prefix array) or save memory at the cost of time. Knowing which way to trade is half of optimization.",
    "java": "**Auxiliary space** = extra memory beyond the input, as a function of n.\n\n`O(1)` = in place · `O(n)` = a set/array of everything · recursion = `O(depth)`.\n\n**Time–space tradeoff:** spend memory to save time (or vice-versa).",
    "language": "algorithms",
    "lesson": "**Space complexity** is Big-O for memory: how much *extra* storage an algorithm\nneeds as *n* grows. We usually mean **auxiliary space** — memory beyond the\ninput you were given.\n\n## Reading space off code\n\n| Pattern | Auxiliary space |\n|---|---|\n| A few scalar variables (counters, pointers) | O(1) — \"in place\" |\n| A copy of the array, or a set/map of all elements | O(n) |\n| A 2-D table of size n×n (e.g. some DP) | O(n^2) |\n| Recursion n levels deep | O(n) for the call stack |\n\nThat last row surprises people: recursion isn't free. Each pending call keeps a\nstack frame alive, so recursing to depth *d* costs **O(d)** memory even if each\nframe is tiny. A recursive sum over n elements is O(n) *space*, while the loop\nversion is O(1).\n\n## The time–space tradeoff\n\nThe central bargain of algorithms: **you can usually trade one resource for the\nother.** Spend memory to go faster, or accept slower code to fit in less memory.\n\nClassic example — *\"have I seen this value before?\"*\n\n```text\n# O(n^2) time, O(1) space: re-scan for each element\nfor each x in list:\n    for each y in earlier part of list:\n        if x == y: return true\n\n# O(n) time, O(n) space: remember what you've seen\nseen = empty set\nfor each x in list:\n    if x in seen: return true\n    add x to seen\n```\n\nThe second version spends **O(n) memory** to cut the time from **O(n^2) to\nO(n)**. That is the tradeoff, made on purpose.\n\nOther everyday trades:\n- **Prefix sums** — precompute an O(n) array so each range-sum query is O(1)\n  instead of O(n).\n- **Memoization / caching** — store subresults so you never recompute them\n  (turns exponential recursion into polynomial).\n- **Lookup tables** — precompute answers to avoid recomputing on the fly.\n\n## When space is the constraint\n\nSometimes memory is the scarce resource and you trade the other way: process a\nhuge file in a **streaming** pass (O(1) space, maybe more time), reverse an\narray **in place** instead of building a copy, or use an iterative loop to avoid\ndeep-recursion stack space.\n\n### Common pitfalls\n- Forgetting recursion's **O(depth)** stack cost when you claim \"O(1) space\".\n- Counting the input itself as auxiliary space — usually we don't; we count the\n  *extra* memory.\n- Building a whole copy when an in-place two-pointer swap would do.\n\n### Key takeaways\n- Space complexity = extra memory vs. n; O(1) is \"in place\", O(n) is \"remember\n  everything\".\n- Recursion costs O(depth) in stack frames.\n- Optimization is often just *choosing which way to trade time for space*.\n",
    "exercises": [],
    "cards": [],
    "quiz": [
      {
        "question": "You store every element you've seen in a hash set while scanning an array of size n. Auxiliary space is:",
        "options": [
          "O(1)",
          "O(log n)",
          "O(n)",
          "O(n^2)"
        ],
        "answer": 2,
        "explanation": "The set can hold up to n distinct elements, so it uses O(n) extra memory."
      },
      {
        "question": "A recursive function that recurses to a depth of n uses how much space just for the call stack?",
        "options": [
          "O(1) — recursion is free",
          "O(log n)",
          "O(n)",
          "O(n^2)"
        ],
        "answer": 2,
        "explanation": "Each pending call keeps a stack frame alive, so depth-n recursion costs O(n) stack space even with tiny frames."
      },
      {
        "question": "The 'seen set' trick turns an O(n^2)-time duplicate check into O(n) time. What is it spending to do that?",
        "options": [
          "Nothing",
          "O(n) extra memory",
          "O(n^2) extra memory",
          "CPU cache only"
        ],
        "answer": 1,
        "explanation": "It's a time–space tradeoff: O(n) extra memory (the set) buys the drop from O(n^2) to O(n) time."
      },
      {
        "question": "Reversing an array 'in place' with two pointers uses how much auxiliary space?",
        "options": [
          "O(1)",
          "O(n)",
          "O(log n)",
          "O(n^2)"
        ],
        "answer": 0,
        "explanation": "In-place means only a constant number of extra variables (the two indices and a temp) → O(1) auxiliary space."
      },
      {
        "question": "Precomputing a prefix-sum array so each range-sum query is O(1) is an example of:",
        "options": [
          "Saving memory at the cost of time",
          "Spending O(n) memory to make queries faster",
          "Reducing both time and space to zero",
          "An in-place algorithm"
        ],
        "answer": 1,
        "explanation": "You build an O(n) array once so that later queries drop from O(n) to O(1) — spending memory to buy query speed."
      }
    ],
    "practice": [
      {
        "slug": "contains-duplicate",
        "note": "The O(n) space / O(n) time set trick in action."
      },
      {
        "slug": "reverse-string",
        "note": "In-place two-pointer reversal — O(1) auxiliary space."
      },
      {
        "slug": "running-sum",
        "note": "Prefix sums: spend O(n) memory to answer range questions fast."
      }
    ]
  },
  {
    "key": "alg_pattern_recognition",
    "name": "Reading a Problem → Picking a Technique",
    "category": "Algo: Foundations",
    "what": "The single most useful skill: map the *signals* in a problem statement to the right tool.",
    "deep": "Experienced problem-solvers don't try every technique — they read the problem and recognize signals that point to a specific tool. 'Sorted array' whispers binary search or two pointers. 'Contiguous subarray' means sliding window or prefix sums. 'All combinations / permutations' means backtracking. 'Shortest path in an unweighted graph' means BFS. This lesson is a decision cheat-sheet: learn the signal→tool mapping and most problems announce their own solution.",
    "java": "Read the **signal words** in the statement, then reach for the matching tool:\n\n*sorted* → binary search / two pointers · *contiguous subarray* → sliding window / prefix sums · *all combinations* → backtracking · *shortest unweighted path* → BFS.",
    "language": "algorithms",
    "lesson": "The hardest part of a problem is usually *knowing which technique applies*. Good\nnews: problem statements leak their intended approach through **signal words**.\nTrain yourself to read for them.\n\n## The signal → technique cheat-sheet\n\n| If the problem says… | Reach for… | Why |\n|---|---|---|\n| \"**sorted** array\" / \"find a pair/target\" | Binary search, or two pointers | Sortedness lets you discard half, or move from both ends |\n| \"**contiguous** subarray / substring\" | Sliding window, or prefix sums | A window grows/shrinks over a run; prefix sums answer range totals |\n| \"**subarray/range sum**\" queries | Prefix sums | Precompute once, answer each query in O(1) |\n| \"**k-th largest / smallest**\", \"top k\" | Heap (priority queue) | A heap keeps the k best without full sorting |\n| \"**all** subsets / permutations / combinations\" | Backtracking | You must explore the whole decision tree |\n| \"**shortest path**\", unweighted grid/graph | BFS | BFS explores in rings of increasing distance |\n| \"shortest path with **weights**\" | Dijkstra | Greedy expansion by smallest known distance |\n| \"**overlapping** subproblems\", \"min/max ways to…\" | Dynamic programming | Reuse subresults instead of recomputing |\n| \"**next greater / smaller** element\" | Monotonic stack | The stack keeps candidates in sorted order |\n| \"detect a **cycle**\", \"connected groups\" | Union-Find, or DFS | Track connectivity / revisits |\n| \"does a valid **X** exist / is it feasible?\" | Binary search **on the answer**, or greedy | Monotonic feasibility → binary search the threshold |\n\n## A worked read\n\n> \"Given a **sorted** array and a target, return the **two indices** whose\n> values sum to the target.\"\n\nSignals: *sorted* + *find a pair summing to a target*. → **Two pointers**: start\none pointer at each end; if the sum is too big move the right pointer left, too\nsmall move the left pointer right. O(n), no extra memory.\n\n> \"Find the length of the **longest contiguous substring** without repeating\n> characters.\"\n\nSignals: *longest contiguous substring* + a validity condition. → **Sliding\nwindow**: expand the right edge, and when a repeat appears, shrink the left edge\nuntil it's valid again. O(n).\n\n## The general procedure\n\n1. **Restate** the input, output, and constraints in your own words.\n2. **Look at the size of n** — it caps your complexity (see [Big-O](#)). n ≤ 20\n   invites exponential/backtracking; n ≤ 10^5 wants O(n log n) or better.\n3. **Scan for signal words** and match them above.\n4. Sketch the **brute force** first; then let the signals guide you to the\n   improvement.\n\n## When nothing matches\n\nFall back to fundamentals: can you **sort** to expose structure? Can a **hash\nmap** turn a search into O(1)? Can you **precompute** something once and reuse\nit? These three — sort, hash, precompute — unlock a huge fraction of problems.\n\n### Key takeaways\n- Problems announce their technique through signal words — learn the mapping.\n- The constraint on *n* narrows the complexity class before you pick a tool.\n- When stuck, try to *sort*, *hash*, or *precompute*.\n",
    "exercises": [],
    "cards": [],
    "quiz": [
      {
        "question": "A problem gives you a SORTED array and asks for a pair summing to a target. The signal points to:",
        "options": [
          "Backtracking",
          "Two pointers (or binary search)",
          "BFS",
          "A heap"
        ],
        "answer": 1,
        "explanation": "'Sorted' plus 'find a pair' is the classic two-pointers signal: move inward from both ends based on the current sum."
      },
      {
        "question": "'Longest contiguous subarray/substring satisfying a condition' most strongly signals:",
        "options": [
          "Dynamic programming",
          "Union-Find",
          "Sliding window",
          "Binary search"
        ],
        "answer": 2,
        "explanation": "A contiguous run with a validity condition is the sliding-window fingerprint: grow the right edge, shrink the left when it becomes invalid."
      },
      {
        "question": "You must return the k largest elements of a big stream. Best-matched tool?",
        "options": [
          "Sort the whole stream every time",
          "A heap / priority queue of size k",
          "Backtracking",
          "Prefix sums"
        ],
        "answer": 1,
        "explanation": "A size-k heap keeps the top k without fully sorting everything — the standard 'top k' technique."
      },
      {
        "question": "The problem asks for ALL permutations of a set. This signals:",
        "options": [
          "Greedy",
          "Backtracking",
          "Binary search",
          "Sliding window"
        ],
        "answer": 1,
        "explanation": "'All arrangements/combinations/subsets' means you must explore the full decision tree — that's backtracking."
      },
      {
        "question": "Constraints say n ≤ 20 and you must consider every subset. What complexity is acceptable here?",
        "options": [
          "Only O(n) is allowed",
          "Exponential like O(2^n) is fine because n is tiny",
          "O(n log n) is required",
          "O(1) is required"
        ],
        "answer": 1,
        "explanation": "Small n (≤ ~20) is a hint that an exponential/backtracking solution (2^20 ≈ 10^6) is intended and fast enough."
      },
      {
        "question": "Shortest path in an UNWEIGHTED grid or graph is the signal for:",
        "options": [
          "Dijkstra",
          "BFS",
          "DFS",
          "A heap"
        ],
        "answer": 1,
        "explanation": "BFS explores in rings of increasing distance, so the first time it reaches a node is via a shortest (fewest-edges) path."
      }
    ],
    "practice": [
      {
        "slug": "two-sum-sorted",
        "note": "Read the signals: sorted + find a pair → two pointers."
      },
      {
        "slug": "longest-unique-substring",
        "note": "Contiguous + validity condition → sliding window."
      },
      {
        "slug": "number-of-islands",
        "note": "Connected groups in a grid → BFS/DFS flood fill."
      }
    ]
  },
  {
    "key": "alg_linear_search",
    "name": "Linear Search & Scanning",
    "category": "Algo: Searching & Scanning",
    "what": "The humble one-pass scan — and why it's often exactly the right answer.",
    "deep": "Linear search walks the collection one element at a time until it finds what it wants (or runs out). It's O(n) and needs no preparation — no sorting, no extra structure. It's the baseline every other search improves on, but it's also the *correct* choice for unsorted data, single queries, and the running-aggregate pattern (max, min, sum, count) that shows up everywhere. Master the clean scan before you reach for anything fancier.",
    "java": "Walk every element once, keep what you need.\n\n```\nfor x in list:\n    update running answer with x\n```\n\n**O(n) time, O(1) space.** No sorting or setup required.",
    "language": "algorithms",
    "lesson": "**Linear search** (a *scan*) examines elements one by one from start to end. It's\nthe simplest search there is and the honest baseline for everything else.\n\n```text\nfunction find(list, target):\n    for i in 0 .. length(list)-1:\n        if list[i] == target:\n            return i        # found it\n    return -1               # not present\n```\n\n- **Time:** O(n) worst case (target last or absent), O(1) best case.\n- **Space:** O(1).\n- **Requires:** nothing — works on unsorted data with no preprocessing.\n\n## The running-aggregate pattern\n\nMost \"scan\" problems aren't literally searching for a value — they sweep once\nwhile maintaining a *running answer*: a max, min, sum, count, or best-so-far.\n\n```text\nfunction maximum(list):\n    best = list[0]\n    for i in 1 .. length(list)-1:\n        if list[i] > best:\n            best = list[i]\n    return best\n```\n\nTrace `maximum([3, 9, 2, 9, 5])`:\n\n| i | list[i] | best before | best after |\n|---|---|---|---|\n| 1 | 9 | 3 | 9 |\n| 2 | 2 | 9 | 9 |\n| 3 | 9 | 9 | 9 |\n| 4 | 5 | 9 | 9 |\n\nResult: **9**. One pass, O(n), O(1). The same skeleton computes a sum (start at\n0, add each), a count (start at 0, add 1 when a condition holds), or \"best so\nfar\" for many greedy problems.\n\n## When linear search is the right tool\n\n- The data is **unsorted** and you'll query it **once** — sorting first (O(n log\n  n)) would cost more than the single O(n) scan.\n- You need a **running aggregate** (max/min/sum/count).\n- The collection is **small**, where clarity beats cleverness.\n\n## When to reach past it\n\n- **Repeated** lookups on the same data → build a **hash set/map** once (O(n)),\n  then each lookup is O(1).\n- The data is (or can be) **sorted** and you'll query many times → [binary\n  search](#) at O(log n).\n\n### Common pitfalls\n- Initializing `best` to 0 for a max over possibly-negative numbers — start from\n  the first element instead.\n- Returning early on the *first* match when the problem wants the *last* or\n  *all* matches.\n- Off-by-one on the loop bound (`< length`, not `<= length`).\n\n### Key takeaways\n- Linear search is O(n), needs no setup, and is correct on unsorted data.\n- The running-aggregate skeleton (max/min/sum/count in one pass) is everywhere.\n- Reach for hashing or binary search only when you'll query repeatedly.\n",
    "exercises": [],
    "cards": [],
    "quiz": [
      {
        "question": "What is the worst-case time complexity of linear search over n elements?",
        "options": [
          "O(1)",
          "O(log n)",
          "O(n)",
          "O(n^2)"
        ],
        "answer": 2,
        "explanation": "In the worst case the target is last or absent, so you examine all n elements → O(n)."
      },
      {
        "question": "Linear search requires the data to be sorted first.",
        "options": [
          "True",
          "False"
        ],
        "answer": 1,
        "explanation": "False — linear search works on unsorted data with no preprocessing. That's one of its main advantages."
      },
      {
        "question": "For finding the maximum of a list that may contain negative numbers, what should `best` start as?",
        "options": [
          "0",
          "The first element of the list",
          "Negative one",
          "The last element only"
        ],
        "answer": 1,
        "explanation": "Starting at 0 breaks on all-negative lists (it would wrongly return 0). Initialize best to the first element."
      },
      {
        "question": "You will search the SAME unsorted array thousands of times for different targets. Better than repeated linear search:",
        "options": [
          "Nothing beats linear search",
          "Build a hash set once (O(n)), then each lookup is O(1)",
          "Reverse the array first",
          "Use two pointers"
        ],
        "answer": 1,
        "explanation": "Repeated membership queries are the signal to preprocess into a hash set: O(n) once, then O(1) per lookup."
      },
      {
        "question": "The 'running aggregate' scan (max/min/sum/count in one pass) has what space complexity?",
        "options": [
          "O(n)",
          "O(1)",
          "O(log n)",
          "O(n^2)"
        ],
        "answer": 1,
        "explanation": "It keeps only a constant number of accumulator variables, so O(1) auxiliary space."
      }
    ],
    "practice": [
      {
        "slug": "array-maximum",
        "note": "The running-max skeleton, exactly."
      },
      {
        "slug": "array-minimum",
        "note": "Same pattern, flipped comparison."
      },
      {
        "slug": "count-vowels",
        "note": "A running count in one scan."
      },
      {
        "slug": "max-consecutive-ones",
        "note": "Running best-so-far over a streak."
      }
    ]
  },
  {
    "key": "alg_binary_search",
    "name": "Binary Search",
    "category": "Algo: Searching & Scanning",
    "what": "Halve the search space every step — O(log n) — and the powerful 'binary search on the answer' trick.",
    "deep": "Binary search finds a target in a sorted array in O(log n) by repeatedly discarding half the remaining range: check the middle, then recurse into the half that must contain the target. The prerequisite is a monotonic structure — sortedness, or any property that flips from false to true once and never back. That second idea, 'binary search on the answer', is a superpower: when feasibility is monotonic in some threshold, you can binary search the threshold itself even when there's no array to search.",
    "java": "**Needs a sorted / monotonic space.** Each step halves it → **O(log n)**.\n\n```\nlo, hi = 0, n-1\nwhile lo <= hi:\n    mid = lo + (hi-lo)/2\n    if a[mid] == t: return mid\n    if a[mid] < t: lo = mid+1\n    else: hi = mid-1\n```",
    "language": "algorithms",
    "lesson": "**Binary search** locates a target in a **sorted** array by halving the search\nrange each step. Because the range shrinks geometrically, it finishes in about\nlog₂ n comparisons — 20 steps for a million elements.\n\n```text\nfunction binarySearch(a, target):\n    lo = 0\n    hi = length(a) - 1\n    while lo <= hi:\n        mid = lo + (hi - lo) / 2      # avoids overflow vs. (lo+hi)/2\n        if a[mid] == target: return mid\n        else if a[mid] < target: lo = mid + 1\n        else: hi = mid - 1\n    return -1\n```\n\nTrace a search for **7** in `[1, 3, 5, 7, 9, 11]` (indices 0–5):\n\n| lo | hi | mid | a[mid] | vs 7 | action |\n|---|---|---|---|---|---|\n| 0 | 5 | 2 | 5 | too small | lo = 3 |\n| 3 | 5 | 4 | 9 | too big | hi = 3 |\n| 3 | 3 | 3 | 7 | equal | return 3 |\n\nThree comparisons instead of four — and the gap explodes with size: at n =\n1,000,000 it's ~20 steps versus a million.\n\n## The one prerequisite: monotonicity\n\nBinary search works whenever the space is **monotonic** — the \"is my answer to\nthe right?\" question flips from *no* to *yes* exactly once. A sorted array is the\nobvious case, but so is any predicate that's false, false, …, true, true, ….\n\n## Binary search on the answer\n\nHere's the leap. Suppose you can't search an array, but you can *ask a\nyes/no feasibility question* about a candidate answer, and that answer is\n**monotonic**: if X works, everything ≥ X works too. Then binary search the\n**answer** itself.\n\n> \"What's the *minimum* capacity a ship needs to move all packages within D\n> days?\" Feasibility (\"can capacity C finish in D days?\") is monotonic: bigger\n> C is always at least as feasible. So binary search C between the largest single\n> package and the total weight, testing feasibility at each mid.\n\n```text\nlo = lowest possible answer\nhi = highest possible answer\nwhile lo < hi:\n    mid = lo + (hi - lo) / 2\n    if feasible(mid): hi = mid       # mid works — try smaller\n    else:             lo = mid + 1   # mid fails — need larger\nreturn lo\n```\n\nThis converts many \"minimize the maximum / maximize the minimum\" problems from\nhard into O(n log(range)).\n\n## Boundary variants\n\nReal problems often want the **first** or **last** index satisfying a condition\n(first occurrence, insertion point, lower/upper bound), not just *any* match.\nThe trick is to keep searching after a hit instead of returning: on a match,\nrecord it and move `hi = mid - 1` (for the first occurrence) to look further\nleft.\n\n### Common pitfalls\n- **Forgetting the array must be sorted** — binary search on unsorted data is\n  simply wrong.\n- **Overflow**: use `mid = lo + (hi - lo)/2`, not `(lo + hi)/2`, in fixed-width\n  integer languages.\n- **Infinite loops**: make sure the range strictly shrinks every iteration\n  (`lo = mid + 1` / `hi = mid - 1`, or a careful `lo < hi` form).\n- Off-by-one between the `lo <= hi` and `lo < hi` templates — pick one and know\n  why.\n\n### Key takeaways\n- Binary search needs a **monotonic** space and runs in **O(log n)**.\n- Compute mid overflow-safely and always shrink the range.\n- \"Binary search on the answer\" solves feasibility-threshold problems even with\n  no array in sight.\n",
    "exercises": [],
    "cards": [],
    "quiz": [
      {
        "question": "What must be true of the array for standard binary search to be correct?",
        "options": [
          "It must contain no duplicates",
          "It must be sorted (monotonic)",
          "It must have even length",
          "It must contain the target"
        ],
        "answer": 1,
        "explanation": "Binary search relies on order to decide which half to discard. On unsorted data it gives wrong answers."
      },
      {
        "question": "How many comparisons does binary search need in the worst case for n = 1,000,000?",
        "options": [
          "About 1,000,000",
          "About 1,000",
          "About 20",
          "About 6"
        ],
        "answer": 2,
        "explanation": "log₂(1,000,000) ≈ 20, so ~20 halving steps — that's the whole point of O(log n)."
      },
      {
        "question": "Why compute mid = lo + (hi - lo)/2 instead of (lo + hi)/2?",
        "options": [
          "It's faster",
          "It avoids integer overflow when lo+hi is large",
          "It searches the right half",
          "It's required by the sorted order"
        ],
        "answer": 1,
        "explanation": "In fixed-width integer languages lo+hi can overflow; lo + (hi-lo)/2 computes the same midpoint without that risk."
      },
      {
        "question": "'Binary search on the answer' applies when…",
        "options": [
          "The array is already sorted",
          "Feasibility is monotonic in the candidate answer (if X works, so does every larger X)",
          "There are no duplicates",
          "n is small"
        ],
        "answer": 1,
        "explanation": "If the yes/no feasibility of a candidate answer flips once (monotonic), you can binary search the answer value itself."
      },
      {
        "question": "A binary search loop keeps running forever. The most likely cause is:",
        "options": [
          "The array is too big",
          "The range doesn't strictly shrink each iteration (e.g. lo = mid instead of mid+1)",
          "The target is negative",
          "Using O(log n)"
        ],
        "answer": 1,
        "explanation": "If lo or hi can stay put (lo = mid with lo==mid), the range never shrinks and the loop spins. Always move past mid."
      },
      {
        "question": "To find the FIRST occurrence of a value (not just any), on a match you should:",
        "options": [
          "Return immediately",
          "Record the index and keep searching the left half (hi = mid - 1)",
          "Keep searching the right half",
          "Restart from lo = 0"
        ],
        "answer": 1,
        "explanation": "Don't stop at the first hit — record it and continue left to find an earlier occurrence; the last recorded index is the first occurrence."
      }
    ],
    "practice": [
      {
        "slug": "binary-search-first",
        "note": "First-occurrence variant — the boundary template."
      },
      {
        "slug": "search-insert-position",
        "note": "Lower-bound / insertion-point binary search."
      },
      {
        "slug": "two-sum-sorted",
        "note": "Binary search or two pointers on a sorted array."
      },
      {
        "slug": "is-sorted",
        "note": "Check the prerequisite that makes binary search legal."
      }
    ]
  },
  {
    "key": "alg_two_pointers",
    "name": "Two Pointers",
    "category": "Algo: Searching & Scanning",
    "what": "Two indices moving through the data to replace a nested loop with a single O(n) pass.",
    "deep": "The two-pointer technique keeps two indices into a sequence and moves them based on a rule, collapsing what looks like an O(n^2) all-pairs search into one O(n) pass. There are two main flavors: converging pointers from both ends (great on sorted data — pair sums, palindromes, reversing) and same-direction slow/fast pointers (deduplication, partitioning, cycle detection). The magic is that each pointer only moves forward, so the total work is linear.",
    "java": "Two indices, each moving forward → **O(n)**, usually **O(1)** space.\n\n**Converging** (both ends, sorted): pair sums, palindrome, reverse.\n**Slow/fast** (same direction): dedupe, partition, detect a cycle.",
    "language": "algorithms",
    "lesson": "**Two pointers** keeps two indices walking through a sequence, advancing them by\na rule until they meet or finish. It turns many O(n^2) \"check all pairs\" problems\ninto a single **O(n)** pass with **O(1)** extra space.\n\n## Flavor 1 — converging pointers (from both ends)\n\nBest on **sorted** data. Start `left` at the front and `right` at the back, then\nmove the pair inward based on what you observe.\n\n*Pair that sums to a target in a sorted array:*\n\n```text\nleft = 0\nright = length(a) - 1\nwhile left < right:\n    s = a[left] + a[right]\n    if s == target: return (left, right)\n    if s < target:  left = left + 1     # need a bigger sum\n    else:           right = right - 1   # need a smaller sum\nreturn none\n```\n\nTrace target **10** on `[1, 3, 4, 6, 8, 9]`:\n\n| left | right | a[left]+a[right] | vs 10 | move |\n|---|---|---|---|---|\n| 0 | 5 | 1+9 = 10 | equal | return (0,5) |\n\nIf instead target were **12**: `1+9=10` too small → left++, `3+9=12` → found.\nEach step throws away one candidate end, so it's O(n).\n\nThe same converging idea reverses an array in place and checks a palindrome\n(compare ends, walk inward).\n\n## Flavor 2 — slow/fast pointers (same direction)\n\nBoth pointers move forward, but at different speeds or under different rules. The\n**slow** pointer marks where the \"good\" prefix ends; the **fast** pointer scans\nahead.\n\n*Remove duplicates from a sorted array in place:*\n\n```text\nslow = 0\nfor fast in 1 .. length(a)-1:\n    if a[fast] != a[slow]:\n        slow = slow + 1\n        a[slow] = a[fast]\nreturn slow + 1        # length of the deduped prefix\n```\n\nThe slow pointer builds the answer in place while fast races ahead — O(n) time,\nO(1) space. The same shape partitions an array (move all zeros to the end,\nDutch-flag partitioning) and, with a fast pointer moving twice as fast, detects a\ncycle in a linked list (Floyd's algorithm).\n\n## Why it's linear\n\nEach pointer only ever moves **forward** (or they only move **inward**). Across\nthe whole run each index advances at most n times, so the total work is O(n) —\nno matter that there are two of them.\n\n### Common pitfalls\n- Using converging pointers on **unsorted** data for a pair-sum — it needs sorted\n  order (or switch to a hash set).\n- Moving the **wrong** pointer in the converging template (grow the sum by moving\n  `left` right, shrink it by moving `right` left).\n- Forgetting the loop guard `left < right`, causing the pointers to cross.\n\n### Key takeaways\n- Two pointers replaces an O(n^2) pair scan with an O(n) pass, O(1) space.\n- Converging (both ends) suits sorted pair/palindrome/reverse problems.\n- Slow/fast (same direction) suits dedupe, partition, and cycle detection.\n",
    "exercises": [],
    "cards": [],
    "quiz": [
      {
        "question": "The converging two-pointer technique (one at each end) assumes the array is:",
        "options": [
          "Unsorted",
          "Sorted",
          "All positive",
          "A power of two in length"
        ],
        "answer": 1,
        "explanation": "Converging pointers rely on order: a too-small sum means move left inward, too-big means move right inward. That logic needs a sorted array."
      },
      {
        "question": "In a sorted-array pair-sum search, the current sum is LESS than the target. You should:",
        "options": [
          "Move the right pointer left",
          "Move the left pointer right",
          "Move both inward",
          "Return not-found"
        ],
        "answer": 1,
        "explanation": "To increase the sum, advance the left pointer to a larger value. Moving right inward would shrink the sum further."
      },
      {
        "question": "Why is two pointers O(n) even though there are two indices?",
        "options": [
          "Because n is small",
          "Because each pointer only moves forward/inward, advancing at most n times total",
          "Because it uses recursion",
          "Because the array is sorted"
        ],
        "answer": 1,
        "explanation": "Neither pointer ever backtracks, so across the whole run each advances ≤ n times — linear total work."
      },
      {
        "question": "Which problem fits the SLOW/FAST (same-direction) variant rather than converging ends?",
        "options": [
          "Check if a string is a palindrome",
          "Remove duplicates from a sorted array in place",
          "Find a pair summing to a target in a sorted array",
          "Reverse an array in place"
        ],
        "answer": 1,
        "explanation": "In-place dedup uses a slow pointer marking the deduped prefix and a fast pointer scanning ahead — a same-direction pattern."
      },
      {
        "question": "Detecting a cycle in a linked list with Floyd's algorithm uses:",
        "options": [
          "Two pointers at opposite ends",
          "A slow pointer and a fast pointer moving twice as fast",
          "Binary search",
          "A prefix-sum array"
        ],
        "answer": 1,
        "explanation": "Floyd's tortoise-and-hare moves one pointer 1 step and the other 2 steps; if there's a cycle they eventually meet."
      }
    ],
    "practice": [
      {
        "slug": "two-sum-sorted",
        "note": "Converging pointers on a sorted array."
      },
      {
        "slug": "is-palindrome-fn",
        "note": "Compare from both ends, walk inward."
      },
      {
        "slug": "reverse-string",
        "note": "In-place reversal with converging pointers."
      },
      {
        "slug": "merge-sorted-arrays",
        "note": "Two pointers advancing through two sorted inputs."
      }
    ]
  },
  {
    "key": "alg_sliding_window",
    "name": "Sliding Window",
    "category": "Algo: Searching & Scanning",
    "what": "A window that grows and shrinks over a contiguous run — turning O(n·k) into O(n).",
    "deep": "The sliding-window technique maintains a contiguous window over an array or string and slides it across in one pass, adding elements on the right and removing them on the left, while keeping a running summary of what's inside. It answers questions about contiguous subarrays/substrings — longest, shortest, sum, count — in O(n) instead of recomputing each window from scratch. Windows come in two kinds: fixed size (k) and variable size (expand until valid, then contract).",
    "java": "One contiguous window slid across the data → **O(n)**.\n\n**Fixed k:** add right, drop left, keep size k.\n**Variable:** expand right; while invalid, shrink left; track best.",
    "language": "algorithms",
    "lesson": "A **sliding window** is a contiguous range `[left, right]` over an array or\nstring that moves across the data in a single pass. Instead of recomputing each\nwindow from scratch (O(n·k)), you *update* a running summary as the window moves\n— O(1) per step, **O(n)** overall.\n\nReach for it whenever the problem is about a **contiguous** subarray or substring:\nlongest, shortest, sum, average, or count satisfying a condition.\n\n## Fixed-size window (size k)\n\nSlide a window of constant width k: when it moves one step right, add the new\nright element and remove the old left one.\n\n*Maximum sum of any k consecutive elements:*\n\n```text\nwindowSum = sum of first k elements\nbest = windowSum\nfor right in k .. n-1:\n    windowSum = windowSum + a[right] - a[right - k]   # add new, drop old\n    best = max(best, windowSum)\nreturn best\n```\n\nTrace k = 3 on `[2, 1, 5, 1, 3, 2]`:\n\n| right | added | dropped | windowSum | best |\n|---|---|---|---|---|\n| (init) | 2,1,5 | — | 8 | 8 |\n| 3 | 1 | 2 | 7 | 8 |\n| 4 | 3 | 1 | 9 | 9 |\n| 5 | 2 | 5 | 6 | 9 |\n\nAnswer **9** (the window `[5,1,3]`), in one O(n) pass — no re-summing.\n\n## Variable-size window (expand / contract)\n\nWhen the window size isn't fixed, grow the right edge to include more, and\nshrink the left edge whenever the window becomes **invalid**, tracking the best\nvalid window seen.\n\n*Longest substring with no repeating character:*\n\n```text\nleft = 0\nseen = empty set\nbest = 0\nfor right in 0 .. n-1:\n    while a[right] is in seen:         # window became invalid\n        remove a[left] from seen\n        left = left + 1\n    add a[right] to seen\n    best = max(best, right - left + 1)\nreturn best\n```\n\nBoth edges only ever move **right**, so even with the inner `while`, each index\nenters and leaves the window at most once → **O(n)** total.\n\n## The mental template\n\n1. Expand `right` by one, updating the running summary.\n2. While the window violates the constraint, shrink from `left`.\n3. Record the answer (a length, a sum, a count) for the current valid window.\n\n### Common pitfalls\n- Recomputing the whole window each step — that's O(n·k) and defeats the purpose;\n  *update incrementally*.\n- Forgetting to remove the left element's contribution when it leaves the window.\n- Using a window for a **non-contiguous** requirement — windows only model\n  contiguous runs.\n\n### Key takeaways\n- Sliding window handles **contiguous** subarray/substring questions in O(n).\n- Fixed windows add-right/drop-left; variable windows expand then contract.\n- Both pointers move only forward, which is why it stays linear.\n",
    "exercises": [],
    "cards": [],
    "quiz": [
      {
        "question": "Sliding window is the right tool when the problem concerns a … subarray or substring.",
        "options": [
          "sorted",
          "contiguous",
          "reversed",
          "unique"
        ],
        "answer": 1,
        "explanation": "Windows model contiguous runs. If the elements needn't be adjacent, a window doesn't apply."
      },
      {
        "question": "When a fixed-size window of width k slides one step to the right, you should:",
        "options": [
          "Recompute the sum of all k elements",
          "Add the new right element and subtract the element that just left",
          "Sort the window",
          "Reset the sum to zero"
        ],
        "answer": 1,
        "explanation": "Incremental update is the whole point: +new right, −old left keeps each step O(1) instead of O(k)."
      },
      {
        "question": "In a variable-size window, when the window becomes INVALID you should:",
        "options": [
          "Expand the right edge further",
          "Shrink from the left until it's valid again",
          "Return the current answer",
          "Start over from index 0"
        ],
        "answer": 1,
        "explanation": "Contract from the left to restore validity, then continue expanding right — that's the expand/contract pattern."
      },
      {
        "question": "Why is the variable-size window O(n) despite the inner while-loop that shrinks left?",
        "options": [
          "Because n is small",
          "Because each index enters and leaves the window at most once, so total moves are ≤ 2n",
          "Because it sorts first",
          "Because the window is fixed size"
        ],
        "answer": 1,
        "explanation": "left and right each advance at most n times over the whole run, so the combined work is linear even with the inner loop."
      },
      {
        "question": "Recomputing each window from scratch instead of updating incrementally makes a size-k window cost:",
        "options": [
          "O(n)",
          "O(n·k)",
          "O(log n)",
          "O(1)"
        ],
        "answer": 1,
        "explanation": "Recomputing k elements for each of ~n positions is O(n·k) — the incremental update is what brings it down to O(n)."
      }
    ],
    "practice": [
      {
        "slug": "longest-unique-substring",
        "note": "The canonical variable-size window."
      },
      {
        "slug": "max-consecutive-ones",
        "note": "A window over a run of 1s."
      },
      {
        "slug": "subarray-sum-k",
        "note": "Contiguous-sum question — window / prefix-sum territory."
      },
      {
        "slug": "sliding-window-maximum",
        "note": "A window with a monotonic-deque summary."
      }
    ]
  },
  {
    "key": "alg_prefix_sums",
    "name": "Prefix Sums & Difference Arrays",
    "category": "Algo: Searching & Scanning",
    "what": "Precompute cumulative totals once so any range-sum query is O(1).",
    "deep": "A prefix-sum array stores the running total up to each index, so the sum of any range [i, j] becomes a single subtraction: prefix[j+1] − prefix[i]. You pay O(n) once to build it, then answer unlimited range-sum queries in O(1) each — a classic time–space tradeoff. The mirror-image idea, a difference array, lets you apply many range *updates* in O(1) each and reconstruct the final array once. Together they turn repeated range work from O(n) per query into O(1).",
    "java": "Build `prefix[k] = a[0]+…+a[k-1]` once in O(n).\n\nRange sum `[i..j]` = `prefix[j+1] - prefix[i]`  → **O(1) per query**.\n\n**Difference array:** O(1) range *updates*, reconstruct once.",
    "language": "algorithms",
    "lesson": "A **prefix-sum** (cumulative-sum) array lets you answer \"what's the sum of\nelements from index i to j?\" in **O(1)** after an **O(n)** setup — instead of\nre-adding the range (O(n)) for every query.\n\n## Building it\n\nDefine `prefix[0] = 0` and `prefix[k] = a[0] + a[1] + … + a[k-1]`. Each entry is\nthe previous one plus the next element:\n\n```text\nprefix[0] = 0\nfor k in 1 .. n:\n    prefix[k] = prefix[k-1] + a[k-1]\n```\n\nFor `a = [3, 1, 4, 1, 5]`:\n\n| k | 0 | 1 | 2 | 3 | 4 | 5 |\n|---|---|---|---|---|---|---|\n| prefix[k] | 0 | 3 | 4 | 8 | 9 | 14 |\n\n## Querying a range in O(1)\n\nThe sum of `a[i..j]` (inclusive) is just a subtraction:\n\n```text\nrangeSum(i, j) = prefix[j+1] - prefix[i]\n```\n\nSum of `a[1..3]` = `prefix[4] - prefix[1]` = `9 - 3` = **6** (that's `1+4+1`). No\nloop — one subtraction, regardless of how wide the range is. The `prefix[0]=0`\nsentinel is what makes the `i = 0` case work without a special branch.\n\n## Why it's a tradeoff\n\nYou spend **O(n) memory** and one **O(n) build** so that each of possibly\nmillions of range queries drops from **O(n)** to **O(1)**. If you'll query the\ndata more than a couple of times, it pays for itself immediately.\n\n## Prefix sums + hashing\n\nA powerful combo: \"how many subarrays sum to k?\" Since `sum(i..j) =\nprefix[j+1] - prefix[i]`, a range sums to k exactly when `prefix[j+1] - k`\nequals some earlier prefix. Count earlier prefixes in a hash map as you go →\nO(n) instead of O(n^2).\n\n## The mirror: difference arrays (range updates)\n\nIf instead you need to *add v to every element in `[i, j]`* many times, a\n**difference array** does each update in O(1): add v at index i, subtract v at\nindex j+1, and take a prefix sum at the end to materialize the result.\n\n```text\ndiff[i]   += v\ndiff[j+1] -= v\n# after all updates:\na = prefix sum of diff\n```\n\nMany range-add operations become O(1) each plus one O(n) final pass.\n\n### Common pitfalls\n- Off-by-one in the query: with a size-(n+1) prefix and the 0-sentinel, the\n  formula is `prefix[j+1] - prefix[i]`. Write it once, trust it.\n- Overflow: cumulative sums grow large — use a wide enough integer type.\n- Reaching for prefix sums on a **single** query — a plain O(n) scan is simpler\n  and just as fast; prefix sums win on *repeated* queries.\n\n### Key takeaways\n- O(n) build → O(1) range-sum queries via one subtraction.\n- The 0-sentinel (`prefix[0]=0`) removes the i=0 edge case.\n- Difference arrays are the dual: O(1) range *updates*, reconstruct once.\n",
    "exercises": [],
    "cards": [],
    "quiz": [
      {
        "question": "With prefix[0]=0 and prefix[k]=a[0]+…+a[k-1], the sum of a[i..j] inclusive equals:",
        "options": [
          "prefix[j] - prefix[i]",
          "prefix[j+1] - prefix[i]",
          "prefix[j] - prefix[i-1]",
          "prefix[i] - prefix[j+1]"
        ],
        "answer": 1,
        "explanation": "With the 0-sentinel and size-(n+1) prefix, rangeSum(i,j) = prefix[j+1] - prefix[i]."
      },
      {
        "question": "After an O(n) build, each range-sum query costs:",
        "options": [
          "O(n)",
          "O(log n)",
          "O(1)",
          "O(n^2)"
        ],
        "answer": 2,
        "explanation": "A range sum is a single subtraction of two prefix entries → O(1) per query."
      },
      {
        "question": "Prefix sums are most worth it when you…",
        "options": [
          "ask for exactly one range sum",
          "ask for many range sums over the same array",
          "need to sort the array",
          "have negative numbers"
        ],
        "answer": 1,
        "explanation": "The O(n) setup pays off across repeated queries. For a single query, a plain O(n) scan is simpler and equally fast."
      },
      {
        "question": "What does the prefix[0] = 0 sentinel buy you?",
        "options": [
          "Faster queries",
          "It removes the special case when the range starts at index 0",
          "Less memory",
          "Sorted order"
        ],
        "answer": 1,
        "explanation": "The 0-sentinel makes prefix[j+1] - prefix[i] correct even when i = 0, avoiding a separate branch."
      },
      {
        "question": "To apply many 'add v to every element in [i, j]' updates efficiently, use:",
        "options": [
          "A prefix-sum array queried repeatedly",
          "A difference array: diff[i] += v, diff[j+1] -= v, then prefix-sum once",
          "Binary search",
          "A sliding window"
        ],
        "answer": 1,
        "explanation": "A difference array records each range-add in O(1) at the two endpoints, then one prefix-sum pass reconstructs the final array."
      }
    ],
    "practice": [
      {
        "slug": "running-sum",
        "note": "Build the prefix-sum array itself."
      },
      {
        "slug": "subarray-sum-k",
        "note": "Prefix sums + hashing to count subarrays summing to k."
      },
      {
        "slug": "prefix-counts",
        "note": "Cumulative counts, the same idea on frequencies."
      }
    ]
  },
  {
    "key": "alg_sorting_basics",
    "name": "Quadratic Sorts (Bubble, Insertion, Selection)",
    "category": "Algo: Sorting",
    "what": "The O(n^2) sorts — slow at scale, but the clearest way to understand what sorting *is*.",
    "deep": "The three elementary sorts — bubble, selection, and insertion — all run in O(n^2) and are too slow for large inputs, but they're worth knowing cold. They teach the mechanics of ordering, they're simple and in-place, and insertion sort is genuinely the best choice for tiny or nearly-sorted arrays (real libraries switch to it for small subarrays). Understanding why they're quadratic is also the clearest possible motivation for the O(n log n) sorts that follow.",
    "java": "All three are **O(n^2)** time, **O(1)** space, in place.\n\n**Bubble:** swap adjacent out-of-order pairs, repeat.\n**Selection:** pick the min, place it, repeat.\n**Insertion:** grow a sorted prefix, inserting each new item.",
    "language": "algorithms",
    "lesson": "Three classic sorts run in **O(n^2)**. You won't use them on big inputs, but they\nbuild intuition, they're all **in place** (O(1) extra space), and one of them\n(insertion) is the right tool for small or nearly-sorted data.\n\n## Selection sort — pick the smallest, repeat\n\nFind the minimum of the unsorted part and swap it into place.\n\n```text\nfor i in 0 .. n-1:\n    m = index of minimum of a[i..n-1]\n    swap a[i], a[m]\n```\n\nAlways ~n²/2 comparisons regardless of input → **O(n^2)** best, average, and\nworst. Minimal *swaps* (n of them), which occasionally matters.\n\n## Bubble sort — swap adjacent pairs\n\nRepeatedly pass through, swapping neighbors that are out of order; the largest\n\"bubbles\" to the end each pass.\n\n```text\nrepeat:\n    swapped = false\n    for i in 0 .. n-2:\n        if a[i] > a[i+1]:\n            swap a[i], a[i+1]\n            swapped = true\n    until not swapped\n```\n\nWith the `swapped` flag, an already-sorted array is detected in **O(n)** (best\ncase); otherwise **O(n^2)**. Mostly of teaching value.\n\n## Insertion sort — grow a sorted prefix\n\nKeep `a[0..i-1]` sorted; take the next element and slide it left into place, like\nsorting a hand of cards.\n\n```text\nfor i in 1 .. n-1:\n    key = a[i]\n    j = i - 1\n    while j >= 0 and a[j] > key:\n        a[j+1] = a[j]     # shift right\n        j = j - 1\n    a[j+1] = key\n```\n\nTrace `[5, 2, 4, 1]`:\n\n| after i | array |\n|---|---|\n| i=1 | [2, 5, 4, 1] |\n| i=2 | [2, 4, 5, 1] |\n| i=3 | [1, 2, 4, 5] |\n\n**O(n^2)** worst case, but **O(n)** on already-sorted or nearly-sorted input, and\nvery low overhead — which is why production sorts (like Timsort) fall back to\ninsertion sort for small runs.\n\n## Stability\n\nA sort is **stable** if equal elements keep their original relative order.\nInsertion and bubble are stable; selection sort is not (a swap can leap an equal\nelement over another). Stability matters when you sort by one key but want ties\nto preserve a previous ordering.\n\n| Sort | Best | Avg | Worst | Space | Stable? |\n|---|---|---|---|---|---|\n| Selection | O(n^2) | O(n^2) | O(n^2) | O(1) | no |\n| Bubble | O(n) | O(n^2) | O(n^2) | O(1) | yes |\n| Insertion | O(n) | O(n^2) | O(n^2) | O(1) | yes |\n\n### Common pitfalls\n- Using these on large n — 100,000 elements is ~10^10 operations, far too slow.\n- Assuming selection sort is stable — it isn't.\n- Forgetting bubble sort's early-exit flag, losing its one redeeming best case.\n\n### Key takeaways\n- All three are O(n^2), in place; know them for intuition, not for scale.\n- Insertion sort shines on **small** or **nearly-sorted** data (O(n) best case).\n- Stability (preserving ties' order) distinguishes them: insertion/bubble yes,\n  selection no.\n",
    "exercises": [],
    "cards": [],
    "quiz": [
      {
        "question": "What is the worst-case time complexity of bubble, selection, and insertion sort?",
        "options": [
          "O(n log n)",
          "O(n)",
          "O(n^2)",
          "O(log n)"
        ],
        "answer": 2,
        "explanation": "All three elementary sorts are O(n^2) in the worst case — that's why they don't scale."
      },
      {
        "question": "Which quadratic sort is the best practical choice for a small or nearly-sorted array?",
        "options": [
          "Selection sort",
          "Bubble sort",
          "Insertion sort",
          "None — always use merge sort"
        ],
        "answer": 2,
        "explanation": "Insertion sort runs in O(n) on nearly-sorted data with tiny overhead, which is why libraries use it for small runs."
      },
      {
        "question": "A sort is 'stable' when…",
        "options": [
          "it never crashes",
          "equal elements keep their original relative order",
          "it uses O(1) space",
          "it's faster than O(n log n)"
        ],
        "answer": 1,
        "explanation": "Stability means equal keys retain their prior order — important when sorting by a secondary key."
      },
      {
        "question": "Which of these elementary sorts is NOT stable?",
        "options": [
          "Insertion sort",
          "Bubble sort",
          "Selection sort",
          "All are stable"
        ],
        "answer": 2,
        "explanation": "Selection sort's long-distance swaps can reorder equal elements, so it is not stable."
      },
      {
        "question": "All three quadratic sorts share which space complexity?",
        "options": [
          "O(n)",
          "O(1) — they sort in place",
          "O(n log n)",
          "O(n^2)"
        ],
        "answer": 1,
        "explanation": "Bubble, selection, and insertion sort rearrange the array in place using only O(1) extra memory."
      }
    ],
    "practice": [
      {
        "slug": "is-sorted",
        "note": "Verify order — the postcondition every sort must achieve."
      },
      {
        "slug": "sort-by-frequency",
        "note": "Sorting with a custom key and tie-breaking (stability matters)."
      }
    ]
  },
  {
    "key": "alg_efficient_sorts",
    "name": "Efficient Sorts (Merge, Quick, Heap)",
    "category": "Algo: Sorting",
    "what": "The O(n log n) workhorses — and why comparison sorting can't beat n log n.",
    "deep": "Merge sort, quicksort, and heapsort all sort in O(n log n), the best possible for comparison-based sorting. Merge sort splits, sorts halves, and merges — a clean divide-and-conquer that's stable and guarantees O(n log n) but needs O(n) scratch space. Quicksort partitions around a pivot; it's usually fastest in practice and in place, but a bad pivot degrades it to O(n^2). Heapsort sorts in place with a guaranteed O(n log n) using a heap. Knowing their tradeoffs is standard interview fare.",
    "java": "All three: **O(n log n)** (comparison-sort optimum).\n\n**Merge:** stable, O(n) space, guaranteed n log n.\n**Quick:** in place, fastest in practice, O(n^2) worst case.\n**Heap:** in place, guaranteed n log n, not stable.",
    "language": "algorithms",
    "lesson": "Three sorts achieve **O(n log n)** — the provable floor for any sort that works\nby comparing elements. Each makes a different tradeoff.\n\n## Merge sort — divide, sort, merge\n\nSplit the array in half, recursively sort each half, then **merge** the two\nsorted halves in linear time.\n\n```text\nfunction mergeSort(a):\n    if length(a) <= 1: return a\n    mid = length(a) / 2\n    left  = mergeSort(a[0..mid-1])\n    right = mergeSort(a[mid..end])\n    return merge(left, right)      # two-pointer merge, O(n)\n```\n\nIts cost is the recurrence `T(n) = 2·T(n/2) + O(n)` → **O(n log n)** (log n\nlevels, O(n) merging per level). It's **stable** and its worst case is also\nO(n log n), but merging needs **O(n)** scratch space.\n\n## Quicksort — partition around a pivot\n\nChoose a **pivot**, partition the array so smaller elements go left and larger go\nright, then recursively sort each side.\n\n```text\nfunction quickSort(a, lo, hi):\n    if lo >= hi: return\n    p = partition(a, lo, hi)   # pivot lands in its final spot\n    quickSort(a, lo, p-1)\n    quickSort(a, p+1, hi)\n```\n\n- **Average:** O(n log n), and typically the **fastest in practice** (great\n  cache behavior, in place, O(log n) stack).\n- **Worst:** O(n^2) when pivots are consistently bad (e.g. always the smallest —\n  which happens on already-sorted input with a naive first-element pivot).\n- Mitigation: choose the pivot **randomly** or as the median-of-three, which\n  makes the worst case astronomically unlikely.\n\nQuicksort is generally **not stable**.\n\n## Heapsort — sort with a heap\n\nBuild a max-heap in O(n), then repeatedly extract the maximum (O(log n) each) and\nplace it at the end.\n\n```text\nbuild a max-heap from a\nfor end in n-1 .. 1:\n    swap a[0], a[end]        # biggest to its final slot\n    sift-down a[0] within a[0..end-1]\n```\n\n**Guaranteed O(n log n)**, **in place** (O(1) extra), but **not stable** and\nusually a bit slower than quicksort due to cache-unfriendly jumps.\n\n## The n log n lower bound\n\nAny comparison sort must distinguish all n! possible orderings using yes/no\ncomparisons. A binary decision tree tall enough to have n! leaves has height\n≥ log₂(n!) ≈ n log n. So **no comparison sort can beat O(n log n)** — merge and\nheap *reach* that floor. (To go faster you must stop comparing — see\n[non-comparison sorts](#).)\n\n## Choosing between them\n\n| Sort | Time (avg / worst) | Space | Stable | Pick it when |\n|---|---|---|---|---|\n| Merge | n log n / n log n | O(n) | yes | you need stability or guaranteed n log n; linked lists |\n| Quick | n log n / n^2 | O(log n) | no | general in-memory speed (most libraries' default) |\n| Heap | n log n / n log n | O(1) | no | you need in-place + a hard worst-case guarantee |\n\n### Common pitfalls\n- Claiming quicksort is \"always O(n log n)\" — its worst case is O(n^2) without\n  pivot randomization.\n- Forgetting merge sort's O(n) auxiliary space.\n- Assuming any of the three (except merge) is stable.\n\n### Key takeaways\n- Merge/quick/heap all hit O(n log n); comparison sorting can't do better.\n- Merge = stable + guaranteed but O(n) space; quick = fastest but O(n^2) worst;\n  heap = in-place + guaranteed but not stable.\n- Randomize quicksort's pivot to dodge the worst case.\n",
    "exercises": [],
    "cards": [],
    "quiz": [
      {
        "question": "What is the best worst-case time achievable by any comparison-based sort?",
        "options": [
          "O(n)",
          "O(n log n)",
          "O(n^2)",
          "O(log n)"
        ],
        "answer": 1,
        "explanation": "Distinguishing all n! orderings by comparisons needs ≥ log₂(n!) ≈ n log n comparisons — so O(n log n) is the floor."
      },
      {
        "question": "Quicksort's worst-case time complexity is:",
        "options": [
          "O(n log n)",
          "O(n)",
          "O(n^2)",
          "O(1)"
        ],
        "answer": 2,
        "explanation": "With consistently bad pivots (e.g. already-sorted input, naive pivot), partitions are maximally unbalanced → O(n^2)."
      },
      {
        "question": "Which sort is stable AND guarantees O(n log n), at the cost of O(n) extra space?",
        "options": [
          "Quicksort",
          "Heapsort",
          "Merge sort",
          "Selection sort"
        ],
        "answer": 2,
        "explanation": "Merge sort is stable with a guaranteed O(n log n), but its merge step needs O(n) scratch space."
      },
      {
        "question": "Which sort is in-place (O(1) extra) with a guaranteed O(n log n), but not stable?",
        "options": [
          "Merge sort",
          "Heapsort",
          "Insertion sort",
          "Bubble sort"
        ],
        "answer": 1,
        "explanation": "Heapsort sorts in place with a hard O(n log n) guarantee, but its heap operations reorder equal elements → not stable."
      },
      {
        "question": "How do you make quicksort's O(n^2) worst case astronomically unlikely?",
        "options": [
          "Sort the array first",
          "Choose the pivot randomly (or median-of-three)",
          "Use more memory",
          "Switch to recursion"
        ],
        "answer": 1,
        "explanation": "A randomized or median-of-three pivot makes consistently bad partitions vanishingly improbable, keeping it near O(n log n)."
      },
      {
        "question": "Merge sort's cost comes from the recurrence T(n) = 2·T(n/2) + O(n). That solves to:",
        "options": [
          "O(n)",
          "O(n^2)",
          "O(n log n)",
          "O(log n)"
        ],
        "answer": 2,
        "explanation": "log n levels of recursion, each doing O(n) merging, gives O(n log n)."
      }
    ],
    "practice": [
      {
        "slug": "merge-sorted-arrays",
        "note": "The linear merge step at the heart of merge sort."
      },
      {
        "slug": "merge-two-sorted-lists",
        "note": "Merging on linked lists — merge sort's core move."
      },
      {
        "slug": "merge-k-sorted",
        "note": "Generalize merging with a heap — ties sorting to priority queues."
      },
      {
        "slug": "sort-by-frequency",
        "note": "Apply a full sort with a custom comparator."
      }
    ]
  },
  {
    "key": "alg_non_comparison_sorts",
    "name": "Non-Comparison Sorts (Counting, Radix, Bucket)",
    "category": "Algo: Sorting",
    "what": "Beat the n log n barrier by not comparing at all — when the keys are small integers.",
    "deep": "Comparison sorts are stuck at O(n log n), but if you stop comparing you can go faster. Counting sort tallies how many of each key value there are and rebuilds the array from the tally — O(n + k) for keys in a small range k. Radix sort applies counting sort digit by digit to sort large integers or strings in O(d·(n+k)). Bucket sort scatters uniformly-distributed values into buckets, sorts each, and concatenates. These win only under conditions (small integer keys, known range) — but when they apply they're linear.",
    "java": "Skip comparisons → beat O(n log n) when keys are small integers.\n\n**Counting:** tally each value, rebuild → O(n + k).\n**Radix:** counting sort per digit → O(d·(n+k)).\n**Bucket:** scatter, sort buckets, concatenate.",
    "language": "algorithms",
    "lesson": "Comparison sorts can't beat **O(n log n)** — but that bound only applies to\nalgorithms that *compare* elements. If your keys are small integers (or short\nstrings), you can sort by **counting** or **bucketing** and reach **linear** time.\n\n## Counting sort — tally and rebuild\n\nIf every key is an integer in `[0, k)`, count how many times each value appears,\nthen write the values back out in order.\n\n```text\ncount = array of k zeros\nfor x in a: count[x] += 1\nout = empty\nfor v in 0 .. k-1:\n    append v to out, count[v] times\n```\n\nTrace `a = [2, 0, 2, 1, 0]`, k = 3:\n\n| value v | 0 | 1 | 2 |\n|---|---|---|---|\n| count | 2 | 1 | 2 |\n\nRebuild → `[0, 0, 1, 2, 2]`. Cost **O(n + k)**: linear when k is comparable to n.\nMade stable by walking prefix sums of the counts — the version radix sort needs.\n\n## Radix sort — sort digit by digit\n\nTo sort large integers (where k would be huge), sort by each **digit**, least\nsignificant first, using a *stable* counting sort at each step. After processing\nall d digits, the array is fully sorted.\n\n```text\nfor each digit position from least to most significant:\n    stable-counting-sort a by that digit\n```\n\nCost **O(d·(n + b))** for d digits in base b — effectively **linear** when d is\nsmall (fixed-width integers). Stability at each digit pass is essential, or\nearlier digits' work is lost.\n\n## Bucket sort — scatter, sort, gather\n\nFor values spread roughly **uniformly** over a range, split the range into n\nbuckets, drop each value into its bucket, sort each bucket (small, often\ninsertion sort), then concatenate.\n\n```text\ncreate n empty buckets spanning the value range\nfor x in a: put x in its bucket\nsort each bucket\nconcatenate buckets in order\n```\n\n**O(n)** expected when the distribution is uniform (buckets stay tiny); degrades\nif everything lands in one bucket.\n\n## The catch — these aren't free lunches\n\nThey win **only under conditions**:\n\n| Sort | Needs | Time | Space |\n|---|---|---|---|\n| Counting | integer keys in a small range k | O(n + k) | O(n + k) |\n| Radix | fixed-width integer/string keys | O(d·(n + b)) | O(n + b) |\n| Bucket | roughly uniform distribution | O(n) expected | O(n) |\n\nIf keys are huge, arbitrary, or must be compared by a custom rule, fall back to\nan **O(n log n)** comparison sort. And note the extra **space** cost — none of\nthese is in place.\n\n### Common pitfalls\n- Using counting sort when k ≫ n (e.g. 32-bit keys) — the O(k) count array\n  dominates. Use radix instead.\n- Forgetting radix sort's counting passes must be **stable**, or the sort breaks.\n- Assuming bucket sort stays linear on skewed (non-uniform) data — it doesn't.\n\n### Key takeaways\n- Not comparing lets you beat O(n log n) — but only for small-integer / uniform\n  keys.\n- Counting = tally+rebuild O(n+k); radix = stable counting per digit; bucket =\n  scatter/sort/gather.\n- They trade O(n)-ish extra space and only apply under their conditions.\n",
    "exercises": [],
    "cards": [],
    "quiz": [
      {
        "question": "How can non-comparison sorts beat the O(n log n) comparison-sort lower bound?",
        "options": [
          "They use faster comparisons",
          "They don't compare elements at all — they count or bucket by key value",
          "They use recursion",
          "They only work on sorted data"
        ],
        "answer": 1,
        "explanation": "The n log n bound only applies to comparison-based sorting. Counting/radix/bucket sort by key value without comparing, so the bound doesn't bind them."
      },
      {
        "question": "Counting sort's time complexity for n integers in the range [0, k) is:",
        "options": [
          "O(n log n)",
          "O(n + k)",
          "O(n^2)",
          "O(k^2)"
        ],
        "answer": 1,
        "explanation": "You do O(n) work to tally and O(k) to walk the value range → O(n + k). It's linear when k is comparable to n."
      },
      {
        "question": "When is counting sort a BAD choice?",
        "options": [
          "When keys are 0..9",
          "When the key range k is enormous compared to n (e.g. 32-bit integers)",
          "When the array is small",
          "When keys are integers"
        ],
        "answer": 1,
        "explanation": "If k ≫ n, the O(k) count array dominates and wastes space/time. Radix sort handles large integer keys instead."
      },
      {
        "question": "Radix sort requires that the per-digit counting sort be:",
        "options": [
          "In place",
          "Stable",
          "Recursive",
          "Comparison-based"
        ],
        "answer": 1,
        "explanation": "Each digit pass must preserve the order established by previous (less significant) digits — that requires a stable sort."
      },
      {
        "question": "Bucket sort achieves expected O(n) time under what assumption about the input?",
        "options": [
          "It's already sorted",
          "The values are roughly uniformly distributed across the range",
          "It contains no duplicates",
          "n is a power of two"
        ],
        "answer": 1,
        "explanation": "Uniform distribution keeps each bucket tiny, so sorting all buckets is linear. Skewed data can pile into one bucket and degrade it."
      }
    ],
    "practice": [
      {
        "slug": "sort-by-frequency",
        "note": "Frequency tallies — the counting-sort mindset."
      },
      {
        "slug": "prefix-counts",
        "note": "Cumulative counts, exactly the stable-counting-sort machinery."
      },
      {
        "slug": "group-anagrams-count",
        "note": "Bucketing by a canonical key."
      }
    ]
  },
  {
    "key": "alg_recursion",
    "name": "Recursion & the Call Stack",
    "category": "Algo: Recursion",
    "what": "A function that calls itself — with a base case, a smaller subproblem, and a real memory cost.",
    "deep": "Recursion solves a problem by reducing it to a smaller instance of the same problem, until it hits a base case that's trivially solvable. Every correct recursion has two parts: one or more base cases (when to stop) and a recursive case that makes progress toward them. Under the hood, each call gets a stack frame, so recursion depth d costs O(d) memory — and infinite or too-deep recursion overflows the stack. Recursion is the natural shape for trees, divide-and-conquer, and backtracking; every recursion can also be rewritten as a loop.",
    "java": "**Base case** (stop) + **recursive case** (shrink toward it).\n\n```\nf(n):\n  if base case: return answer\n  return combine(f(smaller))\n```\n\nDepth d costs **O(d)** stack space. No base case → stack overflow.",
    "language": "algorithms",
    "lesson": "**Recursion** is when a function solves a problem by calling itself on a\n**smaller** version of the same problem, stopping at a **base case** it can\nanswer directly.\n\nEvery correct recursion has exactly two ingredients:\n\n1. **Base case** — the smallest input, solved without recursing. *When to stop.*\n2. **Recursive case** — reduce the problem toward the base case and combine.\n   *How to make progress.*\n\nMiss the base case (or fail to shrink toward it) and the recursion never stops —\na **stack overflow**.\n\n```text\nfunction factorial(n):\n    if n <= 1: return 1          # base case\n    return n * factorial(n - 1)  # recursive case: smaller n\n```\n\n## How the call stack works\n\nEach call gets a **stack frame** holding its local state, pushed when the call\nstarts and popped when it returns. `factorial(4)` builds this tower before\nanything returns:\n\n```text\nfactorial(4)  -> 4 * factorial(3)\n                     3 * factorial(2)\n                         2 * factorial(1)\n                             returns 1\n                         returns 2\n                     returns 6\n                 returns 24\n```\n\nFour frames are alive at the deepest point. That's the key cost: **recursion of\ndepth d uses O(d) memory** for the stack, even when each frame is tiny. Deep\nrecursion (say depth 10^6) can overflow the stack where an equivalent loop would\nbe fine.\n\n## Trusting the recursion (\"the leap of faith\")\n\nDon't trace every level in your head. Instead: assume the recursive call\n**already returns the correct answer for the smaller input**, and just write the\nstep that combines it. If the base case is right and each step makes correct\nprogress, the whole thing is correct by induction.\n\n## Where recursion is the natural fit\n\n- **Trees** — a tree is defined recursively (a node with subtrees), so traversals\n  are naturally recursive.\n- **Divide and conquer** — merge sort, quicksort, binary search all split into\n  self-similar subproblems.\n- **Backtracking** — explore choices, recurse, undo — the shape of permutations,\n  subsets, and search.\n\n## Recursion ↔ iteration\n\nAny recursion can be rewritten as a loop (sometimes using an explicit stack), and\nvice versa. The recursive `factorial` above is O(n) time and **O(n) space** (the\nstack); the loop version is O(n) time and **O(1) space**. Choose recursion for\nclarity on self-similar structure; choose iteration when depth or stack space is\na concern.\n\n*Tail recursion* — where the recursive call is the very last action — can be\nturned into a loop automatically by some languages (no growing stack), but many\nmainstream languages don't guarantee this optimization.\n\n### Common pitfalls\n- **No base case**, or a recursive call that doesn't move toward it → infinite\n  recursion / stack overflow.\n- Forgetting recursion's **O(depth) stack cost** when analyzing space.\n- Re-solving the same subproblem exponentially (naive Fibonacci) — fix with\n  **memoization** (see the DP track).\n\n### Key takeaways\n- Every recursion = base case + a step that shrinks toward it.\n- Each call costs a stack frame → O(depth) space; too deep overflows.\n- Natural for trees, divide-and-conquer, and backtracking; convertible to loops.\n",
    "exercises": [],
    "cards": [],
    "quiz": [
      {
        "question": "What two ingredients does every correct recursive function need?",
        "options": [
          "A loop and a counter",
          "A base case and a recursive case that shrinks toward it",
          "A hash map and a set",
          "Two pointers"
        ],
        "answer": 1,
        "explanation": "A base case tells it when to stop; the recursive case must make progress toward that base case, or it never terminates."
      },
      {
        "question": "Recursion of depth d uses how much memory just for the call stack?",
        "options": [
          "O(1)",
          "O(log d)",
          "O(d)",
          "O(d^2)"
        ],
        "answer": 2,
        "explanation": "Each pending call keeps a live stack frame, so depth-d recursion costs O(d) stack space."
      },
      {
        "question": "A recursive function with no reachable base case will most likely:",
        "options": [
          "Return 0",
          "Overflow the stack",
          "Run in O(1)",
          "Sort the input"
        ],
        "answer": 1,
        "explanation": "Without a base case (or without progressing toward it), it recurses forever, exhausting the stack — a stack overflow."
      },
      {
        "question": "The recursive factorial uses O(n) space while the loop version uses O(1). The extra space in the recursive one is:",
        "options": [
          "The output array",
          "The call stack of n frames",
          "A hash set",
          "The input"
        ],
        "answer": 1,
        "explanation": "The recursion stacks n frames before any returns, costing O(n); the loop keeps a single accumulator, O(1)."
      },
      {
        "question": "Which problem shape is MOST naturally expressed with recursion?",
        "options": [
          "Summing a flat array",
          "Traversing a tree",
          "Reading one line of input",
          "Swapping two variables"
        ],
        "answer": 1,
        "explanation": "Trees are defined recursively (a node plus subtrees), so recursive traversal mirrors the structure directly."
      },
      {
        "question": "The 'leap of faith' when writing recursion means:",
        "options": [
          "Skip the base case",
          "Assume the recursive call already correctly solves the smaller subproblem, and just combine it",
          "Never test your code",
          "Always use a loop instead"
        ],
        "answer": 1,
        "explanation": "You trust that the recursion handles the smaller input correctly and focus on the combine step — correctness then follows by induction."
      }
    ],
    "practice": [
      {
        "slug": "factorial",
        "note": "The textbook base-case + recursive-case example."
      },
      {
        "slug": "nth-fibonacci",
        "note": "Naive recursion here is exponential — motivates memoization."
      },
      {
        "slug": "sum-to-n",
        "note": "Recurse: sum(n) = n + sum(n-1)."
      },
      {
        "slug": "countdown",
        "note": "Simple recursion that shrinks toward a base case."
      },
      {
        "slug": "gcd",
        "note": "Euclid's algorithm — recursion that shrinks fast."
      }
    ]
  },
  {
    "key": "alg_recurrences",
    "name": "Recurrences & the Master Theorem",
    "category": "Algo: Recursion",
    "what": "Turn a recursive algorithm's structure into its Big-O — mechanically.",
    "deep": "A recurrence expresses a recursive algorithm's running time in terms of itself: T(n) = a·T(n/b) + f(n) means 'a subproblems of size n/b, plus f(n) work to split and combine'. The Master Theorem reads off the answer by comparing the work of the recursive calls against f(n): whichever dominates wins, and if they balance you get an extra log factor. It instantly gives you O(n log n) for merge sort, O(n) for linear-work divide-and-conquer, and O(log n) for binary search — without unrolling anything by hand.",
    "java": "`T(n) = a·T(n/b) + f(n)`  → compare `n^(log_b a)` against `f(n)`:\n\nleaves win → `O(n^{log_b a})` · balanced → `O(f(n)·log n)` · root wins → `O(f(n))`.\n\nMerge sort `2T(n/2)+O(n)` → **O(n log n)**.",
    "language": "algorithms",
    "lesson": "A **recurrence** describes a recursive algorithm's cost in terms of smaller\ninputs. Most divide-and-conquer algorithms fit the shape:\n\n```text\nT(n) = a · T(n / b) + f(n)\n```\n\n- **a** — how many recursive subproblems you make,\n- **n / b** — the size of each subproblem,\n- **f(n)** — the work to divide and combine *outside* the recursive calls.\n\n## The intuition: a tree of work\n\nPicture the recursion as a tree. Each level splits the work; the question is\nwhether most of the total work happens near the **root** (the top combine step),\nis spread **evenly** across levels, or piles up in the **leaves** (the many tiny\nbase cases). The Master Theorem just formalizes which of the three it is.\n\nThe deciding quantity is **n^(log_b a)** — the number of leaves — compared\nagainst **f(n)**, the per-level combine work.\n\n## The Master Theorem (three cases)\n\nLet `c = log_b a`. Compare `f(n)` with `n^c`:\n\n| Case | Condition | Result | Who dominates |\n|---|---|---|---|\n| 1 | f(n) grows slower than n^c | T(n) = O(n^c) | the leaves |\n| 2 | f(n) ≈ n^c | T(n) = O(n^c · log n) | balanced (log factor) |\n| 3 | f(n) grows faster than n^c | T(n) = O(f(n)) | the root work |\n\n## Worked examples\n\n**Merge sort:** `T(n) = 2·T(n/2) + O(n)`. Here a=2, b=2, so c = log₂2 = 1, and\nn^c = n. f(n) = n ≈ n^c → **Case 2** → **O(n log n)**. ✓\n\n**Binary search:** `T(n) = 1·T(n/2) + O(1)`. a=1, b=2, c = log₂1 = 0, n^c = 1.\nf(n) = 1 ≈ n^0 → **Case 2** → **O(1 · log n) = O(log n)**. ✓\n\n**Linear-work split (e.g. some tree algorithms):** `T(n) = 2·T(n/2) + O(1)`.\nc = 1, n^c = n, f(n) = 1 grows slower → **Case 1** → **O(n)**.\n\n**Heavy combine:** `T(n) = 2·T(n/2) + O(n^2)`. c = 1, f(n) = n² grows faster →\n**Case 3** → **O(n^2)**.\n\n## When the Master Theorem doesn't apply\n\nIt needs the clean `a·T(n/b) + f(n)` form with equal-sized subproblems. It does\n**not** directly cover:\n- **Unequal splits** like `T(n) = T(n/3) + T(2n/3) + O(n)` (quicksort-style) —\n  use a recursion tree or substitution.\n- **Subtract-and-conquer** like `T(n) = T(n-1) + O(1)` (linear recursion) — that\n  one just unrolls to **O(n)** directly.\n- f(n) that sits *between* cases (a log-factor gap) — needs the extended theorem.\n\n## The by-hand fallback: unrolling\n\nWhen in doubt, expand a few levels and spot the pattern. `T(n) = T(n-1) + O(1)`\nunrolls to `O(1) + O(1) + … (n times) = O(n)`. `T(n) = 2·T(n-1) + O(1)` doubles\neach level → `O(2^n)` (the naive-Fibonacci / all-subsets blowup).\n\n### Common pitfalls\n- Plugging a **subtract**-form recurrence (`T(n-1)`) into the Master Theorem,\n  which is for **divide** forms (`T(n/b)`).\n- Miscomputing `log_b a` (for merge sort it's log₂2 = 1, not 2).\n- Forgetting that Case 2 is the one that *adds the log factor*.\n\n### Key takeaways\n- Model divide-and-conquer as `T(n) = a·T(n/b) + f(n)`.\n- Compare f(n) to n^(log_b a): leaves win, balanced adds a log, or the root wins.\n- Merge sort → O(n log n), binary search → O(log n), linear recursion → O(n).\n",
    "exercises": [],
    "cards": [],
    "quiz": [
      {
        "question": "In the recurrence T(n) = a·T(n/b) + f(n), what does f(n) represent?",
        "options": [
          "The number of subproblems",
          "The work to divide and combine outside the recursive calls",
          "The base case value",
          "The recursion depth"
        ],
        "answer": 1,
        "explanation": "f(n) is the non-recursive work per call — splitting the input and combining subresults. a and n/b describe the recursive calls."
      },
      {
        "question": "Merge sort is T(n) = 2·T(n/2) + O(n). By the Master Theorem this is:",
        "options": [
          "O(n)",
          "O(n log n)",
          "O(n^2)",
          "O(log n)"
        ],
        "answer": 1,
        "explanation": "log₂2 = 1 so n^c = n, and f(n) = n matches it → Case 2 → O(n log n)."
      },
      {
        "question": "Binary search is T(n) = T(n/2) + O(1). This solves to:",
        "options": [
          "O(1)",
          "O(log n)",
          "O(n)",
          "O(n log n)"
        ],
        "answer": 1,
        "explanation": "a=1, b=2 gives n^0 = 1, and f(n)=O(1) matches → Case 2 → O(1·log n) = O(log n)."
      },
      {
        "question": "The Master Theorem in the a·T(n/b)+f(n) form does NOT directly apply to which recurrence?",
        "options": [
          "T(n) = 2T(n/2) + O(n)",
          "T(n) = T(n-1) + O(1)",
          "T(n) = 4T(n/2) + O(n)",
          "T(n) = T(n/2) + O(1)"
        ],
        "answer": 1,
        "explanation": "T(n-1) is a subtract-and-conquer form, not a divide form (n/b). The Master Theorem is for divide recurrences; this one just unrolls to O(n)."
      },
      {
        "question": "T(n) = 2·T(n-1) + O(1) unrolls to which complexity?",
        "options": [
          "O(n)",
          "O(n log n)",
          "O(2^n)",
          "O(n^2)"
        ],
        "answer": 2,
        "explanation": "Doubling the number of calls at each of n levels gives 2^n total — the classic exponential blowup of naive branching recursion."
      },
      {
        "question": "In the Master Theorem, which case ADDS a logarithmic factor to the result?",
        "options": [
          "Case 1 (leaves dominate)",
          "Case 2 (f(n) balances n^{log_b a})",
          "Case 3 (root work dominates)",
          "None of them"
        ],
        "answer": 1,
        "explanation": "Case 2 is the balanced case where per-level work is equal across log n levels, contributing the extra log factor (e.g. n log n)."
      }
    ],
    "practice": [
      {
        "slug": "nth-fibonacci",
        "note": "Naive recursion T(n)=T(n-1)+T(n-2) → exponential; see the blowup."
      },
      {
        "slug": "binary-search-first",
        "note": "T(n)=T(n/2)+O(1) → O(log n) in practice."
      },
      {
        "slug": "merge-sorted-arrays",
        "note": "The O(n) combine step of the merge-sort recurrence."
      },
      {
        "slug": "climbing-stairs",
        "note": "A recurrence you can either explode or memoize."
      }
    ]
  },
  {
    "key": "jv_keywords",
    "name": "Keywords & Syntax",
    "category": "JV: Language Core",
    "what": "The reserved words and punctuation every Java program is built from.",
    "deep": "Java has ~50 reserved keywords you can't use as names. This set covers the everyday ones — declaring things, branching, looping, and the tiny syntax rules (semicolons, blocks, the entry point) that make a file compile.",
    "java": "Every row gives you **two** explanations: the **textbook definition** (what you'd write on an exam) and a **plain-English** line (what it actually means when you're staring at code). Skim the glossary, then hit **🎴 Study cards** to drill them with spaced repetition, and **❓ Check yourself** at the bottom.",
    "language": "java_vocab",
    "lesson": "| Term | Textbook definition | In plain English | Example |\n|---|---|---|---|\n| `class` | A blueprint that groups fields and methods into a type. | The box you put your code in — nothing runs outside a class. | `class Dog { }` |\n| `void` | A return type meaning the method hands nothing back. | This method does stuff but gives you nothing in return. | `void print() { }` |\n| `return` | Exits a method, optionally handing a value back. | Stop here and give this value back to whoever called me. | `return x + 1;` |\n| `if / else` | Runs a block only when a boolean condition holds. | A fork in the road: go this way, otherwise that way. | `if (x > 0) { } else { }` |\n| `for` | A loop with init, condition, and step in one line. | Repeat this a known number of times. | `for (int i = 0; i < n; i++) { }` |\n| `while` | A loop that repeats while a condition stays true. | Keep going until this stops being true. | `while (running) { }` |\n| `do` | A loop that runs the body once before checking. | Do it at least once, then decide whether to repeat. | `do { } while (x < 5);` |\n| `switch` | Branches to a case matching a value. | A cleaner tower of if/else for one variable. | `switch (day) { case 1: break; }` |\n| `break` | Immediately exits the nearest loop or switch. | Bail out of this loop right now. | `if (found) break;` |\n| `continue` | Skips to the next iteration of a loop. | Skip the rest of this round and start the next. | `if (x < 0) continue;` |\n| `new` | Allocates an object and calls its constructor. | Make me a fresh one of these. | `Dog d = new Dog();` |\n| `this` | A reference to the current object. | Me — the object whose method is running. | `this.name = name;` |\n| `super` | A reference to the parent class's members. | The version of me that my parent defined. | `super.toString();` |\n| `instanceof` | Tests whether an object is of a given type. | Is this thing actually one of those? | `if (o instanceof Dog) { }` |\n| `null` | A reference that points at no object. | A box labeled 'nothing here'. | `String s = null;` |\n| `true / false` | The two boolean literals. | Yes and no, the only two values a boolean holds. | `boolean ok = true;` |\n| `package` | A namespace that groups related classes. | The folder your class lives in, spelled with dots. | `package com.app.util;` |\n| `import` | Brings a type into scope by short name. | Tells Java where to find a class so you can drop the long name. | `import java.util.List;` |\n| `main` | The entry-point method the JVM calls first. | Where the program starts running. | `public static void main(String[] a) { }` |\n| `System.out.println` | Prints a line to standard output. | The classic 'print something to the console'. | `System.out.println(x);` |\n| `assert` | Checks an invariant; throws if it's false (when enabled). | A sanity check that only fires when you turn asserts on. | `assert n > 0;` |\n| `var` | Infers a local variable's type from its initializer. | Let the compiler figure out the type for me. | `var list = new ArrayList<String>();` |\n| `yield` | Returns a value from a switch expression arm. | The switch-expression version of return. | `case 1 -> { yield 10; }` |\n| `enum` | A type whose values are a fixed named set. | A type that can only be one of these few things. | `enum Color { RED, GREEN }` |\n",
    "exercises": [],
    "cards": [
      {
        "front": "class",
        "reading": "",
        "meaning": "A blueprint that groups fields and methods into a type.",
        "example_ja": "class Dog { }",
        "example_en": "The box you put your code in — nothing runs outside a class."
      },
      {
        "front": "void",
        "reading": "",
        "meaning": "A return type meaning the method hands nothing back.",
        "example_ja": "void print() { }",
        "example_en": "This method does stuff but gives you nothing in return."
      },
      {
        "front": "return",
        "reading": "",
        "meaning": "Exits a method, optionally handing a value back.",
        "example_ja": "return x + 1;",
        "example_en": "Stop here and give this value back to whoever called me."
      },
      {
        "front": "if / else",
        "reading": "",
        "meaning": "Runs a block only when a boolean condition holds.",
        "example_ja": "if (x > 0) { } else { }",
        "example_en": "A fork in the road: go this way, otherwise that way."
      },
      {
        "front": "for",
        "reading": "",
        "meaning": "A loop with init, condition, and step in one line.",
        "example_ja": "for (int i = 0; i < n; i++) { }",
        "example_en": "Repeat this a known number of times."
      },
      {
        "front": "while",
        "reading": "",
        "meaning": "A loop that repeats while a condition stays true.",
        "example_ja": "while (running) { }",
        "example_en": "Keep going until this stops being true."
      },
      {
        "front": "do",
        "reading": "",
        "meaning": "A loop that runs the body once before checking.",
        "example_ja": "do { } while (x < 5);",
        "example_en": "Do it at least once, then decide whether to repeat."
      },
      {
        "front": "switch",
        "reading": "",
        "meaning": "Branches to a case matching a value.",
        "example_ja": "switch (day) { case 1: break; }",
        "example_en": "A cleaner tower of if/else for one variable."
      },
      {
        "front": "break",
        "reading": "",
        "meaning": "Immediately exits the nearest loop or switch.",
        "example_ja": "if (found) break;",
        "example_en": "Bail out of this loop right now."
      },
      {
        "front": "continue",
        "reading": "",
        "meaning": "Skips to the next iteration of a loop.",
        "example_ja": "if (x < 0) continue;",
        "example_en": "Skip the rest of this round and start the next."
      },
      {
        "front": "new",
        "reading": "",
        "meaning": "Allocates an object and calls its constructor.",
        "example_ja": "Dog d = new Dog();",
        "example_en": "Make me a fresh one of these."
      },
      {
        "front": "this",
        "reading": "",
        "meaning": "A reference to the current object.",
        "example_ja": "this.name = name;",
        "example_en": "Me — the object whose method is running."
      },
      {
        "front": "super",
        "reading": "",
        "meaning": "A reference to the parent class's members.",
        "example_ja": "super.toString();",
        "example_en": "The version of me that my parent defined."
      },
      {
        "front": "instanceof",
        "reading": "",
        "meaning": "Tests whether an object is of a given type.",
        "example_ja": "if (o instanceof Dog) { }",
        "example_en": "Is this thing actually one of those?"
      },
      {
        "front": "null",
        "reading": "",
        "meaning": "A reference that points at no object.",
        "example_ja": "String s = null;",
        "example_en": "A box labeled 'nothing here'."
      },
      {
        "front": "true / false",
        "reading": "",
        "meaning": "The two boolean literals.",
        "example_ja": "boolean ok = true;",
        "example_en": "Yes and no, the only two values a boolean holds."
      },
      {
        "front": "package",
        "reading": "",
        "meaning": "A namespace that groups related classes.",
        "example_ja": "package com.app.util;",
        "example_en": "The folder your class lives in, spelled with dots."
      },
      {
        "front": "import",
        "reading": "",
        "meaning": "Brings a type into scope by short name.",
        "example_ja": "import java.util.List;",
        "example_en": "Tells Java where to find a class so you can drop the long name."
      },
      {
        "front": "main",
        "reading": "",
        "meaning": "The entry-point method the JVM calls first.",
        "example_ja": "public static void main(String[] a) { }",
        "example_en": "Where the program starts running."
      },
      {
        "front": "System.out.println",
        "reading": "",
        "meaning": "Prints a line to standard output.",
        "example_ja": "System.out.println(x);",
        "example_en": "The classic 'print something to the console'."
      },
      {
        "front": "assert",
        "reading": "",
        "meaning": "Checks an invariant; throws if it's false (when enabled).",
        "example_ja": "assert n > 0;",
        "example_en": "A sanity check that only fires when you turn asserts on."
      },
      {
        "front": "var",
        "reading": "",
        "meaning": "Infers a local variable's type from its initializer.",
        "example_ja": "var list = new ArrayList<String>();",
        "example_en": "Let the compiler figure out the type for me."
      },
      {
        "front": "yield",
        "reading": "",
        "meaning": "Returns a value from a switch expression arm.",
        "example_ja": "case 1 -> { yield 10; }",
        "example_en": "The switch-expression version of return."
      },
      {
        "front": "enum",
        "reading": "",
        "meaning": "A type whose values are a fixed named set.",
        "example_ja": "enum Color { RED, GREEN }",
        "example_en": "A type that can only be one of these few things."
      }
    ],
    "quiz": [
      {
        "question": "Which keyword allocates a new object and runs its constructor?",
        "options": [
          "new",
          "class",
          "this",
          "void"
        ],
        "answer": 0,
        "explanation": "`new` reserves heap space and invokes the constructor, handing back a reference."
      },
      {
        "question": "What does `void` as a return type mean?",
        "options": [
          "The method returns nothing",
          "The method returns null",
          "The method returns 0",
          "The method never ends"
        ],
        "answer": 0,
        "explanation": "`void` means the method produces no return value at all — you can't assign its result."
      },
      {
        "question": "`continue` inside a loop will…",
        "options": [
          "skip to the next iteration",
          "exit the loop entirely",
          "restart the whole loop",
          "throw an exception"
        ],
        "answer": 0,
        "explanation": "`continue` skips the rest of the current iteration; `break` is what exits the loop."
      },
      {
        "question": "Which is TRUE about `var`?",
        "options": [
          "The type is inferred from the initializer",
          "It makes the variable dynamically typed",
          "It works for fields too",
          "It means the value can't change"
        ],
        "answer": 0,
        "explanation": "`var` is still statically typed — the compiler infers the type; it only works for local variables with an initializer."
      },
      {
        "question": "`instanceof` evaluates to…",
        "options": [
          "a boolean",
          "the object's class name",
          "an int",
          "the object itself"
        ],
        "answer": 0,
        "explanation": "`o instanceof Dog` is a boolean test of whether `o`'s runtime type is Dog (or a subtype)."
      }
    ],
    "practice": []
  },
  {
    "key": "jv_oop",
    "name": "OOP Concepts",
    "category": "JV: OOP",
    "what": "The object-oriented ideas Java is built around: classes, inheritance, polymorphism.",
    "deep": "Java is object-oriented to the core. These are the terms interviewers love: the four pillars (encapsulation, inheritance, polymorphism, abstraction) plus the machinery — constructors, overriding, interfaces — that makes them work.",
    "java": "Every row gives you **two** explanations: the **textbook definition** (what you'd write on an exam) and a **plain-English** line (what it actually means when you're staring at code). Skim the glossary, then hit **🎴 Study cards** to drill them with spaced repetition, and **❓ Check yourself** at the bottom.",
    "language": "java_vocab",
    "lesson": "| Term | Textbook definition | In plain English | Example |\n|---|---|---|---|\n| `object` | A concrete instance of a class held in memory. | An actual thing built from the blueprint. | `Dog d = new Dog();` |\n| `instance` | One specific object created from a class. | A single copy made from the mold. | `new Dog()` |\n| `field` | A variable that belongs to an object or class. | A thing an object remembers. | `int age;` |\n| `method` | A named block of behavior on a class. | A thing an object can do. | `void bark() { }` |\n| `constructor` | A special method that initializes a new object. | The setup routine that runs when you say `new`. | `Dog(String n) { name = n; }` |\n| `encapsulation` | Bundling data with the methods that guard it. | Hide the wires; expose safe buttons. | `private int bal;` |\n| `inheritance` | A class acquiring another class's members. | Get a parent's stuff for free, then add your own. | `class Dog extends Animal { }` |\n| `polymorphism` | One reference behaving as many runtime types. | Same call, different behavior depending on the real object. | `Animal a = new Dog();` |\n| `abstraction` | Exposing behavior while hiding implementation. | You use the 'what' without caring about the 'how'. | `interface Shape { }` |\n| `override` | Replacing an inherited method's behavior. | Redo a parent's method your own way. | `@Override String toString() { }` |\n| `overload` | Same method name, different parameter lists. | One name, several versions for different inputs. | `add(int a); add(double a);` |\n| `interface` | A contract of methods a class promises to provide. | A to-do list a class agrees to complete. | `interface Runnable { void run(); }` |\n| `abstract class` | A partial class that can't be instantiated. | A half-built blueprint others must finish. | `abstract class Shape { }` |\n| `subclass` | A class that extends another. | The child that inherits. | `class Cat extends Animal { }` |\n| `superclass` | The class being extended. | The parent being inherited from. | `class Animal { }` |\n| `is-a relationship` | A subtype genuinely being its supertype. | A Dog IS-A Animal — inheritance is right. | `Dog extends Animal` |\n| `has-a relationship` | One object holding another (composition). | A Car HAS-A Engine — a field, not inheritance. | `class Car { Engine e; }` |\n| `composition` | Building behavior by holding other objects. | Assemble from parts instead of inheriting. | `class Car { Engine e; }` |\n| `getter / setter` | Accessor methods for a private field. | The safe read/write buttons around hidden data. | `int getAge() { return age; }` |\n| `this()` | A constructor calling another constructor. | Reuse another constructor instead of repeating setup. | `Dog() { this(\"Rex\"); }` |\n| `static member` | A field or method owned by the class, not instances. | Shared by everyone; belongs to the class itself. | `static int count;` |\n| `upcasting` | Treating an object as its supertype. | See a Dog as just an Animal — always safe. | `Animal a = new Dog();` |\n| `downcasting` | Treating a supertype reference as a subtype. | Insist this Animal is really a Dog — can fail. | `Dog d = (Dog) a;` |\n| `object identity` | Whether two references are the same object. | Same object vs. merely equal — `==` vs `equals`. | `a == b` |\n",
    "exercises": [],
    "cards": [
      {
        "front": "object",
        "reading": "",
        "meaning": "A concrete instance of a class held in memory.",
        "example_ja": "Dog d = new Dog();",
        "example_en": "An actual thing built from the blueprint."
      },
      {
        "front": "instance",
        "reading": "",
        "meaning": "One specific object created from a class.",
        "example_ja": "new Dog()",
        "example_en": "A single copy made from the mold."
      },
      {
        "front": "field",
        "reading": "",
        "meaning": "A variable that belongs to an object or class.",
        "example_ja": "int age;",
        "example_en": "A thing an object remembers."
      },
      {
        "front": "method",
        "reading": "",
        "meaning": "A named block of behavior on a class.",
        "example_ja": "void bark() { }",
        "example_en": "A thing an object can do."
      },
      {
        "front": "constructor",
        "reading": "",
        "meaning": "A special method that initializes a new object.",
        "example_ja": "Dog(String n) { name = n; }",
        "example_en": "The setup routine that runs when you say `new`."
      },
      {
        "front": "encapsulation",
        "reading": "",
        "meaning": "Bundling data with the methods that guard it.",
        "example_ja": "private int bal;",
        "example_en": "Hide the wires; expose safe buttons."
      },
      {
        "front": "inheritance",
        "reading": "",
        "meaning": "A class acquiring another class's members.",
        "example_ja": "class Dog extends Animal { }",
        "example_en": "Get a parent's stuff for free, then add your own."
      },
      {
        "front": "polymorphism",
        "reading": "",
        "meaning": "One reference behaving as many runtime types.",
        "example_ja": "Animal a = new Dog();",
        "example_en": "Same call, different behavior depending on the real object."
      },
      {
        "front": "abstraction",
        "reading": "",
        "meaning": "Exposing behavior while hiding implementation.",
        "example_ja": "interface Shape { }",
        "example_en": "You use the 'what' without caring about the 'how'."
      },
      {
        "front": "override",
        "reading": "",
        "meaning": "Replacing an inherited method's behavior.",
        "example_ja": "@Override String toString() { }",
        "example_en": "Redo a parent's method your own way."
      },
      {
        "front": "overload",
        "reading": "",
        "meaning": "Same method name, different parameter lists.",
        "example_ja": "add(int a); add(double a);",
        "example_en": "One name, several versions for different inputs."
      },
      {
        "front": "interface",
        "reading": "",
        "meaning": "A contract of methods a class promises to provide.",
        "example_ja": "interface Runnable { void run(); }",
        "example_en": "A to-do list a class agrees to complete."
      },
      {
        "front": "abstract class",
        "reading": "",
        "meaning": "A partial class that can't be instantiated.",
        "example_ja": "abstract class Shape { }",
        "example_en": "A half-built blueprint others must finish."
      },
      {
        "front": "subclass",
        "reading": "",
        "meaning": "A class that extends another.",
        "example_ja": "class Cat extends Animal { }",
        "example_en": "The child that inherits."
      },
      {
        "front": "superclass",
        "reading": "",
        "meaning": "The class being extended.",
        "example_ja": "class Animal { }",
        "example_en": "The parent being inherited from."
      },
      {
        "front": "is-a relationship",
        "reading": "",
        "meaning": "A subtype genuinely being its supertype.",
        "example_ja": "Dog extends Animal",
        "example_en": "A Dog IS-A Animal — inheritance is right."
      },
      {
        "front": "has-a relationship",
        "reading": "",
        "meaning": "One object holding another (composition).",
        "example_ja": "class Car { Engine e; }",
        "example_en": "A Car HAS-A Engine — a field, not inheritance."
      },
      {
        "front": "composition",
        "reading": "",
        "meaning": "Building behavior by holding other objects.",
        "example_ja": "class Car { Engine e; }",
        "example_en": "Assemble from parts instead of inheriting."
      },
      {
        "front": "getter / setter",
        "reading": "",
        "meaning": "Accessor methods for a private field.",
        "example_ja": "int getAge() { return age; }",
        "example_en": "The safe read/write buttons around hidden data."
      },
      {
        "front": "this()",
        "reading": "",
        "meaning": "A constructor calling another constructor.",
        "example_ja": "Dog() { this(\"Rex\"); }",
        "example_en": "Reuse another constructor instead of repeating setup."
      },
      {
        "front": "static member",
        "reading": "",
        "meaning": "A field or method owned by the class, not instances.",
        "example_ja": "static int count;",
        "example_en": "Shared by everyone; belongs to the class itself."
      },
      {
        "front": "upcasting",
        "reading": "",
        "meaning": "Treating an object as its supertype.",
        "example_ja": "Animal a = new Dog();",
        "example_en": "See a Dog as just an Animal — always safe."
      },
      {
        "front": "downcasting",
        "reading": "",
        "meaning": "Treating a supertype reference as a subtype.",
        "example_ja": "Dog d = (Dog) a;",
        "example_en": "Insist this Animal is really a Dog — can fail."
      },
      {
        "front": "object identity",
        "reading": "",
        "meaning": "Whether two references are the same object.",
        "example_ja": "a == b",
        "example_en": "Same object vs. merely equal — `==` vs `equals`."
      }
    ],
    "quiz": [
      {
        "question": "Which pillar is about hiding internal data behind methods?",
        "options": [
          "Encapsulation",
          "Inheritance",
          "Polymorphism",
          "Abstraction"
        ],
        "answer": 0,
        "explanation": "Encapsulation bundles data with the methods that control access, typically via private fields + getters/setters."
      },
      {
        "question": "`Animal a = new Dog();` then `a.speak()` runs Dog's version. This is…",
        "options": [
          "Polymorphism",
          "Overloading",
          "Encapsulation",
          "Composition"
        ],
        "answer": 0,
        "explanation": "The call is resolved at runtime to the actual object's type — runtime polymorphism via overriding."
      },
      {
        "question": "Overloading differs from overriding because overloading…",
        "options": [
          "changes the parameter list, same name",
          "replaces a parent method",
          "requires inheritance",
          "only works on constructors"
        ],
        "answer": 0,
        "explanation": "Overloading = same name, different parameters, resolved at compile time. Overriding replaces an inherited method."
      },
      {
        "question": "Which describes a HAS-A relationship?",
        "options": [
          "A Car holds an Engine field",
          "A Cat extends Animal",
          "A Dog implements Runnable",
          "A Shape is abstract"
        ],
        "answer": 0,
        "explanation": "HAS-A is composition (holding another object as a field). IS-A is inheritance."
      },
      {
        "question": "Why can't you write `new Shape()` if `Shape` is an abstract class?",
        "options": [
          "Abstract classes can't be instantiated",
          "It has no fields",
          "It's final",
          "It's private"
        ],
        "answer": 0,
        "explanation": "An abstract class may have unfinished (abstract) methods, so Java forbids instantiating it directly."
      }
    ],
    "practice": []
  },
  {
    "key": "jv_modifiers",
    "name": "Modifiers & Access",
    "category": "JV: Modifiers",
    "what": "The keywords that control visibility, mutability, and where members live.",
    "deep": "Modifiers are the little words in front of a declaration that change its rules: who can see it (access), whether it can change (final), whether it belongs to the class or the instance (static), and more.",
    "java": "Every row gives you **two** explanations: the **textbook definition** (what you'd write on an exam) and a **plain-English** line (what it actually means when you're staring at code). Skim the glossary, then hit **🎴 Study cards** to drill them with spaced repetition, and **❓ Check yourself** at the bottom.",
    "language": "java_vocab",
    "lesson": "| Term | Textbook definition | In plain English | Example |\n|---|---|---|---|\n| `public` | Visible to all other classes. | Anyone, anywhere can use this. | `public int x;` |\n| `private` | Visible only inside the declaring class. | Nobody outside this class gets to touch it. | `private int x;` |\n| `protected` | Visible to the package and to subclasses. | Family and neighbors only. | `protected int x;` |\n| `package-private` | Default access: visible within the same package. | No keyword = only classes in the same folder. | `int x;` |\n| `static` | Belongs to the class, shared across instances. | One copy for everyone, not per-object. | `static int count;` |\n| `final (variable)` | A value that can be assigned only once. | Set it once, then it's locked. | `final int MAX = 10;` |\n| `final (method)` | A method that can't be overridden. | Subclasses can't change this behavior. | `final void run() { }` |\n| `final (class)` | A class that can't be extended. | Nobody can inherit from this. | `final class Math { }` |\n| `abstract` | Declared without an implementation. | A promise to be filled in by a subclass. | `abstract void draw();` |\n| `synchronized` | Allows only one thread in at a time. | A one-at-a-time door for threads. | `synchronized void inc() { }` |\n| `volatile` | Forces reads/writes to go to main memory. | Always see the freshest value across threads. | `volatile boolean flag;` |\n| `transient` | Excludes a field from serialization. | Don't save this one to disk. | `transient String cache;` |\n| `native` | Implemented in non-Java (C/C++) code. | The body lives outside Java, in native code. | `native void hardware();` |\n| `strictfp` | Forces portable IEEE floating-point math. | Make float math identical on every machine. | `strictfp class Calc { }` |\n| `default (method)` | An interface method with a body. | A free implementation an interface hands you. | `default void hi() { }` |\n| `static import` | Imports static members for unqualified use. | Use `sqrt` instead of `Math.sqrt`. | `import static java.lang.Math.*;` |\n| `sealed` | Restricts which classes may extend it. | Only these specific classes may inherit. | `sealed interface Shape permits Circle { }` |\n| `non-sealed` | Reopens a sealed hierarchy for extension. | Undo the 'permits' lock for this branch. | `non-sealed class Circle { }` |\n| `record component` | An immutable field declared in a record header. | A field a record generates for you automatically. | `record Point(int x, int y) { }` |\n| `access modifier order` | Convention: modifiers before type. | The habitual `public static final` word order. | `public static final int N = 1;` |\n",
    "exercises": [],
    "cards": [
      {
        "front": "public",
        "reading": "",
        "meaning": "Visible to all other classes.",
        "example_ja": "public int x;",
        "example_en": "Anyone, anywhere can use this."
      },
      {
        "front": "private",
        "reading": "",
        "meaning": "Visible only inside the declaring class.",
        "example_ja": "private int x;",
        "example_en": "Nobody outside this class gets to touch it."
      },
      {
        "front": "protected",
        "reading": "",
        "meaning": "Visible to the package and to subclasses.",
        "example_ja": "protected int x;",
        "example_en": "Family and neighbors only."
      },
      {
        "front": "package-private",
        "reading": "",
        "meaning": "Default access: visible within the same package.",
        "example_ja": "int x;",
        "example_en": "No keyword = only classes in the same folder."
      },
      {
        "front": "static",
        "reading": "",
        "meaning": "Belongs to the class, shared across instances.",
        "example_ja": "static int count;",
        "example_en": "One copy for everyone, not per-object."
      },
      {
        "front": "final (variable)",
        "reading": "",
        "meaning": "A value that can be assigned only once.",
        "example_ja": "final int MAX = 10;",
        "example_en": "Set it once, then it's locked."
      },
      {
        "front": "final (method)",
        "reading": "",
        "meaning": "A method that can't be overridden.",
        "example_ja": "final void run() { }",
        "example_en": "Subclasses can't change this behavior."
      },
      {
        "front": "final (class)",
        "reading": "",
        "meaning": "A class that can't be extended.",
        "example_ja": "final class Math { }",
        "example_en": "Nobody can inherit from this."
      },
      {
        "front": "abstract",
        "reading": "",
        "meaning": "Declared without an implementation.",
        "example_ja": "abstract void draw();",
        "example_en": "A promise to be filled in by a subclass."
      },
      {
        "front": "synchronized",
        "reading": "",
        "meaning": "Allows only one thread in at a time.",
        "example_ja": "synchronized void inc() { }",
        "example_en": "A one-at-a-time door for threads."
      },
      {
        "front": "volatile",
        "reading": "",
        "meaning": "Forces reads/writes to go to main memory.",
        "example_ja": "volatile boolean flag;",
        "example_en": "Always see the freshest value across threads."
      },
      {
        "front": "transient",
        "reading": "",
        "meaning": "Excludes a field from serialization.",
        "example_ja": "transient String cache;",
        "example_en": "Don't save this one to disk."
      },
      {
        "front": "native",
        "reading": "",
        "meaning": "Implemented in non-Java (C/C++) code.",
        "example_ja": "native void hardware();",
        "example_en": "The body lives outside Java, in native code."
      },
      {
        "front": "strictfp",
        "reading": "",
        "meaning": "Forces portable IEEE floating-point math.",
        "example_ja": "strictfp class Calc { }",
        "example_en": "Make float math identical on every machine."
      },
      {
        "front": "default (method)",
        "reading": "",
        "meaning": "An interface method with a body.",
        "example_ja": "default void hi() { }",
        "example_en": "A free implementation an interface hands you."
      },
      {
        "front": "static import",
        "reading": "",
        "meaning": "Imports static members for unqualified use.",
        "example_ja": "import static java.lang.Math.*;",
        "example_en": "Use `sqrt` instead of `Math.sqrt`."
      },
      {
        "front": "sealed",
        "reading": "",
        "meaning": "Restricts which classes may extend it.",
        "example_ja": "sealed interface Shape permits Circle { }",
        "example_en": "Only these specific classes may inherit."
      },
      {
        "front": "non-sealed",
        "reading": "",
        "meaning": "Reopens a sealed hierarchy for extension.",
        "example_ja": "non-sealed class Circle { }",
        "example_en": "Undo the 'permits' lock for this branch."
      },
      {
        "front": "record component",
        "reading": "",
        "meaning": "An immutable field declared in a record header.",
        "example_ja": "record Point(int x, int y) { }",
        "example_en": "A field a record generates for you automatically."
      },
      {
        "front": "access modifier order",
        "reading": "",
        "meaning": "Convention: modifiers before type.",
        "example_ja": "public static final int N = 1;",
        "example_en": "The habitual `public static final` word order."
      }
    ],
    "quiz": [
      {
        "question": "Which access level is the DEFAULT when you write no modifier?",
        "options": [
          "Package-private",
          "public",
          "private",
          "protected"
        ],
        "answer": 0,
        "explanation": "No keyword means package-private: visible only to classes in the same package."
      },
      {
        "question": "`volatile` on a field mainly guarantees…",
        "options": [
          "visibility of the latest value across threads",
          "that only one thread runs the method",
          "the field can't change",
          "the field isn't serialized"
        ],
        "answer": 0,
        "explanation": "`volatile` ensures reads/writes hit main memory so threads see the newest value; it does NOT make compound actions atomic."
      },
      {
        "question": "What does `final` on a method do?",
        "options": [
          "Prevents subclasses from overriding it",
          "Prevents it from returning",
          "Makes it static",
          "Makes it thread-safe"
        ],
        "answer": 0,
        "explanation": "A final method is locked — no subclass may override it. (final class = can't be extended; final variable = can't be reassigned.)"
      },
      {
        "question": "`transient` affects a field during…",
        "options": [
          "serialization",
          "garbage collection",
          "compilation",
          "method dispatch"
        ],
        "answer": 0,
        "explanation": "A transient field is skipped when the object is serialized, so it isn't written to the stream."
      },
      {
        "question": "A `static` field is best described as…",
        "options": [
          "one shared copy owned by the class",
          "a copy per object",
          "always final",
          "thread-safe by default"
        ],
        "answer": 0,
        "explanation": "Static members belong to the class itself, so all instances share the single copy."
      }
    ],
    "practice": []
  },
  {
    "key": "jv_types",
    "name": "Types & Primitives",
    "category": "JV: Types",
    "what": "The built-in value types, their wrappers, and how conversion works.",
    "deep": "Java has eight primitive types (raw values) and matching wrapper classes (objects). Knowing the sizes, the boxing rules, and how casting/promotion work saves you from overflow and precision bugs.",
    "java": "Every row gives you **two** explanations: the **textbook definition** (what you'd write on an exam) and a **plain-English** line (what it actually means when you're staring at code). Skim the glossary, then hit **🎴 Study cards** to drill them with spaced repetition, and **❓ Check yourself** at the bottom.",
    "language": "java_vocab",
    "lesson": "| Term | Textbook definition | In plain English | Example |\n|---|---|---|---|\n| `int` | A 32-bit signed integer. | The default whole number, ±2 billion. | `int n = 42;` |\n| `long` | A 64-bit signed integer. | A whole number when int isn't big enough. | `long big = 9_000_000_000L;` |\n| `short` | A 16-bit signed integer. | A small whole number, rarely used. | `short s = 100;` |\n| `byte` | An 8-bit signed integer (-128..127). | The tiniest integer, used for raw bytes. | `byte b = 7;` |\n| `double` | A 64-bit floating-point number. | The default decimal number. | `double pi = 3.14;` |\n| `float` | A 32-bit floating-point number. | A smaller, less precise decimal. | `float f = 1.5f;` |\n| `char` | A 16-bit Unicode code unit. | A single character, stored as a number. | `char c = 'A';` |\n| `boolean` | A true/false value. | A yes/no switch. | `boolean ok = true;` |\n| `wrapper class` | An object form of a primitive (Integer, Double…). | A primitive wearing an object costume. | `Integer i = 42;` |\n| `autoboxing` | Automatic primitive-to-wrapper conversion. | Java quietly puts the int into an Integer. | `Integer i = 5;` |\n| `unboxing` | Automatic wrapper-to-primitive conversion. | Java quietly takes the int back out. | `int x = i;` |\n| `widening` | Implicit conversion to a bigger type. | int fits in a long, so no cast needed. | `long l = 5;` |\n| `narrowing` | Explicit conversion to a smaller type. | Force a long into an int — may lose bits. | `int x = (int) 5L;` |\n| `cast` | Explicitly converting between types. | Telling the compiler 'trust me, treat it as this'. | `double d = (double) n;` |\n| `overflow` | A value wrapping past its type's max. | Add 1 to the biggest int and it flips negative. | `Integer.MAX_VALUE + 1` |\n| `literal` | A fixed value written directly in code. | A hard-coded value like 42 or \"hi\". | `int n = 0xFF;` |\n| `type promotion` | Small types promoted to int in arithmetic. | byte + byte is actually computed as int. | `int r = b1 + b2;` |\n| `BigInteger / BigDecimal` | Arbitrary-precision number classes. | For numbers too big or money too exact for double. | `new BigDecimal(\"0.1\")` |\n",
    "exercises": [],
    "cards": [
      {
        "front": "int",
        "reading": "",
        "meaning": "A 32-bit signed integer.",
        "example_ja": "int n = 42;",
        "example_en": "The default whole number, ±2 billion."
      },
      {
        "front": "long",
        "reading": "",
        "meaning": "A 64-bit signed integer.",
        "example_ja": "long big = 9_000_000_000L;",
        "example_en": "A whole number when int isn't big enough."
      },
      {
        "front": "short",
        "reading": "",
        "meaning": "A 16-bit signed integer.",
        "example_ja": "short s = 100;",
        "example_en": "A small whole number, rarely used."
      },
      {
        "front": "byte",
        "reading": "",
        "meaning": "An 8-bit signed integer (-128..127).",
        "example_ja": "byte b = 7;",
        "example_en": "The tiniest integer, used for raw bytes."
      },
      {
        "front": "double",
        "reading": "",
        "meaning": "A 64-bit floating-point number.",
        "example_ja": "double pi = 3.14;",
        "example_en": "The default decimal number."
      },
      {
        "front": "float",
        "reading": "",
        "meaning": "A 32-bit floating-point number.",
        "example_ja": "float f = 1.5f;",
        "example_en": "A smaller, less precise decimal."
      },
      {
        "front": "char",
        "reading": "",
        "meaning": "A 16-bit Unicode code unit.",
        "example_ja": "char c = 'A';",
        "example_en": "A single character, stored as a number."
      },
      {
        "front": "boolean",
        "reading": "",
        "meaning": "A true/false value.",
        "example_ja": "boolean ok = true;",
        "example_en": "A yes/no switch."
      },
      {
        "front": "wrapper class",
        "reading": "",
        "meaning": "An object form of a primitive (Integer, Double…).",
        "example_ja": "Integer i = 42;",
        "example_en": "A primitive wearing an object costume."
      },
      {
        "front": "autoboxing",
        "reading": "",
        "meaning": "Automatic primitive-to-wrapper conversion.",
        "example_ja": "Integer i = 5;",
        "example_en": "Java quietly puts the int into an Integer."
      },
      {
        "front": "unboxing",
        "reading": "",
        "meaning": "Automatic wrapper-to-primitive conversion.",
        "example_ja": "int x = i;",
        "example_en": "Java quietly takes the int back out."
      },
      {
        "front": "widening",
        "reading": "",
        "meaning": "Implicit conversion to a bigger type.",
        "example_ja": "long l = 5;",
        "example_en": "int fits in a long, so no cast needed."
      },
      {
        "front": "narrowing",
        "reading": "",
        "meaning": "Explicit conversion to a smaller type.",
        "example_ja": "int x = (int) 5L;",
        "example_en": "Force a long into an int — may lose bits."
      },
      {
        "front": "cast",
        "reading": "",
        "meaning": "Explicitly converting between types.",
        "example_ja": "double d = (double) n;",
        "example_en": "Telling the compiler 'trust me, treat it as this'."
      },
      {
        "front": "overflow",
        "reading": "",
        "meaning": "A value wrapping past its type's max.",
        "example_ja": "Integer.MAX_VALUE + 1",
        "example_en": "Add 1 to the biggest int and it flips negative."
      },
      {
        "front": "literal",
        "reading": "",
        "meaning": "A fixed value written directly in code.",
        "example_ja": "int n = 0xFF;",
        "example_en": "A hard-coded value like 42 or \"hi\"."
      },
      {
        "front": "type promotion",
        "reading": "",
        "meaning": "Small types promoted to int in arithmetic.",
        "example_ja": "int r = b1 + b2;",
        "example_en": "byte + byte is actually computed as int."
      },
      {
        "front": "BigInteger / BigDecimal",
        "reading": "",
        "meaning": "Arbitrary-precision number classes.",
        "example_ja": "new BigDecimal(\"0.1\")",
        "example_en": "For numbers too big or money too exact for double."
      }
    ],
    "quiz": [
      {
        "question": "Which primitive is the DEFAULT type of an integer literal like `42`?",
        "options": [
          "int",
          "long",
          "short",
          "byte"
        ],
        "answer": 0,
        "explanation": "An un-suffixed integer literal is an `int`; append `L` for a `long`."
      },
      {
        "question": "Converting `Integer` to `int` automatically is called…",
        "options": [
          "unboxing",
          "autoboxing",
          "widening",
          "casting"
        ],
        "answer": 0,
        "explanation": "Wrapper → primitive is unboxing; primitive → wrapper is autoboxing."
      },
      {
        "question": "Which conversion REQUIRES an explicit cast?",
        "options": [
          "long to int (narrowing)",
          "int to long (widening)",
          "int to double",
          "byte to int"
        ],
        "answer": 0,
        "explanation": "Narrowing (bigger → smaller) can lose data, so Java forces an explicit cast."
      },
      {
        "question": "`Integer.MAX_VALUE + 1` produces…",
        "options": [
          "a large negative number (overflow)",
          "a compile error",
          "an exception",
          "0"
        ],
        "answer": 0,
        "explanation": "Integer arithmetic wraps around silently; exceeding MAX_VALUE flips to the most negative int."
      },
      {
        "question": "For exact monetary math you should use…",
        "options": [
          "BigDecimal",
          "double",
          "float",
          "long only"
        ],
        "answer": 0,
        "explanation": "`double`/`float` are binary approximations; `BigDecimal` stores exact decimal values."
      }
    ],
    "practice": []
  },
  {
    "key": "jv_collections",
    "name": "Collections & Data Structures",
    "category": "JV: Collections",
    "what": "The java.util toolkit: lists, maps, sets, queues, and their trade-offs.",
    "deep": "The Collections Framework is the daily bread of Java. Knowing which structure gives O(1) lookup, which keeps order, and which allows duplicates is exactly what interviews and real code demand.",
    "java": "Every row gives you **two** explanations: the **textbook definition** (what you'd write on an exam) and a **plain-English** line (what it actually means when you're staring at code). Skim the glossary, then hit **🎴 Study cards** to drill them with spaced repetition, and **❓ Check yourself** at the bottom.",
    "language": "java_vocab",
    "lesson": "| Term | Textbook definition | In plain English | Example |\n|---|---|---|---|\n| `Collection` | The root interface for groups of elements. | The umbrella term for 'a bunch of things'. | `Collection<String> c;` |\n| `List` | An ordered collection allowing duplicates. | A sequence you index into, like a row of boxes. | `List<Integer> l;` |\n| `ArrayList` | A List backed by a resizable array. | The default list — fast random access. | `new ArrayList<>()` |\n| `LinkedList` | A List/Deque backed by a doubly-linked list. | Fast add/remove at the ends, slow indexing. | `new LinkedList<>()` |\n| `Set` | A collection with no duplicate elements. | A bag that ignores repeats. | `Set<String> s;` |\n| `HashSet` | A Set backed by a hash table. | Unordered set with O(1) contains. | `new HashSet<>()` |\n| `LinkedHashSet` | A HashSet that remembers insertion order. | A set that keeps things in the order you added them. | `new LinkedHashSet<>()` |\n| `TreeSet` | A Set kept in sorted order. | A set that's always sorted for you. | `new TreeSet<>()` |\n| `Map` | A collection of key-to-value pairs. | A dictionary: look up a value by its key. | `Map<String,Integer> m;` |\n| `HashMap` | A Map backed by a hash table. | The default map — O(1) get/put, no order. | `new HashMap<>()` |\n| `LinkedHashMap` | A HashMap that preserves insertion order. | A map that iterates in add order. | `new LinkedHashMap<>()` |\n| `TreeMap` | A Map sorted by key. | A map that's always ordered by key. | `new TreeMap<>()` |\n| `Queue` | A collection processed front-to-back (FIFO). | A line: first in, first out. | `Queue<Integer> q;` |\n| `Deque` | A double-ended queue. | A line you can push/pop from both ends. | `Deque<Integer> d;` |\n| `ArrayDeque` | A fast resizable-array Deque. | The go-to stack/queue — beats Stack and LinkedList. | `new ArrayDeque<>()` |\n| `Stack` | A LIFO structure (legacy class). | Last in, first out — but prefer ArrayDeque. | `new ArrayDeque<>()` |\n| `PriorityQueue` | A queue ordered by priority (a heap). | Always hands you the smallest (or largest) next. | `new PriorityQueue<>()` |\n| `Iterator` | An object to walk a collection one item at a time. | A cursor that says 'next?' and 'got more?'. | `Iterator<T> it = c.iterator();` |\n| `Iterable` | Anything a for-each loop can traverse. | The 'you can loop over me' contract. | `for (T x : list) { }` |\n| `Comparable` | A type's natural ordering via compareTo. | 'Here's how to sort me by default.' | `class N implements Comparable<N> { }` |\n| `Comparator` | An external ordering strategy. | A custom 'sort by this instead' rule. | `Comparator.comparingInt(x -> x)` |\n| `Collections` | Static helpers for collections (sort, etc.). | The utility belt for lists and sets. | `Collections.sort(list);` |\n| `Arrays` | Static helpers for arrays. | The utility belt for raw arrays. | `Arrays.sort(arr);` |\n| `entrySet` | A Map's set of key-value pairs. | The way to loop over both keys and values. | `for (var e : m.entrySet()) { }` |\n| `keySet / values` | A Map's keys / values as collections. | Just the keys, or just the values. | `m.keySet()` |\n| `load factor` | HashMap's fill ratio before it resizes. | How full the table gets before it grows (0.75). | `new HashMap<>(16, 0.75f)` |\n| `capacity` | The number of buckets a hash table has. | How many slots the map allocated under the hood. | `new HashMap<>(64)` |\n| `fail-fast` | Iterators that throw on concurrent modification. | Change a list mid-loop and it slaps your hand. | `ConcurrentModificationException` |\n",
    "exercises": [],
    "cards": [
      {
        "front": "Collection",
        "reading": "",
        "meaning": "The root interface for groups of elements.",
        "example_ja": "Collection<String> c;",
        "example_en": "The umbrella term for 'a bunch of things'."
      },
      {
        "front": "List",
        "reading": "",
        "meaning": "An ordered collection allowing duplicates.",
        "example_ja": "List<Integer> l;",
        "example_en": "A sequence you index into, like a row of boxes."
      },
      {
        "front": "ArrayList",
        "reading": "",
        "meaning": "A List backed by a resizable array.",
        "example_ja": "new ArrayList<>()",
        "example_en": "The default list — fast random access."
      },
      {
        "front": "LinkedList",
        "reading": "",
        "meaning": "A List/Deque backed by a doubly-linked list.",
        "example_ja": "new LinkedList<>()",
        "example_en": "Fast add/remove at the ends, slow indexing."
      },
      {
        "front": "Set",
        "reading": "",
        "meaning": "A collection with no duplicate elements.",
        "example_ja": "Set<String> s;",
        "example_en": "A bag that ignores repeats."
      },
      {
        "front": "HashSet",
        "reading": "",
        "meaning": "A Set backed by a hash table.",
        "example_ja": "new HashSet<>()",
        "example_en": "Unordered set with O(1) contains."
      },
      {
        "front": "LinkedHashSet",
        "reading": "",
        "meaning": "A HashSet that remembers insertion order.",
        "example_ja": "new LinkedHashSet<>()",
        "example_en": "A set that keeps things in the order you added them."
      },
      {
        "front": "TreeSet",
        "reading": "",
        "meaning": "A Set kept in sorted order.",
        "example_ja": "new TreeSet<>()",
        "example_en": "A set that's always sorted for you."
      },
      {
        "front": "Map",
        "reading": "",
        "meaning": "A collection of key-to-value pairs.",
        "example_ja": "Map<String,Integer> m;",
        "example_en": "A dictionary: look up a value by its key."
      },
      {
        "front": "HashMap",
        "reading": "",
        "meaning": "A Map backed by a hash table.",
        "example_ja": "new HashMap<>()",
        "example_en": "The default map — O(1) get/put, no order."
      },
      {
        "front": "LinkedHashMap",
        "reading": "",
        "meaning": "A HashMap that preserves insertion order.",
        "example_ja": "new LinkedHashMap<>()",
        "example_en": "A map that iterates in add order."
      },
      {
        "front": "TreeMap",
        "reading": "",
        "meaning": "A Map sorted by key.",
        "example_ja": "new TreeMap<>()",
        "example_en": "A map that's always ordered by key."
      },
      {
        "front": "Queue",
        "reading": "",
        "meaning": "A collection processed front-to-back (FIFO).",
        "example_ja": "Queue<Integer> q;",
        "example_en": "A line: first in, first out."
      },
      {
        "front": "Deque",
        "reading": "",
        "meaning": "A double-ended queue.",
        "example_ja": "Deque<Integer> d;",
        "example_en": "A line you can push/pop from both ends."
      },
      {
        "front": "ArrayDeque",
        "reading": "",
        "meaning": "A fast resizable-array Deque.",
        "example_ja": "new ArrayDeque<>()",
        "example_en": "The go-to stack/queue — beats Stack and LinkedList."
      },
      {
        "front": "Stack",
        "reading": "",
        "meaning": "A LIFO structure (legacy class).",
        "example_ja": "new ArrayDeque<>()",
        "example_en": "Last in, first out — but prefer ArrayDeque."
      },
      {
        "front": "PriorityQueue",
        "reading": "",
        "meaning": "A queue ordered by priority (a heap).",
        "example_ja": "new PriorityQueue<>()",
        "example_en": "Always hands you the smallest (or largest) next."
      },
      {
        "front": "Iterator",
        "reading": "",
        "meaning": "An object to walk a collection one item at a time.",
        "example_ja": "Iterator<T> it = c.iterator();",
        "example_en": "A cursor that says 'next?' and 'got more?'."
      },
      {
        "front": "Iterable",
        "reading": "",
        "meaning": "Anything a for-each loop can traverse.",
        "example_ja": "for (T x : list) { }",
        "example_en": "The 'you can loop over me' contract."
      },
      {
        "front": "Comparable",
        "reading": "",
        "meaning": "A type's natural ordering via compareTo.",
        "example_ja": "class N implements Comparable<N> { }",
        "example_en": "'Here's how to sort me by default.'"
      },
      {
        "front": "Comparator",
        "reading": "",
        "meaning": "An external ordering strategy.",
        "example_ja": "Comparator.comparingInt(x -> x)",
        "example_en": "A custom 'sort by this instead' rule."
      },
      {
        "front": "Collections",
        "reading": "",
        "meaning": "Static helpers for collections (sort, etc.).",
        "example_ja": "Collections.sort(list);",
        "example_en": "The utility belt for lists and sets."
      },
      {
        "front": "Arrays",
        "reading": "",
        "meaning": "Static helpers for arrays.",
        "example_ja": "Arrays.sort(arr);",
        "example_en": "The utility belt for raw arrays."
      },
      {
        "front": "entrySet",
        "reading": "",
        "meaning": "A Map's set of key-value pairs.",
        "example_ja": "for (var e : m.entrySet()) { }",
        "example_en": "The way to loop over both keys and values."
      },
      {
        "front": "keySet / values",
        "reading": "",
        "meaning": "A Map's keys / values as collections.",
        "example_ja": "m.keySet()",
        "example_en": "Just the keys, or just the values."
      },
      {
        "front": "load factor",
        "reading": "",
        "meaning": "HashMap's fill ratio before it resizes.",
        "example_ja": "new HashMap<>(16, 0.75f)",
        "example_en": "How full the table gets before it grows (0.75)."
      },
      {
        "front": "capacity",
        "reading": "",
        "meaning": "The number of buckets a hash table has.",
        "example_ja": "new HashMap<>(64)",
        "example_en": "How many slots the map allocated under the hood."
      },
      {
        "front": "fail-fast",
        "reading": "",
        "meaning": "Iterators that throw on concurrent modification.",
        "example_ja": "ConcurrentModificationException",
        "example_en": "Change a list mid-loop and it slaps your hand."
      }
    ],
    "quiz": [
      {
        "question": "Which structure gives average O(1) lookup by key with NO ordering?",
        "options": [
          "HashMap",
          "TreeMap",
          "LinkedList",
          "ArrayList"
        ],
        "answer": 0,
        "explanation": "HashMap hashes keys to buckets for O(1) average get/put but makes no ordering promise."
      },
      {
        "question": "You need elements kept in sorted order automatically. Use…",
        "options": [
          "TreeSet",
          "HashSet",
          "ArrayList",
          "ArrayDeque"
        ],
        "answer": 0,
        "explanation": "TreeSet (a red-black tree) keeps elements sorted; HashSet is unordered."
      },
      {
        "question": "The modern recommended class for a stack is…",
        "options": [
          "ArrayDeque",
          "Stack",
          "LinkedList",
          "PriorityQueue"
        ],
        "answer": 0,
        "explanation": "`ArrayDeque` is faster and cleaner than the legacy synchronized `Stack` class."
      },
      {
        "question": "A PriorityQueue always removes…",
        "options": [
          "the highest-priority (e.g. smallest) element",
          "the most recently added",
          "the oldest added",
          "a random element"
        ],
        "answer": 0,
        "explanation": "A PriorityQueue is a heap: `poll()` returns the head according to the comparator (smallest by default)."
      },
      {
        "question": "`ConcurrentModificationException` typically means…",
        "options": [
          "a collection was structurally modified during iteration",
          "two threads deadlocked",
          "the map ran out of capacity",
          "a null key was used"
        ],
        "answer": 0,
        "explanation": "Fail-fast iterators throw it when the backing collection is modified while you're looping over it."
      },
      {
        "question": "To iterate keys AND values of a Map efficiently, loop over…",
        "options": [
          "entrySet()",
          "keySet() then get()",
          "values()",
          "the map directly"
        ],
        "answer": 0,
        "explanation": "`entrySet()` gives both key and value per entry, avoiding a second lookup per key."
      }
    ],
    "practice": []
  },
  {
    "key": "jv_generics",
    "name": "Generics & Type System",
    "category": "JV: Generics",
    "what": "Type parameters, wildcards, and the erasure that powers them.",
    "deep": "Generics let one class or method work over many types with compile-time safety. The catch is type erasure — generics vanish at runtime — which explains most of the weird rules and error messages.",
    "java": "Every row gives you **two** explanations: the **textbook definition** (what you'd write on an exam) and a **plain-English** line (what it actually means when you're staring at code). Skim the glossary, then hit **🎴 Study cards** to drill them with spaced repetition, and **❓ Check yourself** at the bottom.",
    "language": "java_vocab",
    "lesson": "| Term | Textbook definition | In plain English | Example |\n|---|---|---|---|\n| `generic type` | A class/interface parameterized by a type. | A container that works for any type you plug in. | `class Box<T> { }` |\n| `type parameter` | The placeholder like <T> in a declaration. | The blank you fill with a real type later. | `class Box<T> { T val; }` |\n| `type argument` | The concrete type you supply. | The real type you put in the blank. | `Box<String> b;` |\n| `bounded type` | A parameter limited by extends. | 'Any type, as long as it's a Number.' | `class Box<T extends Number> { }` |\n| `wildcard` | An unknown type written as ?. | 'Some type, I don't care which.' | `List<?> anything;` |\n| `upper bound` | `? extends T` — T or a subtype. | Reading side: producers of T. | `List<? extends Number> nums;` |\n| `lower bound` | `? super T` — T or a supertype. | Writing side: consumers of T. | `List<? super Integer> sink;` |\n| `type erasure` | Generics removed at compile time. | At runtime a List<String> is just a List. | `List<String> -> List` |\n| `raw type` | A generic used without a type argument. | The old, unsafe pre-generics form. | `List l = new ArrayList();` |\n| `generic method` | A method with its own type parameter. | A method that's generic on its own, apart from the class. | `<T> T first(List<T> xs) { }` |\n| `PECS` | Producer-Extends, Consumer-Super rule. | Use extends to read, super to write. | `List<? extends T> src;` |\n| `diamond operator` | `<>` that infers type arguments. | Skip repeating the type on the right side. | `List<String> l = new ArrayList<>();` |\n| `unchecked warning` | A compiler warning about lost generic safety. | 'I can't prove this cast is safe' from erasure. | `@SuppressWarnings(\"unchecked\")` |\n| `recursive bound` | A type bounded by itself (F-bound). | The trick behind Comparable<T>. | `T extends Comparable<T>` |\n",
    "exercises": [],
    "cards": [
      {
        "front": "generic type",
        "reading": "",
        "meaning": "A class/interface parameterized by a type.",
        "example_ja": "class Box<T> { }",
        "example_en": "A container that works for any type you plug in."
      },
      {
        "front": "type parameter",
        "reading": "",
        "meaning": "The placeholder like <T> in a declaration.",
        "example_ja": "class Box<T> { T val; }",
        "example_en": "The blank you fill with a real type later."
      },
      {
        "front": "type argument",
        "reading": "",
        "meaning": "The concrete type you supply.",
        "example_ja": "Box<String> b;",
        "example_en": "The real type you put in the blank."
      },
      {
        "front": "bounded type",
        "reading": "",
        "meaning": "A parameter limited by extends.",
        "example_ja": "class Box<T extends Number> { }",
        "example_en": "'Any type, as long as it's a Number.'"
      },
      {
        "front": "wildcard",
        "reading": "",
        "meaning": "An unknown type written as ?.",
        "example_ja": "List<?> anything;",
        "example_en": "'Some type, I don't care which.'"
      },
      {
        "front": "upper bound",
        "reading": "",
        "meaning": "`? extends T` — T or a subtype.",
        "example_ja": "List<? extends Number> nums;",
        "example_en": "Reading side: producers of T."
      },
      {
        "front": "lower bound",
        "reading": "",
        "meaning": "`? super T` — T or a supertype.",
        "example_ja": "List<? super Integer> sink;",
        "example_en": "Writing side: consumers of T."
      },
      {
        "front": "type erasure",
        "reading": "",
        "meaning": "Generics removed at compile time.",
        "example_ja": "List<String> -> List",
        "example_en": "At runtime a List<String> is just a List."
      },
      {
        "front": "raw type",
        "reading": "",
        "meaning": "A generic used without a type argument.",
        "example_ja": "List l = new ArrayList();",
        "example_en": "The old, unsafe pre-generics form."
      },
      {
        "front": "generic method",
        "reading": "",
        "meaning": "A method with its own type parameter.",
        "example_ja": "<T> T first(List<T> xs) { }",
        "example_en": "A method that's generic on its own, apart from the class."
      },
      {
        "front": "PECS",
        "reading": "",
        "meaning": "Producer-Extends, Consumer-Super rule.",
        "example_ja": "List<? extends T> src;",
        "example_en": "Use extends to read, super to write."
      },
      {
        "front": "diamond operator",
        "reading": "",
        "meaning": "`<>` that infers type arguments.",
        "example_ja": "List<String> l = new ArrayList<>();",
        "example_en": "Skip repeating the type on the right side."
      },
      {
        "front": "unchecked warning",
        "reading": "",
        "meaning": "A compiler warning about lost generic safety.",
        "example_ja": "@SuppressWarnings(\"unchecked\")",
        "example_en": "'I can't prove this cast is safe' from erasure."
      },
      {
        "front": "recursive bound",
        "reading": "",
        "meaning": "A type bounded by itself (F-bound).",
        "example_ja": "T extends Comparable<T>",
        "example_en": "The trick behind Comparable<T>."
      }
    ],
    "quiz": [
      {
        "question": "What happens to generic type info at runtime?",
        "options": [
          "It's erased",
          "It's fully preserved via reflection",
          "It's stored in each object",
          "It's turned into Object always"
        ],
        "answer": 0,
        "explanation": "Type erasure removes generic parameters at compile time, so `List<String>` and `List<Integer>` share one runtime class."
      },
      {
        "question": "PECS says: use `? extends T` when you want to…",
        "options": [
          "read (produce) values out",
          "write (consume) values in",
          "avoid casts",
          "compare values"
        ],
        "answer": 0,
        "explanation": "Producer-Extends: an `? extends T` source produces Ts you read; `? super T` consumes Ts you write."
      },
      {
        "question": "The diamond operator `<>` lets you…",
        "options": [
          "omit repeating the type arguments on the right",
          "create raw types",
          "cast safely",
          "bound a parameter"
        ],
        "answer": 0,
        "explanation": "`new ArrayList<>()` infers the type from the variable declaration, avoiding repetition."
      },
      {
        "question": "A `raw type` like `List l = new ArrayList();` is discouraged because…",
        "options": [
          "it loses compile-time type checking",
          "it won't compile",
          "it's slower at runtime",
          "it can't be iterated"
        ],
        "answer": 0,
        "explanation": "Raw types bypass generic checks, reintroducing the ClassCastExceptions generics were meant to prevent."
      },
      {
        "question": "`<T extends Number>` means T can be…",
        "options": [
          "Number or any subclass of it",
          "any type",
          "only Number",
          "any superclass of Number"
        ],
        "answer": 0,
        "explanation": "An upper bound restricts T to Number and its subtypes (Integer, Double, …)."
      }
    ],
    "practice": []
  },
  {
    "key": "jv_exceptions",
    "name": "Exceptions & Errors",
    "category": "JV: Exceptions",
    "what": "How Java signals, propagates, and handles things going wrong.",
    "deep": "Exceptions are Java's structured way to say 'something broke.' The key distinctions — checked vs unchecked, Exception vs Error, and the try/catch/finally flow — come up constantly in code review and interviews.",
    "java": "Every row gives you **two** explanations: the **textbook definition** (what you'd write on an exam) and a **plain-English** line (what it actually means when you're staring at code). Skim the glossary, then hit **🎴 Study cards** to drill them with spaced repetition, and **❓ Check yourself** at the bottom.",
    "language": "java_vocab",
    "lesson": "| Term | Textbook definition | In plain English | Example |\n|---|---|---|---|\n| `exception` | An object signaling an abnormal event. | A thrown 'something went wrong' package. | `throw new IOException();` |\n| `Throwable` | The root of all errors and exceptions. | The ancestor of everything you can throw. | `class Throwable { }` |\n| `Error` | A serious problem apps shouldn't catch. | The JVM is in trouble — don't try to recover. | `OutOfMemoryError` |\n| `checked exception` | Must be declared or caught (compile-time). | The compiler forces you to deal with it. | `throws IOException` |\n| `unchecked exception` | A RuntimeException; not enforced. | Bugs you don't have to declare — usually programmer error. | `NullPointerException` |\n| `RuntimeException` | The base of unchecked exceptions. | The 'your code has a bug' family. | `class RuntimeException { }` |\n| `try` | Wraps code that might throw. | The 'attempt this' block. | `try { risky(); }` |\n| `catch` | Handles a thrown exception. | The 'if it breaks, do this' block. | `catch (IOException e) { }` |\n| `finally` | Runs whether or not an exception occurred. | Cleanup that always happens. | `finally { file.close(); }` |\n| `throw` | Raises an exception now. | Fire the alarm right here. | `throw new IllegalArgumentException();` |\n| `throws` | Declares exceptions a method may raise. | A warning label on the method signature. | `void read() throws IOException` |\n| `stack trace` | The call chain at the point of failure. | The breadcrumb trail of who called what. | `e.printStackTrace();` |\n| `NullPointerException` | Dereferencing a null reference. | You used something that was empty. | `s.length() // s == null` |\n| `try-with-resources` | Auto-closes resources on exit. | Opens and guarantees closing for you. | `try (var f = open()) { }` |\n| `custom exception` | A user-defined Exception subclass. | Your own named error type. | `class BankError extends Exception { }` |\n| `exception chaining` | Wrapping one exception in another. | 'This failed because of that' with the cause kept. | `new X(\"msg\", cause)` |\n",
    "exercises": [],
    "cards": [
      {
        "front": "exception",
        "reading": "",
        "meaning": "An object signaling an abnormal event.",
        "example_ja": "throw new IOException();",
        "example_en": "A thrown 'something went wrong' package."
      },
      {
        "front": "Throwable",
        "reading": "",
        "meaning": "The root of all errors and exceptions.",
        "example_ja": "class Throwable { }",
        "example_en": "The ancestor of everything you can throw."
      },
      {
        "front": "Error",
        "reading": "",
        "meaning": "A serious problem apps shouldn't catch.",
        "example_ja": "OutOfMemoryError",
        "example_en": "The JVM is in trouble — don't try to recover."
      },
      {
        "front": "checked exception",
        "reading": "",
        "meaning": "Must be declared or caught (compile-time).",
        "example_ja": "throws IOException",
        "example_en": "The compiler forces you to deal with it."
      },
      {
        "front": "unchecked exception",
        "reading": "",
        "meaning": "A RuntimeException; not enforced.",
        "example_ja": "NullPointerException",
        "example_en": "Bugs you don't have to declare — usually programmer error."
      },
      {
        "front": "RuntimeException",
        "reading": "",
        "meaning": "The base of unchecked exceptions.",
        "example_ja": "class RuntimeException { }",
        "example_en": "The 'your code has a bug' family."
      },
      {
        "front": "try",
        "reading": "",
        "meaning": "Wraps code that might throw.",
        "example_ja": "try { risky(); }",
        "example_en": "The 'attempt this' block."
      },
      {
        "front": "catch",
        "reading": "",
        "meaning": "Handles a thrown exception.",
        "example_ja": "catch (IOException e) { }",
        "example_en": "The 'if it breaks, do this' block."
      },
      {
        "front": "finally",
        "reading": "",
        "meaning": "Runs whether or not an exception occurred.",
        "example_ja": "finally { file.close(); }",
        "example_en": "Cleanup that always happens."
      },
      {
        "front": "throw",
        "reading": "",
        "meaning": "Raises an exception now.",
        "example_ja": "throw new IllegalArgumentException();",
        "example_en": "Fire the alarm right here."
      },
      {
        "front": "throws",
        "reading": "",
        "meaning": "Declares exceptions a method may raise.",
        "example_ja": "void read() throws IOException",
        "example_en": "A warning label on the method signature."
      },
      {
        "front": "stack trace",
        "reading": "",
        "meaning": "The call chain at the point of failure.",
        "example_ja": "e.printStackTrace();",
        "example_en": "The breadcrumb trail of who called what."
      },
      {
        "front": "NullPointerException",
        "reading": "",
        "meaning": "Dereferencing a null reference.",
        "example_ja": "s.length() // s == null",
        "example_en": "You used something that was empty."
      },
      {
        "front": "try-with-resources",
        "reading": "",
        "meaning": "Auto-closes resources on exit.",
        "example_ja": "try (var f = open()) { }",
        "example_en": "Opens and guarantees closing for you."
      },
      {
        "front": "custom exception",
        "reading": "",
        "meaning": "A user-defined Exception subclass.",
        "example_ja": "class BankError extends Exception { }",
        "example_en": "Your own named error type."
      },
      {
        "front": "exception chaining",
        "reading": "",
        "meaning": "Wrapping one exception in another.",
        "example_ja": "new X(\"msg\", cause)",
        "example_en": "'This failed because of that' with the cause kept."
      }
    ],
    "quiz": [
      {
        "question": "A CHECKED exception is one that…",
        "options": [
          "must be declared with `throws` or caught",
          "extends RuntimeException",
          "the JVM throws only",
          "can never be caught"
        ],
        "answer": 0,
        "explanation": "Checked exceptions are enforced by the compiler; unchecked (RuntimeException) ones are not."
      },
      {
        "question": "The `finally` block runs…",
        "options": [
          "whether or not an exception was thrown",
          "only when an exception is thrown",
          "only when none is thrown",
          "only if you call it"
        ],
        "answer": 0,
        "explanation": "`finally` always executes (barring JVM exit), which is why it's used for cleanup."
      },
      {
        "question": "`NullPointerException` is a…",
        "options": [
          "unchecked (runtime) exception",
          "checked exception",
          "an Error",
          "compile error"
        ],
        "answer": 0,
        "explanation": "NPE extends RuntimeException, so it's unchecked — the compiler doesn't force you to handle it."
      },
      {
        "question": "You should generally NOT catch an `Error` (e.g. OutOfMemoryError) because…",
        "options": [
          "it signals a serious unrecoverable JVM problem",
          "it's a checked exception",
          "it can't be caught",
          "it's always a bug in the JDK"
        ],
        "answer": 0,
        "explanation": "Errors indicate conditions the application usually can't sensibly recover from."
      },
      {
        "question": "`try-with-resources` is preferred because it…",
        "options": [
          "automatically closes resources",
          "catches all exceptions",
          "retries on failure",
          "runs faster"
        ],
        "answer": 0,
        "explanation": "Any resource implementing AutoCloseable is closed automatically when the try block exits, even on exception."
      }
    ],
    "practice": []
  },
  {
    "key": "jv_concurrency",
    "name": "Concurrency & Threads",
    "category": "JV: Concurrency",
    "what": "Running code in parallel safely: threads, locks, and the memory model.",
    "deep": "Concurrency is where Java gets subtle. These terms — race conditions, synchronization, the happens-before relationship, thread pools — are the vocabulary you need to reason about (and survive interviews on) multi-threaded code.",
    "java": "Every row gives you **two** explanations: the **textbook definition** (what you'd write on an exam) and a **plain-English** line (what it actually means when you're staring at code). Skim the glossary, then hit **🎴 Study cards** to drill them with spaced repetition, and **❓ Check yourself** at the bottom.",
    "language": "java_vocab",
    "lesson": "| Term | Textbook definition | In plain English | Example |\n|---|---|---|---|\n| `thread` | An independent path of execution. | A worker running your code in parallel. | `new Thread(task).start();` |\n| `Runnable` | A task with a run() and no result. | A job you hand a thread to do. | `Runnable r = () -> work();` |\n| `Callable` | A task that returns a value or throws. | Like Runnable, but it hands something back. | `Callable<Integer> c = () -> 1;` |\n| `process vs thread` | Isolated program vs shared-memory worker. | Separate apartments vs roommates sharing a room. | `Thread t = new Thread();` |\n| `race condition` | Result depends on unlucky thread timing. | Two threads stomp on the same data. | `count++; // not atomic` |\n| `synchronized` | A lock allowing one thread at a time. | A one-at-a-time turnstile. | `synchronized (lock) { }` |\n| `lock` | An explicit mutual-exclusion object. | A manual key you acquire and release. | `lock.lock(); ... lock.unlock();` |\n| `deadlock` | Threads each waiting on the other's lock. | Two people each holding the door the other needs. | `// A holds L1 waits L2...` |\n| `atomic` | An operation that completes indivisibly. | All-or-nothing, no thread sees it half-done. | `new AtomicInteger();` |\n| `volatile` | Guarantees visibility of a field's writes. | Every thread sees the latest value. | `volatile boolean stop;` |\n| `happens-before` | The ordering rule making writes visible. | The guarantee that A's result is seen by B. | `// unlock -> lock` |\n| `thread pool` | A reusable set of worker threads. | A team you hand tasks to instead of hiring per task. | `Executors.newFixedThreadPool(4);` |\n| `ExecutorService` | Manages and schedules async tasks. | The manager that runs your tasks for you. | `es.submit(task);` |\n| `Future` | A handle to a not-yet-ready result. | An IOU for a value coming later. | `Future<Integer> f = es.submit(c);` |\n| `wait / notify` | Low-level thread coordination on a monitor. | 'Sleep until someone taps me.' | `obj.wait(); obj.notify();` |\n| `ConcurrentHashMap` | A thread-safe, scalable HashMap. | A map many threads can hit at once safely. | `new ConcurrentHashMap<>()` |\n| `thread-safe` | Correct when used by many threads. | Won't corrupt if several threads share it. | `// synchronized/immutable` |\n| `immutable` | State that can't change after creation. | Frozen data — inherently thread-safe. | `final int x;` |\n| `CompletableFuture` | A composable async result. | A Future you can chain .then steps onto. | `CompletableFuture.supplyAsync(f)` |\n| `daemon thread` | A background thread that won't block exit. | A helper the JVM won't wait for on shutdown. | `t.setDaemon(true);` |\n",
    "exercises": [],
    "cards": [
      {
        "front": "thread",
        "reading": "",
        "meaning": "An independent path of execution.",
        "example_ja": "new Thread(task).start();",
        "example_en": "A worker running your code in parallel."
      },
      {
        "front": "Runnable",
        "reading": "",
        "meaning": "A task with a run() and no result.",
        "example_ja": "Runnable r = () -> work();",
        "example_en": "A job you hand a thread to do."
      },
      {
        "front": "Callable",
        "reading": "",
        "meaning": "A task that returns a value or throws.",
        "example_ja": "Callable<Integer> c = () -> 1;",
        "example_en": "Like Runnable, but it hands something back."
      },
      {
        "front": "process vs thread",
        "reading": "",
        "meaning": "Isolated program vs shared-memory worker.",
        "example_ja": "Thread t = new Thread();",
        "example_en": "Separate apartments vs roommates sharing a room."
      },
      {
        "front": "race condition",
        "reading": "",
        "meaning": "Result depends on unlucky thread timing.",
        "example_ja": "count++; // not atomic",
        "example_en": "Two threads stomp on the same data."
      },
      {
        "front": "synchronized",
        "reading": "",
        "meaning": "A lock allowing one thread at a time.",
        "example_ja": "synchronized (lock) { }",
        "example_en": "A one-at-a-time turnstile."
      },
      {
        "front": "lock",
        "reading": "",
        "meaning": "An explicit mutual-exclusion object.",
        "example_ja": "lock.lock(); ... lock.unlock();",
        "example_en": "A manual key you acquire and release."
      },
      {
        "front": "deadlock",
        "reading": "",
        "meaning": "Threads each waiting on the other's lock.",
        "example_ja": "// A holds L1 waits L2...",
        "example_en": "Two people each holding the door the other needs."
      },
      {
        "front": "atomic",
        "reading": "",
        "meaning": "An operation that completes indivisibly.",
        "example_ja": "new AtomicInteger();",
        "example_en": "All-or-nothing, no thread sees it half-done."
      },
      {
        "front": "volatile",
        "reading": "",
        "meaning": "Guarantees visibility of a field's writes.",
        "example_ja": "volatile boolean stop;",
        "example_en": "Every thread sees the latest value."
      },
      {
        "front": "happens-before",
        "reading": "",
        "meaning": "The ordering rule making writes visible.",
        "example_ja": "// unlock -> lock",
        "example_en": "The guarantee that A's result is seen by B."
      },
      {
        "front": "thread pool",
        "reading": "",
        "meaning": "A reusable set of worker threads.",
        "example_ja": "Executors.newFixedThreadPool(4);",
        "example_en": "A team you hand tasks to instead of hiring per task."
      },
      {
        "front": "ExecutorService",
        "reading": "",
        "meaning": "Manages and schedules async tasks.",
        "example_ja": "es.submit(task);",
        "example_en": "The manager that runs your tasks for you."
      },
      {
        "front": "Future",
        "reading": "",
        "meaning": "A handle to a not-yet-ready result.",
        "example_ja": "Future<Integer> f = es.submit(c);",
        "example_en": "An IOU for a value coming later."
      },
      {
        "front": "wait / notify",
        "reading": "",
        "meaning": "Low-level thread coordination on a monitor.",
        "example_ja": "obj.wait(); obj.notify();",
        "example_en": "'Sleep until someone taps me.'"
      },
      {
        "front": "ConcurrentHashMap",
        "reading": "",
        "meaning": "A thread-safe, scalable HashMap.",
        "example_ja": "new ConcurrentHashMap<>()",
        "example_en": "A map many threads can hit at once safely."
      },
      {
        "front": "thread-safe",
        "reading": "",
        "meaning": "Correct when used by many threads.",
        "example_ja": "// synchronized/immutable",
        "example_en": "Won't corrupt if several threads share it."
      },
      {
        "front": "immutable",
        "reading": "",
        "meaning": "State that can't change after creation.",
        "example_ja": "final int x;",
        "example_en": "Frozen data — inherently thread-safe."
      },
      {
        "front": "CompletableFuture",
        "reading": "",
        "meaning": "A composable async result.",
        "example_ja": "CompletableFuture.supplyAsync(f)",
        "example_en": "A Future you can chain .then steps onto."
      },
      {
        "front": "daemon thread",
        "reading": "",
        "meaning": "A background thread that won't block exit.",
        "example_ja": "t.setDaemon(true);",
        "example_en": "A helper the JVM won't wait for on shutdown."
      }
    ],
    "quiz": [
      {
        "question": "A race condition happens when…",
        "options": [
          "the result depends on thread timing over shared data",
          "a thread runs too slowly",
          "two processes share a file",
          "a lock is never used"
        ],
        "answer": 0,
        "explanation": "Race conditions arise from unsynchronized access to shared mutable state, where interleaving changes the outcome."
      },
      {
        "question": "`volatile` guarantees ______ but NOT ______.",
        "options": [
          "visibility; atomicity of compound actions",
          "atomicity; visibility",
          "ordering; speed",
          "locking; unlocking"
        ],
        "answer": 0,
        "explanation": "volatile makes the latest value visible, but `count++` (read-modify-write) is still not atomic."
      },
      {
        "question": "Deadlock requires (among other conditions)…",
        "options": [
          "threads holding locks while waiting for others",
          "a single thread",
          "no locks at all",
          "a full thread pool"
        ],
        "answer": 0,
        "explanation": "Classic deadlock: each thread holds one lock and waits for a lock the other holds, forming a cycle."
      },
      {
        "question": "The safest way to share a counter across threads is…",
        "options": [
          "AtomicInteger",
          "a plain int",
          "a volatile int with count++",
          "a local variable"
        ],
        "answer": 0,
        "explanation": "AtomicInteger's incrementAndGet is a single atomic operation; a volatile int++ still races."
      },
      {
        "question": "A `Future` represents…",
        "options": [
          "a result that may not be ready yet",
          "a background thread",
          "a lock",
          "a completed task only"
        ],
        "answer": 0,
        "explanation": "A Future is a handle to an asynchronous computation's eventual result; `get()` blocks until it's ready."
      },
      {
        "question": "Why are immutable objects inherently thread-safe?",
        "options": [
          "their state can't change after construction",
          "they use synchronized",
          "they're always static",
          "the GC protects them"
        ],
        "answer": 0,
        "explanation": "If nothing can mutate the object, concurrent readers can never see an inconsistent state."
      }
    ],
    "practice": []
  },
  {
    "key": "jv_streams",
    "name": "Streams, Lambdas & Functional",
    "category": "JV: Functional",
    "what": "Java's functional style: lambdas, method references, and the Stream pipeline.",
    "deep": "Since Java 8 you can process data declaratively. Lambdas, functional interfaces, and streams let you say WHAT you want (filter, map, collect) instead of writing the loop — cleaner code and a favorite interview topic.",
    "java": "Every row gives you **two** explanations: the **textbook definition** (what you'd write on an exam) and a **plain-English** line (what it actually means when you're staring at code). Skim the glossary, then hit **🎴 Study cards** to drill them with spaced repetition, and **❓ Check yourself** at the bottom.",
    "language": "java_vocab",
    "lesson": "| Term | Textbook definition | In plain English | Example |\n|---|---|---|---|\n| `lambda` | A concise anonymous function. | A throwaway function with no name. | `x -> x * 2` |\n| `functional interface` | An interface with one abstract method. | A one-method contract a lambda can fill. | `@FunctionalInterface` |\n| `method reference` | A shorthand pointing at an existing method. | Reuse a method by name instead of a lambda. | `String::length` |\n| `Stream` | A pipeline over a sequence of elements. | A conveyor belt you attach operations to. | `list.stream()` |\n| `filter` | Keeps elements matching a predicate. | Throw away everything that fails the test. | `.filter(x -> x > 0)` |\n| `map` | Transforms each element. | Turn every item into something else. | `.map(x -> x * x)` |\n| `reduce` | Combines elements into one value. | Fold the whole stream into a single result. | `.reduce(0, Integer::sum)` |\n| `collect` | Gathers a stream into a collection. | Pour the results into a list/set/map. | `.collect(Collectors.toList())` |\n| `forEach` | Runs an action per element. | Do this for every item. | `.forEach(System.out::println)` |\n| `Optional` | A container that may or may not hold a value. | A box that says 'maybe there's something here'. | `Optional<String> o;` |\n| `Predicate` | A function returning a boolean. | A yes/no test. | `Predicate<Integer> p = x -> x > 0;` |\n| `Function` | A function from one type to another. | An input-to-output transformer. | `Function<Integer,Integer> f;` |\n| `Consumer` | A function that takes input, returns nothing. | Do-something-with-it, hand nothing back. | `Consumer<String> c = System.out::println;` |\n| `Supplier` | A function that produces a value. | Give-me-one-on-demand. | `Supplier<Double> s = Math::random;` |\n| `lazy evaluation` | Intermediate ops run only on terminal. | Nothing happens until you ask for the result. | `.filter(...).map(...).count()` |\n| `intermediate operation` | A stream op returning a new stream. | A step that sets up more steps. | `.map(f)` |\n| `terminal operation` | A stream op producing a result/side effect. | The step that actually runs the pipeline. | `.collect(...)` |\n| `Collectors` | Factory of reduction recipes for collect. | Prebuilt ways to gather stream results. | `Collectors.groupingBy(f)` |\n| `parallelStream` | A stream that splits work across cores. | The same pipeline, run on many threads. | `list.parallelStream()` |\n| `flatMap` | Maps then flattens nested streams. | Turn a list of lists into one flat stream. | `.flatMap(List::stream)` |\n",
    "exercises": [],
    "cards": [
      {
        "front": "lambda",
        "reading": "",
        "meaning": "A concise anonymous function.",
        "example_ja": "x -> x * 2",
        "example_en": "A throwaway function with no name."
      },
      {
        "front": "functional interface",
        "reading": "",
        "meaning": "An interface with one abstract method.",
        "example_ja": "@FunctionalInterface",
        "example_en": "A one-method contract a lambda can fill."
      },
      {
        "front": "method reference",
        "reading": "",
        "meaning": "A shorthand pointing at an existing method.",
        "example_ja": "String::length",
        "example_en": "Reuse a method by name instead of a lambda."
      },
      {
        "front": "Stream",
        "reading": "",
        "meaning": "A pipeline over a sequence of elements.",
        "example_ja": "list.stream()",
        "example_en": "A conveyor belt you attach operations to."
      },
      {
        "front": "filter",
        "reading": "",
        "meaning": "Keeps elements matching a predicate.",
        "example_ja": ".filter(x -> x > 0)",
        "example_en": "Throw away everything that fails the test."
      },
      {
        "front": "map",
        "reading": "",
        "meaning": "Transforms each element.",
        "example_ja": ".map(x -> x * x)",
        "example_en": "Turn every item into something else."
      },
      {
        "front": "reduce",
        "reading": "",
        "meaning": "Combines elements into one value.",
        "example_ja": ".reduce(0, Integer::sum)",
        "example_en": "Fold the whole stream into a single result."
      },
      {
        "front": "collect",
        "reading": "",
        "meaning": "Gathers a stream into a collection.",
        "example_ja": ".collect(Collectors.toList())",
        "example_en": "Pour the results into a list/set/map."
      },
      {
        "front": "forEach",
        "reading": "",
        "meaning": "Runs an action per element.",
        "example_ja": ".forEach(System.out::println)",
        "example_en": "Do this for every item."
      },
      {
        "front": "Optional",
        "reading": "",
        "meaning": "A container that may or may not hold a value.",
        "example_ja": "Optional<String> o;",
        "example_en": "A box that says 'maybe there's something here'."
      },
      {
        "front": "Predicate",
        "reading": "",
        "meaning": "A function returning a boolean.",
        "example_ja": "Predicate<Integer> p = x -> x > 0;",
        "example_en": "A yes/no test."
      },
      {
        "front": "Function",
        "reading": "",
        "meaning": "A function from one type to another.",
        "example_ja": "Function<Integer,Integer> f;",
        "example_en": "An input-to-output transformer."
      },
      {
        "front": "Consumer",
        "reading": "",
        "meaning": "A function that takes input, returns nothing.",
        "example_ja": "Consumer<String> c = System.out::println;",
        "example_en": "Do-something-with-it, hand nothing back."
      },
      {
        "front": "Supplier",
        "reading": "",
        "meaning": "A function that produces a value.",
        "example_ja": "Supplier<Double> s = Math::random;",
        "example_en": "Give-me-one-on-demand."
      },
      {
        "front": "lazy evaluation",
        "reading": "",
        "meaning": "Intermediate ops run only on terminal.",
        "example_ja": ".filter(...).map(...).count()",
        "example_en": "Nothing happens until you ask for the result."
      },
      {
        "front": "intermediate operation",
        "reading": "",
        "meaning": "A stream op returning a new stream.",
        "example_ja": ".map(f)",
        "example_en": "A step that sets up more steps."
      },
      {
        "front": "terminal operation",
        "reading": "",
        "meaning": "A stream op producing a result/side effect.",
        "example_ja": ".collect(...)",
        "example_en": "The step that actually runs the pipeline."
      },
      {
        "front": "Collectors",
        "reading": "",
        "meaning": "Factory of reduction recipes for collect.",
        "example_ja": "Collectors.groupingBy(f)",
        "example_en": "Prebuilt ways to gather stream results."
      },
      {
        "front": "parallelStream",
        "reading": "",
        "meaning": "A stream that splits work across cores.",
        "example_ja": "list.parallelStream()",
        "example_en": "The same pipeline, run on many threads."
      },
      {
        "front": "flatMap",
        "reading": "",
        "meaning": "Maps then flattens nested streams.",
        "example_ja": ".flatMap(List::stream)",
        "example_en": "Turn a list of lists into one flat stream."
      }
    ],
    "quiz": [
      {
        "question": "A lambda in Java can be assigned to…",
        "options": [
          "a functional interface (one abstract method)",
          "any interface",
          "any class",
          "only Runnable"
        ],
        "answer": 0,
        "explanation": "Lambdas target functional interfaces — interfaces with exactly one abstract method (SAM)."
      },
      {
        "question": "Which is a TERMINAL stream operation?",
        "options": [
          "collect",
          "filter",
          "map",
          "sorted"
        ],
        "answer": 0,
        "explanation": "`collect` produces a result and triggers the pipeline; filter/map/sorted are intermediate and lazy."
      },
      {
        "question": "Streams are LAZY, meaning…",
        "options": [
          "intermediate ops don't run until a terminal op",
          "they run in the background",
          "they cache results",
          "they're slower"
        ],
        "answer": 0,
        "explanation": "Nothing is computed until a terminal operation pulls elements through the pipeline."
      },
      {
        "question": "`Optional` exists mainly to…",
        "options": [
          "represent a possibly-absent value explicitly",
          "make code faster",
          "replace all nulls at runtime",
          "hold multiple values"
        ],
        "answer": 0,
        "explanation": "Optional forces callers to consider the empty case instead of silently hitting a NullPointerException."
      },
      {
        "question": "`String::length` is an example of a…",
        "options": [
          "method reference",
          "lambda with parameters",
          "constructor",
          "field access"
        ],
        "answer": 0,
        "explanation": "It's a method reference — shorthand for `s -> s.length()`."
      },
      {
        "question": "`flatMap` differs from `map` because it…",
        "options": [
          "flattens nested streams into one",
          "filters elements",
          "sorts elements",
          "runs in parallel"
        ],
        "answer": 0,
        "explanation": "map produces a stream of streams for nested data; flatMap merges them into a single stream."
      }
    ],
    "practice": []
  },
  {
    "key": "jv_jvm",
    "name": "The JVM & Memory",
    "category": "JV: JVM & Memory",
    "what": "How your code compiles, loads, runs, and gets garbage-collected.",
    "deep": "'Java' is a language AND a virtual machine. Understanding bytecode, the heap/stack split, class loading, and garbage collection explains performance, 'why is it null', and the difference between the JDK, JRE, and JVM.",
    "java": "Every row gives you **two** explanations: the **textbook definition** (what you'd write on an exam) and a **plain-English** line (what it actually means when you're staring at code). Skim the glossary, then hit **🎴 Study cards** to drill them with spaced repetition, and **❓ Check yourself** at the bottom.",
    "language": "java_vocab",
    "lesson": "| Term | Textbook definition | In plain English | Example |\n|---|---|---|---|\n| `JVM` | The virtual machine that runs bytecode. | The engine that actually runs Java. | `java Main` |\n| `JRE` | JVM plus core libraries to run apps. | Everything needed to RUN Java (not build it). | `// runtime only` |\n| `JDK` | JRE plus compiler and dev tools. | Everything to BUILD and run Java. | `javac Main.java` |\n| `bytecode` | The .class instructions the JVM executes. | Half-compiled code the JVM understands. | `Main.class` |\n| `javac` | The Java source-to-bytecode compiler. | Turns your .java into .class. | `javac Main.java` |\n| `JIT compiler` | Compiles hot bytecode to native code. | Speeds up code that runs a lot. | `// HotSpot JIT` |\n| `class loader` | Loads .class files into the JVM. | The part that finds and reads your classes. | `getClass().getClassLoader()` |\n| `heap` | Memory where objects live. | The big shared pool for `new` objects. | `new Object()` |\n| `stack` | Per-thread memory for calls and locals. | Each thread's scratchpad of method frames. | `int local = 1;` |\n| `stack frame` | One method call's slice of the stack. | A method's private workspace while it runs. | `// pushed per call` |\n| `garbage collection` | Automatic reclaiming of unreachable objects. | Java cleans up memory you stopped using. | `System.gc(); // hint` |\n| `reachability` | Whether an object is still referenced. | Can anything still find this object? | `obj = null; // now unreachable` |\n| `memory leak` | Holding references you no longer need. | Java can't free it because you still point at it. | `cache.put(k, v); // never removed` |\n| `StackOverflowError` | The call stack grew too deep. | Runaway recursion filled the stack. | `void f() { f(); }` |\n| `OutOfMemoryError` | The heap couldn't fit more objects. | You ran out of room for new objects. | `new int[Integer.MAX_VALUE];` |\n| `generational GC` | GC split into young and old regions. | Most objects die young, so scan those first. | `// young/old gen` |\n| `metaspace` | Native memory holding class metadata. | Where class definitions live (post-PermGen). | `-XX:MaxMetaspaceSize` |\n| `just-in-time (JIT)` | Runtime native compilation of bytecode. | Compile-while-running for speed. | `// warm-up then fast` |\n| `classpath` | Where the JVM looks for classes. | The list of folders/jars Java searches. | `java -cp lib.jar Main` |\n| `finalize (deprecated)` | An old pre-GC callback, now removed. | A cleanup hook you should never rely on. | `// use try-with-resources` |\n",
    "exercises": [],
    "cards": [
      {
        "front": "JVM",
        "reading": "",
        "meaning": "The virtual machine that runs bytecode.",
        "example_ja": "java Main",
        "example_en": "The engine that actually runs Java."
      },
      {
        "front": "JRE",
        "reading": "",
        "meaning": "JVM plus core libraries to run apps.",
        "example_ja": "// runtime only",
        "example_en": "Everything needed to RUN Java (not build it)."
      },
      {
        "front": "JDK",
        "reading": "",
        "meaning": "JRE plus compiler and dev tools.",
        "example_ja": "javac Main.java",
        "example_en": "Everything to BUILD and run Java."
      },
      {
        "front": "bytecode",
        "reading": "",
        "meaning": "The .class instructions the JVM executes.",
        "example_ja": "Main.class",
        "example_en": "Half-compiled code the JVM understands."
      },
      {
        "front": "javac",
        "reading": "",
        "meaning": "The Java source-to-bytecode compiler.",
        "example_ja": "javac Main.java",
        "example_en": "Turns your .java into .class."
      },
      {
        "front": "JIT compiler",
        "reading": "",
        "meaning": "Compiles hot bytecode to native code.",
        "example_ja": "// HotSpot JIT",
        "example_en": "Speeds up code that runs a lot."
      },
      {
        "front": "class loader",
        "reading": "",
        "meaning": "Loads .class files into the JVM.",
        "example_ja": "getClass().getClassLoader()",
        "example_en": "The part that finds and reads your classes."
      },
      {
        "front": "heap",
        "reading": "",
        "meaning": "Memory where objects live.",
        "example_ja": "new Object()",
        "example_en": "The big shared pool for `new` objects."
      },
      {
        "front": "stack",
        "reading": "",
        "meaning": "Per-thread memory for calls and locals.",
        "example_ja": "int local = 1;",
        "example_en": "Each thread's scratchpad of method frames."
      },
      {
        "front": "stack frame",
        "reading": "",
        "meaning": "One method call's slice of the stack.",
        "example_ja": "// pushed per call",
        "example_en": "A method's private workspace while it runs."
      },
      {
        "front": "garbage collection",
        "reading": "",
        "meaning": "Automatic reclaiming of unreachable objects.",
        "example_ja": "System.gc(); // hint",
        "example_en": "Java cleans up memory you stopped using."
      },
      {
        "front": "reachability",
        "reading": "",
        "meaning": "Whether an object is still referenced.",
        "example_ja": "obj = null; // now unreachable",
        "example_en": "Can anything still find this object?"
      },
      {
        "front": "memory leak",
        "reading": "",
        "meaning": "Holding references you no longer need.",
        "example_ja": "cache.put(k, v); // never removed",
        "example_en": "Java can't free it because you still point at it."
      },
      {
        "front": "StackOverflowError",
        "reading": "",
        "meaning": "The call stack grew too deep.",
        "example_ja": "void f() { f(); }",
        "example_en": "Runaway recursion filled the stack."
      },
      {
        "front": "OutOfMemoryError",
        "reading": "",
        "meaning": "The heap couldn't fit more objects.",
        "example_ja": "new int[Integer.MAX_VALUE];",
        "example_en": "You ran out of room for new objects."
      },
      {
        "front": "generational GC",
        "reading": "",
        "meaning": "GC split into young and old regions.",
        "example_ja": "// young/old gen",
        "example_en": "Most objects die young, so scan those first."
      },
      {
        "front": "metaspace",
        "reading": "",
        "meaning": "Native memory holding class metadata.",
        "example_ja": "-XX:MaxMetaspaceSize",
        "example_en": "Where class definitions live (post-PermGen)."
      },
      {
        "front": "just-in-time (JIT)",
        "reading": "",
        "meaning": "Runtime native compilation of bytecode.",
        "example_ja": "// warm-up then fast",
        "example_en": "Compile-while-running for speed."
      },
      {
        "front": "classpath",
        "reading": "",
        "meaning": "Where the JVM looks for classes.",
        "example_ja": "java -cp lib.jar Main",
        "example_en": "The list of folders/jars Java searches."
      },
      {
        "front": "finalize (deprecated)",
        "reading": "",
        "meaning": "An old pre-GC callback, now removed.",
        "example_ja": "// use try-with-resources",
        "example_en": "A cleanup hook you should never rely on."
      }
    ],
    "quiz": [
      {
        "question": "Objects created with `new` live on the…",
        "options": [
          "heap",
          "stack",
          "classpath",
          "metaspace"
        ],
        "answer": 0,
        "explanation": "Objects go on the shared heap; local variables and call frames live on each thread's stack."
      },
      {
        "question": "The difference between the JDK and the JRE is that the JDK also includes…",
        "options": [
          "the compiler and dev tools",
          "the JVM",
          "the heap",
          "garbage collection"
        ],
        "answer": 0,
        "explanation": "JRE = run Java; JDK = JRE + `javac` and tools to build it."
      },
      {
        "question": "Deep infinite recursion typically causes…",
        "options": [
          "StackOverflowError",
          "OutOfMemoryError",
          "NullPointerException",
          "a deadlock"
        ],
        "answer": 0,
        "explanation": "Each call adds a stack frame; too many exhausts the thread's stack → StackOverflowError."
      },
      {
        "question": "A memory leak in Java usually means…",
        "options": [
          "you still hold references to objects you don't need",
          "the GC is disabled",
          "the heap is too small",
          "you forgot to call free()"
        ],
        "answer": 0,
        "explanation": "The GC only frees unreachable objects; lingering references (e.g. in a growing cache) keep them alive."
      },
      {
        "question": "Bytecode (`.class`) is…",
        "options": [
          "portable instructions the JVM executes",
          "native machine code",
          "the Java source",
          "a compressed jar"
        ],
        "answer": 0,
        "explanation": "`javac` produces platform-independent bytecode; the JVM (optionally JIT-compiling) runs it."
      },
      {
        "question": "Generational GC is based on the observation that…",
        "options": [
          "most objects die young",
          "old objects are largest",
          "the stack fills first",
          "threads share the heap"
        ],
        "answer": 0,
        "explanation": "The weak generational hypothesis: most objects become garbage quickly, so the young generation is collected often and cheaply."
      }
    ],
    "practice": []
  },
  {
    "key": "jv_strings",
    "name": "Strings & Text",
    "category": "JV: Strings",
    "what": "How Java represents text, and why strings are immutable.",
    "deep": "Strings feel simple but hide sharp edges: immutability, the string pool, `==` vs `equals`, and when to reach for StringBuilder. These come up in almost every interview and every performance review.",
    "java": "Every row gives you **two** explanations: the **textbook definition** (what you'd write on an exam) and a **plain-English** line (what it actually means when you're staring at code). Skim the glossary, then hit **🎴 Study cards** to drill them with spaced repetition, and **❓ Check yourself** at the bottom.",
    "language": "java_vocab",
    "lesson": "| Term | Textbook definition | In plain English | Example |\n|---|---|---|---|\n| `String` | An immutable sequence of characters. | Text you can read but never change in place. | `String s = \"hi\";` |\n| `immutable` | Cannot be changed after creation. | Every 'edit' actually makes a new string. | `s = s + \"!\";` |\n| `string pool` | A cache of interned string literals. | Java reuses identical literals to save memory. | `\"a\" == \"a\" // true` |\n| `intern` | Puts a string into the shared pool. | Force this string to be the shared copy. | `s.intern()` |\n| `StringBuilder` | A mutable, efficient string buffer. | The right way to build strings in a loop. | `new StringBuilder()` |\n| `StringBuffer` | A thread-safe StringBuilder. | Same, but synchronized (rarely needed). | `new StringBuffer()` |\n| `== vs equals` | Reference identity vs content equality. | Same object vs same text. | `s.equals(t)` |\n| `charAt` | Returns the char at an index. | Grab one character by position. | `s.charAt(0)` |\n| `substring` | Extracts part of a string. | Snip out a slice of text. | `s.substring(1, 3)` |\n| `split` | Breaks a string on a regex. | Chop text into pieces. | `s.split(\",\")` |\n| `format` | Builds a string from a template. | printf-style string assembly. | `String.format(\"%d\", n)` |\n| `char` | A single 16-bit code unit. | One character, stored as a number. | `'A'` |\n| `Unicode / UTF-16` | Java's internal character encoding. | Each char is a 16-bit Unicode unit. | `\"\\u0041\"` |\n| `text block` | A multi-line string literal (\"\"\"). | Paste multi-line text without \\n soup. | `String j = \"\"\"...\"\"\";` |\n",
    "exercises": [],
    "cards": [
      {
        "front": "String",
        "reading": "",
        "meaning": "An immutable sequence of characters.",
        "example_ja": "String s = \"hi\";",
        "example_en": "Text you can read but never change in place."
      },
      {
        "front": "immutable",
        "reading": "",
        "meaning": "Cannot be changed after creation.",
        "example_ja": "s = s + \"!\";",
        "example_en": "Every 'edit' actually makes a new string."
      },
      {
        "front": "string pool",
        "reading": "",
        "meaning": "A cache of interned string literals.",
        "example_ja": "\"a\" == \"a\" // true",
        "example_en": "Java reuses identical literals to save memory."
      },
      {
        "front": "intern",
        "reading": "",
        "meaning": "Puts a string into the shared pool.",
        "example_ja": "s.intern()",
        "example_en": "Force this string to be the shared copy."
      },
      {
        "front": "StringBuilder",
        "reading": "",
        "meaning": "A mutable, efficient string buffer.",
        "example_ja": "new StringBuilder()",
        "example_en": "The right way to build strings in a loop."
      },
      {
        "front": "StringBuffer",
        "reading": "",
        "meaning": "A thread-safe StringBuilder.",
        "example_ja": "new StringBuffer()",
        "example_en": "Same, but synchronized (rarely needed)."
      },
      {
        "front": "== vs equals",
        "reading": "",
        "meaning": "Reference identity vs content equality.",
        "example_ja": "s.equals(t)",
        "example_en": "Same object vs same text."
      },
      {
        "front": "charAt",
        "reading": "",
        "meaning": "Returns the char at an index.",
        "example_ja": "s.charAt(0)",
        "example_en": "Grab one character by position."
      },
      {
        "front": "substring",
        "reading": "",
        "meaning": "Extracts part of a string.",
        "example_ja": "s.substring(1, 3)",
        "example_en": "Snip out a slice of text."
      },
      {
        "front": "split",
        "reading": "",
        "meaning": "Breaks a string on a regex.",
        "example_ja": "s.split(\",\")",
        "example_en": "Chop text into pieces."
      },
      {
        "front": "format",
        "reading": "",
        "meaning": "Builds a string from a template.",
        "example_ja": "String.format(\"%d\", n)",
        "example_en": "printf-style string assembly."
      },
      {
        "front": "char",
        "reading": "",
        "meaning": "A single 16-bit code unit.",
        "example_ja": "'A'",
        "example_en": "One character, stored as a number."
      },
      {
        "front": "Unicode / UTF-16",
        "reading": "",
        "meaning": "Java's internal character encoding.",
        "example_ja": "\"\\u0041\"",
        "example_en": "Each char is a 16-bit Unicode unit."
      },
      {
        "front": "text block",
        "reading": "",
        "meaning": "A multi-line string literal (\"\"\").",
        "example_ja": "String j = \"\"\"...\"\"\";",
        "example_en": "Paste multi-line text without \\n soup."
      }
    ],
    "quiz": [
      {
        "question": "Java strings are immutable, so `s = s + \"x\"`…",
        "options": [
          "creates a new String object",
          "modifies s in place",
          "throws an exception",
          "does nothing"
        ],
        "answer": 0,
        "explanation": "Concatenation builds a brand-new String; the original is unchanged (which is why loops should use StringBuilder)."
      },
      {
        "question": "To compare the CONTENTS of two strings you should use…",
        "options": [
          "equals()",
          "==",
          "compareTo() only",
          "hashCode()"
        ],
        "answer": 0,
        "explanation": "`==` compares references; `.equals()` compares character content."
      },
      {
        "question": "Building a string in a tight loop should use…",
        "options": [
          "StringBuilder",
          "String + in the loop",
          "StringBuffer always",
          "char[] only"
        ],
        "answer": 0,
        "explanation": "Repeated `+` creates many throwaway Strings; StringBuilder mutates one buffer."
      },
      {
        "question": "The string pool means two identical string LITERALS…",
        "options": [
          "may reference the same object",
          "are always different objects",
          "are mutable",
          "are garbage collected immediately"
        ],
        "answer": 0,
        "explanation": "Interned literals are cached, so `\"a\" == \"a\"` is often true — but never rely on `==` for content."
      },
      {
        "question": "`StringBuffer` differs from `StringBuilder` in that it is…",
        "options": [
          "thread-safe (synchronized)",
          "immutable",
          "faster",
          "for chars only"
        ],
        "answer": 0,
        "explanation": "StringBuffer's methods are synchronized; StringBuilder isn't, making it faster for single-threaded use."
      }
    ],
    "practice": []
  },
  {
    "key": "jv_io",
    "name": "I/O, Files & Networking",
    "category": "JV: I/O",
    "what": "Reading and writing data: streams, readers, buffers, and files.",
    "deep": "Java's I/O splits into byte streams and character streams, with buffering for speed and the newer NIO/Files API for convenience. Knowing the layers keeps you from re-reading files byte by byte.",
    "java": "Every row gives you **two** explanations: the **textbook definition** (what you'd write on an exam) and a **plain-English** line (what it actually means when you're staring at code). Skim the glossary, then hit **🎴 Study cards** to drill them with spaced repetition, and **❓ Check yourself** at the bottom.",
    "language": "java_vocab",
    "lesson": "| Term | Textbook definition | In plain English | Example |\n|---|---|---|---|\n| `stream (I/O)` | A flow of bytes to or from a source. | A pipe data travels through. | `InputStream in;` |\n| `InputStream` | A source of raw bytes. | Something you read bytes out of. | `FileInputStream f;` |\n| `OutputStream` | A sink for raw bytes. | Something you write bytes into. | `FileOutputStream f;` |\n| `Reader / Writer` | Character-oriented I/O. | Streams that speak text, not raw bytes. | `FileReader r;` |\n| `BufferedReader` | A reader that buffers for efficiency. | Reads in chunks so you're not hitting disk per char. | `new BufferedReader(r)` |\n| `Scanner` | Parses primitive tokens from input. | The beginner-friendly input reader. | `new Scanner(System.in)` |\n| `System.in` | Standard input stream. | Where typed/piped input comes from. | `new Scanner(System.in)` |\n| `File` | An abstract path to a file or dir. | A handle to a location on disk. | `new File(\"a.txt\")` |\n| `Path / Files (NIO)` | Modern file API in java.nio. | The cleaner, newer way to touch files. | `Files.readAllLines(path)` |\n| `serialization` | Turning an object into bytes. | Freeze an object so you can save/send it. | `implements Serializable` |\n| `deserialization` | Rebuilding an object from bytes. | Thaw the bytes back into an object. | `ois.readObject()` |\n| `buffer` | An in-memory staging area for data. | A holding tank so I/O happens in bulk. | `byte[] buf = new byte[8192];` |\n| `flush` | Forces buffered data out. | Push whatever's waiting out now. | `writer.flush();` |\n| `Socket` | An endpoint of a network connection. | A phone line between two programs. | `new Socket(host, port)` |\n",
    "exercises": [],
    "cards": [
      {
        "front": "stream (I/O)",
        "reading": "",
        "meaning": "A flow of bytes to or from a source.",
        "example_ja": "InputStream in;",
        "example_en": "A pipe data travels through."
      },
      {
        "front": "InputStream",
        "reading": "",
        "meaning": "A source of raw bytes.",
        "example_ja": "FileInputStream f;",
        "example_en": "Something you read bytes out of."
      },
      {
        "front": "OutputStream",
        "reading": "",
        "meaning": "A sink for raw bytes.",
        "example_ja": "FileOutputStream f;",
        "example_en": "Something you write bytes into."
      },
      {
        "front": "Reader / Writer",
        "reading": "",
        "meaning": "Character-oriented I/O.",
        "example_ja": "FileReader r;",
        "example_en": "Streams that speak text, not raw bytes."
      },
      {
        "front": "BufferedReader",
        "reading": "",
        "meaning": "A reader that buffers for efficiency.",
        "example_ja": "new BufferedReader(r)",
        "example_en": "Reads in chunks so you're not hitting disk per char."
      },
      {
        "front": "Scanner",
        "reading": "",
        "meaning": "Parses primitive tokens from input.",
        "example_ja": "new Scanner(System.in)",
        "example_en": "The beginner-friendly input reader."
      },
      {
        "front": "System.in",
        "reading": "",
        "meaning": "Standard input stream.",
        "example_ja": "new Scanner(System.in)",
        "example_en": "Where typed/piped input comes from."
      },
      {
        "front": "File",
        "reading": "",
        "meaning": "An abstract path to a file or dir.",
        "example_ja": "new File(\"a.txt\")",
        "example_en": "A handle to a location on disk."
      },
      {
        "front": "Path / Files (NIO)",
        "reading": "",
        "meaning": "Modern file API in java.nio.",
        "example_ja": "Files.readAllLines(path)",
        "example_en": "The cleaner, newer way to touch files."
      },
      {
        "front": "serialization",
        "reading": "",
        "meaning": "Turning an object into bytes.",
        "example_ja": "implements Serializable",
        "example_en": "Freeze an object so you can save/send it."
      },
      {
        "front": "deserialization",
        "reading": "",
        "meaning": "Rebuilding an object from bytes.",
        "example_ja": "ois.readObject()",
        "example_en": "Thaw the bytes back into an object."
      },
      {
        "front": "buffer",
        "reading": "",
        "meaning": "An in-memory staging area for data.",
        "example_ja": "byte[] buf = new byte[8192];",
        "example_en": "A holding tank so I/O happens in bulk."
      },
      {
        "front": "flush",
        "reading": "",
        "meaning": "Forces buffered data out.",
        "example_ja": "writer.flush();",
        "example_en": "Push whatever's waiting out now."
      },
      {
        "front": "Socket",
        "reading": "",
        "meaning": "An endpoint of a network connection.",
        "example_ja": "new Socket(host, port)",
        "example_en": "A phone line between two programs."
      }
    ],
    "quiz": [
      {
        "question": "The difference between InputStream and Reader is that Reader handles…",
        "options": [
          "characters (text)",
          "raw bytes",
          "only files",
          "network data"
        ],
        "answer": 0,
        "explanation": "Byte streams (InputStream/OutputStream) move raw bytes; Reader/Writer decode/encode characters."
      },
      {
        "question": "`BufferedReader` improves performance by…",
        "options": [
          "reading data in larger chunks",
          "compressing the data",
          "using multiple threads",
          "caching the whole file"
        ],
        "answer": 0,
        "explanation": "Buffering reduces the number of underlying read calls by fetching bigger blocks at a time."
      },
      {
        "question": "A class must implement ______ to be serialized with ObjectOutputStream.",
        "options": [
          "Serializable",
          "Cloneable",
          "Comparable",
          "Runnable"
        ],
        "answer": 0,
        "explanation": "`Serializable` is the marker interface that permits default Java serialization."
      },
      {
        "question": "`flush()` on a writer…",
        "options": [
          "forces buffered output out immediately",
          "closes the file",
          "clears the file",
          "reads input"
        ],
        "answer": 0,
        "explanation": "flush pushes any buffered bytes to the destination without closing the stream."
      },
      {
        "question": "For modern file reading, the recommended API is…",
        "options": [
          "java.nio.file.Files / Path",
          "java.io.File only",
          "RandomAccessFile",
          "Scanner only"
        ],
        "answer": 0,
        "explanation": "NIO's `Files`/`Path` offer concise, robust file operations over the older `java.io.File`."
      }
    ],
    "practice": []
  },
  {
    "key": "jv_tooling",
    "name": "Build, Tooling & Ecosystem",
    "category": "JV: Tooling",
    "what": "The tools around the code: build systems, packaging, testing, versions.",
    "deep": "Real Java projects live inside a toolchain. Maven/Gradle build them, JARs package them, JUnit tests them, and the ecosystem words (dependency, artifact, POM) are what you'll hear on any team.",
    "java": "Every row gives you **two** explanations: the **textbook definition** (what you'd write on an exam) and a **plain-English** line (what it actually means when you're staring at code). Skim the glossary, then hit **🎴 Study cards** to drill them with spaced repetition, and **❓ Check yourself** at the bottom.",
    "language": "java_vocab",
    "lesson": "| Term | Textbook definition | In plain English | Example |\n|---|---|---|---|\n| `Maven` | A build/dependency tool using pom.xml. | The declarative build most Java teams use. | `mvn package` |\n| `Gradle` | A build tool using a Groovy/Kotlin script. | The flexible, scriptable alternative to Maven. | `gradle build` |\n| `pom.xml` | Maven's project & dependency config. | The file listing what your project needs. | `<dependency>...</dependency>` |\n| `dependency` | An external library your code needs. | Someone else's code you rely on. | `<artifactId>guava</artifactId>` |\n| `artifact` | A built, versioned output (jar). | The packaged thing a build produces. | `app-1.0.jar` |\n| `JAR` | A zip of classes and resources. | Your whole app or library in one file. | `java -jar app.jar` |\n| `classpath` | Where the JVM finds classes/jars. | The search path for your dependencies. | `-cp lib/*` |\n| `JUnit` | The standard unit-testing framework. | How you write automated tests. | `@Test void works() { }` |\n| `assertion (test)` | A check that a value meets expectations. | The 'is this right?' line in a test. | `assertEquals(4, add(2,2));` |\n| `mock` | A fake collaborator for a test. | A stand-in object so you test one thing. | `mock(Repo.class)` |\n| `SLF4J / Logback` | Logging facade and backend. | The standard way apps write logs. | `logger.info(\"started\");` |\n| `LTS version` | A long-term-support Java release. | The versions companies actually pin to (8, 11, 17, 21). | `// Java 21 LTS` |\n| `javadoc` | Doc generated from /** */ comments. | API docs built straight from your comments. | `/** Adds two ints. */` |\n| `annotation processor` | Code that reacts to annotations at build. | A plugin that generates code from annotations. | `@AutoValue` |\n| `semantic versioning` | MAJOR.MINOR.PATCH version scheme. | Version numbers that signal what changed. | `1.4.2` |\n| `IDE` | An integrated development environment. | The editor that also builds/debugs (IntelliJ, Eclipse). | `// IntelliJ IDEA` |\n",
    "exercises": [],
    "cards": [
      {
        "front": "Maven",
        "reading": "",
        "meaning": "A build/dependency tool using pom.xml.",
        "example_ja": "mvn package",
        "example_en": "The declarative build most Java teams use."
      },
      {
        "front": "Gradle",
        "reading": "",
        "meaning": "A build tool using a Groovy/Kotlin script.",
        "example_ja": "gradle build",
        "example_en": "The flexible, scriptable alternative to Maven."
      },
      {
        "front": "pom.xml",
        "reading": "",
        "meaning": "Maven's project & dependency config.",
        "example_ja": "<dependency>...</dependency>",
        "example_en": "The file listing what your project needs."
      },
      {
        "front": "dependency",
        "reading": "",
        "meaning": "An external library your code needs.",
        "example_ja": "<artifactId>guava</artifactId>",
        "example_en": "Someone else's code you rely on."
      },
      {
        "front": "artifact",
        "reading": "",
        "meaning": "A built, versioned output (jar).",
        "example_ja": "app-1.0.jar",
        "example_en": "The packaged thing a build produces."
      },
      {
        "front": "JAR",
        "reading": "",
        "meaning": "A zip of classes and resources.",
        "example_ja": "java -jar app.jar",
        "example_en": "Your whole app or library in one file."
      },
      {
        "front": "classpath",
        "reading": "",
        "meaning": "Where the JVM finds classes/jars.",
        "example_ja": "-cp lib/*",
        "example_en": "The search path for your dependencies."
      },
      {
        "front": "JUnit",
        "reading": "",
        "meaning": "The standard unit-testing framework.",
        "example_ja": "@Test void works() { }",
        "example_en": "How you write automated tests."
      },
      {
        "front": "assertion (test)",
        "reading": "",
        "meaning": "A check that a value meets expectations.",
        "example_ja": "assertEquals(4, add(2,2));",
        "example_en": "The 'is this right?' line in a test."
      },
      {
        "front": "mock",
        "reading": "",
        "meaning": "A fake collaborator for a test.",
        "example_ja": "mock(Repo.class)",
        "example_en": "A stand-in object so you test one thing."
      },
      {
        "front": "SLF4J / Logback",
        "reading": "",
        "meaning": "Logging facade and backend.",
        "example_ja": "logger.info(\"started\");",
        "example_en": "The standard way apps write logs."
      },
      {
        "front": "LTS version",
        "reading": "",
        "meaning": "A long-term-support Java release.",
        "example_ja": "// Java 21 LTS",
        "example_en": "The versions companies actually pin to (8, 11, 17, 21)."
      },
      {
        "front": "javadoc",
        "reading": "",
        "meaning": "Doc generated from /** */ comments.",
        "example_ja": "/** Adds two ints. */",
        "example_en": "API docs built straight from your comments."
      },
      {
        "front": "annotation processor",
        "reading": "",
        "meaning": "Code that reacts to annotations at build.",
        "example_ja": "@AutoValue",
        "example_en": "A plugin that generates code from annotations."
      },
      {
        "front": "semantic versioning",
        "reading": "",
        "meaning": "MAJOR.MINOR.PATCH version scheme.",
        "example_ja": "1.4.2",
        "example_en": "Version numbers that signal what changed."
      },
      {
        "front": "IDE",
        "reading": "",
        "meaning": "An integrated development environment.",
        "example_ja": "// IntelliJ IDEA",
        "example_en": "The editor that also builds/debugs (IntelliJ, Eclipse)."
      }
    ],
    "quiz": [
      {
        "question": "A Maven project's dependencies and build config live in…",
        "options": [
          "pom.xml",
          "build.gradle",
          "MANIFEST.MF",
          "module-info.java"
        ],
        "answer": 0,
        "explanation": "Maven reads `pom.xml`; Gradle uses `build.gradle`."
      },
      {
        "question": "A JAR file is essentially…",
        "options": [
          "a zip archive of classes and resources",
          "compiled native code",
          "a source bundle",
          "a database"
        ],
        "answer": 0,
        "explanation": "A JAR is a ZIP with a manifest, packaging compiled classes and resources for distribution."
      },
      {
        "question": "Which Java versions are LONG-TERM-SUPPORT (LTS)?",
        "options": [
          "8, 11, 17, 21",
          "9, 10, 12",
          "all even versions",
          "only the latest"
        ],
        "answer": 0,
        "explanation": "LTS releases (8, 11, 17, 21…) get extended support and are what most teams standardize on."
      },
      {
        "question": "A `mock` in a unit test is…",
        "options": [
          "a fake collaborator so you can test one unit in isolation",
          "a slow test",
          "a production dependency",
          "a build artifact"
        ],
        "answer": 0,
        "explanation": "Mocks stand in for real collaborators so a test exercises one class without its dependencies."
      },
      {
        "question": "Semantic version `2.5.1` — the `1` is the…",
        "options": [
          "patch (bug-fix) number",
          "major version",
          "minor version",
          "build date"
        ],
        "answer": 0,
        "explanation": "MAJOR.MINOR.PATCH: a patch bump means backward-compatible bug fixes only."
      }
    ],
    "practice": []
  },
  {
    "key": "jv_modern",
    "name": "Modern Java & Annotations",
    "category": "JV: Modern Java",
    "what": "Records, sealed types, pattern matching, and the annotation/reflection tools.",
    "deep": "Java keeps evolving. Records kill boilerplate, sealed types tame hierarchies, switch got expressions and pattern matching, and annotations + reflection let frameworks work their magic. This is the vocabulary of Java 14+.",
    "java": "Every row gives you **two** explanations: the **textbook definition** (what you'd write on an exam) and a **plain-English** line (what it actually means when you're staring at code). Skim the glossary, then hit **🎴 Study cards** to drill them with spaced repetition, and **❓ Check yourself** at the bottom.",
    "language": "java_vocab",
    "lesson": "| Term | Textbook definition | In plain English | Example |\n|---|---|---|---|\n| `record` | An immutable data class with generated members. | A tiny class that's just fields — Java writes the rest. | `record Point(int x, int y) { }` |\n| `sealed class` | A class limiting which types may extend it. | A hierarchy with a fixed guest list. | `sealed interface Shape permits Circle { }` |\n| `permits` | Lists a sealed type's allowed subtypes. | The guest list of a sealed type. | `sealed ... permits A, B` |\n| `pattern matching` | Testing and binding a type in one step. | Check the type and grab it at once. | `if (o instanceof Dog d) { }` |\n| `switch expression` | A switch that returns a value. | Switch that hands back a result, arrow-style. | `int n = switch (d) { case 1 -> 10; default -> 0; };` |\n| `text block` | A multi-line \"\"\" string literal. | Multi-line text without escaping newlines. | `String s = \"\"\"hi\"\"\";` |\n| `var (local)` | Local variable type inference. | Skip writing the obvious type. | `var list = new ArrayList<String>();` |\n| `annotation` | Metadata attached to code. | A sticky note the compiler/tools read. | `@Override` |\n| `@Override` | Marks a method as overriding a parent. | 'I meant to replace the parent's version.' | `@Override void run() { }` |\n| `@Deprecated` | Flags an API as outdated. | 'Don't use this anymore.' | `@Deprecated void old() { }` |\n| `@FunctionalInterface` | Asserts a single-abstract-method interface. | 'This interface is lambda-ready.' | `@FunctionalInterface` |\n| `@SuppressWarnings` | Silences specific compiler warnings. | 'Yes I know, hush.' | `@SuppressWarnings(\"unchecked\")` |\n| `retention policy` | How long an annotation is kept. | Whether the annotation survives to runtime. | `@Retention(RUNTIME)` |\n| `reflection` | Inspecting/altering code at runtime. | Code that reads and pokes at other code live. | `obj.getClass().getMethods()` |\n| `Class object` | Runtime metadata for a type. | The 'file card' describing a class at runtime. | `String.class` |\n| `module (JPMS)` | A named, encapsulated set of packages. | A bigger box around packages, with declared exports. | `module app { requires x; }` |\n| `enhanced instanceof` | instanceof that binds a variable. | Test-and-cast in one clean move. | `o instanceof String s` |\n| `Optional (modern)` | Explicit maybe-a-value container. | A null-free way to say 'might be empty'. | `Optional.ofNullable(x)` |\n",
    "exercises": [],
    "cards": [
      {
        "front": "record",
        "reading": "",
        "meaning": "An immutable data class with generated members.",
        "example_ja": "record Point(int x, int y) { }",
        "example_en": "A tiny class that's just fields — Java writes the rest."
      },
      {
        "front": "sealed class",
        "reading": "",
        "meaning": "A class limiting which types may extend it.",
        "example_ja": "sealed interface Shape permits Circle { }",
        "example_en": "A hierarchy with a fixed guest list."
      },
      {
        "front": "permits",
        "reading": "",
        "meaning": "Lists a sealed type's allowed subtypes.",
        "example_ja": "sealed ... permits A, B",
        "example_en": "The guest list of a sealed type."
      },
      {
        "front": "pattern matching",
        "reading": "",
        "meaning": "Testing and binding a type in one step.",
        "example_ja": "if (o instanceof Dog d) { }",
        "example_en": "Check the type and grab it at once."
      },
      {
        "front": "switch expression",
        "reading": "",
        "meaning": "A switch that returns a value.",
        "example_ja": "int n = switch (d) { case 1 -> 10; default -> 0; };",
        "example_en": "Switch that hands back a result, arrow-style."
      },
      {
        "front": "text block",
        "reading": "",
        "meaning": "A multi-line \"\"\" string literal.",
        "example_ja": "String s = \"\"\"hi\"\"\";",
        "example_en": "Multi-line text without escaping newlines."
      },
      {
        "front": "var (local)",
        "reading": "",
        "meaning": "Local variable type inference.",
        "example_ja": "var list = new ArrayList<String>();",
        "example_en": "Skip writing the obvious type."
      },
      {
        "front": "annotation",
        "reading": "",
        "meaning": "Metadata attached to code.",
        "example_ja": "@Override",
        "example_en": "A sticky note the compiler/tools read."
      },
      {
        "front": "@Override",
        "reading": "",
        "meaning": "Marks a method as overriding a parent.",
        "example_ja": "@Override void run() { }",
        "example_en": "'I meant to replace the parent's version.'"
      },
      {
        "front": "@Deprecated",
        "reading": "",
        "meaning": "Flags an API as outdated.",
        "example_ja": "@Deprecated void old() { }",
        "example_en": "'Don't use this anymore.'"
      },
      {
        "front": "@FunctionalInterface",
        "reading": "",
        "meaning": "Asserts a single-abstract-method interface.",
        "example_ja": "@FunctionalInterface",
        "example_en": "'This interface is lambda-ready.'"
      },
      {
        "front": "@SuppressWarnings",
        "reading": "",
        "meaning": "Silences specific compiler warnings.",
        "example_ja": "@SuppressWarnings(\"unchecked\")",
        "example_en": "'Yes I know, hush.'"
      },
      {
        "front": "retention policy",
        "reading": "",
        "meaning": "How long an annotation is kept.",
        "example_ja": "@Retention(RUNTIME)",
        "example_en": "Whether the annotation survives to runtime."
      },
      {
        "front": "reflection",
        "reading": "",
        "meaning": "Inspecting/altering code at runtime.",
        "example_ja": "obj.getClass().getMethods()",
        "example_en": "Code that reads and pokes at other code live."
      },
      {
        "front": "Class object",
        "reading": "",
        "meaning": "Runtime metadata for a type.",
        "example_ja": "String.class",
        "example_en": "The 'file card' describing a class at runtime."
      },
      {
        "front": "module (JPMS)",
        "reading": "",
        "meaning": "A named, encapsulated set of packages.",
        "example_ja": "module app { requires x; }",
        "example_en": "A bigger box around packages, with declared exports."
      },
      {
        "front": "enhanced instanceof",
        "reading": "",
        "meaning": "instanceof that binds a variable.",
        "example_ja": "o instanceof String s",
        "example_en": "Test-and-cast in one clean move."
      },
      {
        "front": "Optional (modern)",
        "reading": "",
        "meaning": "Explicit maybe-a-value container.",
        "example_ja": "Optional.ofNullable(x)",
        "example_en": "A null-free way to say 'might be empty'."
      }
    ],
    "quiz": [
      {
        "question": "A `record` automatically generates…",
        "options": [
          "constructor, accessors, equals/hashCode/toString",
          "getters and setters",
          "a builder",
          "thread-safe methods"
        ],
        "answer": 0,
        "explanation": "Records generate a canonical constructor, component accessors, and value-based equals/hashCode/toString — and are immutable."
      },
      {
        "question": "A `sealed` interface is useful because it…",
        "options": [
          "restricts which classes can implement it",
          "makes methods final",
          "prevents instantiation",
          "adds thread safety"
        ],
        "answer": 0,
        "explanation": "Sealed types name their permitted subtypes, enabling exhaustive switches and safer hierarchies."
      },
      {
        "question": "Enhanced `instanceof` (`o instanceof String s`) lets you…",
        "options": [
          "test the type and bind a variable at once",
          "cast without checking",
          "compare two objects",
          "avoid generics"
        ],
        "answer": 0,
        "explanation": "Pattern matching for instanceof both tests the type and, on success, binds `s` to the cast value."
      },
      {
        "question": "A switch EXPRESSION differs from a switch statement because it…",
        "options": [
          "produces a value",
          "can't have a default",
          "only works on ints",
          "requires break"
        ],
        "answer": 0,
        "explanation": "Switch expressions (`->` arms, optional `yield`) return a value and don't fall through."
      },
      {
        "question": "Reflection lets you…",
        "options": [
          "inspect and invoke code at runtime",
          "speed up the JVM",
          "avoid compilation",
          "manage memory"
        ],
        "answer": 0,
        "explanation": "Reflection reads class metadata and can access fields/methods dynamically — the basis of many frameworks."
      },
      {
        "question": "For an annotation to be readable at runtime (e.g. by a framework), it needs…",
        "options": [
          "@Retention(RUNTIME)",
          "@Override",
          "@Deprecated",
          "no policy"
        ],
        "answer": 0,
        "explanation": "The default retention is CLASS (not visible at runtime); RUNTIME retention keeps it available to reflection."
      }
    ],
    "practice": []
  }
]