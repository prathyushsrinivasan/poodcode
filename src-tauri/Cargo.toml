[package]
name = "poodcode"
version = "0.1.0"
description = "Personal offline coding-interview & algorithm practice desktop app"
authors = ["Poodcode"]
edition = "2021"
rust-version = "1.77"

[lib]
name = "poodcode_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
# `hooks` adds no dependencies; it exposes sqlite3_progress_handler and
# sqlite3_set_authorizer, which src/sqlexec.rs uses to time-limit and sandbox
# the SQL a learner runs in the SQL track.
rusqlite = { version = "0.32", features = ["bundled", "hooks"] }
chrono = { version = "0.4", features = ["serde"] }
anyhow = "1"
thiserror = "1"
wait-timeout = "0.2"
uuid = { version = "1", features = ["v4"] }

[features]
# This feature is used for production builds or when a dev server is not
# specified, DO NOT REMOVE!!
custom-protocol = ["tauri/custom-protocol"]

[profile.dev]
incremental = true

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true
