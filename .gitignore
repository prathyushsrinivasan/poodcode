node_modules
dist
dist-ssr
*.local
.DS_Store
*.tsbuildinfo

# Rust / Tauri
src-tauri/target
src-tauri/gen

# App data (local database created at runtime in the OS app-data dir)
*.sqlite
*.sqlite-*

# Python bytecode from the tools/ generators
__pycache__/

# Visual-regression output (tools/screenshots.mjs). The baseline is regenerated
# on the machine that checks it — antialiasing differs between machines, so a
# committed baseline would fail everywhere but where it was made.
screenshots/
